/*
THIS IS A GENERATED/BUNDLED FILE BY ESBUILD
if you want to view the source, please visit the github repository of this plugin
*/
const PLUGIN_VERSION="4.1.1";
const PLUGIN_WHATS_NEW="### Fixed\n- The note title and the file name no longer appear together at the top of a note. This\n  happened with themes that style the inline title, such as anuPpuccin. Now only the note\n  title is shown (#280, #234, #123).";

var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __typeError = (msg) => {
  throw TypeError(msg);
};
var __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __commonJS = (cb, mod) => function __require() {
  try {
    return mod || (0, cb[__getOwnPropNames(cb)[0]])((mod = { exports: {} }).exports, mod), mod.exports;
  } catch (e2) {
    throw mod = 0, e2;
  }
};
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var __decorateClass = (decorators, target, key, kind) => {
  var result = kind > 1 ? void 0 : kind ? __getOwnPropDesc(target, key) : target;
  for (var i2 = decorators.length - 1, decorator; i2 >= 0; i2--)
    if (decorator = decorators[i2])
      result = (kind ? decorator(target, key, result) : decorator(result)) || result;
  if (kind && result) __defProp(target, key, result);
  return result;
};
var __decorateParam = (index, decorator) => (target, key) => decorator(target, key, index);
var __publicField = (obj, key, value) => __defNormalProp(obj, typeof key !== "symbol" ? key + "" : key, value);
var __accessCheck = (obj, member, msg) => member.has(obj) || __typeError("Cannot " + msg);
var __privateGet = (obj, member, getter) => (__accessCheck(obj, member, "read from private field"), getter ? getter.call(obj) : member.get(obj));
var __privateAdd = (obj, member, value) => member.has(obj) ? __typeError("Cannot add the same private member more than once") : member instanceof WeakSet ? member.add(obj) : member.set(obj, value);
var __privateSet = (obj, member, value, setter) => (__accessCheck(obj, member, "write to private field"), setter ? setter.call(obj, value) : member.set(obj, value), value);

// node_modules/reflect-metadata/Reflect.js
var require_Reflect = __commonJS({
  "node_modules/reflect-metadata/Reflect.js"() {
    var Reflect2;
    (function(Reflect3) {
      (function(factory) {
        var root = typeof globalThis === "object" ? globalThis : typeof global === "object" ? global : typeof self === "object" ? self : typeof this === "object" ? this : sloppyModeThis();
        var exporter = makeExporter(Reflect3);
        if (typeof root.Reflect !== "undefined") {
          exporter = makeExporter(root.Reflect, exporter);
        }
        factory(exporter, root);
        if (typeof root.Reflect === "undefined") {
          root.Reflect = Reflect3;
        }
        function makeExporter(target, previous) {
          return function(key, value) {
            Object.defineProperty(target, key, { configurable: true, writable: true, value });
            if (previous)
              previous(key, value);
          };
        }
        function functionThis() {
          try {
            return Function("return this;")();
          } catch (_3) {
          }
        }
        function indirectEvalThis() {
          try {
            return (void 0, eval)("(function() { return this; })()");
          } catch (_3) {
          }
        }
        function sloppyModeThis() {
          return functionThis() || indirectEvalThis();
        }
      })(function(exporter, root) {
        var hasOwn = Object.prototype.hasOwnProperty;
        var supportsSymbol = typeof Symbol === "function";
        var toPrimitiveSymbol = supportsSymbol && typeof Symbol.toPrimitive !== "undefined" ? Symbol.toPrimitive : "@@toPrimitive";
        var iteratorSymbol = supportsSymbol && typeof Symbol.iterator !== "undefined" ? Symbol.iterator : "@@iterator";
        var supportsCreate = typeof Object.create === "function";
        var supportsProto = { __proto__: [] } instanceof Array;
        var downLevel = !supportsCreate && !supportsProto;
        var HashMap = {
          // create an object in dictionary mode (a.k.a. "slow" mode in v8)
          create: supportsCreate ? function() {
            return MakeDictionary(/* @__PURE__ */ Object.create(null));
          } : supportsProto ? function() {
            return MakeDictionary({ __proto__: null });
          } : function() {
            return MakeDictionary({});
          },
          has: downLevel ? function(map, key) {
            return hasOwn.call(map, key);
          } : function(map, key) {
            return key in map;
          },
          get: downLevel ? function(map, key) {
            return hasOwn.call(map, key) ? map[key] : void 0;
          } : function(map, key) {
            return map[key];
          }
        };
        var functionPrototype = Object.getPrototypeOf(Function);
        var _Map = typeof Map === "function" && typeof Map.prototype.entries === "function" ? Map : CreateMapPolyfill();
        var _Set = typeof Set === "function" && typeof Set.prototype.entries === "function" ? Set : CreateSetPolyfill();
        var _WeakMap = typeof WeakMap === "function" ? WeakMap : CreateWeakMapPolyfill();
        var registrySymbol = supportsSymbol ? /* @__PURE__ */ Symbol.for("@reflect-metadata:registry") : void 0;
        var metadataRegistry = GetOrCreateMetadataRegistry();
        var metadataProvider = CreateMetadataProvider(metadataRegistry);
        function decorate(decorators, target, propertyKey, attributes) {
          if (!IsUndefined(propertyKey)) {
            if (!IsArray(decorators))
              throw new TypeError();
            if (!IsObject(target))
              throw new TypeError();
            if (!IsObject(attributes) && !IsUndefined(attributes) && !IsNull(attributes))
              throw new TypeError();
            if (IsNull(attributes))
              attributes = void 0;
            propertyKey = ToPropertyKey(propertyKey);
            return DecorateProperty(decorators, target, propertyKey, attributes);
          } else {
            if (!IsArray(decorators))
              throw new TypeError();
            if (!IsConstructor(target))
              throw new TypeError();
            return DecorateConstructor(decorators, target);
          }
        }
        exporter("decorate", decorate);
        function metadata(metadataKey, metadataValue) {
          function decorator(target, propertyKey) {
            if (!IsObject(target))
              throw new TypeError();
            if (!IsUndefined(propertyKey) && !IsPropertyKey(propertyKey))
              throw new TypeError();
            OrdinaryDefineOwnMetadata(metadataKey, metadataValue, target, propertyKey);
          }
          return decorator;
        }
        exporter("metadata", metadata);
        function defineMetadata(metadataKey, metadataValue, target, propertyKey) {
          if (!IsObject(target))
            throw new TypeError();
          if (!IsUndefined(propertyKey))
            propertyKey = ToPropertyKey(propertyKey);
          return OrdinaryDefineOwnMetadata(metadataKey, metadataValue, target, propertyKey);
        }
        exporter("defineMetadata", defineMetadata);
        function hasMetadata(metadataKey, target, propertyKey) {
          if (!IsObject(target))
            throw new TypeError();
          if (!IsUndefined(propertyKey))
            propertyKey = ToPropertyKey(propertyKey);
          return OrdinaryHasMetadata(metadataKey, target, propertyKey);
        }
        exporter("hasMetadata", hasMetadata);
        function hasOwnMetadata(metadataKey, target, propertyKey) {
          if (!IsObject(target))
            throw new TypeError();
          if (!IsUndefined(propertyKey))
            propertyKey = ToPropertyKey(propertyKey);
          return OrdinaryHasOwnMetadata(metadataKey, target, propertyKey);
        }
        exporter("hasOwnMetadata", hasOwnMetadata);
        function getMetadata(metadataKey, target, propertyKey) {
          if (!IsObject(target))
            throw new TypeError();
          if (!IsUndefined(propertyKey))
            propertyKey = ToPropertyKey(propertyKey);
          return OrdinaryGetMetadata(metadataKey, target, propertyKey);
        }
        exporter("getMetadata", getMetadata);
        function getOwnMetadata(metadataKey, target, propertyKey) {
          if (!IsObject(target))
            throw new TypeError();
          if (!IsUndefined(propertyKey))
            propertyKey = ToPropertyKey(propertyKey);
          return OrdinaryGetOwnMetadata(metadataKey, target, propertyKey);
        }
        exporter("getOwnMetadata", getOwnMetadata);
        function getMetadataKeys(target, propertyKey) {
          if (!IsObject(target))
            throw new TypeError();
          if (!IsUndefined(propertyKey))
            propertyKey = ToPropertyKey(propertyKey);
          return OrdinaryMetadataKeys(target, propertyKey);
        }
        exporter("getMetadataKeys", getMetadataKeys);
        function getOwnMetadataKeys(target, propertyKey) {
          if (!IsObject(target))
            throw new TypeError();
          if (!IsUndefined(propertyKey))
            propertyKey = ToPropertyKey(propertyKey);
          return OrdinaryOwnMetadataKeys(target, propertyKey);
        }
        exporter("getOwnMetadataKeys", getOwnMetadataKeys);
        function deleteMetadata(metadataKey, target, propertyKey) {
          if (!IsObject(target))
            throw new TypeError();
          if (!IsUndefined(propertyKey))
            propertyKey = ToPropertyKey(propertyKey);
          if (!IsObject(target))
            throw new TypeError();
          if (!IsUndefined(propertyKey))
            propertyKey = ToPropertyKey(propertyKey);
          var provider = GetMetadataProvider(
            target,
            propertyKey,
            /*Create*/
            false
          );
          if (IsUndefined(provider))
            return false;
          return provider.OrdinaryDeleteMetadata(metadataKey, target, propertyKey);
        }
        exporter("deleteMetadata", deleteMetadata);
        function DecorateConstructor(decorators, target) {
          for (var i2 = decorators.length - 1; i2 >= 0; --i2) {
            var decorator = decorators[i2];
            var decorated = decorator(target);
            if (!IsUndefined(decorated) && !IsNull(decorated)) {
              if (!IsConstructor(decorated))
                throw new TypeError();
              target = decorated;
            }
          }
          return target;
        }
        function DecorateProperty(decorators, target, propertyKey, descriptor) {
          for (var i2 = decorators.length - 1; i2 >= 0; --i2) {
            var decorator = decorators[i2];
            var decorated = decorator(target, propertyKey, descriptor);
            if (!IsUndefined(decorated) && !IsNull(decorated)) {
              if (!IsObject(decorated))
                throw new TypeError();
              descriptor = decorated;
            }
          }
          return descriptor;
        }
        function OrdinaryHasMetadata(MetadataKey, O3, P2) {
          var hasOwn2 = OrdinaryHasOwnMetadata(MetadataKey, O3, P2);
          if (hasOwn2)
            return true;
          var parent = OrdinaryGetPrototypeOf(O3);
          if (!IsNull(parent))
            return OrdinaryHasMetadata(MetadataKey, parent, P2);
          return false;
        }
        function OrdinaryHasOwnMetadata(MetadataKey, O3, P2) {
          var provider = GetMetadataProvider(
            O3,
            P2,
            /*Create*/
            false
          );
          if (IsUndefined(provider))
            return false;
          return ToBoolean(provider.OrdinaryHasOwnMetadata(MetadataKey, O3, P2));
        }
        function OrdinaryGetMetadata(MetadataKey, O3, P2) {
          var hasOwn2 = OrdinaryHasOwnMetadata(MetadataKey, O3, P2);
          if (hasOwn2)
            return OrdinaryGetOwnMetadata(MetadataKey, O3, P2);
          var parent = OrdinaryGetPrototypeOf(O3);
          if (!IsNull(parent))
            return OrdinaryGetMetadata(MetadataKey, parent, P2);
          return void 0;
        }
        function OrdinaryGetOwnMetadata(MetadataKey, O3, P2) {
          var provider = GetMetadataProvider(
            O3,
            P2,
            /*Create*/
            false
          );
          if (IsUndefined(provider))
            return;
          return provider.OrdinaryGetOwnMetadata(MetadataKey, O3, P2);
        }
        function OrdinaryDefineOwnMetadata(MetadataKey, MetadataValue, O3, P2) {
          var provider = GetMetadataProvider(
            O3,
            P2,
            /*Create*/
            true
          );
          provider.OrdinaryDefineOwnMetadata(MetadataKey, MetadataValue, O3, P2);
        }
        function OrdinaryMetadataKeys(O3, P2) {
          var ownKeys = OrdinaryOwnMetadataKeys(O3, P2);
          var parent = OrdinaryGetPrototypeOf(O3);
          if (parent === null)
            return ownKeys;
          var parentKeys = OrdinaryMetadataKeys(parent, P2);
          if (parentKeys.length <= 0)
            return ownKeys;
          if (ownKeys.length <= 0)
            return parentKeys;
          var set = new _Set();
          var keys = [];
          for (var _i2 = 0, ownKeys_1 = ownKeys; _i2 < ownKeys_1.length; _i2++) {
            var key = ownKeys_1[_i2];
            var hasKey = set.has(key);
            if (!hasKey) {
              set.add(key);
              keys.push(key);
            }
          }
          for (var _a4 = 0, parentKeys_1 = parentKeys; _a4 < parentKeys_1.length; _a4++) {
            var key = parentKeys_1[_a4];
            var hasKey = set.has(key);
            if (!hasKey) {
              set.add(key);
              keys.push(key);
            }
          }
          return keys;
        }
        function OrdinaryOwnMetadataKeys(O3, P2) {
          var provider = GetMetadataProvider(
            O3,
            P2,
            /*create*/
            false
          );
          if (!provider) {
            return [];
          }
          return provider.OrdinaryOwnMetadataKeys(O3, P2);
        }
        function Type(x3) {
          if (x3 === null)
            return 1;
          switch (typeof x3) {
            case "undefined":
              return 0;
            case "boolean":
              return 2;
            case "string":
              return 3;
            case "symbol":
              return 4;
            case "number":
              return 5;
            case "object":
              return x3 === null ? 1 : 6;
            default:
              return 6;
          }
        }
        function IsUndefined(x3) {
          return x3 === void 0;
        }
        function IsNull(x3) {
          return x3 === null;
        }
        function IsSymbol(x3) {
          return typeof x3 === "symbol";
        }
        function IsObject(x3) {
          return typeof x3 === "object" ? x3 !== null : typeof x3 === "function";
        }
        function ToPrimitive(input, PreferredType) {
          switch (Type(input)) {
            case 0:
              return input;
            case 1:
              return input;
            case 2:
              return input;
            case 3:
              return input;
            case 4:
              return input;
            case 5:
              return input;
          }
          var hint = PreferredType === 3 ? "string" : PreferredType === 5 ? "number" : "default";
          var exoticToPrim = GetMethod(input, toPrimitiveSymbol);
          if (exoticToPrim !== void 0) {
            var result = exoticToPrim.call(input, hint);
            if (IsObject(result))
              throw new TypeError();
            return result;
          }
          return OrdinaryToPrimitive(input, hint === "default" ? "number" : hint);
        }
        function OrdinaryToPrimitive(O3, hint) {
          if (hint === "string") {
            var toString_1 = O3.toString;
            if (IsCallable(toString_1)) {
              var result = toString_1.call(O3);
              if (!IsObject(result))
                return result;
            }
            var valueOf = O3.valueOf;
            if (IsCallable(valueOf)) {
              var result = valueOf.call(O3);
              if (!IsObject(result))
                return result;
            }
          } else {
            var valueOf = O3.valueOf;
            if (IsCallable(valueOf)) {
              var result = valueOf.call(O3);
              if (!IsObject(result))
                return result;
            }
            var toString_2 = O3.toString;
            if (IsCallable(toString_2)) {
              var result = toString_2.call(O3);
              if (!IsObject(result))
                return result;
            }
          }
          throw new TypeError();
        }
        function ToBoolean(argument) {
          return !!argument;
        }
        function ToString(argument) {
          return "" + argument;
        }
        function ToPropertyKey(argument) {
          var key = ToPrimitive(
            argument,
            3
            /* String */
          );
          if (IsSymbol(key))
            return key;
          return ToString(key);
        }
        function IsArray(argument) {
          return Array.isArray ? Array.isArray(argument) : argument instanceof Object ? argument instanceof Array : Object.prototype.toString.call(argument) === "[object Array]";
        }
        function IsCallable(argument) {
          return typeof argument === "function";
        }
        function IsConstructor(argument) {
          return typeof argument === "function";
        }
        function IsPropertyKey(argument) {
          switch (Type(argument)) {
            case 3:
              return true;
            case 4:
              return true;
            default:
              return false;
          }
        }
        function SameValueZero(x3, y3) {
          return x3 === y3 || x3 !== x3 && y3 !== y3;
        }
        function GetMethod(V2, P2) {
          var func = V2[P2];
          if (func === void 0 || func === null)
            return void 0;
          if (!IsCallable(func))
            throw new TypeError();
          return func;
        }
        function GetIterator(obj) {
          var method = GetMethod(obj, iteratorSymbol);
          if (!IsCallable(method))
            throw new TypeError();
          var iterator = method.call(obj);
          if (!IsObject(iterator))
            throw new TypeError();
          return iterator;
        }
        function IteratorValue(iterResult) {
          return iterResult.value;
        }
        function IteratorStep(iterator) {
          var result = iterator.next();
          return result.done ? false : result;
        }
        function IteratorClose(iterator) {
          var f3 = iterator["return"];
          if (f3)
            f3.call(iterator);
        }
        function OrdinaryGetPrototypeOf(O3) {
          var proto = Object.getPrototypeOf(O3);
          if (typeof O3 !== "function" || O3 === functionPrototype)
            return proto;
          if (proto !== functionPrototype)
            return proto;
          var prototype = O3.prototype;
          var prototypeProto = prototype && Object.getPrototypeOf(prototype);
          if (prototypeProto == null || prototypeProto === Object.prototype)
            return proto;
          var constructor = prototypeProto.constructor;
          if (typeof constructor !== "function")
            return proto;
          if (constructor === O3)
            return proto;
          return constructor;
        }
        function CreateMetadataRegistry() {
          var fallback;
          if (!IsUndefined(registrySymbol) && typeof root.Reflect !== "undefined" && !(registrySymbol in root.Reflect) && typeof root.Reflect.defineMetadata === "function") {
            fallback = CreateFallbackProvider(root.Reflect);
          }
          var first;
          var second;
          var rest;
          var targetProviderMap = new _WeakMap();
          var registry = {
            registerProvider,
            getProvider,
            setProvider
          };
          return registry;
          function registerProvider(provider) {
            if (!Object.isExtensible(registry)) {
              throw new Error("Cannot add provider to a frozen registry.");
            }
            switch (true) {
              case fallback === provider:
                break;
              case IsUndefined(first):
                first = provider;
                break;
              case first === provider:
                break;
              case IsUndefined(second):
                second = provider;
                break;
              case second === provider:
                break;
              default:
                if (rest === void 0)
                  rest = new _Set();
                rest.add(provider);
                break;
            }
          }
          function getProviderNoCache(O3, P2) {
            if (!IsUndefined(first)) {
              if (first.isProviderFor(O3, P2))
                return first;
              if (!IsUndefined(second)) {
                if (second.isProviderFor(O3, P2))
                  return first;
                if (!IsUndefined(rest)) {
                  var iterator = GetIterator(rest);
                  while (true) {
                    var next = IteratorStep(iterator);
                    if (!next) {
                      return void 0;
                    }
                    var provider = IteratorValue(next);
                    if (provider.isProviderFor(O3, P2)) {
                      IteratorClose(iterator);
                      return provider;
                    }
                  }
                }
              }
            }
            if (!IsUndefined(fallback) && fallback.isProviderFor(O3, P2)) {
              return fallback;
            }
            return void 0;
          }
          function getProvider(O3, P2) {
            var providerMap = targetProviderMap.get(O3);
            var provider;
            if (!IsUndefined(providerMap)) {
              provider = providerMap.get(P2);
            }
            if (!IsUndefined(provider)) {
              return provider;
            }
            provider = getProviderNoCache(O3, P2);
            if (!IsUndefined(provider)) {
              if (IsUndefined(providerMap)) {
                providerMap = new _Map();
                targetProviderMap.set(O3, providerMap);
              }
              providerMap.set(P2, provider);
            }
            return provider;
          }
          function hasProvider(provider) {
            if (IsUndefined(provider))
              throw new TypeError();
            return first === provider || second === provider || !IsUndefined(rest) && rest.has(provider);
          }
          function setProvider(O3, P2, provider) {
            if (!hasProvider(provider)) {
              throw new Error("Metadata provider not registered.");
            }
            var existingProvider = getProvider(O3, P2);
            if (existingProvider !== provider) {
              if (!IsUndefined(existingProvider)) {
                return false;
              }
              var providerMap = targetProviderMap.get(O3);
              if (IsUndefined(providerMap)) {
                providerMap = new _Map();
                targetProviderMap.set(O3, providerMap);
              }
              providerMap.set(P2, provider);
            }
            return true;
          }
        }
        function GetOrCreateMetadataRegistry() {
          var metadataRegistry2;
          if (!IsUndefined(registrySymbol) && IsObject(root.Reflect) && Object.isExtensible(root.Reflect)) {
            metadataRegistry2 = root.Reflect[registrySymbol];
          }
          if (IsUndefined(metadataRegistry2)) {
            metadataRegistry2 = CreateMetadataRegistry();
          }
          if (!IsUndefined(registrySymbol) && IsObject(root.Reflect) && Object.isExtensible(root.Reflect)) {
            Object.defineProperty(root.Reflect, registrySymbol, {
              enumerable: false,
              configurable: false,
              writable: false,
              value: metadataRegistry2
            });
          }
          return metadataRegistry2;
        }
        function CreateMetadataProvider(registry) {
          var metadata2 = new _WeakMap();
          var provider = {
            isProviderFor: function(O3, P2) {
              var targetMetadata = metadata2.get(O3);
              if (IsUndefined(targetMetadata))
                return false;
              return targetMetadata.has(P2);
            },
            OrdinaryDefineOwnMetadata: OrdinaryDefineOwnMetadata2,
            OrdinaryHasOwnMetadata: OrdinaryHasOwnMetadata2,
            OrdinaryGetOwnMetadata: OrdinaryGetOwnMetadata2,
            OrdinaryOwnMetadataKeys: OrdinaryOwnMetadataKeys2,
            OrdinaryDeleteMetadata
          };
          metadataRegistry.registerProvider(provider);
          return provider;
          function GetOrCreateMetadataMap(O3, P2, Create) {
            var targetMetadata = metadata2.get(O3);
            var createdTargetMetadata = false;
            if (IsUndefined(targetMetadata)) {
              if (!Create)
                return void 0;
              targetMetadata = new _Map();
              metadata2.set(O3, targetMetadata);
              createdTargetMetadata = true;
            }
            var metadataMap = targetMetadata.get(P2);
            if (IsUndefined(metadataMap)) {
              if (!Create)
                return void 0;
              metadataMap = new _Map();
              targetMetadata.set(P2, metadataMap);
              if (!registry.setProvider(O3, P2, provider)) {
                targetMetadata.delete(P2);
                if (createdTargetMetadata) {
                  metadata2.delete(O3);
                }
                throw new Error("Wrong provider for target.");
              }
            }
            return metadataMap;
          }
          function OrdinaryHasOwnMetadata2(MetadataKey, O3, P2) {
            var metadataMap = GetOrCreateMetadataMap(
              O3,
              P2,
              /*Create*/
              false
            );
            if (IsUndefined(metadataMap))
              return false;
            return ToBoolean(metadataMap.has(MetadataKey));
          }
          function OrdinaryGetOwnMetadata2(MetadataKey, O3, P2) {
            var metadataMap = GetOrCreateMetadataMap(
              O3,
              P2,
              /*Create*/
              false
            );
            if (IsUndefined(metadataMap))
              return void 0;
            return metadataMap.get(MetadataKey);
          }
          function OrdinaryDefineOwnMetadata2(MetadataKey, MetadataValue, O3, P2) {
            var metadataMap = GetOrCreateMetadataMap(
              O3,
              P2,
              /*Create*/
              true
            );
            metadataMap.set(MetadataKey, MetadataValue);
          }
          function OrdinaryOwnMetadataKeys2(O3, P2) {
            var keys = [];
            var metadataMap = GetOrCreateMetadataMap(
              O3,
              P2,
              /*Create*/
              false
            );
            if (IsUndefined(metadataMap))
              return keys;
            var keysObj = metadataMap.keys();
            var iterator = GetIterator(keysObj);
            var k3 = 0;
            while (true) {
              var next = IteratorStep(iterator);
              if (!next) {
                keys.length = k3;
                return keys;
              }
              var nextValue = IteratorValue(next);
              try {
                keys[k3] = nextValue;
              } catch (e2) {
                try {
                  IteratorClose(iterator);
                } finally {
                  throw e2;
                }
              }
              k3++;
            }
          }
          function OrdinaryDeleteMetadata(MetadataKey, O3, P2) {
            var metadataMap = GetOrCreateMetadataMap(
              O3,
              P2,
              /*Create*/
              false
            );
            if (IsUndefined(metadataMap))
              return false;
            if (!metadataMap.delete(MetadataKey))
              return false;
            if (metadataMap.size === 0) {
              var targetMetadata = metadata2.get(O3);
              if (!IsUndefined(targetMetadata)) {
                targetMetadata.delete(P2);
                if (targetMetadata.size === 0) {
                  metadata2.delete(targetMetadata);
                }
              }
            }
            return true;
          }
        }
        function CreateFallbackProvider(reflect) {
          var defineMetadata2 = reflect.defineMetadata, hasOwnMetadata2 = reflect.hasOwnMetadata, getOwnMetadata2 = reflect.getOwnMetadata, getOwnMetadataKeys2 = reflect.getOwnMetadataKeys, deleteMetadata2 = reflect.deleteMetadata;
          var metadataOwner = new _WeakMap();
          var provider = {
            isProviderFor: function(O3, P2) {
              var metadataPropertySet = metadataOwner.get(O3);
              if (!IsUndefined(metadataPropertySet) && metadataPropertySet.has(P2)) {
                return true;
              }
              if (getOwnMetadataKeys2(O3, P2).length) {
                if (IsUndefined(metadataPropertySet)) {
                  metadataPropertySet = new _Set();
                  metadataOwner.set(O3, metadataPropertySet);
                }
                metadataPropertySet.add(P2);
                return true;
              }
              return false;
            },
            OrdinaryDefineOwnMetadata: defineMetadata2,
            OrdinaryHasOwnMetadata: hasOwnMetadata2,
            OrdinaryGetOwnMetadata: getOwnMetadata2,
            OrdinaryOwnMetadataKeys: getOwnMetadataKeys2,
            OrdinaryDeleteMetadata: deleteMetadata2
          };
          return provider;
        }
        function GetMetadataProvider(O3, P2, Create) {
          var registeredProvider = metadataRegistry.getProvider(O3, P2);
          if (!IsUndefined(registeredProvider)) {
            return registeredProvider;
          }
          if (Create) {
            if (metadataRegistry.setProvider(O3, P2, metadataProvider)) {
              return metadataProvider;
            }
            throw new Error("Illegal state.");
          }
          return void 0;
        }
        function CreateMapPolyfill() {
          var cacheSentinel = {};
          var arraySentinel = [];
          var MapIterator = (
            /** @class */
            (function() {
              function MapIterator2(keys, values, selector) {
                this._index = 0;
                this._keys = keys;
                this._values = values;
                this._selector = selector;
              }
              MapIterator2.prototype["@@iterator"] = function() {
                return this;
              };
              MapIterator2.prototype[iteratorSymbol] = function() {
                return this;
              };
              MapIterator2.prototype.next = function() {
                var index = this._index;
                if (index >= 0 && index < this._keys.length) {
                  var result = this._selector(this._keys[index], this._values[index]);
                  if (index + 1 >= this._keys.length) {
                    this._index = -1;
                    this._keys = arraySentinel;
                    this._values = arraySentinel;
                  } else {
                    this._index++;
                  }
                  return { value: result, done: false };
                }
                return { value: void 0, done: true };
              };
              MapIterator2.prototype.throw = function(error) {
                if (this._index >= 0) {
                  this._index = -1;
                  this._keys = arraySentinel;
                  this._values = arraySentinel;
                }
                throw error;
              };
              MapIterator2.prototype.return = function(value) {
                if (this._index >= 0) {
                  this._index = -1;
                  this._keys = arraySentinel;
                  this._values = arraySentinel;
                }
                return { value, done: true };
              };
              return MapIterator2;
            })()
          );
          var Map2 = (
            /** @class */
            (function() {
              function Map3() {
                this._keys = [];
                this._values = [];
                this._cacheKey = cacheSentinel;
                this._cacheIndex = -2;
              }
              Object.defineProperty(Map3.prototype, "size", {
                get: function() {
                  return this._keys.length;
                },
                enumerable: true,
                configurable: true
              });
              Map3.prototype.has = function(key) {
                return this._find(
                  key,
                  /*insert*/
                  false
                ) >= 0;
              };
              Map3.prototype.get = function(key) {
                var index = this._find(
                  key,
                  /*insert*/
                  false
                );
                return index >= 0 ? this._values[index] : void 0;
              };
              Map3.prototype.set = function(key, value) {
                var index = this._find(
                  key,
                  /*insert*/
                  true
                );
                this._values[index] = value;
                return this;
              };
              Map3.prototype.delete = function(key) {
                var index = this._find(
                  key,
                  /*insert*/
                  false
                );
                if (index >= 0) {
                  var size = this._keys.length;
                  for (var i2 = index + 1; i2 < size; i2++) {
                    this._keys[i2 - 1] = this._keys[i2];
                    this._values[i2 - 1] = this._values[i2];
                  }
                  this._keys.length--;
                  this._values.length--;
                  if (SameValueZero(key, this._cacheKey)) {
                    this._cacheKey = cacheSentinel;
                    this._cacheIndex = -2;
                  }
                  return true;
                }
                return false;
              };
              Map3.prototype.clear = function() {
                this._keys.length = 0;
                this._values.length = 0;
                this._cacheKey = cacheSentinel;
                this._cacheIndex = -2;
              };
              Map3.prototype.keys = function() {
                return new MapIterator(this._keys, this._values, getKey);
              };
              Map3.prototype.values = function() {
                return new MapIterator(this._keys, this._values, getValue);
              };
              Map3.prototype.entries = function() {
                return new MapIterator(this._keys, this._values, getEntry);
              };
              Map3.prototype["@@iterator"] = function() {
                return this.entries();
              };
              Map3.prototype[iteratorSymbol] = function() {
                return this.entries();
              };
              Map3.prototype._find = function(key, insert) {
                if (!SameValueZero(this._cacheKey, key)) {
                  this._cacheIndex = -1;
                  for (var i2 = 0; i2 < this._keys.length; i2++) {
                    if (SameValueZero(this._keys[i2], key)) {
                      this._cacheIndex = i2;
                      break;
                    }
                  }
                }
                if (this._cacheIndex < 0 && insert) {
                  this._cacheIndex = this._keys.length;
                  this._keys.push(key);
                  this._values.push(void 0);
                }
                return this._cacheIndex;
              };
              return Map3;
            })()
          );
          return Map2;
          function getKey(key, _3) {
            return key;
          }
          function getValue(_3, value) {
            return value;
          }
          function getEntry(key, value) {
            return [key, value];
          }
        }
        function CreateSetPolyfill() {
          var Set2 = (
            /** @class */
            (function() {
              function Set3() {
                this._map = new _Map();
              }
              Object.defineProperty(Set3.prototype, "size", {
                get: function() {
                  return this._map.size;
                },
                enumerable: true,
                configurable: true
              });
              Set3.prototype.has = function(value) {
                return this._map.has(value);
              };
              Set3.prototype.add = function(value) {
                return this._map.set(value, value), this;
              };
              Set3.prototype.delete = function(value) {
                return this._map.delete(value);
              };
              Set3.prototype.clear = function() {
                this._map.clear();
              };
              Set3.prototype.keys = function() {
                return this._map.keys();
              };
              Set3.prototype.values = function() {
                return this._map.keys();
              };
              Set3.prototype.entries = function() {
                return this._map.entries();
              };
              Set3.prototype["@@iterator"] = function() {
                return this.keys();
              };
              Set3.prototype[iteratorSymbol] = function() {
                return this.keys();
              };
              return Set3;
            })()
          );
          return Set2;
        }
        function CreateWeakMapPolyfill() {
          var UUID_SIZE = 16;
          var keys = HashMap.create();
          var rootKey = CreateUniqueKey();
          return (
            /** @class */
            (function() {
              function WeakMap2() {
                this._key = CreateUniqueKey();
              }
              WeakMap2.prototype.has = function(target) {
                var table = GetOrCreateWeakMapTable(
                  target,
                  /*create*/
                  false
                );
                return table !== void 0 ? HashMap.has(table, this._key) : false;
              };
              WeakMap2.prototype.get = function(target) {
                var table = GetOrCreateWeakMapTable(
                  target,
                  /*create*/
                  false
                );
                return table !== void 0 ? HashMap.get(table, this._key) : void 0;
              };
              WeakMap2.prototype.set = function(target, value) {
                var table = GetOrCreateWeakMapTable(
                  target,
                  /*create*/
                  true
                );
                table[this._key] = value;
                return this;
              };
              WeakMap2.prototype.delete = function(target) {
                var table = GetOrCreateWeakMapTable(
                  target,
                  /*create*/
                  false
                );
                return table !== void 0 ? delete table[this._key] : false;
              };
              WeakMap2.prototype.clear = function() {
                this._key = CreateUniqueKey();
              };
              return WeakMap2;
            })()
          );
          function CreateUniqueKey() {
            var key;
            do
              key = "@@WeakMap@@" + CreateUUID();
            while (HashMap.has(keys, key));
            keys[key] = true;
            return key;
          }
          function GetOrCreateWeakMapTable(target, create2) {
            if (!hasOwn.call(target, rootKey)) {
              if (!create2)
                return void 0;
              Object.defineProperty(target, rootKey, { value: HashMap.create() });
            }
            return target[rootKey];
          }
          function FillRandomBytes(buffer, size) {
            for (var i2 = 0; i2 < size; ++i2)
              buffer[i2] = Math.random() * 255 | 0;
            return buffer;
          }
          function GenRandomBytes(size) {
            if (typeof Uint8Array === "function") {
              var array = new Uint8Array(size);
              if (typeof crypto !== "undefined") {
                crypto.getRandomValues(array);
              } else if (typeof msCrypto !== "undefined") {
                msCrypto.getRandomValues(array);
              } else {
                FillRandomBytes(array, size);
              }
              return array;
            }
            return FillRandomBytes(new Array(size), size);
          }
          function CreateUUID() {
            var data = GenRandomBytes(UUID_SIZE);
            data[6] = data[6] & 79 | 64;
            data[8] = data[8] & 191 | 128;
            var result = "";
            for (var offset = 0; offset < UUID_SIZE; ++offset) {
              var byte = data[offset];
              if (offset === 4 || offset === 6 || offset === 8)
                result += "-";
              if (byte < 16)
                result += "0";
              result += byte.toString(16).toLowerCase();
            }
            return result;
          }
        }
        function MakeDictionary(obj) {
          obj.__ = void 0;
          delete obj.__;
          return obj;
        }
      });
    })(Reflect2 || (Reflect2 = {}));
  }
});

// main.ts
var main_exports = {};
__export(main_exports, {
  default: () => MetaTitlePlugin
});
module.exports = __toCommonJS(main_exports);
var import_reflect_metadata3 = __toESM(require_Reflect());
var import_obsidian21 = require("obsidian");

// src/Settings/SettingsTab.ts
var import_obsidian2 = require("obsidian");

// src/Components/EventDispatcher/Event.ts
var Event = class {
  constructor(data) {
    this.data = data;
  }
  get() {
    return this.data;
  }
  stop() {
    return;
  }
};

// src/Utils/ObjectHelper.ts
var ObjectHelper = class _ObjectHelper {
  static compare(old = {}, actual = {}) {
    const changed = {};
    const keys = [...Object.keys(old), ...Object.keys(actual)].filter((e2, i2, s3) => s3.indexOf(e2) === i2);
    for (const k3 of keys) {
      const first = old[k3];
      const second = actual[k3];
      const type = typeof (first !== void 0 ? first : second);
      const thereIsNull = first === null || second === null;
      if (type === "object" && !thereIsNull) {
        if (Array.isArray(first)) {
          const hasDiff = first.some((x3) => !second.includes(x3));
          const isChanged = hasDiff ? true : second.some((x3) => !first.includes(x3));
          if (isChanged) {
            changed[k3] = true;
          }
        } else {
          const c3 = _ObjectHelper.compare(first, second);
          if (Object.values(c3).some((e2) => e2 !== false)) {
            changed[k3] = c3;
          }
        }
      } else {
        if (first !== second) {
          changed[k3] = true;
        }
      }
    }
    return changed;
  }
  static fillFrom(to, from) {
    for (const [k3, v3] of Object.entries(to)) {
      const fv = from[k3];
      if (fv === void 0) {
        continue;
      }
      const type = typeof v3;
      if (type === "object" && !Array.isArray(fv) && v3 !== null) {
        if (fv !== null) {
          _ObjectHelper.fillFrom(v3, fv);
        }
        continue;
      }
      to[k3] = fv;
    }
    return to;
  }
  static entries(object) {
    return Object.entries(object);
  }
  static values(object) {
    return Object.values(object);
  }
};

// node_modules/inversify/lib/esm/index.js
var import_reflect_metadata = __toESM(require_Reflect(), 1);

// node_modules/@inversifyjs/common/lib/esm/index.js
var t = /* @__PURE__ */ Symbol.for("@inversifyjs/common/islazyServiceIdentifier");
var _a, _e;
_a = t;
var r = class {
  constructor(e2) {
    __publicField(this, _a);
    __privateAdd(this, _e);
    __privateSet(this, _e, e2), this[t] = true;
  }
  static is(e2) {
    return "object" == typeof e2 && null !== e2 && true === e2[t];
  }
  unwrap() {
    return __privateGet(this, _e).call(this);
  }
};
_e = new WeakMap();

// node_modules/@inversifyjs/reflect-metadata-utils/lib/esm/index.js
function t2(t5, e2) {
  return Reflect.getMetadata(e2, t5);
}
function e(e2, n, a3, c3) {
  var _a4;
  const f3 = c3((_a4 = t2(e2, n)) != null ? _a4 : a3);
  Reflect.defineMetadata(n, f3, e2);
}

// node_modules/@inversifyjs/core/lib/esm/index.js
var a = "named";
var r2 = "name";
var i = "unmanaged";
var o = "optional";
var s = "inject";
var c = "multi_inject";
var u = "post_construct";
var d = "pre_destroy";
var m = [s, c, r2, i, a, o];
var l = /* @__PURE__ */ Symbol.for("@inversifyjs/core/InversifyCoreError");
var _a2, _b;
var f = class _f extends (_b = Error, _a2 = l, _b) {
  constructor(t5, e2, n) {
    super(e2, n);
    __publicField(this, _a2);
    __publicField(this, "kind");
    this[l] = true, this.kind = t5;
  }
  static is(t5) {
    return "object" == typeof t5 && null !== t5 && true === t5[l];
  }
  static isErrorOfKind(t5, e2) {
    return _f.is(t5) && t5.kind === e2;
  }
};
var g;
var p;
function h(t5, e2) {
  const n = [];
  for (let t6 = 0; t6 < e2.length; ++t6) {
    void 0 === e2[t6] && n.push(t6);
  }
  if (n.length > 0) throw new f(g.missingInjectionDecorator, `Found unexpected missing metadata on type "${t5.name}" at constructor indexes "${n.join('", "')}".

Are you using @inject, @multiInject or @unmanaged decorators at those indexes?

If you're using typescript and want to rely on auto injection, set "emitDecoratorMetadata" compiler option to true`);
}
function y(t5) {
  return { kind: p.singleInjection, name: void 0, optional: false, tags: /* @__PURE__ */ new Map(), targetName: void 0, value: t5 };
}
function v(t5) {
  const e2 = t5.find(((t6) => t6.key === s)), n = t5.find(((t6) => t6.key === c));
  if (void 0 !== t5.find(((t6) => t6.key === i))) return (function(t6, e3) {
    if (void 0 !== e3 || void 0 !== t6) throw new f(g.missingInjectionDecorator, "Expected a single @inject, @multiInject or @unmanaged metadata");
    return { kind: p.unmanaged };
  })(e2, n);
  if (void 0 === n && void 0 === e2) throw new f(g.missingInjectionDecorator, "Expected @inject, @multiInject or @unmanaged metadata");
  const u3 = t5.find(((t6) => t6.key === a)), d3 = t5.find(((t6) => t6.key === o)), l3 = t5.find(((t6) => t6.key === r2));
  return { kind: void 0 === e2 ? p.multipleInjection : p.singleInjection, name: u3 == null ? void 0 : u3.value, optional: void 0 !== d3, tags: new Map(t5.filter(((t6) => m.every(((e3) => t6.key !== e3)))).map(((t6) => [t6.key, t6.value]))), targetName: l3 == null ? void 0 : l3.value, value: void 0 === e2 ? n == null ? void 0 : n.value : e2.value };
}
function j(t5, e2, n) {
  try {
    return v(n);
  } catch (n2) {
    throw f.isErrorOfKind(n2, g.missingInjectionDecorator) ? new f(g.missingInjectionDecorator, `Expected a single @inject, @multiInject or @unmanaged decorator at type "${t5.name}" at constructor arguments at index "${e2.toString()}"`, { cause: n2 }) : n2;
  }
}
function k(e2) {
  const n = t2(e2, "design:paramtypes"), a3 = t2(e2, "inversify:tagged"), r3 = [];
  if (void 0 !== a3) for (const [t5, n2] of Object.entries(a3)) {
    const a4 = parseInt(t5);
    r3[a4] = j(e2, a4, n2);
  }
  if (void 0 !== n) {
    for (let t5 = 0; t5 < n.length; ++t5) if (void 0 === r3[t5]) {
      const e3 = n[t5];
      r3[t5] = y(e3);
    }
  }
  return h(e2, r3), r3;
}
function I(t5, e2, n) {
  try {
    return v(n);
  } catch (n2) {
    throw f.isErrorOfKind(n2, g.missingInjectionDecorator) ? new f(g.missingInjectionDecorator, `Expected a single @inject, @multiInject or @unmanaged decorator at type "${t5.name}" at property "${e2.toString()}"`, { cause: n2 }) : n2;
  }
}
function w(e2) {
  const n = t2(e2, "inversify:tagged_props"), a3 = /* @__PURE__ */ new Map();
  if (void 0 !== n) for (const t5 of Reflect.ownKeys(n)) {
    const r3 = n[t5];
    a3.set(t5, I(e2, t5, r3));
  }
  return a3;
}
function M(e2) {
  const n = t2(e2, u), a3 = t2(e2, d);
  return { constructorArguments: k(e2), lifecycle: { postConstructMethodName: n == null ? void 0 : n.value, preDestroyMethodName: a3 == null ? void 0 : a3.value }, properties: w(e2) };
}
function N(t5, e2) {
  const n = e2.getConstructorMetadata(t5), a3 = [];
  for (const [e3, r3] of Object.entries(n.userGeneratedMetadata)) {
    const n2 = parseInt(e3);
    a3[n2] = j(t5, n2, r3);
  }
  if (void 0 !== n.compilerGeneratedMetadata) {
    for (let t6 = 0; t6 < n.compilerGeneratedMetadata.length; ++t6) if (void 0 === a3[t6]) {
      const e3 = n.compilerGeneratedMetadata[t6];
      a3[t6] = y(e3);
    }
  }
  return h(t5, a3), a3;
}
function E(t5, e2) {
  const n = e2.getPropertiesMetadata(t5), a3 = /* @__PURE__ */ new Map();
  for (const e3 of Reflect.ownKeys(n)) {
    const r3 = n[e3];
    a3.set(e3, I(t5, e3, r3));
  }
  return a3;
}
function D(e2, n) {
  const a3 = t2(e2, u), r3 = t2(e2, d);
  return { constructorArguments: N(e2, n), lifecycle: { postConstructMethodName: a3 == null ? void 0 : a3.value, preDestroyMethodName: r3 == null ? void 0 : r3.value }, properties: E(e2, n) };
}
function b(t5) {
  const e2 = Object.getPrototypeOf(t5.prototype), n = e2 == null ? void 0 : e2.constructor;
  return n;
}
function O(t5) {
  return t5.kind === p.unmanaged ? [{ key: i, value: true }] : (function(t6) {
    const e2 = [A(t6)];
    void 0 !== t6.name && e2.push({ key: a, value: t6.name });
    t6.optional && e2.push({ key: o, value: true });
    for (const [n, a3] of t6.tags) e2.push({ key: n, value: a3 });
    void 0 !== t6.targetName && e2.push({ key: r2, value: t6.targetName });
    return e2;
  })(t5);
}
function A(t5) {
  let e2;
  switch (t5.kind) {
    case p.multipleInjection:
      e2 = { key: c, value: t5.value };
      break;
    case p.singleInjection:
      e2 = { key: s, value: t5.value };
  }
  return e2;
}
!(function(t5) {
  t5[t5.injectionDecoratorConflict = 0] = "injectionDecoratorConflict", t5[t5.missingInjectionDecorator = 1] = "missingInjectionDecorator", t5[t5.planning = 2] = "planning", t5[t5.unknown = 3] = "unknown";
})(g || (g = {})), (function(t5) {
  t5[t5.multipleInjection = 0] = "multipleInjection", t5[t5.singleInjection = 1] = "singleInjection", t5[t5.unmanaged = 2] = "unmanaged";
})(p || (p = {}));
var _t;
var x = class {
  constructor(t5) {
    __privateAdd(this, _t);
    __privateSet(this, _t, t5);
  }
  startsWith(t5) {
    return __privateGet(this, _t).startsWith(t5);
  }
  endsWith(t5) {
    return __privateGet(this, _t).endsWith(t5);
  }
  contains(t5) {
    return __privateGet(this, _t).includes(t5);
  }
  equals(t5) {
    return __privateGet(this, _t) === t5;
  }
  value() {
    return __privateGet(this, _t);
  }
};
_t = new WeakMap();
var C = "@inversifyjs/core/targetId";
var _e2, _n, _a3, _r, _i, _o;
var T = class {
  constructor(n, a3, r3) {
    __privateAdd(this, _e2);
    __privateAdd(this, _n);
    __privateAdd(this, _a3);
    __privateAdd(this, _r);
    __privateAdd(this, _i);
    __privateAdd(this, _o);
    __privateSet(this, _n, (function() {
      var _a4;
      const n2 = (_a4 = t2(Object, C)) != null ? _a4 : 0;
      return n2 === Number.MAX_SAFE_INTEGER ? e(Object, C, n2, (() => Number.MIN_SAFE_INTEGER)) : e(Object, C, n2, ((t5) => t5 + 1)), n2;
    })()), __privateSet(this, _a3, n), __privateSet(this, _r, void 0), __privateSet(this, _e2, a3), __privateSet(this, _i, new x("string" == typeof n ? n : n.toString().slice(7, -1))), __privateSet(this, _o, r3);
  }
  get id() {
    return __privateGet(this, _n);
  }
  get identifier() {
    return __privateGet(this, _a3);
  }
  get metadata() {
    return void 0 === __privateGet(this, _r) && __privateSet(this, _r, O(__privateGet(this, _e2))), __privateGet(this, _r);
  }
  get name() {
    return __privateGet(this, _i);
  }
  get type() {
    return __privateGet(this, _o);
  }
  get serviceIdentifier() {
    return r.is(__privateGet(this, _e2).value) ? __privateGet(this, _e2).value.unwrap() : __privateGet(this, _e2).value;
  }
  getCustomTags() {
    return [...__privateGet(this, _e2).tags.entries()].map((([t5, e2]) => ({ key: t5, value: e2 })));
  }
  getNamedTag() {
    return void 0 === __privateGet(this, _e2).name ? null : { key: a, value: __privateGet(this, _e2).name };
  }
  hasTag(t5) {
    return this.metadata.some(((e2) => e2.key === t5));
  }
  isArray() {
    return __privateGet(this, _e2).kind === p.multipleInjection;
  }
  isNamed() {
    return void 0 !== __privateGet(this, _e2).name;
  }
  isOptional() {
    return __privateGet(this, _e2).optional;
  }
  isTagged() {
    return __privateGet(this, _e2).tags.size > 0;
  }
  matchesArray(t5) {
    return this.isArray() && __privateGet(this, _e2).value === t5;
  }
  matchesNamedTag(t5) {
    return __privateGet(this, _e2).name === t5;
  }
  matchesTag(t5) {
    return (e2) => this.metadata.some(((n) => n.key === t5 && n.value === e2));
  }
};
_e2 = new WeakMap();
_n = new WeakMap();
_a3 = new WeakMap();
_r = new WeakMap();
_i = new WeakMap();
_o = new WeakMap();
var _ = (t5) => /* @__PURE__ */ (function(t6, e2) {
  return function(n) {
    var _a4, _b2;
    const a3 = t6(n);
    let r3 = b(n);
    for (; void 0 !== r3 && r3 !== Object; ) {
      const t7 = e2(r3);
      for (const [e3, n2] of t7) a3.properties.has(e3) || a3.properties.set(e3, n2);
      r3 = b(r3);
    }
    const i2 = [];
    for (const t7 of a3.constructorArguments) if (t7.kind !== p.unmanaged) {
      const e3 = (_a4 = t7.targetName) != null ? _a4 : "";
      i2.push(new T(e3, t7, "ConstructorArgument"));
    }
    for (const [t7, e3] of a3.properties) if (e3.kind !== p.unmanaged) {
      const n2 = (_b2 = e3.targetName) != null ? _b2 : t7;
      i2.push(new T(n2, e3, "ClassProperty"));
    }
    return i2;
  };
})(void 0 === t5 ? M : (e2) => D(e2, t5), void 0 === t5 ? w : (e2) => E(e2, t5));

// node_modules/inversify/lib/esm/index.js
var s2 = "named";
var o2 = "name";
var a2 = "unmanaged";
var c2 = "optional";
var d2 = "inject";
var u2 = "multi_inject";
var h2 = "inversify:tagged";
var l2 = "inversify:tagged_props";
var g2 = "inversify:paramtypes";
var y2 = "design:paramtypes";
var p2 = "post_construct";
var f2 = "pre_destroy";
var _2 = [d2, u2, o2, a2, s2, c2];
var v2 = Object.freeze({ __proto__: null, DESIGN_PARAM_TYPES: y2, INJECT_TAG: d2, MULTI_INJECT_TAG: u2, NAMED_TAG: s2, NAME_TAG: o2, NON_CUSTOM_TAG_KEYS: _2, OPTIONAL_TAG: c2, PARAM_TYPES: g2, POST_CONSTRUCT: p2, PRE_DESTROY: f2, TAGGED: h2, TAGGED_PROP: l2, UNMANAGED_TAG: a2 });
var b2 = { Request: "Request", Singleton: "Singleton", Transient: "Transient" };
var w2 = { ConstantValue: "ConstantValue", Constructor: "Constructor", DynamicValue: "DynamicValue", Factory: "Factory", Function: "Function", Instance: "Instance", Invalid: "Invalid", Provider: "Provider" };
var m2 = { ClassProperty: "ClassProperty", ConstructorArgument: "ConstructorArgument", Variable: "Variable" };
var A2 = 0;
function S() {
  return A2++;
}
var I2 = class _I {
  constructor(t5, n) {
    __publicField(this, "id");
    __publicField(this, "moduleId");
    __publicField(this, "activated");
    __publicField(this, "serviceIdentifier");
    __publicField(this, "implementationType");
    __publicField(this, "cache");
    __publicField(this, "dynamicValue");
    __publicField(this, "scope");
    __publicField(this, "type");
    __publicField(this, "factory");
    __publicField(this, "provider");
    __publicField(this, "constraint");
    __publicField(this, "onActivation");
    __publicField(this, "onDeactivation");
    this.id = S(), this.activated = false, this.serviceIdentifier = t5, this.scope = n, this.type = w2.Invalid, this.constraint = (t6) => true, this.implementationType = null, this.cache = null, this.factory = null, this.provider = null, this.onActivation = null, this.onDeactivation = null, this.dynamicValue = null;
  }
  clone() {
    const t5 = new _I(this.serviceIdentifier, this.scope);
    return t5.activated = t5.scope === b2.Singleton && this.activated, t5.implementationType = this.implementationType, t5.dynamicValue = this.dynamicValue, t5.scope = this.scope, t5.type = this.type, t5.factory = this.factory, t5.provider = this.provider, t5.constraint = this.constraint, t5.onActivation = this.onActivation, t5.onDeactivation = this.onDeactivation, t5.cache = this.cache, t5;
  }
};
var T2 = "Metadata key was used more than once in a parameter:";
var D2 = "NULL argument";
var N2 = "Key Not Found";
var C2 = "Ambiguous match found for serviceIdentifier:";
var x2 = "No matching bindings found for serviceIdentifier:";
var R = "The @inject @multiInject @tagged and @named decorators must be applied to the parameters of a class constructor or a class property.";
var E2 = (t5, n) => `onDeactivation() error in class ${t5}: ${n}`;
var M2 = class {
  getConstructorMetadata(t5) {
    var _a4, _b2;
    return { compilerGeneratedMetadata: (_a4 = Reflect.getMetadata(y2, t5)) != null ? _a4 : [], userGeneratedMetadata: (_b2 = Reflect.getMetadata(h2, t5)) != null ? _b2 : {} };
  }
  getPropertiesMetadata(t5) {
    var _a4;
    return (_a4 = Reflect.getMetadata(l2, t5)) != null ? _a4 : {};
  }
};
var B;
function P(t5) {
  return t5 instanceof RangeError || "Maximum call stack size exceeded" === t5.message;
}
!(function(t5) {
  t5[t5.MultipleBindingsAvailable = 2] = "MultipleBindingsAvailable", t5[t5.NoBindingsAvailable = 0] = "NoBindingsAvailable", t5[t5.OnlyOneBindingAvailable = 1] = "OnlyOneBindingAvailable";
})(B || (B = {}));
function k2(t5) {
  return "function" == typeof t5 ? t5.name : "symbol" == typeof t5 ? t5.toString() : t5;
}
function q(t5, n, e2) {
  let i2 = "";
  const r3 = e2(t5, n);
  return 0 !== r3.length && (i2 = "\nRegistered bindings:", r3.forEach(((t6) => {
    let n2 = "Object";
    null !== t6.implementationType && (n2 = O2(t6.implementationType)), i2 = `${i2}
 ${n2}`, t6.constraint.metaData && (i2 = `${i2} - ${t6.constraint.metaData}`);
  }))), i2;
}
function F(t5, n) {
  return null !== t5.parentRequest && (t5.parentRequest.serviceIdentifier === n || F(t5.parentRequest, n));
}
function j2(t5) {
  t5.childRequests.forEach(((n) => {
    if (F(t5, n.serviceIdentifier)) {
      const t6 = (function(t7) {
        const n2 = (function t8(n3, e2 = []) {
          const i2 = k2(n3.serviceIdentifier);
          return e2.push(i2), null !== n3.parentRequest ? t8(n3.parentRequest, e2) : e2;
        })(t7);
        return n2.reverse().join(" --> ");
      })(n);
      throw new Error(`Circular dependency found: ${t6}`);
    }
    j2(n);
  }));
}
function O2(t5) {
  if (null != t5.name && "" !== t5.name) return t5.name;
  {
    const n = t5.toString(), e2 = n.match(/^function\s*([^\s(]+)/);
    return null === e2 ? `Anonymous function: ${n}` : e2[1];
  }
}
function $(t5) {
  return `{"key":"${t5.key.toString()}","value":"${t5.value.toString()}"}`;
}
var V = class {
  constructor(t5) {
    __publicField(this, "id");
    __publicField(this, "container");
    __publicField(this, "plan");
    __publicField(this, "currentRequest");
    this.id = S(), this.container = t5;
  }
  addPlan(t5) {
    this.plan = t5;
  }
  setCurrentRequest(t5) {
    this.currentRequest = t5;
  }
};
var W = class {
  constructor(t5, n) {
    __publicField(this, "key");
    __publicField(this, "value");
    this.key = t5, this.value = n;
  }
  toString() {
    return this.key === s2 ? `named: ${String(this.value).toString()} ` : `tagged: { key:${this.key.toString()}, value: ${String(this.value)} }`;
  }
};
var G = class {
  constructor(t5, n) {
    __publicField(this, "parentContext");
    __publicField(this, "rootRequest");
    this.parentContext = t5, this.rootRequest = n;
  }
};
function K(t5, e2) {
  const i2 = (function(t6) {
    const n = Object.getPrototypeOf(t6.prototype), e3 = n == null ? void 0 : n.constructor;
    return e3;
  })(e2);
  if (void 0 === i2 || i2 === Object) return 0;
  const r3 = _(t5)(i2), s3 = r3.map(((t6) => t6.metadata.filter(((t7) => t7.key === a2)))), o3 = [].concat.apply([], s3).length, c3 = r3.length - o3;
  return c3 > 0 ? c3 : K(t5, i2);
}
var H = class _H {
  constructor(t5, n, e2, i2, r3) {
    __publicField(this, "id");
    __publicField(this, "serviceIdentifier");
    __publicField(this, "parentContext");
    __publicField(this, "parentRequest");
    __publicField(this, "bindings");
    __publicField(this, "childRequests");
    __publicField(this, "target");
    __publicField(this, "requestScope");
    this.id = S(), this.serviceIdentifier = t5, this.parentContext = n, this.parentRequest = e2, this.target = r3, this.childRequests = [], this.bindings = Array.isArray(i2) ? i2 : [i2], this.requestScope = null === e2 ? /* @__PURE__ */ new Map() : null;
  }
  addChildRequest(t5, n, e2) {
    const i2 = new _H(t5, this.parentContext, this, n, e2);
    return this.childRequests.push(i2), i2;
  }
};
function U(t5) {
  return t5._bindingDictionary;
}
function L(t5, n, e2, i2, r3) {
  let s3 = J(e2.container, r3.serviceIdentifier), o3 = [];
  return s3.length === B.NoBindingsAvailable && true === e2.container.options.autoBindInjectable && "function" == typeof r3.serviceIdentifier && t5.getConstructorMetadata(r3.serviceIdentifier).compilerGeneratedMetadata && (e2.container.bind(r3.serviceIdentifier).toSelf(), s3 = J(e2.container, r3.serviceIdentifier)), o3 = n ? s3 : s3.filter(((t6) => {
    const n2 = new H(t6.serviceIdentifier, e2, i2, t6, r3);
    return t6.constraint(n2);
  })), (function(t6, n2, e3, i3, r4) {
    switch (n2.length) {
      case B.NoBindingsAvailable:
        if (i3.isOptional()) return n2;
        {
          const n3 = k2(t6);
          let s4 = x2;
          throw s4 += (function(t7, n4) {
            if (n4.isTagged() || n4.isNamed()) {
              let e4 = "";
              const i4 = n4.getNamedTag(), r5 = n4.getCustomTags();
              return null !== i4 && (e4 += $(i4) + "\n"), null !== r5 && r5.forEach(((t8) => {
                e4 += $(t8) + "\n";
              })), ` ${t7}
 ${t7} - ${e4}`;
            }
            return ` ${t7}`;
          })(n3, i3), s4 += q(r4, n3, J), null !== e3 && (s4 += `
Trying to resolve bindings for "${k2(e3.serviceIdentifier)}"`), new Error(s4);
        }
      case B.OnlyOneBindingAvailable:
        return n2;
      case B.MultipleBindingsAvailable:
      default:
        if (i3.isArray()) return n2;
        {
          const n3 = k2(t6);
          let e4 = `${C2} ${n3}`;
          throw e4 += q(r4, n3, J), new Error(e4);
        }
    }
  })(r3.serviceIdentifier, o3, i2, r3, e2.container), o3;
}
function Y(t5, n) {
  const e2 = n.isMultiInject ? u2 : d2, i2 = [new W(e2, t5)];
  return void 0 !== n.customTag && i2.push(new W(n.customTag.key, n.customTag.value)), true === n.isOptional && i2.push(new W(c2, true)), i2;
}
function z(t5, e2, i2, r3, s3, o3) {
  let a3, c3;
  if (null === s3) {
    a3 = L(t5, e2, r3, null, o3), c3 = new H(i2, r3, null, a3, o3);
    const n = new G(r3, c3);
    r3.addPlan(n);
  } else a3 = L(t5, e2, r3, s3, o3), c3 = s3.addChildRequest(o3.serviceIdentifier, a3, o3);
  a3.forEach(((e3) => {
    let i3 = null;
    if (o3.isArray()) i3 = c3.addChildRequest(e3.serviceIdentifier, e3, o3);
    else {
      if (null !== e3.cache) return;
      i3 = c3;
    }
    if (e3.type === w2.Instance && null !== e3.implementationType) {
      const s4 = (function(t6, e4) {
        return _(t6)(e4);
      })(t5, e3.implementationType);
      if (true !== r3.container.options.skipBaseClassChecks) {
        const n = K(t5, e3.implementationType);
        if (s4.length < n) {
          const t6 = `The number of constructor arguments in the derived class ${O2(e3.implementationType)} must be >= than the number of constructor arguments of its base class.`;
          throw new Error(t6);
        }
      }
      s4.forEach(((n) => {
        z(t5, false, n.serviceIdentifier, r3, i3, n);
      }));
    }
  }));
}
function J(t5, n) {
  let e2 = [];
  const i2 = U(t5);
  return i2.hasKey(n) ? e2 = i2.get(n) : null !== t5.parent && (e2 = J(t5.parent, n)), e2;
}
function Q(t5, n, s3, o3, a3, c3 = false) {
  const d3 = new V(n), u3 = (function(t6, n2, s4) {
    const o4 = Y(n2, s4), a4 = v(o4);
    if (a4.kind === p.unmanaged) throw new Error("Unexpected metadata when creating target");
    return new T("", a4, t6);
  })(s3, o3, a3);
  try {
    return z(t5, c3, o3, d3, null, u3), d3;
  } catch (t6) {
    throw P(t6) && j2(d3.plan.rootRequest), t6;
  }
}
function X(t5) {
  return ("object" == typeof t5 && null !== t5 || "function" == typeof t5) && "function" == typeof t5.then;
}
function Z(t5) {
  return !!X(t5) || Array.isArray(t5) && t5.some(X);
}
var tt = (t5, n, e2) => {
  t5.has(n.id) || t5.set(n.id, e2);
};
var nt = (t5, n) => {
  t5.cache = n, t5.activated = true, X(n) && et(t5, n);
};
var et = async (t5, n) => {
  try {
    const e2 = await n;
    t5.cache = e2;
  } catch (n2) {
    throw t5.cache = null, t5.activated = false, n2;
  }
};
var it;
!(function(t5) {
  t5.DynamicValue = "toDynamicValue", t5.Factory = "toFactory", t5.Provider = "toProvider";
})(it || (it = {}));
function st(t5, n, e2) {
  let i2;
  if (n.length > 0) {
    const r3 = (function(t6, n2) {
      return t6.reduce(((t7, e3) => {
        const i3 = n2(e3);
        return e3.target.type === m2.ConstructorArgument ? t7.constructorInjections.push(i3) : (t7.propertyRequests.push(e3), t7.propertyInjections.push(i3)), t7.isAsync || (t7.isAsync = Z(i3)), t7;
      }), { constructorInjections: [], isAsync: false, propertyInjections: [], propertyRequests: [] });
    })(n, e2), s3 = { ...r3, constr: t5 };
    i2 = r3.isAsync ? (async function(t6) {
      const n2 = await at(t6.constructorInjections), e3 = await at(t6.propertyInjections);
      return ot({ ...t6, constructorInjections: n2, propertyInjections: e3 });
    })(s3) : ot(s3);
  } else i2 = new t5();
  return i2;
}
function ot(t5) {
  const n = new t5.constr(...t5.constructorInjections);
  return t5.propertyRequests.forEach(((e2, i2) => {
    const r3 = e2.target.identifier, s3 = t5.propertyInjections[i2];
    e2.target.isOptional() && void 0 === s3 || (n[r3] = s3);
  })), n;
}
async function at(t5) {
  const n = [];
  for (const e2 of t5) Array.isArray(e2) ? n.push(Promise.all(e2)) : n.push(e2);
  return Promise.all(n);
}
function ct(t5, n) {
  const e2 = (function(t6, n2) {
    var _a4;
    if (Reflect.hasMetadata(p2, t6)) {
      const r3 = Reflect.getMetadata(p2, t6);
      try {
        return (_a4 = n2[r3.value]) == null ? void 0 : _a4.call(n2);
      } catch (n3) {
        if (n3 instanceof Error) throw new Error((e3 = t6.name, i2 = n3.message, `@postConstruct error in class ${e3}: ${i2}`));
      }
    }
    var e3, i2;
  })(t5, n);
  return X(e2) ? e2.then((() => n)) : n;
}
function dt(t5, n) {
  t5.scope !== b2.Singleton && (function(t6, n2) {
    const e2 = `Class cannot be instantiated in ${t6.scope === b2.Request ? "request" : "transient"} scope.`;
    if ("function" == typeof t6.onDeactivation) throw new Error(E2(n2.name, e2));
    if (Reflect.hasMetadata(f2, n2)) throw new Error(`@preDestroy error in class ${n2.name}: ${e2}`);
  })(t5, n);
}
var ut = (t5) => (n) => {
  n.parentContext.setCurrentRequest(n);
  const e2 = n.bindings, i2 = n.childRequests, r3 = n.target && n.target.isArray(), s3 = !(n.parentRequest && n.parentRequest.target && n.target && n.parentRequest.target.matchesArray(n.target.serviceIdentifier));
  if (r3 && s3) return i2.map(((n2) => ut(t5)(n2)));
  {
    if (n.target.isOptional() && 0 === e2.length) return;
    const i3 = e2[0];
    return yt(t5, n, i3);
  }
};
var ht = (t5, n) => {
  const e2 = ((t6) => {
    switch (t6.type) {
      case w2.Factory:
        return { factory: t6.factory, factoryType: it.Factory };
      case w2.Provider:
        return { factory: t6.provider, factoryType: it.Provider };
      case w2.DynamicValue:
        return { factory: t6.dynamicValue, factoryType: it.DynamicValue };
      default:
        throw new Error(`Unexpected factory type ${t6.type}`);
    }
  })(t5);
  return ((t6, n2) => {
    try {
      return t6();
    } catch (t7) {
      if (P(t7)) throw n2();
      throw t7;
    }
  })((() => e2.factory.bind(t5)(n)), (() => {
    return new Error((t6 = e2.factoryType, i2 = n.currentRequest.serviceIdentifier.toString(), `It looks like there is a circular dependency in one of the '${t6}' bindings. Please investigate bindings with service identifier '${i2}'.`));
    var t6, i2;
  }));
};
var lt = (t5, n, e2) => {
  let i2;
  const r3 = n.childRequests;
  switch (((t6) => {
    let n2 = null;
    switch (t6.type) {
      case w2.ConstantValue:
      case w2.Function:
        n2 = t6.cache;
        break;
      case w2.Constructor:
      case w2.Instance:
        n2 = t6.implementationType;
        break;
      case w2.DynamicValue:
        n2 = t6.dynamicValue;
        break;
      case w2.Provider:
        n2 = t6.provider;
        break;
      case w2.Factory:
        n2 = t6.factory;
    }
    if (null === n2) {
      const n3 = k2(t6.serviceIdentifier);
      throw new Error(`Invalid binding type: ${n3}`);
    }
  })(e2), e2.type) {
    case w2.ConstantValue:
    case w2.Function:
      i2 = e2.cache;
      break;
    case w2.Constructor:
      i2 = e2.implementationType;
      break;
    case w2.Instance:
      i2 = (function(t6, n2, e3, i3) {
        dt(t6, n2);
        const r4 = st(n2, e3, i3);
        return X(r4) ? r4.then(((t7) => ct(n2, t7))) : ct(n2, r4);
      })(e2, e2.implementationType, r3, ut(t5));
      break;
    default:
      i2 = ht(e2, n.parentContext);
  }
  return i2;
};
var gt = (t5, n, e2) => {
  let i2 = ((t6, n2) => n2.scope === b2.Singleton && n2.activated ? n2.cache : n2.scope === b2.Request && t6.has(n2.id) ? t6.get(n2.id) : null)(t5, n);
  return null !== i2 || (i2 = e2(), ((t6, n2, e3) => {
    n2.scope === b2.Singleton && nt(n2, e3), n2.scope === b2.Request && tt(t6, n2, e3);
  })(t5, n, i2)), i2;
};
var yt = (t5, n, e2) => gt(t5, e2, (() => {
  let i2 = lt(t5, n, e2);
  return i2 = X(i2) ? i2.then(((t6) => pt(n, e2, t6))) : pt(n, e2, i2), i2;
}));
function pt(t5, n, e2) {
  let i2 = ft(t5.parentContext, n, e2);
  const r3 = wt(t5.parentContext.container);
  let s3, o3 = r3.next();
  do {
    s3 = o3.value;
    const n2 = t5.parentContext, e3 = t5.serviceIdentifier, a3 = bt(s3, e3);
    i2 = X(i2) ? vt(a3, n2, i2) : _t2(a3, n2, i2), o3 = r3.next();
  } while (true !== o3.done && !U(s3).hasKey(t5.serviceIdentifier));
  return i2;
}
var ft = (t5, n, e2) => {
  let i2;
  return i2 = "function" == typeof n.onActivation ? n.onActivation(t5, e2) : e2, i2;
};
var _t2 = (t5, n, e2) => {
  let i2 = t5.next();
  for (; true !== i2.done; ) {
    if (X(e2 = i2.value(n, e2))) return vt(t5, n, e2);
    i2 = t5.next();
  }
  return e2;
};
var vt = async (t5, n, e2) => {
  let i2 = await e2, r3 = t5.next();
  for (; true !== r3.done; ) i2 = await r3.value(n, i2), r3 = t5.next();
  return i2;
};
var bt = (t5, n) => {
  const e2 = t5._activations;
  return e2.hasKey(n) ? e2.get(n).values() : [].values();
};
var wt = (t5) => {
  const n = [t5];
  let e2 = t5.parent;
  for (; null !== e2; ) n.push(e2), e2 = e2.parent;
  return { next: () => {
    const t6 = n.pop();
    return void 0 !== t6 ? { done: false, value: t6 } : { done: true, value: void 0 };
  } };
};
var mt = (t5, n) => {
  const e2 = t5.parentRequest;
  return null !== e2 && (!!n(e2) || mt(e2, n));
};
var At = (t5) => (n) => {
  const e2 = (e3) => null !== e3 && null !== e3.target && e3.target.matchesTag(t5)(n);
  return e2.metaData = new W(t5, n), e2;
};
var St = At(s2);
var It = (t5) => (n) => {
  let e2 = null;
  if (null !== n) {
    if (e2 = n.bindings[0], "string" == typeof t5) return e2.serviceIdentifier === t5;
    {
      const e3 = n.bindings[0].implementationType;
      return t5 === e3;
    }
  }
  return false;
};
var Tt = class {
  constructor(t5) {
    __publicField(this, "_binding");
    this._binding = t5;
  }
  when(t5) {
    return this._binding.constraint = t5, new Dt(this._binding);
  }
  whenTargetNamed(t5) {
    return this._binding.constraint = St(t5), new Dt(this._binding);
  }
  whenTargetIsDefault() {
    return this._binding.constraint = (t5) => {
      if (null === t5) return false;
      return null !== t5.target && !t5.target.isNamed() && !t5.target.isTagged();
    }, new Dt(this._binding);
  }
  whenTargetTagged(t5, n) {
    return this._binding.constraint = At(t5)(n), new Dt(this._binding);
  }
  whenInjectedInto(t5) {
    return this._binding.constraint = (n) => null !== n && It(t5)(n.parentRequest), new Dt(this._binding);
  }
  whenParentNamed(t5) {
    return this._binding.constraint = (n) => null !== n && St(t5)(n.parentRequest), new Dt(this._binding);
  }
  whenParentTagged(t5, n) {
    return this._binding.constraint = (e2) => null !== e2 && At(t5)(n)(e2.parentRequest), new Dt(this._binding);
  }
  whenAnyAncestorIs(t5) {
    return this._binding.constraint = (n) => null !== n && mt(n, It(t5)), new Dt(this._binding);
  }
  whenNoAncestorIs(t5) {
    return this._binding.constraint = (n) => null !== n && !mt(n, It(t5)), new Dt(this._binding);
  }
  whenAnyAncestorNamed(t5) {
    return this._binding.constraint = (n) => null !== n && mt(n, St(t5)), new Dt(this._binding);
  }
  whenNoAncestorNamed(t5) {
    return this._binding.constraint = (n) => null !== n && !mt(n, St(t5)), new Dt(this._binding);
  }
  whenAnyAncestorTagged(t5, n) {
    return this._binding.constraint = (e2) => null !== e2 && mt(e2, At(t5)(n)), new Dt(this._binding);
  }
  whenNoAncestorTagged(t5, n) {
    return this._binding.constraint = (e2) => null !== e2 && !mt(e2, At(t5)(n)), new Dt(this._binding);
  }
  whenAnyAncestorMatches(t5) {
    return this._binding.constraint = (n) => null !== n && mt(n, t5), new Dt(this._binding);
  }
  whenNoAncestorMatches(t5) {
    return this._binding.constraint = (n) => null !== n && !mt(n, t5), new Dt(this._binding);
  }
};
var Dt = class {
  constructor(t5) {
    __publicField(this, "_binding");
    this._binding = t5;
  }
  onActivation(t5) {
    return this._binding.onActivation = t5, new Tt(this._binding);
  }
  onDeactivation(t5) {
    return this._binding.onDeactivation = t5, new Tt(this._binding);
  }
};
var Nt = class {
  constructor(t5) {
    __publicField(this, "_bindingWhenSyntax");
    __publicField(this, "_bindingOnSyntax");
    __publicField(this, "_binding");
    this._binding = t5, this._bindingWhenSyntax = new Tt(this._binding), this._bindingOnSyntax = new Dt(this._binding);
  }
  when(t5) {
    return this._bindingWhenSyntax.when(t5);
  }
  whenTargetNamed(t5) {
    return this._bindingWhenSyntax.whenTargetNamed(t5);
  }
  whenTargetIsDefault() {
    return this._bindingWhenSyntax.whenTargetIsDefault();
  }
  whenTargetTagged(t5, n) {
    return this._bindingWhenSyntax.whenTargetTagged(t5, n);
  }
  whenInjectedInto(t5) {
    return this._bindingWhenSyntax.whenInjectedInto(t5);
  }
  whenParentNamed(t5) {
    return this._bindingWhenSyntax.whenParentNamed(t5);
  }
  whenParentTagged(t5, n) {
    return this._bindingWhenSyntax.whenParentTagged(t5, n);
  }
  whenAnyAncestorIs(t5) {
    return this._bindingWhenSyntax.whenAnyAncestorIs(t5);
  }
  whenNoAncestorIs(t5) {
    return this._bindingWhenSyntax.whenNoAncestorIs(t5);
  }
  whenAnyAncestorNamed(t5) {
    return this._bindingWhenSyntax.whenAnyAncestorNamed(t5);
  }
  whenAnyAncestorTagged(t5, n) {
    return this._bindingWhenSyntax.whenAnyAncestorTagged(t5, n);
  }
  whenNoAncestorNamed(t5) {
    return this._bindingWhenSyntax.whenNoAncestorNamed(t5);
  }
  whenNoAncestorTagged(t5, n) {
    return this._bindingWhenSyntax.whenNoAncestorTagged(t5, n);
  }
  whenAnyAncestorMatches(t5) {
    return this._bindingWhenSyntax.whenAnyAncestorMatches(t5);
  }
  whenNoAncestorMatches(t5) {
    return this._bindingWhenSyntax.whenNoAncestorMatches(t5);
  }
  onActivation(t5) {
    return this._bindingOnSyntax.onActivation(t5);
  }
  onDeactivation(t5) {
    return this._bindingOnSyntax.onDeactivation(t5);
  }
};
var Ct = class {
  constructor(t5) {
    __publicField(this, "_binding");
    this._binding = t5;
  }
  inRequestScope() {
    return this._binding.scope = b2.Request, new Nt(this._binding);
  }
  inSingletonScope() {
    return this._binding.scope = b2.Singleton, new Nt(this._binding);
  }
  inTransientScope() {
    return this._binding.scope = b2.Transient, new Nt(this._binding);
  }
};
var xt = class {
  constructor(t5) {
    __publicField(this, "_bindingInSyntax");
    __publicField(this, "_bindingWhenSyntax");
    __publicField(this, "_bindingOnSyntax");
    __publicField(this, "_binding");
    this._binding = t5, this._bindingWhenSyntax = new Tt(this._binding), this._bindingOnSyntax = new Dt(this._binding), this._bindingInSyntax = new Ct(t5);
  }
  inRequestScope() {
    return this._bindingInSyntax.inRequestScope();
  }
  inSingletonScope() {
    return this._bindingInSyntax.inSingletonScope();
  }
  inTransientScope() {
    return this._bindingInSyntax.inTransientScope();
  }
  when(t5) {
    return this._bindingWhenSyntax.when(t5);
  }
  whenTargetNamed(t5) {
    return this._bindingWhenSyntax.whenTargetNamed(t5);
  }
  whenTargetIsDefault() {
    return this._bindingWhenSyntax.whenTargetIsDefault();
  }
  whenTargetTagged(t5, n) {
    return this._bindingWhenSyntax.whenTargetTagged(t5, n);
  }
  whenInjectedInto(t5) {
    return this._bindingWhenSyntax.whenInjectedInto(t5);
  }
  whenParentNamed(t5) {
    return this._bindingWhenSyntax.whenParentNamed(t5);
  }
  whenParentTagged(t5, n) {
    return this._bindingWhenSyntax.whenParentTagged(t5, n);
  }
  whenAnyAncestorIs(t5) {
    return this._bindingWhenSyntax.whenAnyAncestorIs(t5);
  }
  whenNoAncestorIs(t5) {
    return this._bindingWhenSyntax.whenNoAncestorIs(t5);
  }
  whenAnyAncestorNamed(t5) {
    return this._bindingWhenSyntax.whenAnyAncestorNamed(t5);
  }
  whenAnyAncestorTagged(t5, n) {
    return this._bindingWhenSyntax.whenAnyAncestorTagged(t5, n);
  }
  whenNoAncestorNamed(t5) {
    return this._bindingWhenSyntax.whenNoAncestorNamed(t5);
  }
  whenNoAncestorTagged(t5, n) {
    return this._bindingWhenSyntax.whenNoAncestorTagged(t5, n);
  }
  whenAnyAncestorMatches(t5) {
    return this._bindingWhenSyntax.whenAnyAncestorMatches(t5);
  }
  whenNoAncestorMatches(t5) {
    return this._bindingWhenSyntax.whenNoAncestorMatches(t5);
  }
  onActivation(t5) {
    return this._bindingOnSyntax.onActivation(t5);
  }
  onDeactivation(t5) {
    return this._bindingOnSyntax.onDeactivation(t5);
  }
};
var Rt = class {
  constructor(t5) {
    __publicField(this, "_binding");
    this._binding = t5;
  }
  to(t5) {
    return this._binding.type = w2.Instance, this._binding.implementationType = t5, new xt(this._binding);
  }
  toSelf() {
    if ("function" != typeof this._binding.serviceIdentifier) throw new Error("The toSelf function can only be applied when a constructor is used as service identifier");
    const t5 = this._binding.serviceIdentifier;
    return this.to(t5);
  }
  toConstantValue(t5) {
    return this._binding.type = w2.ConstantValue, this._binding.cache = t5, this._binding.dynamicValue = null, this._binding.implementationType = null, this._binding.scope = b2.Singleton, new Nt(this._binding);
  }
  toDynamicValue(t5) {
    return this._binding.type = w2.DynamicValue, this._binding.cache = null, this._binding.dynamicValue = t5, this._binding.implementationType = null, new xt(this._binding);
  }
  toConstructor(t5) {
    return this._binding.type = w2.Constructor, this._binding.implementationType = t5, this._binding.scope = b2.Singleton, new Nt(this._binding);
  }
  toFactory(t5) {
    return this._binding.type = w2.Factory, this._binding.factory = t5, this._binding.scope = b2.Singleton, new Nt(this._binding);
  }
  toFunction(t5) {
    if ("function" != typeof t5) throw new Error("Value provided to function binding must be a function!");
    const n = this.toConstantValue(t5);
    return this._binding.type = w2.Function, this._binding.scope = b2.Singleton, n;
  }
  toAutoFactory(t5) {
    return this._binding.type = w2.Factory, this._binding.factory = (n) => () => n.container.get(t5), this._binding.scope = b2.Singleton, new Nt(this._binding);
  }
  toAutoNamedFactory(t5) {
    return this._binding.type = w2.Factory, this._binding.factory = (n) => (e2) => n.container.getNamed(t5, e2), new Nt(this._binding);
  }
  toProvider(t5) {
    return this._binding.type = w2.Provider, this._binding.provider = t5, this._binding.scope = b2.Singleton, new Nt(this._binding);
  }
  toService(t5) {
    this._binding.type = w2.DynamicValue, Object.defineProperty(this._binding, "cache", { configurable: true, enumerable: true, get: () => null, set(t6) {
    } }), this._binding.dynamicValue = (n) => {
      try {
        return n.container.get(t5);
      } catch (e2) {
        return n.container.getAsync(t5);
      }
    }, this._binding.implementationType = null;
  }
};
var Et = class _Et {
  constructor() {
    __publicField(this, "bindings");
    __publicField(this, "activations");
    __publicField(this, "deactivations");
    __publicField(this, "middleware");
    __publicField(this, "moduleActivationStore");
  }
  static of(t5, n, e2, i2, r3) {
    const s3 = new _Et();
    return s3.bindings = t5, s3.middleware = n, s3.deactivations = i2, s3.activations = e2, s3.moduleActivationStore = r3, s3;
  }
};
var Mt = class _Mt {
  constructor() {
    __publicField(this, "_map");
    this._map = /* @__PURE__ */ new Map();
  }
  getMap() {
    return this._map;
  }
  add(t5, n) {
    if (this._checkNonNulish(t5), null == n) throw new Error(D2);
    const e2 = this._map.get(t5);
    void 0 !== e2 ? e2.push(n) : this._map.set(t5, [n]);
  }
  get(t5) {
    this._checkNonNulish(t5);
    const n = this._map.get(t5);
    if (void 0 !== n) return n;
    throw new Error(N2);
  }
  remove(t5) {
    if (this._checkNonNulish(t5), !this._map.delete(t5)) throw new Error(N2);
  }
  removeIntersection(t5) {
    this.traverse(((n, e2) => {
      const i2 = t5.hasKey(n) ? t5.get(n) : void 0;
      if (void 0 !== i2) {
        const t6 = e2.filter(((t7) => !i2.some(((n2) => t7 === n2))));
        this._setValue(n, t6);
      }
    }));
  }
  removeByCondition(t5) {
    const n = [];
    return this._map.forEach(((e2, i2) => {
      const r3 = [];
      for (const i3 of e2) {
        t5(i3) ? n.push(i3) : r3.push(i3);
      }
      this._setValue(i2, r3);
    })), n;
  }
  hasKey(t5) {
    return this._checkNonNulish(t5), this._map.has(t5);
  }
  clone() {
    const t5 = new _Mt();
    return this._map.forEach(((n, e2) => {
      n.forEach(((n2) => {
        var i2;
        t5.add(e2, "object" == typeof (i2 = n2) && null !== i2 && "clone" in i2 && "function" == typeof i2.clone ? n2.clone() : n2);
      }));
    })), t5;
  }
  traverse(t5) {
    this._map.forEach(((n, e2) => {
      t5(e2, n);
    }));
  }
  _checkNonNulish(t5) {
    if (null == t5) throw new Error(D2);
  }
  _setValue(t5, n) {
    n.length > 0 ? this._map.set(t5, n) : this._map.delete(t5);
  }
};
var Bt = class _Bt {
  constructor() {
    __publicField(this, "_map", /* @__PURE__ */ new Map());
  }
  remove(t5) {
    const n = this._map.get(t5);
    return void 0 === n ? this._getEmptyHandlersStore() : (this._map.delete(t5), n);
  }
  addDeactivation(t5, n, e2) {
    this._getModuleActivationHandlers(t5).onDeactivations.add(n, e2);
  }
  addActivation(t5, n, e2) {
    this._getModuleActivationHandlers(t5).onActivations.add(n, e2);
  }
  clone() {
    const t5 = new _Bt();
    return this._map.forEach(((n, e2) => {
      t5._map.set(e2, { onActivations: n.onActivations.clone(), onDeactivations: n.onDeactivations.clone() });
    })), t5;
  }
  _getModuleActivationHandlers(t5) {
    let n = this._map.get(t5);
    return void 0 === n && (n = this._getEmptyHandlersStore(), this._map.set(t5, n)), n;
  }
  _getEmptyHandlersStore() {
    return { onActivations: new Mt(), onDeactivations: new Mt() };
  }
};
var Pt = class _Pt {
  constructor(t5) {
    __publicField(this, "id");
    __publicField(this, "parent");
    __publicField(this, "options");
    __publicField(this, "_middleware");
    __publicField(this, "_bindingDictionary");
    __publicField(this, "_activations");
    __publicField(this, "_deactivations");
    __publicField(this, "_snapshots");
    __publicField(this, "_metadataReader");
    __publicField(this, "_moduleActivationStore");
    const n = t5 || {};
    if ("object" != typeof n) throw new Error("Invalid Container constructor argument. Container options must be an object.");
    if (void 0 === n.defaultScope) n.defaultScope = b2.Transient;
    else if (n.defaultScope !== b2.Singleton && n.defaultScope !== b2.Transient && n.defaultScope !== b2.Request) throw new Error('Invalid Container option. Default scope must be a string ("singleton" or "transient").');
    if (void 0 === n.autoBindInjectable) n.autoBindInjectable = false;
    else if ("boolean" != typeof n.autoBindInjectable) throw new Error("Invalid Container option. Auto bind injectable must be a boolean");
    if (void 0 === n.skipBaseClassChecks) n.skipBaseClassChecks = false;
    else if ("boolean" != typeof n.skipBaseClassChecks) throw new Error("Invalid Container option. Skip base check must be a boolean");
    this.options = { autoBindInjectable: n.autoBindInjectable, defaultScope: n.defaultScope, skipBaseClassChecks: n.skipBaseClassChecks }, this.id = S(), this._bindingDictionary = new Mt(), this._snapshots = [], this._middleware = null, this._activations = new Mt(), this._deactivations = new Mt(), this.parent = null, this._metadataReader = new M2(), this._moduleActivationStore = new Bt();
  }
  static merge(t5, n, ...e2) {
    const i2 = new _Pt(), r3 = [t5, n, ...e2].map(((t6) => U(t6))), s3 = U(i2);
    return r3.forEach(((t6) => {
      var n2;
      n2 = s3, t6.traverse(((t7, e3) => {
        e3.forEach(((t8) => {
          n2.add(t8.serviceIdentifier, t8.clone());
        }));
      }));
    })), i2;
  }
  load(...t5) {
    const n = this._getContainerModuleHelpersFactory();
    for (const e2 of t5) {
      const t6 = n(e2.id);
      e2.registry(t6.bindFunction, t6.unbindFunction, t6.isboundFunction, t6.rebindFunction, t6.unbindAsyncFunction, t6.onActivationFunction, t6.onDeactivationFunction);
    }
  }
  async loadAsync(...t5) {
    const n = this._getContainerModuleHelpersFactory();
    for (const e2 of t5) {
      const t6 = n(e2.id);
      await e2.registry(t6.bindFunction, t6.unbindFunction, t6.isboundFunction, t6.rebindFunction, t6.unbindAsyncFunction, t6.onActivationFunction, t6.onDeactivationFunction);
    }
  }
  unload(...t5) {
    t5.forEach(((t6) => {
      const n = this._removeModuleBindings(t6.id);
      this._deactivateSingletons(n), this._removeModuleHandlers(t6.id);
    }));
  }
  async unloadAsync(...t5) {
    for (const n of t5) {
      const t6 = this._removeModuleBindings(n.id);
      await this._deactivateSingletonsAsync(t6), this._removeModuleHandlers(n.id);
    }
  }
  bind(t5) {
    return this._bind(this._buildBinding(t5));
  }
  rebind(t5) {
    return this.unbind(t5), this.bind(t5);
  }
  async rebindAsync(t5) {
    return await this.unbindAsync(t5), this.bind(t5);
  }
  unbind(t5) {
    if (this._bindingDictionary.hasKey(t5)) {
      const n = this._bindingDictionary.get(t5);
      this._deactivateSingletons(n);
    }
    this._removeServiceFromDictionary(t5);
  }
  async unbindAsync(t5) {
    if (this._bindingDictionary.hasKey(t5)) {
      const n = this._bindingDictionary.get(t5);
      await this._deactivateSingletonsAsync(n);
    }
    this._removeServiceFromDictionary(t5);
  }
  unbindAll() {
    this._bindingDictionary.traverse(((t5, n) => {
      this._deactivateSingletons(n);
    })), this._bindingDictionary = new Mt();
  }
  async unbindAllAsync() {
    const t5 = [];
    this._bindingDictionary.traverse(((n, e2) => {
      t5.push(this._deactivateSingletonsAsync(e2));
    })), await Promise.all(t5), this._bindingDictionary = new Mt();
  }
  onActivation(t5, n) {
    this._activations.add(t5, n);
  }
  onDeactivation(t5, n) {
    this._deactivations.add(t5, n);
  }
  isBound(t5) {
    let n = this._bindingDictionary.hasKey(t5);
    return !n && this.parent && (n = this.parent.isBound(t5)), n;
  }
  isCurrentBound(t5) {
    return this._bindingDictionary.hasKey(t5);
  }
  isBoundNamed(t5, n) {
    return this.isBoundTagged(t5, s2, n);
  }
  isBoundTagged(t5, n, s3) {
    let o3 = false;
    if (this._bindingDictionary.hasKey(t5)) {
      const a3 = this._bindingDictionary.get(t5), c3 = (function(t6, n2, s4) {
        const o4 = Y(n2, s4), a4 = v(o4);
        if (a4.kind === p.unmanaged) throw new Error("Unexpected metadata when creating target");
        const c4 = new T("", a4, "Variable"), d3 = new V(t6);
        return new H(n2, d3, null, [], c4);
      })(this, t5, { customTag: { key: n, value: s3 }, isMultiInject: false });
      o3 = a3.some(((t6) => t6.constraint(c3)));
    }
    return !o3 && this.parent && (o3 = this.parent.isBoundTagged(t5, n, s3)), o3;
  }
  snapshot() {
    this._snapshots.push(Et.of(this._bindingDictionary.clone(), this._middleware, this._activations.clone(), this._deactivations.clone(), this._moduleActivationStore.clone()));
  }
  restore() {
    const t5 = this._snapshots.pop();
    if (void 0 === t5) throw new Error("No snapshot available to restore.");
    this._bindingDictionary = t5.bindings, this._activations = t5.activations, this._deactivations = t5.deactivations, this._middleware = t5.middleware, this._moduleActivationStore = t5.moduleActivationStore;
  }
  createChild(t5) {
    const n = new _Pt(t5 || this.options);
    return n.parent = this, n;
  }
  applyMiddleware(...t5) {
    const n = this._middleware ? this._middleware : this._planAndResolve();
    this._middleware = t5.reduce(((t6, n2) => n2(t6)), n);
  }
  applyCustomMetadataReader(t5) {
    this._metadataReader = t5;
  }
  get(t5) {
    const n = this._getNotAllArgs(t5, false, false);
    return this._getButThrowIfAsync(n);
  }
  async getAsync(t5) {
    const n = this._getNotAllArgs(t5, false, false);
    return this._get(n);
  }
  getTagged(t5, n, e2) {
    const i2 = this._getNotAllArgs(t5, false, false, n, e2);
    return this._getButThrowIfAsync(i2);
  }
  async getTaggedAsync(t5, n, e2) {
    const i2 = this._getNotAllArgs(t5, false, false, n, e2);
    return this._get(i2);
  }
  getNamed(t5, n) {
    return this.getTagged(t5, s2, n);
  }
  async getNamedAsync(t5, n) {
    return this.getTaggedAsync(t5, s2, n);
  }
  getAll(t5, n) {
    const e2 = this._getAllArgs(t5, n, false);
    return this._getButThrowIfAsync(e2);
  }
  async getAllAsync(t5, n) {
    const e2 = this._getAllArgs(t5, n, false);
    return this._getAll(e2);
  }
  getAllTagged(t5, n, e2) {
    const i2 = this._getNotAllArgs(t5, true, false, n, e2);
    return this._getButThrowIfAsync(i2);
  }
  async getAllTaggedAsync(t5, n, e2) {
    const i2 = this._getNotAllArgs(t5, true, false, n, e2);
    return this._getAll(i2);
  }
  getAllNamed(t5, n) {
    return this.getAllTagged(t5, s2, n);
  }
  async getAllNamedAsync(t5, n) {
    return this.getAllTaggedAsync(t5, s2, n);
  }
  resolve(t5) {
    const n = this.isBound(t5);
    n || this.bind(t5).toSelf();
    const e2 = this.get(t5);
    return n || this.unbind(t5), e2;
  }
  tryGet(t5) {
    const n = this._getNotAllArgs(t5, false, true);
    return this._getButThrowIfAsync(n);
  }
  async tryGetAsync(t5) {
    const n = this._getNotAllArgs(t5, false, true);
    return this._get(n);
  }
  tryGetTagged(t5, n, e2) {
    const i2 = this._getNotAllArgs(t5, false, true, n, e2);
    return this._getButThrowIfAsync(i2);
  }
  async tryGetTaggedAsync(t5, n, e2) {
    const i2 = this._getNotAllArgs(t5, false, true, n, e2);
    return this._get(i2);
  }
  tryGetNamed(t5, n) {
    return this.tryGetTagged(t5, s2, n);
  }
  async tryGetNamedAsync(t5, n) {
    return this.tryGetTaggedAsync(t5, s2, n);
  }
  tryGetAll(t5, n) {
    const e2 = this._getAllArgs(t5, n, true);
    return this._getButThrowIfAsync(e2);
  }
  async tryGetAllAsync(t5, n) {
    const e2 = this._getAllArgs(t5, n, true);
    return this._getAll(e2);
  }
  tryGetAllTagged(t5, n, e2) {
    const i2 = this._getNotAllArgs(t5, true, true, n, e2);
    return this._getButThrowIfAsync(i2);
  }
  async tryGetAllTaggedAsync(t5, n, e2) {
    const i2 = this._getNotAllArgs(t5, true, true, n, e2);
    return this._getAll(i2);
  }
  tryGetAllNamed(t5, n) {
    return this.tryGetAllTagged(t5, s2, n);
  }
  async tryGetAllNamedAsync(t5, n) {
    return this.tryGetAllTaggedAsync(t5, s2, n);
  }
  _preDestroy(t5, n) {
    var _a4;
    if (void 0 !== t5 && Reflect.hasMetadata(f2, t5)) {
      const e2 = Reflect.getMetadata(f2, t5);
      return (_a4 = n[e2.value]) == null ? void 0 : _a4.call(n);
    }
  }
  _removeModuleHandlers(t5) {
    const n = this._moduleActivationStore.remove(t5);
    this._activations.removeIntersection(n.onActivations), this._deactivations.removeIntersection(n.onDeactivations);
  }
  _removeModuleBindings(t5) {
    return this._bindingDictionary.removeByCondition(((n) => n.moduleId === t5));
  }
  _deactivate(t5, n) {
    const e2 = null == n ? void 0 : Object.getPrototypeOf(n).constructor;
    try {
      if (this._deactivations.hasKey(t5.serviceIdentifier)) {
        const i3 = this._deactivateContainer(n, this._deactivations.get(t5.serviceIdentifier).values());
        if (X(i3)) return this._handleDeactivationError(i3.then((async () => this._propagateContainerDeactivationThenBindingAndPreDestroyAsync(t5, n, e2))), t5.serviceIdentifier);
      }
      const i2 = this._propagateContainerDeactivationThenBindingAndPreDestroy(t5, n, e2);
      if (X(i2)) return this._handleDeactivationError(i2, t5.serviceIdentifier);
    } catch (n2) {
      if (n2 instanceof Error) throw new Error(E2(k2(t5.serviceIdentifier), n2.message));
    }
  }
  async _handleDeactivationError(t5, n) {
    try {
      await t5;
    } catch (t6) {
      if (t6 instanceof Error) throw new Error(E2(k2(n), t6.message));
    }
  }
  _deactivateContainer(t5, n) {
    let e2 = n.next();
    for (; "function" == typeof e2.value; ) {
      const i2 = e2.value(t5);
      if (X(i2)) return i2.then((async () => this._deactivateContainerAsync(t5, n)));
      e2 = n.next();
    }
  }
  async _deactivateContainerAsync(t5, n) {
    let e2 = n.next();
    for (; "function" == typeof e2.value; ) await e2.value(t5), e2 = n.next();
  }
  _getContainerModuleHelpersFactory() {
    const t5 = (t6) => (n2) => {
      const e3 = this._buildBinding(n2);
      return e3.moduleId = t6, this._bind(e3);
    }, n = () => (t6) => {
      this.unbind(t6);
    }, e2 = () => async (t6) => this.unbindAsync(t6), i2 = () => (t6) => this.isBound(t6), r3 = (n2) => {
      const e3 = t5(n2);
      return (t6) => (this.unbind(t6), e3(t6));
    }, s3 = (t6) => (n2, e3) => {
      this._moduleActivationStore.addActivation(t6, n2, e3), this.onActivation(n2, e3);
    }, o3 = (t6) => (n2, e3) => {
      this._moduleActivationStore.addDeactivation(t6, n2, e3), this.onDeactivation(n2, e3);
    };
    return (a3) => ({ bindFunction: t5(a3), isboundFunction: i2(), onActivationFunction: s3(a3), onDeactivationFunction: o3(a3), rebindFunction: r3(a3), unbindAsyncFunction: e2(), unbindFunction: n() });
  }
  _bind(t5) {
    return this._bindingDictionary.add(t5.serviceIdentifier, t5), new Rt(t5);
  }
  _buildBinding(t5) {
    const n = this.options.defaultScope || b2.Transient;
    return new I2(t5, n);
  }
  async _getAll(t5) {
    return Promise.all(this._get(t5));
  }
  _get(t5) {
    const n = { ...t5, contextInterceptor: (t6) => t6, targetType: m2.Variable };
    if (this._middleware) {
      const t6 = this._middleware(n);
      if (null == t6) throw new Error("Invalid return type in middleware. Middleware must return!");
      return t6;
    }
    return this._planAndResolve()(n);
  }
  _getButThrowIfAsync(t5) {
    const n = this._get(t5);
    if (Z(n)) throw new Error(`You are attempting to construct ${(function(t6) {
      return "function" == typeof t6 ? `[function/class ${t6.name || "<anonymous>"}]` : "symbol" == typeof t6 ? t6.toString() : `'${t6}'`;
    })(t5.serviceIdentifier)} in a synchronous way but it has asynchronous dependencies.`);
    return n;
  }
  _getAllArgs(t5, n, e2) {
    return { avoidConstraints: !(n == null ? void 0 : n.enforceBindingConstraints), isMultiInject: true, isOptional: e2, serviceIdentifier: t5 };
  }
  _getNotAllArgs(t5, n, e2, i2, r3) {
    return { avoidConstraints: false, isMultiInject: n, isOptional: e2, key: i2, serviceIdentifier: t5, value: r3 };
  }
  _getPlanMetadataFromNextArgs(t5) {
    const n = { isMultiInject: t5.isMultiInject };
    return void 0 !== t5.key && (n.customTag = { key: t5.key, value: t5.value }), true === t5.isOptional && (n.isOptional = true), n;
  }
  _planAndResolve() {
    return (t5) => {
      let n = Q(this._metadataReader, this, t5.targetType, t5.serviceIdentifier, this._getPlanMetadataFromNextArgs(t5), t5.avoidConstraints);
      n = t5.contextInterceptor(n);
      const e2 = (function(t6) {
        return ut(t6.plan.rootRequest.requestScope)(t6.plan.rootRequest);
      })(n);
      return e2;
    };
  }
  _deactivateIfSingleton(t5) {
    if (t5.activated) return X(t5.cache) ? t5.cache.then(((n) => this._deactivate(t5, n))) : this._deactivate(t5, t5.cache);
  }
  _deactivateSingletons(t5) {
    for (const n of t5) {
      if (X(this._deactivateIfSingleton(n))) throw new Error("Attempting to unbind dependency with asynchronous destruction (@preDestroy or onDeactivation)");
    }
  }
  async _deactivateSingletonsAsync(t5) {
    await Promise.all(t5.map((async (t6) => this._deactivateIfSingleton(t6))));
  }
  _propagateContainerDeactivationThenBindingAndPreDestroy(t5, n, e2) {
    return this.parent ? this._deactivate.bind(this.parent)(t5, n) : this._bindingDeactivationAndPreDestroy(t5, n, e2);
  }
  async _propagateContainerDeactivationThenBindingAndPreDestroyAsync(t5, n, e2) {
    this.parent ? await this._deactivate.bind(this.parent)(t5, n) : await this._bindingDeactivationAndPreDestroyAsync(t5, n, e2);
  }
  _removeServiceFromDictionary(t5) {
    try {
      this._bindingDictionary.remove(t5);
    } catch (n) {
      throw new Error(`Could not unbind serviceIdentifier: ${k2(t5)}`);
    }
  }
  _bindingDeactivationAndPreDestroy(t5, n, e2) {
    if ("function" == typeof t5.onDeactivation) {
      const i2 = t5.onDeactivation(n);
      if (X(i2)) return i2.then((() => this._preDestroy(e2, n)));
    }
    return this._preDestroy(e2, n);
  }
  async _bindingDeactivationAndPreDestroyAsync(t5, n, e2) {
    "function" == typeof t5.onDeactivation && await t5.onDeactivation(n), await this._preDestroy(e2, n);
  }
};
var kt = class {
  constructor(t5) {
    __publicField(this, "id");
    __publicField(this, "registry");
    this.id = S(), this.registry = t5;
  }
};
function Ft(t5, n, e2, i2) {
  !(function(t6) {
    if (void 0 !== t6) throw new Error(R);
  })(n), Ot(h2, t5, e2.toString(), i2);
}
function jt(t5) {
  let n = [];
  if (Array.isArray(t5)) {
    n = t5;
    const e2 = (function(t6) {
      const n2 = /* @__PURE__ */ new Set();
      for (const e3 of t6) {
        if (n2.has(e3)) return e3;
        n2.add(e3);
      }
    })(n.map(((t6) => t6.key)));
    if (void 0 !== e2) throw new Error(`${T2} ${e2.toString()}`);
  } else n = [t5];
  return n;
}
function Ot(t5, n, e2, i2) {
  const r3 = jt(i2);
  let s3 = {};
  Reflect.hasOwnMetadata(t5, n) && (s3 = Reflect.getMetadata(t5, n));
  let o3 = s3[e2];
  if (void 0 === o3) o3 = [];
  else for (const t6 of o3) if (r3.some(((n2) => n2.key === t6.key))) throw new Error(`${T2} ${t6.key.toString()}`);
  o3.push(...r3), s3[e2] = o3, Reflect.defineMetadata(t5, s3, n);
}
function $t(t5) {
  return (n, e2, i2) => {
    "number" == typeof i2 ? Ft(n, e2, i2, t5) : (function(t6, n2, e3) {
      if (void 0 !== t6.prototype) throw new Error(R);
      Ot(l2, t6.constructor, n2, e3);
    })(n, e2, t5);
  };
}
function Kt() {
  return function(t5) {
    if (Reflect.hasOwnMetadata(g2, t5)) throw new Error("Cannot apply @injectable decorator multiple times.");
    const n = Reflect.getMetadata(y2, t5) || [];
    return Reflect.defineMetadata(g2, n, t5), t5;
  };
}
function Ut(t5) {
  return $t(new W(s2, t5));
}
function Lt(t5) {
  return (n) => (e2, i2, r3) => {
    if (void 0 === n) {
      const t6 = "function" == typeof e2 ? e2.name : e2.constructor.name;
      throw new Error(`@inject called with undefined this could mean that the class ${t6} has a circular dependency problem. You can use a LazyServiceIdentifer to overcome this limitation.`);
    }
    $t(new W(t5, n))(e2, i2, r3);
  };
}
var Yt = Lt(d2);
var Qt = Lt(u2);
function Zt(t5, n) {
  return () => (e2, i2) => {
    const r3 = new W(t5, i2);
    if (Reflect.hasOwnMetadata(t5, e2.constructor)) throw new Error(n);
    Reflect.defineMetadata(t5, r3, e2.constructor);
  };
}
var tn = Zt(p2, "Cannot apply @postConstruct decorator multiple times in the same class");
var nn = Zt(f2, "Cannot apply @preDestroy decorator multiple times in the same class");

// config/inversify.types.ts
var SI = {
  "factory:replacer": "",
  "event:dispatcher": "",
  "alias:modifier": "",
  "alias:modifier:strategy": "",
  "alias:modifier:validator": "",
  cache: "",
  "component:black_white_list": "",
  "component:extractor": "",
  "component:extractor:strategy": "",
  "creator:creator": "",
  "creator:fallback": "",
  "creator:template": "",
  delimiter: "",
  delayer: "",
  "facade:obsidian": "",
  "factory:alias:modifier:strategy": "",
  "factory:alias:modifier:validator": "",
  "factory:feature": "",
  "factory:metadata:cache": "",
  "factory:obsidian:active:file": "",
  "factory:obsidian:modal": "",
  "factory:obsidian:file": "",
  "factory:obsidian:file_modifiable": "",
  "factory:obsidian:meta": "",
  "factory:placeholder": "",
  "factory:placeholder:resolver": "",
  "factory:creator:template": "",
  "factory:template:resolver": "",
  "factory:feature:explorer:sort": "",
  "newable:obsidian:chooser": "",
  feature: "",
  "feature:composer": "",
  "feature:service": "",
  "feature:alias:config": "",
  "feature:notelink:approve": "",
  "feature:config": "",
  "feature:explorer:file_mutator:factory": "",
  "feature:explorer:sort": "",
  filter: "",
  "getter:delimiter": "",
  listener: "",
  logger: "",
  "feature:helper": "",
  "logger:composer": "",
  "manager:composer": "",
  "obsidian:app": "",
  "obsidian:plugin": "",
  placeholder: "",
  resolver: "",
  "resolver:service": "",
  "factory:resolver:template": "",
  "factory:resolver:resolver": "",
  "service:note:link": "",
  "service:fake_title_element": "",
  "service:search:dom:wrapper": "",
  "settings:feature:builder": "",
  "settings:builder": "",
  "factory:settings:feature:builder": "",
  "factory:settings:builder": "",
  "settings:storage": "",
  "template:pattern": "",
  api: "",
  defer: "",
  "factory:api": "",
  "processor:factory": "",
  processor: "",
  "processor:args": "",
  "processor:argument.transformer": ""
};
for (const key of Object.keys(SI)) {
  SI[key] = Symbol.for(key);
}
var inversify_types_default = SI;

// src/i18n/Locale.ts
var import_obsidian = require("obsidian");

// src/Enum.ts
var Feature = /* @__PURE__ */ ((Feature4) => {
  Feature4["Explorer"] = "explorer";
  Feature4["Graph"] = "graph";
  Feature4["Header"] = "header";
  Feature4["Bookmarks"] = "bookmarks";
  Feature4["Search"] = "search" /* S */;
  Feature4["Tab"] = "tab";
  Feature4["Alias"] = "alias";
  Feature4["Suggest"] = "suggest";
  Feature4["InlineTitle"] = "inlineTitle";
  Feature4["Canvas"] = "canvas";
  Feature4["Backlink"] = "backlink" /* BL */;
  Feature4["NoteLink"] = "noteLink";
  Feature4["WindowFrame"] = "windowFrame";
  return Feature4;
})(Feature || {});
var Managers = [
  "explorer" /* Explorer */,
  "graph" /* Graph */,
  "header" /* Header */,
  "tab" /* Tab */,
  "inlineTitle" /* InlineTitle */,
  "canvas" /* Canvas */
];
var GITHUB_DOCS = "https://github.com/snezhig/obsidian-front-matter-title/blob/master/docs/";

// src/i18n/en/index.ts
var en = {
  validator: "Validator",
  strategy: "Strategy",
  cancel: "Cancel",
  apply: "Apply",
  feature: {
    ["alias" /* Alias */]: {
      name: "Alias",
      desc: "Modify alias in metadata cache. The real alias will not be affected",
      validator: {
        auto: {
          name: "Frontmatter Auto",
          desc: "If frontmatter does not exist, it will be created in cache. Side-effects may occur."
        },
        required: {
          name: "Frontmatter Required",
          desc: "Only files with frontmatter will be processed."
        }
      },
      strategy: {
        ensure: {
          name: "Ensure",
          desc: "Set title as an alias only if the one does not exist"
        },
        adjust: {
          name: "Adjust",
          desc: "Add title to alias and without affect on existing alias"
        },
        replace: {
          name: "Replace",
          desc: "Replace current alias with title"
        }
      }
    },
    ["explorer" /* Explorer */]: {
      name: "Explorer",
      desc: "Replace shown titles in the file explorer",
      sort: {
        name: "Sort",
        desc: "Sort files in explorer by titles from Explorer feature"
      }
    },
    ["graph" /* Graph */]: {
      name: "Graph",
      desc: "Replace shown titles in the graph/local-graph"
    },
    ["header" /* Header */]: {
      name: "Header",
      desc: "Replace titles in header of leaves and update them"
    },
    ["bookmarks" /* Bookmarks */]: {
      name: "Bookmarks",
      desc: "Replace shown titles in built-in bookmarks plugin"
    },
    ["search" /* Search */]: {
      name: "Search",
      desc: "Replace shown titles in search leaf"
    },
    ["suggest" /* Suggest */]: {
      name: "Suggest",
      desc: "Replace shown titles in suggest modals"
    },
    ["tab" /* Tab */]: {
      name: "Tabs",
      desc: "Replace shown titles in tabs"
    },
    ["inlineTitle" /* InlineTitle */]: {
      name: "Inline",
      desc: "Replace shown titles in Inline Title"
    },
    ["canvas" /* Canvas */]: {
      name: "Canvas",
      desc: "Replace shown titles in Canvas"
    },
    ["backlink" /* Backlink */]: {
      name: "Backlink",
      desc: "Replace shown titles in Backlink(Linked mentions)"
    },
    ["noteLink" /* NoteLink */]: {
      name: "Note Link",
      desc: "Replace internal links in files",
      strategy: {
        name: "Strategy",
        desc: "Define the strategy what the feature will do with links",
        options: {
          all: "Replace all links",
          onlyEmpty: "Replace only links without alias"
        }
      },
      approval: {
        name: "Approval",
        desc: "Should plugin ask you to approve changes or not",
        options: {
          showModal: "Show approve modal",
          auto: "Use auto approve"
        }
      }
    },
    ["windowFrame" /* WindowFrame */]: {
      name: "Window Frame Title",
      desc: "Replace shown title in window frame"
    }
  },
  manage: "Manage",
  template: {
    features: {
      name: "Feature's templates",
      desc: "Manage templates for this feature individually"
    },
    main: "Main",
    fallback: "Fallback",
    placeholder: "Type a template",
    common: {
      main: {
        name: "Common main template",
        desc: `Set a yaml path, which value will be used as a file title. Value must be string or numeric. Also you can use template-like path using "{{ }}".
				Also you can use #heading to use first Heading from a file or _basename and another reserved words. 
				See Readme to find out more`
      },
      fallback: {
        name: "Common fallback template",
        desc: "This template will be used as a fallback option if the main template is not resolved"
      }
    },
    used: `Common template will be used. It's value is "{{value}}"`,
    specific: "Current template will be used for this feature"
  },
  settings: "Settings for plugin",
  rule: {
    name: "Rules",
    path: {
      name: "File path rule",
      black: {
        name: "Black list mode",
        desc: "Files that are located by paths will be ignored by plugin. Each path must be written with new line."
      },
      white: {
        name: "White list mode",
        desc: "Files that are located by paths will be processed by plugin. Each path must be written with new line."
      }
    },
    delimiter: {
      name: "List values",
      desc: "Set the rule about how to process list values",
      first: "Use first value",
      join: "Join all by delimiter",
      placeholder: {
        first: "First value will be used",
        join: "Type a delimiter"
      }
    }
  },
  features: "Features",
  util: "Util",
  coffee: "If you like this plugin and you would like to help support continued development, you can use the buttons below:",
  debug_info: {
    title: "Debug info",
    desc: "Show debug info and caught errors in console"
  },
  boot_delay: {
    title: "Boot delay",
    desc: "Plugin will be loaded with specified delay in ms"
  },
  boot_background: {
    title: "Boot in background",
    desc: "Plugin will be loaded in background to not blocking application"
  },
  disabled: "Disabled",
  processor: {
    name: "Processor",
    desc: "Modifies resolved title",
    replace: {
      name: "Replace",
      desc: "What will be executed",
      pattern: {
        name: "Pattern",
        desc: "Will be used as a first argument of RegExp first, and then as a first argument of replace()"
      },
      flags: {
        name: "Flags",
        desc: "Will be used as a second argument of new RegExp"
      },
      replacement: {
        name: "Replacement",
        desc: "Will be used as a second argument of replace()"
      }
    },
    function: {
      name: "Function",
      desc: "How it will work:",
      valueDesc: "Your value of text area"
    },
    functionV2: {
      name: "Function V2",
      desc: "How it will work:",
      valueDesc: "Your value of text area"
    }
  },
  command: {
    features: {
      reload: "Reload features",
      disable: "Disable features"
    }
  }
};
var en_default = en;

// src/i18n/ru/index.ts
var ru = {};
var ru_default = ru;

// node_modules/i18next/dist/esm/i18next.js
var isString = (obj) => typeof obj === "string";
var defer = () => {
  let res;
  let rej;
  const promise = new Promise((resolve, reject) => {
    res = resolve;
    rej = reject;
  });
  promise.resolve = res;
  promise.reject = rej;
  return promise;
};
var makeString = (object) => {
  if (object == null) return "";
  return String(object);
};
var copy = (a3, s3, t5) => {
  a3.forEach((m3) => {
    if (s3[m3]) t5[m3] = s3[m3];
  });
};
var lastOfPathSeparatorRegExp = /###/g;
var cleanKey = (key) => key && key.includes("###") ? key.replace(lastOfPathSeparatorRegExp, ".") : key;
var canNotTraverseDeeper = (object) => !object || isString(object);
var getLastOfPath = (object, path, Empty) => {
  const stack = !isString(path) ? path : path.split(".");
  let stackIndex = 0;
  while (stackIndex < stack.length - 1) {
    if (canNotTraverseDeeper(object)) return {};
    const key = cleanKey(stack[stackIndex]);
    if (!object[key] && Empty) object[key] = new Empty();
    if (Object.prototype.hasOwnProperty.call(object, key)) {
      object = object[key];
    } else {
      object = {};
    }
    ++stackIndex;
  }
  if (canNotTraverseDeeper(object)) return {};
  return {
    obj: object,
    k: cleanKey(stack[stackIndex])
  };
};
var setPath = (object, path, newValue) => {
  const {
    obj,
    k: k3
  } = getLastOfPath(object, path, Object);
  if (obj !== void 0 || path.length === 1) {
    obj[k3] = newValue;
    return;
  }
  let e2 = path[path.length - 1];
  let p3 = path.slice(0, path.length - 1);
  let last = getLastOfPath(object, p3, Object);
  while (last.obj === void 0 && p3.length) {
    e2 = `${p3[p3.length - 1]}.${e2}`;
    p3 = p3.slice(0, p3.length - 1);
    last = getLastOfPath(object, p3, Object);
    if ((last == null ? void 0 : last.obj) && typeof last.obj[`${last.k}.${e2}`] !== "undefined") {
      last.obj = void 0;
    }
  }
  last.obj[`${last.k}.${e2}`] = newValue;
};
var pushPath = (object, path, newValue, concat) => {
  const {
    obj,
    k: k3
  } = getLastOfPath(object, path, Object);
  obj[k3] = obj[k3] || [];
  obj[k3].push(newValue);
};
var getPath = (object, path) => {
  const {
    obj,
    k: k3
  } = getLastOfPath(object, path);
  if (!obj) return void 0;
  if (!Object.prototype.hasOwnProperty.call(obj, k3)) return void 0;
  return obj[k3];
};
var getPathWithDefaults = (data, defaultData, key) => {
  const value = getPath(data, key);
  if (value !== void 0) {
    return value;
  }
  return getPath(defaultData, key);
};
var deepExtend = (target, source, overwrite) => {
  for (const prop in source) {
    if (prop !== "__proto__" && prop !== "constructor") {
      if (prop in target) {
        if (isString(target[prop]) || target[prop] instanceof String || isString(source[prop]) || source[prop] instanceof String) {
          if (overwrite) target[prop] = source[prop];
        } else {
          deepExtend(target[prop], source[prop], overwrite);
        }
      } else {
        target[prop] = source[prop];
      }
    }
  }
  return target;
};
var regexEscape = (str) => str.replace(/[\-\[\]\/\{\}\(\)\*\+\?\.\\\^\$\|]/g, "\\$&");
var _entityMap = {
  "&": "&amp;",
  "<": "&lt;",
  ">": "&gt;",
  '"': "&quot;",
  "'": "&#39;",
  "/": "&#x2F;"
};
var escape = (data) => {
  if (isString(data)) {
    return data.replace(/[&<>"'\/]/g, (s3) => _entityMap[s3]);
  }
  return data;
};
var RegExpCache = class {
  constructor(capacity) {
    this.capacity = capacity;
    this.regExpMap = /* @__PURE__ */ new Map();
    this.regExpQueue = [];
  }
  getRegExp(pattern) {
    const regExpFromCache = this.regExpMap.get(pattern);
    if (regExpFromCache !== void 0) {
      return regExpFromCache;
    }
    const regExpNew = new RegExp(pattern);
    if (this.regExpQueue.length === this.capacity) {
      this.regExpMap.delete(this.regExpQueue.shift());
    }
    this.regExpMap.set(pattern, regExpNew);
    this.regExpQueue.push(pattern);
    return regExpNew;
  }
};
var chars = [" ", ",", "?", "!", ";"];
var looksLikeObjectPathRegExpCache = new RegExpCache(20);
var looksLikeObjectPath = (key, nsSeparator, keySeparator) => {
  nsSeparator = nsSeparator || "";
  keySeparator = keySeparator || "";
  const possibleChars = chars.filter((c3) => !nsSeparator.includes(c3) && !keySeparator.includes(c3));
  if (possibleChars.length === 0) return true;
  const r3 = looksLikeObjectPathRegExpCache.getRegExp(`(${possibleChars.map((c3) => c3 === "?" ? "\\?" : c3).join("|")})`);
  let matched = !r3.test(key);
  if (!matched) {
    const ki = key.indexOf(keySeparator);
    if (ki > 0 && !r3.test(key.substring(0, ki))) {
      matched = true;
    }
  }
  return matched;
};
var deepFind = (obj, path, keySeparator = ".") => {
  if (!obj) return void 0;
  if (obj[path]) {
    if (!Object.prototype.hasOwnProperty.call(obj, path)) return void 0;
    return obj[path];
  }
  const tokens = path.split(keySeparator);
  let current = obj;
  for (let i2 = 0; i2 < tokens.length; ) {
    if (!current || typeof current !== "object") {
      return void 0;
    }
    let next;
    let nextPath = "";
    for (let j3 = i2; j3 < tokens.length; ++j3) {
      if (j3 !== i2) {
        nextPath += keySeparator;
      }
      nextPath += tokens[j3];
      next = current[nextPath];
      if (next !== void 0) {
        if (["string", "number", "boolean"].includes(typeof next) && j3 < tokens.length - 1) {
          continue;
        }
        i2 += j3 - i2 + 1;
        break;
      }
    }
    current = next;
  }
  return current;
};
var getCleanedCode = (code) => code == null ? void 0 : code.replace(/_/g, "-");
var consoleLogger = {
  type: "logger",
  log(args) {
    this.output("log", args);
  },
  warn(args) {
    this.output("warn", args);
  },
  error(args) {
    this.output("error", args);
  },
  output(type, args) {
    var _a4, _b2;
    (_b2 = (_a4 = console == null ? void 0 : console[type]) == null ? void 0 : _a4.apply) == null ? void 0 : _b2.call(_a4, console, args);
  }
};
var Logger = class _Logger {
  constructor(concreteLogger, options = {}) {
    this.init(concreteLogger, options);
  }
  init(concreteLogger, options = {}) {
    this.prefix = options.prefix || "i18next:";
    this.logger = concreteLogger || consoleLogger;
    this.options = options;
    this.debug = options.debug;
  }
  log(...args) {
    return this.forward(args, "log", "", true);
  }
  warn(...args) {
    return this.forward(args, "warn", "", true);
  }
  error(...args) {
    return this.forward(args, "error", "");
  }
  deprecate(...args) {
    return this.forward(args, "warn", "WARNING DEPRECATED: ", true);
  }
  forward(args, lvl, prefix, debugOnly) {
    if (debugOnly && !this.debug) return null;
    args = args.map((a3) => isString(a3) ? a3.replace(/[\r\n\x00-\x1F\x7F]/g, " ") : a3);
    if (isString(args[0])) args[0] = `${prefix}${this.prefix} ${args[0]}`;
    return this.logger[lvl](args);
  }
  create(moduleName) {
    return new _Logger(this.logger, {
      ...{
        prefix: `${this.prefix}:${moduleName}:`
      },
      ...this.options
    });
  }
  clone(options) {
    options = options || this.options;
    options.prefix = options.prefix || this.prefix;
    return new _Logger(this.logger, options);
  }
};
var baseLogger = new Logger();
var EventEmitter = class {
  constructor() {
    this.observers = {};
  }
  on(events, listener) {
    events.split(" ").forEach((event) => {
      if (!this.observers[event]) this.observers[event] = /* @__PURE__ */ new Map();
      const numListeners = this.observers[event].get(listener) || 0;
      this.observers[event].set(listener, numListeners + 1);
    });
    return this;
  }
  off(event, listener) {
    if (!this.observers[event]) return;
    if (!listener) {
      delete this.observers[event];
      return;
    }
    this.observers[event].delete(listener);
  }
  once(event, listener) {
    const wrapper = (...args) => {
      listener(...args);
      this.off(event, wrapper);
    };
    this.on(event, wrapper);
    return this;
  }
  emit(event, ...args) {
    if (this.observers[event]) {
      const cloned = Array.from(this.observers[event].entries());
      cloned.forEach(([observer, numTimesAdded]) => {
        for (let i2 = 0; i2 < numTimesAdded; i2++) {
          observer(...args);
        }
      });
    }
    if (this.observers["*"]) {
      const cloned = Array.from(this.observers["*"].entries());
      cloned.forEach(([observer, numTimesAdded]) => {
        for (let i2 = 0; i2 < numTimesAdded; i2++) {
          observer(event, ...args);
        }
      });
    }
  }
};
var ResourceStore = class extends EventEmitter {
  constructor(data, options = {
    ns: ["translation"],
    defaultNS: "translation"
  }) {
    super();
    this.data = data || {};
    this.options = options;
    if (this.options.keySeparator === void 0) {
      this.options.keySeparator = ".";
    }
    if (this.options.ignoreJSONStructure === void 0) {
      this.options.ignoreJSONStructure = true;
    }
  }
  addNamespaces(ns) {
    if (!this.options.ns.includes(ns)) {
      this.options.ns.push(ns);
    }
  }
  removeNamespaces(ns) {
    const index = this.options.ns.indexOf(ns);
    if (index > -1) {
      this.options.ns.splice(index, 1);
    }
  }
  getResource(lng, ns, key, options = {}) {
    var _a4, _b2;
    const keySeparator = options.keySeparator !== void 0 ? options.keySeparator : this.options.keySeparator;
    const ignoreJSONStructure = options.ignoreJSONStructure !== void 0 ? options.ignoreJSONStructure : this.options.ignoreJSONStructure;
    let path;
    if (lng.includes(".")) {
      path = lng.split(".");
    } else {
      path = [lng, ns];
      if (key) {
        if (Array.isArray(key)) {
          path.push(...key);
        } else if (isString(key) && keySeparator) {
          path.push(...key.split(keySeparator));
        } else {
          path.push(key);
        }
      }
    }
    const result = getPath(this.data, path);
    if (!result && !ns && !key && lng.includes(".")) {
      lng = path[0];
      ns = path[1];
      key = path.slice(2).join(".");
    }
    if (result || !ignoreJSONStructure || !isString(key)) return result;
    return deepFind((_b2 = (_a4 = this.data) == null ? void 0 : _a4[lng]) == null ? void 0 : _b2[ns], key, keySeparator);
  }
  addResource(lng, ns, key, value, options = {
    silent: false
  }) {
    const keySeparator = options.keySeparator !== void 0 ? options.keySeparator : this.options.keySeparator;
    let path = [lng, ns];
    if (key) path = path.concat(keySeparator ? key.split(keySeparator) : key);
    if (lng.includes(".")) {
      path = lng.split(".");
      value = ns;
      ns = path[1];
    }
    this.addNamespaces(ns);
    setPath(this.data, path, value);
    if (!options.silent) this.emit("added", lng, ns, key, value);
  }
  addResources(lng, ns, resources, options = {
    silent: false
  }) {
    for (const m3 in resources) {
      if (isString(resources[m3]) || Array.isArray(resources[m3])) this.addResource(lng, ns, m3, resources[m3], {
        silent: true
      });
    }
    if (!options.silent) this.emit("added", lng, ns, resources);
  }
  addResourceBundle(lng, ns, resources, deep, overwrite, options = {
    silent: false,
    skipCopy: false
  }) {
    let path = [lng, ns];
    if (lng.includes(".")) {
      path = lng.split(".");
      deep = resources;
      resources = ns;
      ns = path[1];
    }
    this.addNamespaces(ns);
    let pack = getPath(this.data, path) || {};
    if (!options.skipCopy) resources = JSON.parse(JSON.stringify(resources));
    if (deep) {
      deepExtend(pack, resources, overwrite);
    } else {
      pack = {
        ...pack,
        ...resources
      };
    }
    setPath(this.data, path, pack);
    if (!options.silent) this.emit("added", lng, ns, resources);
  }
  removeResourceBundle(lng, ns) {
    if (this.hasResourceBundle(lng, ns)) {
      delete this.data[lng][ns];
    }
    this.removeNamespaces(ns);
    this.emit("removed", lng, ns);
  }
  hasResourceBundle(lng, ns) {
    return this.getResource(lng, ns) !== void 0;
  }
  getResourceBundle(lng, ns) {
    if (!ns) ns = this.options.defaultNS;
    return this.getResource(lng, ns);
  }
  getDataByLanguage(lng) {
    return this.data[lng];
  }
  hasLanguageSomeTranslations(lng) {
    const data = this.getDataByLanguage(lng);
    const n = data && Object.keys(data) || [];
    return !!n.find((v3) => data[v3] && Object.keys(data[v3]).length > 0);
  }
  toJSON() {
    return this.data;
  }
};
var postProcessor = {
  processors: {},
  addPostProcessor(module2) {
    this.processors[module2.name] = module2;
  },
  handle(processors, value, key, options, translator) {
    processors.forEach((processor) => {
      var _a4, _b2;
      value = (_b2 = (_a4 = this.processors[processor]) == null ? void 0 : _a4.process(value, key, options, translator)) != null ? _b2 : value;
    });
    return value;
  }
};
var PATH_KEY = /* @__PURE__ */ Symbol("i18next/PATH_KEY");
function createProxy() {
  const state = [];
  const handler = /* @__PURE__ */ Object.create(null);
  let proxy;
  handler.get = (target, key) => {
    var _a4;
    (_a4 = proxy == null ? void 0 : proxy.revoke) == null ? void 0 : _a4.call(proxy);
    if (key === PATH_KEY) return state;
    state.push(key);
    proxy = Proxy.revocable(target, handler);
    return proxy.proxy;
  };
  return Proxy.revocable(/* @__PURE__ */ Object.create(null), handler).proxy;
}
function keysFromSelector(selector, opts) {
  var _a4, _b2;
  const {
    [PATH_KEY]: path
  } = selector(createProxy());
  const keySeparator = (_a4 = opts == null ? void 0 : opts.keySeparator) != null ? _a4 : ".";
  const nsSeparator = (_b2 = opts == null ? void 0 : opts.nsSeparator) != null ? _b2 : ":";
  const strict = (opts == null ? void 0 : opts.enableSelector) === "strict";
  if (path.length > 1 && nsSeparator) {
    const ns = opts == null ? void 0 : opts.ns;
    const nsList = strict ? Array.isArray(ns) ? ns : ns ? [ns] : null : Array.isArray(ns) ? ns : null;
    if (nsList) {
      const candidates = strict ? nsList : nsList.length > 1 ? nsList.slice(1) : [];
      if (candidates.includes(path[0])) {
        return `${path[0]}${nsSeparator}${path.slice(1).join(keySeparator)}`;
      }
    }
  }
  return path.join(keySeparator);
}
var shouldHandleAsObject = (res) => !isString(res) && typeof res !== "boolean" && typeof res !== "number";
var Translator = class _Translator extends EventEmitter {
  constructor(services, options = {}) {
    super();
    copy(["resourceStore", "languageUtils", "pluralResolver", "interpolator", "backendConnector", "i18nFormat", "utils"], services, this);
    this.options = options;
    if (this.options.keySeparator === void 0) {
      this.options.keySeparator = ".";
    }
    this.logger = baseLogger.create("translator");
    this.checkedLoadedFor = {};
  }
  changeLanguage(lng) {
    if (lng) this.language = lng;
  }
  exists(key, o3 = {
    interpolation: {}
  }) {
    const opt = {
      ...o3
    };
    if (key == null) return false;
    const resolved2 = this.resolve(key, opt);
    if ((resolved2 == null ? void 0 : resolved2.res) === void 0) return false;
    const isObject = shouldHandleAsObject(resolved2.res);
    if (opt.returnObjects === false && isObject) {
      return false;
    }
    return true;
  }
  extractFromKey(key, opt) {
    let nsSeparator = opt.nsSeparator !== void 0 ? opt.nsSeparator : this.options.nsSeparator;
    if (nsSeparator === void 0) nsSeparator = ":";
    const keySeparator = opt.keySeparator !== void 0 ? opt.keySeparator : this.options.keySeparator;
    let namespaces = opt.ns || this.options.defaultNS || [];
    const wouldCheckForNsInKey = nsSeparator && key.includes(nsSeparator);
    const seemsNaturalLanguage = !this.options.userDefinedKeySeparator && !opt.keySeparator && !this.options.userDefinedNsSeparator && !opt.nsSeparator && !looksLikeObjectPath(key, nsSeparator, keySeparator);
    if (wouldCheckForNsInKey && !seemsNaturalLanguage) {
      const m3 = key.match(this.interpolator.nestingRegexp);
      if (m3 && m3.length > 0) {
        return {
          key,
          namespaces: isString(namespaces) ? [namespaces] : namespaces
        };
      }
      const parts = key.split(nsSeparator);
      if (nsSeparator !== keySeparator || nsSeparator === keySeparator && this.options.ns.includes(parts[0])) namespaces = parts.shift();
      key = parts.join(keySeparator);
    }
    return {
      key,
      namespaces: isString(namespaces) ? [namespaces] : namespaces
    };
  }
  translate(keys, o3, lastKey) {
    let opt = typeof o3 === "object" ? {
      ...o3
    } : o3;
    if (typeof opt !== "object" && this.options.overloadTranslationOptionHandler) {
      opt = this.options.overloadTranslationOptionHandler(arguments);
    }
    if (typeof opt === "object") opt = {
      ...opt
    };
    if (!opt) opt = {};
    if (keys == null) return "";
    if (typeof keys === "function") keys = keysFromSelector(keys, {
      ...this.options,
      ...opt
    });
    if (!Array.isArray(keys)) keys = [String(keys)];
    keys = keys.map((k3) => typeof k3 === "function" ? keysFromSelector(k3, {
      ...this.options,
      ...opt
    }) : String(k3));
    const returnDetails = opt.returnDetails !== void 0 ? opt.returnDetails : this.options.returnDetails;
    const keySeparator = opt.keySeparator !== void 0 ? opt.keySeparator : this.options.keySeparator;
    const {
      key,
      namespaces
    } = this.extractFromKey(keys[keys.length - 1], opt);
    const namespace = namespaces[namespaces.length - 1];
    let nsSeparator = opt.nsSeparator !== void 0 ? opt.nsSeparator : this.options.nsSeparator;
    if (nsSeparator === void 0) nsSeparator = ":";
    const lng = opt.lng || this.language;
    const appendNamespaceToCIMode = opt.appendNamespaceToCIMode || this.options.appendNamespaceToCIMode;
    if ((lng == null ? void 0 : lng.toLowerCase()) === "cimode") {
      if (appendNamespaceToCIMode) {
        if (returnDetails) {
          return {
            res: `${namespace}${nsSeparator}${key}`,
            usedKey: key,
            exactUsedKey: key,
            usedLng: lng,
            usedNS: namespace,
            usedParams: this.getUsedParamsDetails(opt)
          };
        }
        return `${namespace}${nsSeparator}${key}`;
      }
      if (returnDetails) {
        return {
          res: key,
          usedKey: key,
          exactUsedKey: key,
          usedLng: lng,
          usedNS: namespace,
          usedParams: this.getUsedParamsDetails(opt)
        };
      }
      return key;
    }
    const resolved2 = this.resolve(keys, opt);
    let res = resolved2 == null ? void 0 : resolved2.res;
    const resUsedKey = (resolved2 == null ? void 0 : resolved2.usedKey) || key;
    const resExactUsedKey = (resolved2 == null ? void 0 : resolved2.exactUsedKey) || key;
    const noObject = ["[object Number]", "[object Function]", "[object RegExp]"];
    const joinArrays = opt.joinArrays !== void 0 ? opt.joinArrays : this.options.joinArrays;
    const handleAsObjectInI18nFormat = !this.i18nFormat || this.i18nFormat.handleAsObject;
    const needsPluralHandling = opt.count !== void 0 && !isString(opt.count);
    const hasDefaultValue = _Translator.hasDefaultValue(opt);
    const defaultValueSuffix = needsPluralHandling ? this.pluralResolver.getSuffix(lng, opt.count, opt) : "";
    const defaultValueSuffixOrdinalFallback = opt.ordinal && needsPluralHandling ? this.pluralResolver.getSuffix(lng, opt.count, {
      ordinal: false
    }) : "";
    const needsZeroSuffixLookup = needsPluralHandling && !opt.ordinal && opt.count === 0;
    const defaultValue = needsZeroSuffixLookup && opt[`defaultValue${this.options.pluralSeparator}zero`] || opt[`defaultValue${defaultValueSuffix}`] || opt[`defaultValue${defaultValueSuffixOrdinalFallback}`] || opt.defaultValue;
    let resForObjHndl = res;
    if (handleAsObjectInI18nFormat && !res && hasDefaultValue) {
      resForObjHndl = defaultValue;
    }
    const handleAsObject = shouldHandleAsObject(resForObjHndl);
    const resType = Object.prototype.toString.apply(resForObjHndl);
    if (handleAsObjectInI18nFormat && resForObjHndl && handleAsObject && !noObject.includes(resType) && !(isString(joinArrays) && Array.isArray(resForObjHndl))) {
      if (!opt.returnObjects && !this.options.returnObjects) {
        if (!this.options.returnedObjectHandler) {
          this.logger.warn("accessing an object - but returnObjects options is not enabled!");
        }
        const r3 = this.options.returnedObjectHandler ? this.options.returnedObjectHandler(resUsedKey, resForObjHndl, {
          ...opt,
          ns: namespaces
        }) : `key '${key} (${this.language})' returned an object instead of string.`;
        if (returnDetails) {
          resolved2.res = r3;
          resolved2.usedParams = this.getUsedParamsDetails(opt);
          return resolved2;
        }
        return r3;
      }
      if (keySeparator) {
        const resTypeIsArray = Array.isArray(resForObjHndl);
        const copy2 = resTypeIsArray ? [] : {};
        const newKeyToUse = resTypeIsArray ? resExactUsedKey : resUsedKey;
        for (const m3 in resForObjHndl) {
          if (Object.prototype.hasOwnProperty.call(resForObjHndl, m3)) {
            const deepKey = `${newKeyToUse}${keySeparator}${m3}`;
            if (hasDefaultValue && !res) {
              copy2[m3] = this.translate(deepKey, {
                ...opt,
                defaultValue: shouldHandleAsObject(defaultValue) ? defaultValue[m3] : void 0,
                ...{
                  joinArrays: false,
                  ns: namespaces
                }
              });
            } else {
              copy2[m3] = this.translate(deepKey, {
                ...opt,
                ...{
                  joinArrays: false,
                  ns: namespaces
                }
              });
            }
            if (copy2[m3] === deepKey) copy2[m3] = resForObjHndl[m3];
          }
        }
        res = copy2;
      }
    } else if (handleAsObjectInI18nFormat && isString(joinArrays) && Array.isArray(res)) {
      res = res.join(joinArrays);
      if (res) res = this.extendTranslation(res, keys, opt, lastKey);
    } else {
      let usedDefault = false;
      let usedKey = false;
      if (!this.isValidLookup(res) && hasDefaultValue) {
        usedDefault = true;
        res = defaultValue;
      }
      if (!this.isValidLookup(res)) {
        usedKey = true;
        res = key;
      }
      const missingKeyNoValueFallbackToKey = opt.missingKeyNoValueFallbackToKey || this.options.missingKeyNoValueFallbackToKey;
      const resForMissing = missingKeyNoValueFallbackToKey && usedKey ? void 0 : res;
      const updateMissing = hasDefaultValue && defaultValue !== res && this.options.updateMissing;
      if (usedKey || usedDefault || updateMissing) {
        this.logger.log(updateMissing ? "updateKey" : "missingKey", lng, namespace, needsPluralHandling && !updateMissing ? `${key}${this.pluralResolver.getSuffix(lng, opt.count, opt)}` : key, updateMissing ? defaultValue : res);
        if (keySeparator) {
          const fk = this.resolve(key, {
            ...opt,
            keySeparator: false
          });
          if (fk && fk.res) this.logger.warn("Seems the loaded translations were in flat JSON format instead of nested. Either set keySeparator: false on init or make sure your translations are published in nested format.");
        }
        let lngs = [];
        const fallbackLngs = this.languageUtils.getFallbackCodes(this.options.fallbackLng, opt.lng || this.language);
        if (this.options.saveMissingTo === "fallback" && fallbackLngs && fallbackLngs[0]) {
          for (let i2 = 0; i2 < fallbackLngs.length; i2++) {
            lngs.push(fallbackLngs[i2]);
          }
        } else if (this.options.saveMissingTo === "all") {
          lngs = this.languageUtils.toResolveHierarchy(opt.lng || this.language);
        } else {
          lngs.push(opt.lng || this.language);
        }
        const send = (l3, k3, specificDefaultValue) => {
          var _a4;
          const defaultForMissing = hasDefaultValue && specificDefaultValue !== res ? specificDefaultValue : resForMissing;
          if (this.options.missingKeyHandler) {
            this.options.missingKeyHandler(l3, namespace, k3, defaultForMissing, updateMissing, opt);
          } else if ((_a4 = this.backendConnector) == null ? void 0 : _a4.saveMissing) {
            this.backendConnector.saveMissing(l3, namespace, k3, defaultForMissing, updateMissing, opt);
          }
          this.emit("missingKey", l3, namespace, k3, res);
        };
        if (this.options.saveMissing) {
          if (this.options.saveMissingPlurals && needsPluralHandling) {
            lngs.forEach((language) => {
              const suffixes = this.pluralResolver.getSuffixes(language, opt);
              if (needsZeroSuffixLookup && opt[`defaultValue${this.options.pluralSeparator}zero`] && !suffixes.includes(`${this.options.pluralSeparator}zero`)) {
                suffixes.push(`${this.options.pluralSeparator}zero`);
              }
              suffixes.forEach((suffix) => {
                send([language], key + suffix, opt[`defaultValue${suffix}`] || defaultValue);
              });
            });
          } else {
            send(lngs, key, defaultValue);
          }
        }
      }
      res = this.extendTranslation(res, keys, opt, resolved2, lastKey);
      if (usedKey && res === key && this.options.appendNamespaceToMissingKey) {
        res = `${namespace}${nsSeparator}${key}`;
      }
      if ((usedKey || usedDefault) && this.options.parseMissingKeyHandler) {
        res = this.options.parseMissingKeyHandler(this.options.appendNamespaceToMissingKey ? `${namespace}${nsSeparator}${key}` : key, usedDefault ? res : void 0, opt);
      }
    }
    if (returnDetails) {
      resolved2.res = res;
      resolved2.usedParams = this.getUsedParamsDetails(opt);
      return resolved2;
    }
    return res;
  }
  extendTranslation(res, key, opt, resolved2, lastKey) {
    var _a4, _b2;
    if ((_a4 = this.i18nFormat) == null ? void 0 : _a4.parse) {
      res = this.i18nFormat.parse(res, {
        ...this.options.interpolation.defaultVariables,
        ...opt
      }, opt.lng || this.language || resolved2.usedLng, resolved2.usedNS, resolved2.usedKey, {
        resolved: resolved2
      });
    } else if (!opt.skipInterpolation) {
      if (opt.interpolation) this.interpolator.init({
        ...opt,
        ...{
          interpolation: {
            ...this.options.interpolation,
            ...opt.interpolation
          }
        }
      });
      const skipOnVariables = isString(res) && (((_b2 = opt == null ? void 0 : opt.interpolation) == null ? void 0 : _b2.skipOnVariables) !== void 0 ? opt.interpolation.skipOnVariables : this.options.interpolation.skipOnVariables);
      let nestBef;
      if (skipOnVariables) {
        const nb = res.match(this.interpolator.nestingRegexp);
        nestBef = nb && nb.length;
      }
      let data = opt.replace && !isString(opt.replace) ? opt.replace : opt;
      if (this.options.interpolation.defaultVariables) data = {
        ...this.options.interpolation.defaultVariables,
        ...data
      };
      res = this.interpolator.interpolate(res, data, opt.lng || this.language || resolved2.usedLng, opt);
      if (skipOnVariables) {
        const na = res.match(this.interpolator.nestingRegexp);
        const nestAft = na && na.length;
        if (nestBef < nestAft) opt.nest = false;
      }
      if (!opt.lng && resolved2 && resolved2.res) opt.lng = this.language || resolved2.usedLng;
      if (opt.nest !== false) res = this.interpolator.nest(res, (...args) => {
        if ((lastKey == null ? void 0 : lastKey[0]) === args[0] && !opt.context) {
          this.logger.warn(`It seems you are nesting recursively key: ${args[0]} in key: ${key[0]}`);
          return null;
        }
        return this.translate(...args, key);
      }, opt);
      if (opt.interpolation) this.interpolator.reset();
    }
    const postProcess = opt.postProcess || this.options.postProcess;
    const postProcessorNames = isString(postProcess) ? [postProcess] : postProcess;
    if (res != null && (postProcessorNames == null ? void 0 : postProcessorNames.length) && opt.applyPostProcessor !== false) {
      res = postProcessor.handle(postProcessorNames, res, key, this.options && this.options.postProcessPassResolved ? {
        i18nResolved: {
          ...resolved2,
          usedParams: this.getUsedParamsDetails(opt)
        },
        ...opt
      } : opt, this);
    }
    return res;
  }
  resolve(keys, opt = {}) {
    let found;
    let usedKey;
    let exactUsedKey;
    let usedLng;
    let usedNS;
    if (isString(keys)) keys = [keys];
    if (Array.isArray(keys)) keys = keys.map((k3) => typeof k3 === "function" ? keysFromSelector(k3, {
      ...this.options,
      ...opt
    }) : k3);
    keys.forEach((k3) => {
      if (this.isValidLookup(found)) return;
      const extracted = this.extractFromKey(k3, opt);
      const key = extracted.key;
      usedKey = key;
      let namespaces = extracted.namespaces;
      if (this.options.fallbackNS) namespaces = namespaces.concat(this.options.fallbackNS);
      const needsPluralHandling = opt.count !== void 0 && !isString(opt.count);
      const needsZeroSuffixLookup = needsPluralHandling && !opt.ordinal && opt.count === 0;
      const needsContextHandling = opt.context !== void 0 && (isString(opt.context) || typeof opt.context === "number") && opt.context !== "";
      const codes = opt.lngs ? opt.lngs : this.languageUtils.toResolveHierarchy(opt.lng || this.language, opt.fallbackLng);
      namespaces.forEach((ns) => {
        var _a4, _b2;
        if (this.isValidLookup(found)) return;
        usedNS = ns;
        if (!this.checkedLoadedFor[`${codes[0]}-${ns}`] && ((_a4 = this.utils) == null ? void 0 : _a4.hasLoadedNamespace) && !((_b2 = this.utils) == null ? void 0 : _b2.hasLoadedNamespace(usedNS))) {
          this.checkedLoadedFor[`${codes[0]}-${ns}`] = true;
          this.logger.warn(`key "${usedKey}" for languages "${codes.join(", ")}" won't get resolved as namespace "${usedNS}" was not yet loaded`, "This means something IS WRONG in your setup. You access the t function before i18next.init / i18next.loadNamespace / i18next.changeLanguage was done. Wait for the callback or Promise to resolve before accessing it!!!");
        }
        codes.forEach((code) => {
          var _a5;
          if (this.isValidLookup(found)) return;
          usedLng = code;
          const finalKeys = [key];
          if ((_a5 = this.i18nFormat) == null ? void 0 : _a5.addLookupKeys) {
            this.i18nFormat.addLookupKeys(finalKeys, key, code, ns, opt);
          } else {
            let pluralSuffix;
            if (needsPluralHandling) pluralSuffix = this.pluralResolver.getSuffix(code, opt.count, opt);
            const zeroSuffix = `${this.options.pluralSeparator}zero`;
            const ordinalPrefix = `${this.options.pluralSeparator}ordinal${this.options.pluralSeparator}`;
            if (needsPluralHandling) {
              if (opt.ordinal && pluralSuffix.startsWith(ordinalPrefix)) {
                finalKeys.push(key + pluralSuffix.replace(ordinalPrefix, this.options.pluralSeparator));
              }
              finalKeys.push(key + pluralSuffix);
              if (needsZeroSuffixLookup) {
                finalKeys.push(key + zeroSuffix);
              }
            }
            if (needsContextHandling) {
              const contextKey = `${key}${this.options.contextSeparator || "_"}${opt.context}`;
              finalKeys.push(contextKey);
              if (needsPluralHandling) {
                if (opt.ordinal && pluralSuffix.startsWith(ordinalPrefix)) {
                  finalKeys.push(contextKey + pluralSuffix.replace(ordinalPrefix, this.options.pluralSeparator));
                }
                finalKeys.push(contextKey + pluralSuffix);
                if (needsZeroSuffixLookup) {
                  finalKeys.push(contextKey + zeroSuffix);
                }
              }
            }
          }
          let possibleKey;
          while (possibleKey = finalKeys.pop()) {
            if (!this.isValidLookup(found)) {
              exactUsedKey = possibleKey;
              found = this.getResource(code, ns, possibleKey, opt);
            }
          }
        });
      });
    });
    return {
      res: found,
      usedKey,
      exactUsedKey,
      usedLng,
      usedNS
    };
  }
  isValidLookup(res) {
    return res !== void 0 && !(!this.options.returnNull && res === null) && !(!this.options.returnEmptyString && res === "");
  }
  getResource(code, ns, key, options = {}) {
    var _a4;
    if ((_a4 = this.i18nFormat) == null ? void 0 : _a4.getResource) return this.i18nFormat.getResource(code, ns, key, options);
    return this.resourceStore.getResource(code, ns, key, options);
  }
  getUsedParamsDetails(options = {}) {
    const optionsKeys = ["defaultValue", "ordinal", "context", "replace", "lng", "lngs", "fallbackLng", "ns", "keySeparator", "nsSeparator", "returnObjects", "returnDetails", "joinArrays", "postProcess", "interpolation"];
    const useOptionsReplaceForData = options.replace && !isString(options.replace);
    let data = useOptionsReplaceForData ? options.replace : options;
    if (useOptionsReplaceForData && typeof options.count !== "undefined") {
      data.count = options.count;
    }
    if (this.options.interpolation.defaultVariables) {
      data = {
        ...this.options.interpolation.defaultVariables,
        ...data
      };
    }
    if (!useOptionsReplaceForData) {
      data = {
        ...data
      };
      for (const key of optionsKeys) {
        delete data[key];
      }
    }
    return data;
  }
  static hasDefaultValue(options) {
    const prefix = "defaultValue";
    for (const option in options) {
      if (Object.prototype.hasOwnProperty.call(options, option) && option.startsWith(prefix) && void 0 !== options[option]) {
        return true;
      }
    }
    return false;
  }
};
var LanguageUtil = class {
  constructor(options) {
    this.options = options;
    this.supportedLngs = this.options.supportedLngs || false;
    this.logger = baseLogger.create("languageUtils");
  }
  getScriptPartFromCode(code) {
    code = getCleanedCode(code);
    if (!code || !code.includes("-")) return null;
    const p3 = code.split("-");
    if (p3.length === 2) return null;
    p3.pop();
    if (p3[p3.length - 1].toLowerCase() === "x") return null;
    return this.formatLanguageCode(p3.join("-"));
  }
  getLanguagePartFromCode(code) {
    code = getCleanedCode(code);
    if (!code || !code.includes("-")) return code;
    const p3 = code.split("-");
    return this.formatLanguageCode(p3[0]);
  }
  formatLanguageCode(code) {
    if (isString(code) && code.includes("-")) {
      let formattedCode;
      try {
        formattedCode = Intl.getCanonicalLocales(code)[0];
      } catch (e2) {
      }
      if (formattedCode && this.options.lowerCaseLng) {
        formattedCode = formattedCode.toLowerCase();
      }
      if (formattedCode) return formattedCode;
      if (this.options.lowerCaseLng) {
        return code.toLowerCase();
      }
      return code;
    }
    return this.options.cleanCode || this.options.lowerCaseLng ? code.toLowerCase() : code;
  }
  isSupportedCode(code) {
    if (this.options.load === "languageOnly" || this.options.nonExplicitSupportedLngs) {
      code = this.getLanguagePartFromCode(code);
    }
    return !this.supportedLngs || !this.supportedLngs.length || this.supportedLngs.includes(code);
  }
  getBestMatchFromCodes(codes) {
    if (!codes) return null;
    let found;
    codes.forEach((code) => {
      if (found) return;
      const cleanedLng = this.formatLanguageCode(code);
      if (!this.options.supportedLngs || this.isSupportedCode(cleanedLng)) found = cleanedLng;
    });
    if (!found && this.options.supportedLngs) {
      codes.forEach((code) => {
        if (found) return;
        const lngScOnly = this.getScriptPartFromCode(code);
        if (this.isSupportedCode(lngScOnly)) return found = lngScOnly;
        const lngOnly = this.getLanguagePartFromCode(code);
        if (this.isSupportedCode(lngOnly)) return found = lngOnly;
        found = this.options.supportedLngs.find((supportedLng) => {
          if (supportedLng === lngOnly) return true;
          if (!supportedLng.includes("-") && !lngOnly.includes("-")) return false;
          if (supportedLng.includes("-") && !lngOnly.includes("-") && supportedLng.slice(0, supportedLng.indexOf("-")) === lngOnly) return true;
          if (supportedLng.startsWith(lngOnly) && lngOnly.length > 1) return true;
          return false;
        });
      });
    }
    if (!found) found = this.getFallbackCodes(this.options.fallbackLng)[0];
    return found;
  }
  getFallbackCodes(fallbacks, code) {
    if (!fallbacks) return [];
    if (typeof fallbacks === "function") fallbacks = fallbacks(code);
    if (isString(fallbacks)) fallbacks = [fallbacks];
    if (Array.isArray(fallbacks)) return fallbacks;
    if (!code) return fallbacks.default || [];
    let found = fallbacks[code];
    if (!found) found = fallbacks[this.getScriptPartFromCode(code)];
    if (!found) found = fallbacks[this.formatLanguageCode(code)];
    if (!found) found = fallbacks[this.getLanguagePartFromCode(code)];
    if (!found) found = fallbacks.default;
    return found || [];
  }
  toResolveHierarchy(code, fallbackCode) {
    const fallbackCodes = this.getFallbackCodes((fallbackCode === false ? [] : fallbackCode) || this.options.fallbackLng || [], code);
    const codes = [];
    const addCode = (c3) => {
      if (!c3) return;
      if (this.isSupportedCode(c3)) {
        codes.push(c3);
      } else {
        this.logger.warn(`rejecting language code not found in supportedLngs: ${c3}`);
      }
    };
    if (isString(code) && (code.includes("-") || code.includes("_"))) {
      if (this.options.load !== "languageOnly") addCode(this.formatLanguageCode(code));
      if (this.options.load !== "languageOnly" && this.options.load !== "currentOnly") addCode(this.getScriptPartFromCode(code));
      if (this.options.load !== "currentOnly") addCode(this.getLanguagePartFromCode(code));
    } else if (isString(code)) {
      addCode(this.formatLanguageCode(code));
    }
    fallbackCodes.forEach((fc) => {
      if (!codes.includes(fc)) addCode(this.formatLanguageCode(fc));
    });
    return codes;
  }
};
var suffixesOrder = {
  zero: 0,
  one: 1,
  two: 2,
  few: 3,
  many: 4,
  other: 5
};
var dummyRule = {
  select: (count) => count === 1 ? "one" : "other",
  resolvedOptions: () => ({
    pluralCategories: ["one", "other"]
  })
};
var PluralResolver = class {
  constructor(languageUtils, options = {}) {
    this.languageUtils = languageUtils;
    this.options = options;
    this.logger = baseLogger.create("pluralResolver");
    this.pluralRulesCache = {};
  }
  clearCache() {
    this.pluralRulesCache = {};
  }
  getRule(code, options = {}) {
    const cleanedCode = getCleanedCode(code === "dev" ? "en" : code);
    const type = options.ordinal ? "ordinal" : "cardinal";
    const cacheKey = JSON.stringify({
      cleanedCode,
      type
    });
    if (cacheKey in this.pluralRulesCache) {
      return this.pluralRulesCache[cacheKey];
    }
    let rule;
    try {
      rule = new Intl.PluralRules(cleanedCode, {
        type
      });
    } catch (err) {
      if (typeof Intl === "undefined") {
        this.logger.error("No Intl support, please use an Intl polyfill!");
        return dummyRule;
      }
      if (!code.match(/-|_/)) return dummyRule;
      const lngPart = this.languageUtils.getLanguagePartFromCode(code);
      rule = this.getRule(lngPart, options);
    }
    this.pluralRulesCache[cacheKey] = rule;
    return rule;
  }
  needsPlural(code, options = {}) {
    let rule = this.getRule(code, options);
    if (!rule) rule = this.getRule("dev", options);
    return (rule == null ? void 0 : rule.resolvedOptions().pluralCategories.length) > 1;
  }
  getPluralFormsOfKey(code, key, options = {}) {
    return this.getSuffixes(code, options).map((suffix) => `${key}${suffix}`);
  }
  getSuffixes(code, options = {}) {
    let rule = this.getRule(code, options);
    if (!rule) rule = this.getRule("dev", options);
    if (!rule) return [];
    return rule.resolvedOptions().pluralCategories.sort((pluralCategory1, pluralCategory2) => suffixesOrder[pluralCategory1] - suffixesOrder[pluralCategory2]).map((pluralCategory) => `${this.options.prepend}${options.ordinal ? `ordinal${this.options.prepend}` : ""}${pluralCategory}`);
  }
  getSuffix(code, count, options = {}) {
    const rule = this.getRule(code, options);
    if (rule) {
      return `${this.options.prepend}${options.ordinal ? `ordinal${this.options.prepend}` : ""}${rule.select(count)}`;
    }
    this.logger.warn(`no plural rule found for: ${code}`);
    return this.getSuffix("dev", count, options);
  }
};
var deepFindWithDefaults = (data, defaultData, key, keySeparator = ".", ignoreJSONStructure = true) => {
  let path = getPathWithDefaults(data, defaultData, key);
  if (!path && ignoreJSONStructure && isString(key)) {
    path = deepFind(data, key, keySeparator);
    if (path === void 0) path = deepFind(defaultData, key, keySeparator);
  }
  return path;
};
var regexSafe = (val) => val.replace(/\$/g, "$$$$");
var Interpolator = class {
  constructor(options = {}) {
    var _a4;
    this.logger = baseLogger.create("interpolator");
    this.options = options;
    this.format = ((_a4 = options == null ? void 0 : options.interpolation) == null ? void 0 : _a4.format) || ((value) => value);
    this.init(options);
  }
  init(options = {}) {
    if (!options.interpolation) options.interpolation = {
      escapeValue: true
    };
    const {
      escape: escape$1,
      escapeValue,
      useRawValueToEscape,
      prefix,
      prefixEscaped,
      suffix,
      suffixEscaped,
      formatSeparator,
      unescapeSuffix,
      unescapePrefix,
      nestingPrefix,
      nestingPrefixEscaped,
      nestingSuffix,
      nestingSuffixEscaped,
      nestingOptionsSeparator,
      maxReplaces,
      alwaysFormat
    } = options.interpolation;
    this.escape = escape$1 !== void 0 ? escape$1 : escape;
    this.escapeValue = escapeValue !== void 0 ? escapeValue : true;
    this.useRawValueToEscape = useRawValueToEscape !== void 0 ? useRawValueToEscape : false;
    this.prefix = prefix ? regexEscape(prefix) : prefixEscaped || "{{";
    this.suffix = suffix ? regexEscape(suffix) : suffixEscaped || "}}";
    this.formatSeparator = formatSeparator || ",";
    this.unescapePrefix = unescapeSuffix ? "" : unescapePrefix ? regexEscape(unescapePrefix) : "-";
    this.unescapeSuffix = this.unescapePrefix ? "" : unescapeSuffix ? regexEscape(unescapeSuffix) : "";
    this.nestingPrefix = nestingPrefix ? regexEscape(nestingPrefix) : nestingPrefixEscaped || regexEscape("$t(");
    this.nestingSuffix = nestingSuffix ? regexEscape(nestingSuffix) : nestingSuffixEscaped || regexEscape(")");
    this.nestingOptionsSeparator = nestingOptionsSeparator || ",";
    this.maxReplaces = maxReplaces || 1e3;
    this.alwaysFormat = alwaysFormat !== void 0 ? alwaysFormat : false;
    this.resetRegExp();
  }
  reset() {
    if (this.options) this.init(this.options);
  }
  resetRegExp() {
    const getOrResetRegExp = (existingRegExp, pattern) => {
      if ((existingRegExp == null ? void 0 : existingRegExp.source) === pattern) {
        existingRegExp.lastIndex = 0;
        return existingRegExp;
      }
      return new RegExp(pattern, "g");
    };
    this.regexp = getOrResetRegExp(this.regexp, `${this.prefix}(.+?)${this.suffix}`);
    this.regexpUnescape = getOrResetRegExp(this.regexpUnescape, `${this.prefix}${this.unescapePrefix}(.+?)${this.unescapeSuffix}${this.suffix}`);
    this.nestingRegexp = getOrResetRegExp(this.nestingRegexp, `${this.nestingPrefix}((?:[^()"']+|"[^"]*"|'[^']*'|\\((?:[^()]|"[^"]*"|'[^']*')*\\))*?)${this.nestingSuffix}`);
  }
  interpolate(str, data, lng, options) {
    var _a4;
    let match;
    let value;
    let replaces;
    const defaultData = this.options && this.options.interpolation && this.options.interpolation.defaultVariables || {};
    const handleFormat = (key) => {
      if (!key.includes(this.formatSeparator)) {
        const path = deepFindWithDefaults(data, defaultData, key, this.options.keySeparator, this.options.ignoreJSONStructure);
        return this.alwaysFormat ? this.format(path, void 0, lng, {
          ...options,
          ...data,
          interpolationkey: key
        }) : path;
      }
      const p3 = key.split(this.formatSeparator);
      const k3 = p3.shift().trim();
      const f3 = p3.join(this.formatSeparator).trim();
      return this.format(deepFindWithDefaults(data, defaultData, k3, this.options.keySeparator, this.options.ignoreJSONStructure), f3, lng, {
        ...options,
        ...data,
        interpolationkey: k3
      });
    };
    this.resetRegExp();
    if (!this.escapeValue && typeof str === "string" && /\$t\([^)]*\{[^}]*\{\{/.test(str)) {
      this.logger.warn("nesting options string contains interpolated variables with escapeValue: false \u2014 if any of those values are attacker-controlled they can inject additional nesting options (e.g. redirect lng/ns). Sanitise untrusted input before passing it to t(), or keep escapeValue: true.");
    }
    const missingInterpolationHandler = (options == null ? void 0 : options.missingInterpolationHandler) || this.options.missingInterpolationHandler;
    const skipOnVariables = ((_a4 = options == null ? void 0 : options.interpolation) == null ? void 0 : _a4.skipOnVariables) !== void 0 ? options.interpolation.skipOnVariables : this.options.interpolation.skipOnVariables;
    const todos = [{
      regex: this.regexpUnescape,
      safeValue: (val) => regexSafe(val)
    }, {
      regex: this.regexp,
      safeValue: (val) => this.escapeValue ? regexSafe(this.escape(val)) : regexSafe(val)
    }];
    todos.forEach((todo) => {
      replaces = 0;
      while (match = todo.regex.exec(str)) {
        const matchedVar = match[1].trim();
        value = handleFormat(matchedVar);
        if (value === void 0) {
          if (typeof missingInterpolationHandler === "function") {
            const temp = missingInterpolationHandler(str, match, options);
            value = isString(temp) ? temp : "";
          } else if (options && Object.prototype.hasOwnProperty.call(options, matchedVar)) {
            value = "";
          } else if (skipOnVariables) {
            value = match[0];
            continue;
          } else {
            this.logger.warn(`missed to pass in variable ${matchedVar} for interpolating ${str}`);
            value = "";
          }
        } else if (!isString(value) && !this.useRawValueToEscape) {
          value = makeString(value);
        }
        const safeValue = todo.safeValue(value);
        str = str.replace(match[0], safeValue);
        if (skipOnVariables) {
          todo.regex.lastIndex += value.length;
          todo.regex.lastIndex -= match[0].length;
        } else {
          todo.regex.lastIndex = 0;
        }
        replaces++;
        if (replaces >= this.maxReplaces) {
          break;
        }
      }
    });
    return str;
  }
  nest(str, fc, options = {}) {
    let match;
    let value;
    let clonedOptions;
    const handleHasOptions = (key, inheritedOptions) => {
      var _a4, _b2;
      const sep = this.nestingOptionsSeparator;
      if (!key.includes(sep)) return key;
      const c3 = key.split(new RegExp(`${regexEscape(sep)}[ ]*{`));
      let optionsString = `{${c3[1]}`;
      key = c3[0];
      optionsString = this.interpolate(optionsString, clonedOptions);
      const matchedSingleQuotes = optionsString.match(/'/g);
      const matchedDoubleQuotes = optionsString.match(/"/g);
      if (((_a4 = matchedSingleQuotes == null ? void 0 : matchedSingleQuotes.length) != null ? _a4 : 0) % 2 === 0 && !matchedDoubleQuotes || ((_b2 = matchedDoubleQuotes == null ? void 0 : matchedDoubleQuotes.length) != null ? _b2 : 0) % 2 !== 0) {
        optionsString = optionsString.replace(/'/g, '"');
      }
      try {
        clonedOptions = JSON.parse(optionsString);
        if (inheritedOptions) clonedOptions = {
          ...inheritedOptions,
          ...clonedOptions
        };
      } catch (e2) {
        this.logger.warn(`failed parsing options string in nesting for key ${key}`, e2);
        return `${key}${sep}${optionsString}`;
      }
      if (clonedOptions.defaultValue && clonedOptions.defaultValue.includes(this.prefix)) delete clonedOptions.defaultValue;
      return key;
    };
    while (match = this.nestingRegexp.exec(str)) {
      let formatters = [];
      clonedOptions = {
        ...options
      };
      clonedOptions = clonedOptions.replace && !isString(clonedOptions.replace) ? clonedOptions.replace : clonedOptions;
      clonedOptions.applyPostProcessor = false;
      delete clonedOptions.defaultValue;
      const keyEndIndex = /{.*}/.test(match[1]) ? match[1].lastIndexOf("}") + 1 : match[1].indexOf(this.formatSeparator);
      if (keyEndIndex !== -1) {
        formatters = match[1].slice(keyEndIndex).split(this.formatSeparator).map((elem) => elem.trim()).filter(Boolean);
        match[1] = match[1].slice(0, keyEndIndex);
      }
      value = fc(handleHasOptions.call(this, match[1].trim(), clonedOptions), clonedOptions);
      if (value && match[0] === str && !isString(value)) return value;
      if (!isString(value)) value = makeString(value);
      if (!value) {
        this.logger.warn(`missed to resolve ${match[1]} for nesting ${str}`);
        value = "";
      }
      if (formatters.length) {
        value = formatters.reduce((v3, f3) => this.format(v3, f3, options.lng, {
          ...options,
          interpolationkey: match[1].trim()
        }), value.trim());
      }
      str = str.replace(match[0], value);
      this.regexp.lastIndex = 0;
    }
    return str;
  }
};
var parseFormatStr = (formatStr) => {
  let formatName = formatStr.toLowerCase().trim();
  const formatOptions = {};
  if (formatStr.includes("(")) {
    const p3 = formatStr.split("(");
    formatName = p3[0].toLowerCase().trim();
    const optStr = p3[1].slice(0, -1);
    if (formatName === "currency" && !optStr.includes(":")) {
      if (!formatOptions.currency) formatOptions.currency = optStr.trim();
    } else if (formatName === "relativetime" && !optStr.includes(":")) {
      if (!formatOptions.range) formatOptions.range = optStr.trim();
    } else {
      const opts = optStr.split(";");
      opts.forEach((opt) => {
        if (opt) {
          const [key, ...rest] = opt.split(":");
          const val = rest.join(":").trim().replace(/^'+|'+$/g, "");
          const trimmedKey = key.trim();
          if (!formatOptions[trimmedKey]) formatOptions[trimmedKey] = val;
          if (val === "false") formatOptions[trimmedKey] = false;
          if (val === "true") formatOptions[trimmedKey] = true;
          if (!isNaN(val)) formatOptions[trimmedKey] = parseInt(val, 10);
        }
      });
    }
  }
  return {
    formatName,
    formatOptions
  };
};
var createCachedFormatter = (fn) => {
  const cache = {};
  return (v3, l3, o3) => {
    let optForCache = o3;
    if (o3 && o3.interpolationkey && o3.formatParams && o3.formatParams[o3.interpolationkey] && o3[o3.interpolationkey]) {
      optForCache = {
        ...optForCache,
        [o3.interpolationkey]: void 0
      };
    }
    const key = l3 + JSON.stringify(optForCache);
    let frm = cache[key];
    if (!frm) {
      frm = fn(getCleanedCode(l3), o3);
      cache[key] = frm;
    }
    return frm(v3);
  };
};
var createNonCachedFormatter = (fn) => (v3, l3, o3) => fn(getCleanedCode(l3), o3)(v3);
var Formatter = class {
  constructor(options = {}) {
    this.logger = baseLogger.create("formatter");
    this.options = options;
    this.init(options);
  }
  init(services, options = {
    interpolation: {}
  }) {
    this.formatSeparator = options.interpolation.formatSeparator || ",";
    const cf = options.cacheInBuiltFormats ? createCachedFormatter : createNonCachedFormatter;
    this.formats = {
      number: cf((lng, opt) => {
        const formatter = new Intl.NumberFormat(lng, {
          ...opt
        });
        return (val) => formatter.format(val);
      }),
      currency: cf((lng, opt) => {
        const formatter = new Intl.NumberFormat(lng, {
          ...opt,
          style: "currency"
        });
        return (val) => formatter.format(val);
      }),
      datetime: cf((lng, opt) => {
        const formatter = new Intl.DateTimeFormat(lng, {
          ...opt
        });
        return (val) => formatter.format(val);
      }),
      relativetime: cf((lng, opt) => {
        const formatter = new Intl.RelativeTimeFormat(lng, {
          ...opt
        });
        return (val) => formatter.format(val, opt.range || "day");
      }),
      list: cf((lng, opt) => {
        const formatter = new Intl.ListFormat(lng, {
          ...opt
        });
        return (val) => formatter.format(val);
      })
    };
  }
  add(name, fc) {
    this.formats[name.toLowerCase().trim()] = fc;
  }
  addCached(name, fc) {
    this.formats[name.toLowerCase().trim()] = createCachedFormatter(fc);
  }
  format(value, format, lng, options = {}) {
    if (!format) return value;
    if (value == null) return value;
    const rawFormats = format.split(this.formatSeparator);
    const formats = [];
    for (let i2 = 0; i2 < rawFormats.length; i2++) {
      let f3 = rawFormats[i2];
      while (f3.indexOf("(") > -1 && !f3.includes(")") && i2 + 1 < rawFormats.length) {
        f3 = `${f3}${this.formatSeparator}${rawFormats[++i2]}`;
      }
      formats.push(f3);
    }
    const result = formats.reduce((mem, f3) => {
      var _a4;
      const {
        formatName,
        formatOptions
      } = parseFormatStr(f3);
      if (this.formats[formatName]) {
        let formatted = mem;
        try {
          const valOptions = ((_a4 = options == null ? void 0 : options.formatParams) == null ? void 0 : _a4[options.interpolationkey]) || {};
          const l3 = valOptions.locale || valOptions.lng || options.locale || options.lng || lng;
          formatted = this.formats[formatName](mem, l3, {
            ...formatOptions,
            ...options,
            ...valOptions
          });
        } catch (error) {
          this.logger.warn(error);
        }
        return formatted;
      } else {
        this.logger.warn(`there was no format function for ${formatName}`);
      }
      return mem;
    }, value);
    return result;
  }
};
var removePending = (q2, name) => {
  if (q2.pending[name] !== void 0) {
    delete q2.pending[name];
    q2.pendingCount--;
  }
};
var Connector = class extends EventEmitter {
  constructor(backend, store, services, options = {}) {
    var _a4, _b2;
    super();
    this.backend = backend;
    this.store = store;
    this.services = services;
    this.languageUtils = services.languageUtils;
    this.options = options;
    this.logger = baseLogger.create("backendConnector");
    this.waitingReads = [];
    this.maxParallelReads = options.maxParallelReads || 10;
    this.readingCalls = 0;
    this.maxRetries = options.maxRetries >= 0 ? options.maxRetries : 5;
    this.retryTimeout = options.retryTimeout >= 1 ? options.retryTimeout : 350;
    this.state = {};
    this.queue = [];
    (_b2 = (_a4 = this.backend) == null ? void 0 : _a4.init) == null ? void 0 : _b2.call(_a4, services, options.backend, options);
  }
  queueLoad(languages, namespaces, options, callback) {
    const toLoad = {};
    const pending = {};
    const toLoadLanguages = {};
    const toLoadNamespaces = {};
    languages.forEach((lng) => {
      let hasAllNamespaces = true;
      namespaces.forEach((ns) => {
        const name = `${lng}|${ns}`;
        if (!options.reload && this.store.hasResourceBundle(lng, ns)) {
          this.state[name] = 2;
        } else if (this.state[name] < 0) ;
        else if (this.state[name] === 1) {
          if (pending[name] === void 0) pending[name] = true;
        } else {
          this.state[name] = 1;
          hasAllNamespaces = false;
          if (pending[name] === void 0) pending[name] = true;
          if (toLoad[name] === void 0) toLoad[name] = true;
          if (toLoadNamespaces[ns] === void 0) toLoadNamespaces[ns] = true;
        }
      });
      if (!hasAllNamespaces) toLoadLanguages[lng] = true;
    });
    if (Object.keys(toLoad).length || Object.keys(pending).length) {
      this.queue.push({
        pending,
        pendingCount: Object.keys(pending).length,
        loaded: {},
        errors: [],
        callback
      });
    }
    return {
      toLoad: Object.keys(toLoad),
      pending: Object.keys(pending),
      toLoadLanguages: Object.keys(toLoadLanguages),
      toLoadNamespaces: Object.keys(toLoadNamespaces)
    };
  }
  loaded(name, err, data) {
    const s3 = name.split("|");
    const lng = s3[0];
    const ns = s3[1];
    if (err) this.emit("failedLoading", lng, ns, err);
    if (!err && data) {
      this.store.addResourceBundle(lng, ns, data, void 0, void 0, {
        skipCopy: true
      });
    }
    this.state[name] = err ? -1 : 2;
    if (err && data) this.state[name] = 0;
    const loaded = {};
    this.queue.forEach((q2) => {
      pushPath(q2.loaded, [lng], ns);
      removePending(q2, name);
      if (err) q2.errors.push(err);
      if (q2.pendingCount === 0 && !q2.done) {
        Object.keys(q2.loaded).forEach((l3) => {
          if (!loaded[l3]) loaded[l3] = {};
          const loadedKeys = q2.loaded[l3];
          if (loadedKeys.length) {
            loadedKeys.forEach((n) => {
              if (loaded[l3][n] === void 0) loaded[l3][n] = true;
            });
          }
        });
        q2.done = true;
        if (q2.errors.length) {
          q2.callback(q2.errors);
        } else {
          q2.callback();
        }
      }
    });
    this.emit("loaded", loaded);
    this.queue = this.queue.filter((q2) => !q2.done);
  }
  read(lng, ns, fcName, tried = 0, wait = this.retryTimeout, callback) {
    if (!lng.length) return callback(null, {});
    if (this.readingCalls >= this.maxParallelReads) {
      this.waitingReads.push({
        lng,
        ns,
        fcName,
        tried,
        wait,
        callback
      });
      return;
    }
    this.readingCalls++;
    const resolver = (err, data) => {
      this.readingCalls--;
      if (this.waitingReads.length > 0) {
        const next = this.waitingReads.shift();
        this.read(next.lng, next.ns, next.fcName, next.tried, next.wait, next.callback);
      }
      if (err && data && tried < this.maxRetries) {
        setTimeout(() => {
          this.read(lng, ns, fcName, tried + 1, wait * 2, callback);
        }, wait);
        return;
      }
      callback(err, data);
    };
    const fc = this.backend[fcName].bind(this.backend);
    if (fc.length === 2) {
      try {
        const r3 = fc(lng, ns);
        if (r3 && typeof r3.then === "function") {
          r3.then((data) => resolver(null, data)).catch(resolver);
        } else {
          resolver(null, r3);
        }
      } catch (err) {
        resolver(err);
      }
      return;
    }
    return fc(lng, ns, resolver);
  }
  prepareLoading(languages, namespaces, options = {}, callback) {
    if (!this.backend) {
      this.logger.warn("No backend was added via i18next.use. Will not load resources.");
      return callback && callback();
    }
    if (isString(languages)) languages = this.languageUtils.toResolveHierarchy(languages);
    if (isString(namespaces)) namespaces = [namespaces];
    const toLoad = this.queueLoad(languages, namespaces, options, callback);
    if (!toLoad.toLoad.length) {
      if (!toLoad.pending.length) callback();
      return null;
    }
    toLoad.toLoad.forEach((name) => {
      this.loadOne(name);
    });
  }
  load(languages, namespaces, callback) {
    this.prepareLoading(languages, namespaces, {}, callback);
  }
  reload(languages, namespaces, callback) {
    this.prepareLoading(languages, namespaces, {
      reload: true
    }, callback);
  }
  loadOne(name, prefix = "") {
    const s3 = name.split("|");
    const lng = s3[0];
    const ns = s3[1];
    this.read(lng, ns, "read", void 0, void 0, (err, data) => {
      if (err) this.logger.warn(`${prefix}loading namespace ${ns} for language ${lng} failed`, err);
      if (!err && data) this.logger.log(`${prefix}loaded namespace ${ns} for language ${lng}`, data);
      this.loaded(name, err, data);
    });
  }
  saveMissing(languages, namespace, key, fallbackValue, isUpdate, options = {}, clb = () => {
  }) {
    var _a4, _b2, _c, _d, _e3;
    if (((_b2 = (_a4 = this.services) == null ? void 0 : _a4.utils) == null ? void 0 : _b2.hasLoadedNamespace) && !((_d = (_c = this.services) == null ? void 0 : _c.utils) == null ? void 0 : _d.hasLoadedNamespace(namespace))) {
      this.logger.warn(`did not save key "${key}" as the namespace "${namespace}" was not yet loaded`, "This means something IS WRONG in your setup. You access the t function before i18next.init / i18next.loadNamespace / i18next.changeLanguage was done. Wait for the callback or Promise to resolve before accessing it!!!");
      return;
    }
    if (key === void 0 || key === null || key === "") return;
    if ((_e3 = this.backend) == null ? void 0 : _e3.create) {
      const opts = {
        ...options,
        isUpdate
      };
      const fc = this.backend.create.bind(this.backend);
      if (fc.length < 6) {
        try {
          let r3;
          if (fc.length === 5) {
            r3 = fc(languages, namespace, key, fallbackValue, opts);
          } else {
            r3 = fc(languages, namespace, key, fallbackValue);
          }
          if (r3 && typeof r3.then === "function") {
            r3.then((data) => clb(null, data)).catch(clb);
          } else {
            clb(null, r3);
          }
        } catch (err) {
          clb(err);
        }
      } else {
        fc(languages, namespace, key, fallbackValue, clb, opts);
      }
    }
    if (!languages || !languages[0]) return;
    this.store.addResource(languages[0], namespace, key, fallbackValue);
  }
};
var get = () => ({
  debug: false,
  initAsync: true,
  ns: ["translation"],
  defaultNS: ["translation"],
  fallbackLng: ["dev"],
  fallbackNS: false,
  supportedLngs: false,
  nonExplicitSupportedLngs: false,
  load: "all",
  preload: false,
  keySeparator: ".",
  nsSeparator: ":",
  pluralSeparator: "_",
  contextSeparator: "_",
  enableSelector: false,
  partialBundledLanguages: false,
  saveMissing: false,
  updateMissing: false,
  saveMissingTo: "fallback",
  saveMissingPlurals: true,
  missingKeyHandler: false,
  missingInterpolationHandler: false,
  postProcess: false,
  postProcessPassResolved: false,
  returnNull: false,
  returnEmptyString: true,
  returnObjects: false,
  joinArrays: false,
  returnedObjectHandler: false,
  parseMissingKeyHandler: false,
  appendNamespaceToMissingKey: false,
  appendNamespaceToCIMode: false,
  overloadTranslationOptionHandler: (args) => {
    let ret = {};
    if (typeof args[1] === "object") ret = args[1];
    if (isString(args[1])) ret.defaultValue = args[1];
    if (isString(args[2])) ret.tDescription = args[2];
    if (typeof args[2] === "object" || typeof args[3] === "object") {
      const options = args[3] || args[2];
      Object.keys(options).forEach((key) => {
        ret[key] = options[key];
      });
    }
    return ret;
  },
  interpolation: {
    escapeValue: true,
    prefix: "{{",
    suffix: "}}",
    formatSeparator: ",",
    unescapePrefix: "-",
    nestingPrefix: "$t(",
    nestingSuffix: ")",
    nestingOptionsSeparator: ",",
    maxReplaces: 1e3,
    skipOnVariables: true
  },
  cacheInBuiltFormats: true
});
var transformOptions = (options) => {
  if (isString(options.ns)) options.ns = [options.ns];
  if (isString(options.fallbackLng)) options.fallbackLng = [options.fallbackLng];
  if (isString(options.fallbackNS)) options.fallbackNS = [options.fallbackNS];
  if (options.supportedLngs && !options.supportedLngs.includes("cimode")) {
    options.supportedLngs = options.supportedLngs.concat(["cimode"]);
  }
  return options;
};
var noop = () => {
};
var bindMemberFunctions = (inst) => {
  const mems = Object.getOwnPropertyNames(Object.getPrototypeOf(inst));
  mems.forEach((mem) => {
    if (typeof inst[mem] === "function") {
      inst[mem] = inst[mem].bind(inst);
    }
  });
};
var I18n = class _I18n extends EventEmitter {
  constructor(options = {}, callback) {
    super();
    this.options = transformOptions(options);
    this.services = {};
    this.logger = baseLogger;
    this.modules = {
      external: []
    };
    bindMemberFunctions(this);
    if (callback && !this.isInitialized && !options.isClone) {
      if (!this.options.initAsync) {
        this.init(options, callback);
        return this;
      }
      setTimeout(() => {
        this.init(options, callback);
      }, 0);
    }
  }
  init(options = {}, callback) {
    this.isInitializing = true;
    if (typeof options === "function") {
      callback = options;
      options = {};
    }
    if (options.defaultNS == null && options.ns) {
      if (isString(options.ns)) {
        options.defaultNS = options.ns;
      } else if (!options.ns.includes("translation")) {
        options.defaultNS = options.ns[0];
      }
    }
    const defOpts = get();
    this.options = {
      ...defOpts,
      ...this.options,
      ...transformOptions(options)
    };
    this.options.interpolation = {
      ...defOpts.interpolation,
      ...this.options.interpolation
    };
    if (options.keySeparator !== void 0) {
      this.options.userDefinedKeySeparator = options.keySeparator;
    }
    if (options.nsSeparator !== void 0) {
      this.options.userDefinedNsSeparator = options.nsSeparator;
    }
    if (typeof this.options.overloadTranslationOptionHandler !== "function") {
      this.options.overloadTranslationOptionHandler = defOpts.overloadTranslationOptionHandler;
    }
    const createClassOnDemand = (ClassOrObject) => {
      if (!ClassOrObject) return null;
      if (typeof ClassOrObject === "function") return new ClassOrObject();
      return ClassOrObject;
    };
    if (!this.options.isClone) {
      if (this.modules.logger) {
        baseLogger.init(createClassOnDemand(this.modules.logger), this.options);
      } else {
        baseLogger.init(null, this.options);
      }
      let formatter;
      if (this.modules.formatter) {
        formatter = this.modules.formatter;
      } else {
        formatter = Formatter;
      }
      const lu = new LanguageUtil(this.options);
      this.store = new ResourceStore(this.options.resources, this.options);
      const s3 = this.services;
      s3.logger = baseLogger;
      s3.resourceStore = this.store;
      s3.languageUtils = lu;
      s3.pluralResolver = new PluralResolver(lu, {
        prepend: this.options.pluralSeparator
      });
      if (formatter) {
        s3.formatter = createClassOnDemand(formatter);
        if (s3.formatter.init) s3.formatter.init(s3, this.options);
        this.options.interpolation.format = s3.formatter.format.bind(s3.formatter);
      }
      s3.interpolator = new Interpolator(this.options);
      s3.utils = {
        hasLoadedNamespace: this.hasLoadedNamespace.bind(this)
      };
      s3.backendConnector = new Connector(createClassOnDemand(this.modules.backend), s3.resourceStore, s3, this.options);
      s3.backendConnector.on("*", (event, ...args) => {
        this.emit(event, ...args);
      });
      if (this.modules.languageDetector) {
        s3.languageDetector = createClassOnDemand(this.modules.languageDetector);
        if (s3.languageDetector.init) s3.languageDetector.init(s3, this.options.detection, this.options);
      }
      if (this.modules.i18nFormat) {
        s3.i18nFormat = createClassOnDemand(this.modules.i18nFormat);
        if (s3.i18nFormat.init) s3.i18nFormat.init(this);
      }
      this.translator = new Translator(this.services, this.options);
      this.translator.on("*", (event, ...args) => {
        this.emit(event, ...args);
      });
      this.modules.external.forEach((m3) => {
        if (m3.init) m3.init(this);
      });
    }
    this.format = this.options.interpolation.format;
    if (!callback) callback = noop;
    if (this.options.fallbackLng && !this.services.languageDetector && !this.options.lng) {
      const codes = this.services.languageUtils.getFallbackCodes(this.options.fallbackLng);
      if (codes.length > 0 && codes[0] !== "dev") this.options.lng = codes[0];
    }
    if (!this.services.languageDetector && !this.options.lng) {
      this.logger.warn("init: no languageDetector is used and no lng is defined");
    }
    const storeApi = ["getResource", "hasResourceBundle", "getResourceBundle", "getDataByLanguage"];
    storeApi.forEach((fcName) => {
      this[fcName] = (...args) => this.store[fcName](...args);
    });
    const storeApiChained = ["addResource", "addResources", "addResourceBundle", "removeResourceBundle"];
    storeApiChained.forEach((fcName) => {
      this[fcName] = (...args) => {
        this.store[fcName](...args);
        return this;
      };
    });
    const deferred = defer();
    const load = () => {
      const finish = (err, t5) => {
        this.isInitializing = false;
        if (this.isInitialized && !this.initializedStoreOnce) this.logger.warn("init: i18next is already initialized. You should call init just once!");
        this.isInitialized = true;
        if (!this.options.isClone) this.logger.log("initialized", this.options);
        this.emit("initialized", this.options);
        deferred.resolve(t5);
        callback(err, t5);
      };
      if ((this.languages || this.isLanguageChangingTo) && !this.isInitialized) return finish(null, this.t.bind(this));
      this.changeLanguage(this.options.lng, finish);
    };
    if (this.options.resources || !this.options.initAsync) {
      load();
    } else {
      setTimeout(load, 0);
    }
    return deferred;
  }
  loadResources(language, callback = noop) {
    var _a4, _b2;
    let usedCallback = callback;
    const usedLng = isString(language) ? language : this.language;
    if (typeof language === "function") usedCallback = language;
    if (!this.options.resources || this.options.partialBundledLanguages) {
      if ((usedLng == null ? void 0 : usedLng.toLowerCase()) === "cimode" && (!this.options.preload || this.options.preload.length === 0)) return usedCallback();
      const toLoad = [];
      const append = (lng) => {
        if (!lng) return;
        if (lng === "cimode") return;
        const lngs = this.services.languageUtils.toResolveHierarchy(lng);
        lngs.forEach((l3) => {
          if (l3 === "cimode") return;
          if (!toLoad.includes(l3)) toLoad.push(l3);
        });
      };
      if (!usedLng) {
        const fallbacks = this.services.languageUtils.getFallbackCodes(this.options.fallbackLng);
        fallbacks.forEach((l3) => append(l3));
      } else {
        append(usedLng);
      }
      (_b2 = (_a4 = this.options.preload) == null ? void 0 : _a4.forEach) == null ? void 0 : _b2.call(_a4, (l3) => append(l3));
      this.services.backendConnector.load(toLoad, this.options.ns, (e2) => {
        if (!e2 && !this.resolvedLanguage && this.language) this.setResolvedLanguage(this.language);
        usedCallback(e2);
      });
    } else {
      usedCallback(null);
    }
  }
  reloadResources(lngs, ns, callback) {
    const deferred = defer();
    if (typeof lngs === "function") {
      callback = lngs;
      lngs = void 0;
    }
    if (typeof ns === "function") {
      callback = ns;
      ns = void 0;
    }
    if (!lngs) lngs = this.languages;
    if (!ns) ns = this.options.ns;
    if (!callback) callback = noop;
    this.services.backendConnector.reload(lngs, ns, (err) => {
      deferred.resolve();
      callback(err);
    });
    return deferred;
  }
  use(module2) {
    if (!module2) throw new Error("You are passing an undefined module! Please check the object you are passing to i18next.use()");
    if (!module2.type) throw new Error("You are passing a wrong module! Please check the object you are passing to i18next.use()");
    if (module2.type === "backend") {
      this.modules.backend = module2;
    }
    if (module2.type === "logger" || module2.log && module2.warn && module2.error) {
      this.modules.logger = module2;
    }
    if (module2.type === "languageDetector") {
      this.modules.languageDetector = module2;
    }
    if (module2.type === "i18nFormat") {
      this.modules.i18nFormat = module2;
    }
    if (module2.type === "postProcessor") {
      postProcessor.addPostProcessor(module2);
    }
    if (module2.type === "formatter") {
      this.modules.formatter = module2;
    }
    if (module2.type === "3rdParty") {
      this.modules.external.push(module2);
    }
    return this;
  }
  setResolvedLanguage(l3) {
    if (!l3 || !this.languages) return;
    if (["cimode", "dev"].includes(l3)) return;
    for (let li = 0; li < this.languages.length; li++) {
      const lngInLngs = this.languages[li];
      if (["cimode", "dev"].includes(lngInLngs)) continue;
      if (this.store.hasLanguageSomeTranslations(lngInLngs)) {
        this.resolvedLanguage = lngInLngs;
        break;
      }
    }
    if (!this.resolvedLanguage && !this.languages.includes(l3) && this.store.hasLanguageSomeTranslations(l3)) {
      this.resolvedLanguage = l3;
      this.languages.unshift(l3);
    }
  }
  changeLanguage(lng, callback) {
    this.isLanguageChangingTo = lng;
    const deferred = defer();
    this.emit("languageChanging", lng);
    const setLngProps = (l3) => {
      this.language = l3;
      this.languages = this.services.languageUtils.toResolveHierarchy(l3);
      this.resolvedLanguage = void 0;
      this.setResolvedLanguage(l3);
    };
    const done = (err, l3) => {
      if (l3) {
        if (this.isLanguageChangingTo === lng) {
          setLngProps(l3);
          this.translator.changeLanguage(l3);
          this.isLanguageChangingTo = void 0;
          this.emit("languageChanged", l3);
          this.logger.log("languageChanged", l3);
        }
      } else {
        this.isLanguageChangingTo = void 0;
      }
      deferred.resolve((...args) => this.t(...args));
      if (callback) callback(err, (...args) => this.t(...args));
    };
    const setLng = (lngs) => {
      var _a4, _b2;
      if (!lng && !lngs && this.services.languageDetector) lngs = [];
      const fl = isString(lngs) ? lngs : lngs && lngs[0];
      const l3 = this.store.hasLanguageSomeTranslations(fl) ? fl : this.services.languageUtils.getBestMatchFromCodes(isString(lngs) ? [lngs] : lngs);
      if (l3) {
        if (!this.language) {
          setLngProps(l3);
        }
        if (!this.translator.language) this.translator.changeLanguage(l3);
        (_b2 = (_a4 = this.services.languageDetector) == null ? void 0 : _a4.cacheUserLanguage) == null ? void 0 : _b2.call(_a4, l3);
      }
      this.loadResources(l3, (err) => {
        done(err, l3);
      });
    };
    if (!lng && this.services.languageDetector && !this.services.languageDetector.async) {
      setLng(this.services.languageDetector.detect());
    } else if (!lng && this.services.languageDetector && this.services.languageDetector.async) {
      if (this.services.languageDetector.detect.length === 0) {
        this.services.languageDetector.detect().then(setLng);
      } else {
        this.services.languageDetector.detect(setLng);
      }
    } else {
      setLng(lng);
    }
    return deferred;
  }
  getFixedT(lng, ns, keyPrefix, fixedOpts) {
    const scopeNs = fixedOpts == null ? void 0 : fixedOpts.scopeNs;
    const fixedT = (key, opts, ...rest) => {
      let o3;
      if (typeof opts !== "object") {
        o3 = this.options.overloadTranslationOptionHandler([key, opts].concat(rest));
      } else {
        o3 = {
          ...opts
        };
      }
      o3.lng = o3.lng || fixedT.lng;
      o3.lngs = o3.lngs || fixedT.lngs;
      const explicitCallNs = o3.ns !== void 0 && o3.ns !== null;
      o3.ns = o3.ns || fixedT.ns;
      if (o3.keyPrefix !== "") o3.keyPrefix = o3.keyPrefix || keyPrefix || fixedT.keyPrefix;
      const selectorOpts = {
        ...this.options,
        ...o3
      };
      if (Array.isArray(scopeNs) && !explicitCallNs) selectorOpts.ns = scopeNs;
      if (typeof o3.keyPrefix === "function") o3.keyPrefix = keysFromSelector(o3.keyPrefix, selectorOpts);
      const keySeparator = this.options.keySeparator || ".";
      let resultKey;
      if (o3.keyPrefix && Array.isArray(key)) {
        resultKey = key.map((k3) => {
          if (typeof k3 === "function") k3 = keysFromSelector(k3, selectorOpts);
          return `${o3.keyPrefix}${keySeparator}${k3}`;
        });
      } else {
        if (typeof key === "function") key = keysFromSelector(key, selectorOpts);
        resultKey = o3.keyPrefix ? `${o3.keyPrefix}${keySeparator}${key}` : key;
      }
      return this.t(resultKey, o3);
    };
    if (isString(lng)) {
      fixedT.lng = lng;
    } else {
      fixedT.lngs = lng;
    }
    fixedT.ns = ns;
    fixedT.keyPrefix = keyPrefix;
    return fixedT;
  }
  t(...args) {
    var _a4;
    return (_a4 = this.translator) == null ? void 0 : _a4.translate(...args);
  }
  exists(...args) {
    var _a4;
    return (_a4 = this.translator) == null ? void 0 : _a4.exists(...args);
  }
  setDefaultNamespace(ns) {
    this.options.defaultNS = ns;
  }
  hasLoadedNamespace(ns, options = {}) {
    if (!this.isInitialized) {
      this.logger.warn("hasLoadedNamespace: i18next was not initialized", this.languages);
      return false;
    }
    if (!this.languages || !this.languages.length) {
      this.logger.warn("hasLoadedNamespace: i18n.languages were undefined or empty", this.languages);
      return false;
    }
    const lng = options.lng || this.resolvedLanguage || this.languages[0];
    const fallbackLng = this.options ? this.options.fallbackLng : false;
    const lastLng = this.languages[this.languages.length - 1];
    if (lng.toLowerCase() === "cimode") return true;
    const loadNotPending = (l3, n) => {
      const loadState = this.services.backendConnector.state[`${l3}|${n}`];
      return loadState === -1 || loadState === 0 || loadState === 2;
    };
    if (options.precheck) {
      const preResult = options.precheck(this, loadNotPending);
      if (preResult !== void 0) return preResult;
    }
    if (this.hasResourceBundle(lng, ns)) return true;
    if (!this.services.backendConnector.backend || this.options.resources && !this.options.partialBundledLanguages) return true;
    if (loadNotPending(lng, ns) && (!fallbackLng || loadNotPending(lastLng, ns))) return true;
    return false;
  }
  loadNamespaces(ns, callback) {
    const deferred = defer();
    if (!this.options.ns) {
      if (callback) callback();
      return Promise.resolve();
    }
    if (isString(ns)) ns = [ns];
    ns.forEach((n) => {
      if (!this.options.ns.includes(n)) this.options.ns.push(n);
    });
    this.loadResources((err) => {
      deferred.resolve();
      if (callback) callback(err);
    });
    return deferred;
  }
  loadLanguages(lngs, callback) {
    const deferred = defer();
    if (isString(lngs)) lngs = [lngs];
    const preloaded = this.options.preload || [];
    const newLngs = lngs.filter((lng) => !preloaded.includes(lng) && this.services.languageUtils.isSupportedCode(lng));
    if (!newLngs.length) {
      if (callback) callback();
      return Promise.resolve();
    }
    this.options.preload = preloaded.concat(newLngs);
    this.loadResources((err) => {
      deferred.resolve();
      if (callback) callback(err);
    });
    return deferred;
  }
  dir(lng) {
    var _a4, _b2;
    if (!lng) lng = this.resolvedLanguage || (((_a4 = this.languages) == null ? void 0 : _a4.length) > 0 ? this.languages[0] : this.language);
    if (!lng) return "rtl";
    try {
      const l3 = new Intl.Locale(lng);
      if (l3 && l3.getTextInfo) {
        const ti = l3.getTextInfo();
        if (ti && ti.direction) return ti.direction;
      }
    } catch (e2) {
    }
    const rtlLngs = ["ar", "shu", "sqr", "ssh", "xaa", "yhd", "yud", "aao", "abh", "abv", "acm", "acq", "acw", "acx", "acy", "adf", "ads", "aeb", "aec", "afb", "ajp", "apc", "apd", "arb", "arq", "ars", "ary", "arz", "auz", "avl", "ayh", "ayl", "ayn", "ayp", "bbz", "pga", "he", "iw", "ps", "pbt", "pbu", "pst", "prp", "prd", "ug", "ur", "ydd", "yds", "yih", "ji", "yi", "hbo", "men", "xmn", "fa", "jpr", "peo", "pes", "prs", "dv", "sam", "ckb"];
    const languageUtils = ((_b2 = this.services) == null ? void 0 : _b2.languageUtils) || new LanguageUtil(get());
    if (lng.toLowerCase().indexOf("-latn") > 1) return "ltr";
    return rtlLngs.includes(languageUtils.getLanguagePartFromCode(lng)) || lng.toLowerCase().indexOf("-arab") > 1 ? "rtl" : "ltr";
  }
  static createInstance(options = {}, callback) {
    const instance2 = new _I18n(options, callback);
    instance2.createInstance = _I18n.createInstance;
    return instance2;
  }
  cloneInstance(options = {}, callback = noop) {
    const forkResourceStore = options.forkResourceStore;
    if (forkResourceStore) delete options.forkResourceStore;
    const mergedOptions = {
      ...this.options,
      ...options,
      ...{
        isClone: true
      }
    };
    const clone = new _I18n(mergedOptions);
    if (options.debug !== void 0 || options.prefix !== void 0) {
      clone.logger = clone.logger.clone(options);
    }
    const membersToCopy = ["store", "services", "language"];
    membersToCopy.forEach((m3) => {
      clone[m3] = this[m3];
    });
    clone.services = {
      ...this.services
    };
    clone.services.utils = {
      hasLoadedNamespace: clone.hasLoadedNamespace.bind(clone)
    };
    if (forkResourceStore) {
      const clonedData = Object.keys(this.store.data).reduce((prev, l3) => {
        prev[l3] = {
          ...this.store.data[l3]
        };
        prev[l3] = Object.keys(prev[l3]).reduce((acc, n) => {
          acc[n] = {
            ...prev[l3][n]
          };
          return acc;
        }, prev[l3]);
        return prev;
      }, {});
      clone.store = new ResourceStore(clonedData, mergedOptions);
      clone.services.resourceStore = clone.store;
    }
    if (options.interpolation) {
      const defOpts = get();
      const mergedInterpolation = {
        ...defOpts.interpolation,
        ...this.options.interpolation,
        ...options.interpolation
      };
      const mergedForInterpolator = {
        ...mergedOptions,
        interpolation: mergedInterpolation
      };
      clone.services.interpolator = new Interpolator(mergedForInterpolator);
    }
    clone.translator = new Translator(clone.services, mergedOptions);
    clone.translator.on("*", (event, ...args) => {
      clone.emit(event, ...args);
    });
    clone.init(mergedOptions, callback);
    clone.translator.options = mergedOptions;
    clone.translator.backendConnector.services.utils = {
      hasLoadedNamespace: clone.hasLoadedNamespace.bind(clone)
    };
    return clone;
  }
  toJSON() {
    return {
      options: this.options,
      store: this.store,
      language: this.language,
      languages: this.languages,
      resolvedLanguage: this.resolvedLanguage
    };
  }
};
var instance = I18n.createInstance();
var createInstance = instance.createInstance;
var dir = instance.dir;
var init = instance.init;
var loadResources = instance.loadResources;
var reloadResources = instance.reloadResources;
var use = instance.use;
var changeLanguage = instance.changeLanguage;
var getFixedT = instance.getFixedT;
var t3 = instance.t;
var exists = instance.exists;
var setDefaultNamespace = instance.setDefaultNamespace;
var hasLoadedNamespace = instance.hasLoadedNamespace;
var loadNamespaces = instance.loadNamespaces;
var loadLanguages = instance.loadLanguages;

// src/i18n/zh/index.ts
var zh = {
  validator: "\u9A8C\u8BC1\u65B9\u5F0F",
  strategy: "\u7B56\u7565",
  cancel: "\u53D6\u6D88",
  apply: "\u5E94\u7528",
  feature: {
    ["alias" /* Alias */]: {
      name: "\u522B\u540DAlias",
      desc: "\u4FEE\u6539\u5143\u6570\u636E\uFF08frontmatter\uFF09\u7F13\u5B58\u4E2D\u7684\u522B\u540D\uFF0C\u4E0D\u5F71\u54CD\u5B9E\u9645\u522B\u540D",
      validator: {
        auto: {
          name: "\u81EA\u52A8Frontmatter",
          desc: "\u5982\u679C frontmatter \u4E0D\u5B58\u5728\uFF0C\u5219\u4F1A\u5728\u7F13\u5B58\u4E2D\u521B\u5EFA\u3002\u53EF\u80FD\u4F1A\u53D1\u751F\u526F\u4F5C\u7528\u3002"
        },
        required: {
          name: "\u6709Frontmatter\u624D\u751F\u6548",
          desc: "\u53EA\u5904\u7406\u6709 frontmatter \u7684\u6587\u4EF6\u3002"
        }
      },
      strategy: {
        ensure: {
          name: "\u589E\u5F3A",
          desc: "\u4EC5\u5F53\u6807\u9898\u4E0D\u5728\u522B\u540D\u4E2D\u65F6\uFF0C\u624D\u5C06\u6807\u9898\u8BBE\u7F6E\u4E3A\u522B\u540D"
        },
        adjust: {
          name: "\u8C03\u6574",
          desc: "\u5C06\u6807\u9898\u6DFB\u52A0\u5230\u522B\u540D\u4E2D\uFF0C\u4F46\u4E0D\u5F71\u54CD\u73B0\u6709\u522B\u540D"
        },
        replace: {
          name: "\u66FF\u6362",
          desc: "\u7528\u6807\u9898\u66FF\u6362\u5F53\u524D\u522B\u540D"
        }
      }
    },
    ["explorer" /* Explorer */]: {
      name: "\u6587\u4EF6\u8D44\u6E90\u7BA1\u7406\u5668",
      desc: "\u66FF\u6362\u6587\u4EF6\u8D44\u6E90\u7BA1\u7406\u5668\u4E2D\u663E\u793A\u7684\u6807\u9898"
    },
    ["graph" /* Graph */]: {
      name: "\u56FE\u8C31",
      desc: "\u66FF\u6362\u56FE\u8C31/\u672C\u5730\u56FE\u8C31\u4E2D\u663E\u793A\u7684\u6807\u9898"
    },
    ["header" /* Header */]: {
      name: "\u9875\u9876\u6807\u9898",
      desc: "\u66FF\u6362Tab\u6807\u7B7E\u9875\u9876\u90E8\u7684\u6807\u9898\u5E76\u66F4\u65B0\u5B83\u4EEC"
    },
    ["bookmarks" /* Bookmarks */]: {
      name: "\u4E66\u7B7E",
      desc: "\u66FF\u6362\u5185\u7F6E\u4E66\u7B7E\u63D2\u4EF6\u4E2D\u663E\u793A\u7684\u6807\u9898"
    },
    ["search" /* Search */]: {
      name: "\u641C\u7D22",
      desc: "\u66FF\u6362\u641C\u7D22\u9875\u9762\u4E2D\u663E\u793A\u7684\u6807\u9898"
    },
    ["suggest" /* Suggest */]: {
      name: "\u5EFA\u8BAE",
      desc: "\u66FF\u6362\u5EFA\u8BAE\u7A97\u53E3\u4E2D\u663E\u793A\u7684\u6807\u9898"
    },
    ["tab" /* Tab */]: {
      name: "\u6807\u7B7E\u9875",
      desc: "\u66FF\u6362Tab\u6807\u7B7E\u9875\u4E2D\u663E\u793A\u7684\u6807\u9898"
    },
    ["inlineTitle" /* InlineTitle */]: {
      name: "\u9875\u5185\u6807\u9898",
      desc: "\u66FF\u6362\u9875\u9762\u5185\u663E\u793A\u7684\u6807\u9898"
    },
    ["canvas" /* Canvas */]: {
      name: "\u767D\u677F",
      desc: "\u66FF\u6362\u767D\u677F\u4E2D\u663E\u793A\u7684\u6807\u9898"
    },
    ["backlink" /* Backlink */]: {
      name: "\u53CD\u5411\u94FE\u63A5",
      desc: "\u66FF\u6362\u53CD\u5411\u94FE\u63A5(\u94FE\u63A5\u63D0\u53CA)\u4E2D\u663E\u793A\u7684\u6807\u9898"
    },
    ["noteLink" /* NoteLink */]: {
      name: "\u7B14\u8BB0\u94FE\u63A5",
      desc: "\u66FF\u6362\u7B14\u8BB0\u4E2D\u7684\u5185\u90E8\u94FE\u63A5",
      strategy: {
        options: {
          all: "\u66FF\u6362\u5168\u90E8\u94FE\u63A5",
          onlyEmpty: "\u53EA\u66FF\u6362\u6CA1\u6709\u522B\u540D\u7684\u94FE\u63A5"
        }
      },
      approval: {
        options: {
          showModal: "\u663E\u793A\u786E\u8BA4\u7A97\u53E3",
          auto: "\u4F7F\u7528\u81EA\u52A8\u786E\u8BA4"
        }
      }
    }
  },
  manage: "\u7BA1\u7406",
  template: {
    features: {
      name: "\u7279\u6027\u6A21\u677F",
      desc: "\u6839\u636E\u5404\u7279\u6027\u529F\u80FD\u72EC\u7ACB\u7BA1\u7406\u6A21\u677F"
    },
    main: "\u4E3B\u6A21\u677F",
    fallback: "\u5907\u7528",
    placeholder: "\u8F93\u5165\u6A21\u677F",
    common: {
      main: {
        name: "\u901A\u7528\u4E3B\u6A21\u677F",
        desc: `\u8BBE\u7F6E yaml \u8DEF\u5F84\uFF0C\u8BE5\u503C\u5C06\u7528\u4E8E\u6587\u4EF6\u6807\u9898\u3002\u503C\u5FC5\u987B\u662F\u5B57\u7B26\u4E32\u6216\u6570\u503C\u3002\u8FD8\u53EF\u4EE5\u4F7F\u7528\u7C7B\u4F3C\u6A21\u677F\u8DEF\u5F84 "{{ }}"\u3002
                \u8FD8\u53EF\u4EE5\u4F7F\u7528 #\u6807\u9898 \u6765\u4F7F\u7528\u4E00\u7EA7\u6807\u9898\uFF0C\u6216 _ basename \u548C\u5176\u4ED6\u4FDD\u7559\u5B57\u3002
                \u67E5\u9605 Readme \u4E86\u89E3\u66F4\u591A`
      },
      fallback: {
        name: "\u901A\u7528\u5907\u7528\u6A21\u677F",
        desc: "\u4F5C\u4E3A\u5907\u7528\u9009\u9879\u7684\u6A21\u677F\uFF0C\u5F53\u901A\u7528\u4E3B\u6A21\u677F\u65E0\u6CD5\u89E3\u6790\u51FA\u6765\u65F6\u4F7F\u7528"
      }
    },
    used: "\u901A\u7528\u6A21\u677F\u5C06\u4F1A\u4F7F\u7528. \u503C\u4E3A {{value}}",
    specific: "\u8FD9\u4E2A\u7279\u6027\u529F\u80FD\u5C06\u4F7F\u7528\u5F53\u524D\u6A21\u677F"
  },
  settings: "\u63D2\u4EF6\u8BBE\u7F6E",
  rule: {
    name: "\u89C4\u5219",
    path: {
      name: "\u6587\u4EF6\u8DEF\u5F84\u89C4\u5219",
      black: {
        name: "\u9ED1\u540D\u5355\u6A21\u5F0F",
        desc: "\u63D2\u4EF6\u5C06\u5FFD\u7565\u8DEF\u5F84\u4E2D\u7684\u6587\u4EF6\uFF0C\u6BCF\u4E2A\u8DEF\u5F84\u5FC5\u987B\u4F7F\u7528\u5728\u65B0\u884C\u4E2D\u5199\u5165."
      },
      white: {
        name: "\u767D\u540D\u5355\u6A21\u5F0F",
        desc: "\u63D2\u4EF6\u5C06\u5904\u7406\u8DEF\u5F84\u4E2D\u7684\u6587\u4EF6\uFF0C\u6BCF\u4E2A\u8DEF\u5F84\u5FC5\u987B\u4F7F\u7528\u5728\u65B0\u884C\u4E2D\u5199\u5165."
      }
    },
    delimiter: {
      name: "\u5217\u8868\u503C",
      desc: "\u8BBE\u7F6E\u5904\u7406\u5217\u8868\u503C\u7684\u89C4\u5219",
      first: "\u4F7F\u7528\u7B2C\u4E00\u4E2A\u503C",
      join: "\u7528\u5206\u9694\u7B26\u62FC\u63A5\u5168\u90E8",
      placeholder: {
        first: "\u4F1A\u91C7\u7528\u7B2C\u4E00\u4E2A\u503C",
        join: "\u8F93\u5165\u5206\u9694\u7B26"
      }
    }
  },
  features: "\u7279\u6027\u529F\u80FD",
  util: "\u5DE5\u5177",
  coffee: "\u5982\u679C\u60A8\u559C\u6B22\u6B64\u63D2\u4EF6\u5E76\u5E0C\u671B\u5E2E\u52A9/\u652F\u6301\u4F5C\u8005\u6301\u7EED\u5F00\u53D1\uFF0C\u8BF7\u70B9\u51FB\u4EE5\u4E0B\u6309\u94AE:",
  debug_info: {
    title: "\u8C03\u8BD5\u4FE1\u606F",
    desc: "\u5728\u7EC8\u7AEF\u4E2D\u663E\u793A\u8C03\u8BD5\u4FE1\u606F\u548C\u6355\u6349\u5230\u7684\u9519\u8BEF\u4FE1\u606F"
  },
  boot_delay: {
    title: "\u5EF6\u8FDF\u542F\u52A8\u52A0\u8F7D",
    desc: "\u63D2\u4EF6\u5C06\u5728\u6307\u5B9A\u65F6\u95F4\uFF08\u6BEB\u79D2\uFF09\u540E\u52A0\u8F7D"
  },
  disabled: "\u7981\u7528",
  processor: {
    name: "\u5904\u7406\u5668",
    desc: "\u4FEE\u6539\u89E3\u6790\u7684\u6807\u9898",
    replace: {
      name: "\u66FF\u6362",
      desc: "\u5C06\u6267\u884C",
      pattern: {
        name: "\u6B63\u5219Pattern",
        desc: "\u5C06\u9996\u5148\u7528\u4F5C RegExp \u7684\u7B2C\u4E00\u4E2A\u53C2\u6570\uFF0C\u7136\u540E\u7528\u4F5C replace()\u7684\u7B2C\u4E00\u4E2A\u53C2\u6570"
      },
      flags: {
        name: "\u4FEE\u9970\u7B26Flags",
        desc: "\u5C06\u7528\u4F5C\u65B0RegExp \u7684\u7B2C\u4E8C\u4E2A\u53C2\u6570"
      },
      replacement: {
        name: "\u66FF\u6362\u503CReplacement",
        desc: "\u5C06\u7528\u4F5C replace() \u7684\u7B2C\u4E8C\u4E2A\u53C2\u6570"
      }
    },
    function: {
      name: "\u51FD\u6570",
      desc: "\u5C06\u600E\u6837\u8D77\u4F5C\u7528:",
      valueDesc: "\u4F60\u5728\u6587\u672C\u533A\u8F93\u5165\u7684\u503C"
    }
  },
  command: {
    features: {
      reload: "\u91CD\u65B0\u52A0\u8F7D\u7279\u6027\u529F\u80FD",
      disable: "\u7981\u7528\u7279\u6027\u529F\u80FD"
    }
  }
};
var zh_default = zh;

// src/i18n/zh-tw/index.ts
var zh_tw = {
  validator: "\u9A57\u8B49\u65B9\u5F0F",
  strategy: "\u7B56\u7565",
  cancel: "\u53D6\u6D88",
  apply: "\u61C9\u7528",
  feature: {
    ["alias" /* Alias */]: {
      name: "\u5225\u540DAlias",
      desc: "\u4FEE\u6539\u5143\u6578\u64DA\uFF08frontmatter\uFF09\u7DE9\u5B58\u4E2D\u7684\u5225\u540D\uFF0C\u4E0D\u5F71\u97FF\u5BE6\u969B\u5225\u540D",
      validator: {
        auto: {
          name: "\u81EA\u52D5Frontmatter",
          desc: "\u5982\u679C frontmatter \u4E0D\u5B58\u5728\uFF0C\u5247\u6703\u5728\u7DE9\u5B58\u4E2D\u5275\u5EFA\u3002\u53EF\u80FD\u6703\u767C\u751F\u526F\u4F5C\u7528\u3002"
        },
        required: {
          name: "\u6709Frontmatter\u624D\u751F\u6548",
          desc: "\u53EA\u8655\u7406\u6709 frontmatter \u7684\u6A94\u3002"
        }
      },
      strategy: {
        ensure: {
          name: "\u589E\u5F37",
          desc: "\u50C5\u7576\u6A19\u984C\u4E0D\u5728\u5225\u540D\u4E2D\u6642\uFF0C\u624D\u5C07\u6A19\u984C\u8A2D\u7F6E\u70BA\u5225\u540D"
        },
        adjust: {
          name: "\u8ABF\u6574",
          desc: "\u5C07\u6A19\u984C\u6DFB\u52A0\u5230\u5225\u540D\u4E2D\uFF0C\u4F46\u4E0D\u5F71\u97FF\u73FE\u6709\u5225\u540D"
        },
        replace: {
          name: "\u66FF\u63DB",
          desc: "\u7528\u6A19\u984C\u66FF\u63DB\u7576\u524D\u5225\u540D"
        }
      }
    },
    ["explorer" /* Explorer */]: {
      name: "\u6A94\u8CC7\u6E90\u7BA1\u7406\u5668",
      desc: "\u66FF\u63DB\u6A94\u8CC7\u6E90\u7BA1\u7406\u5668\u4E2D\u986F\u793A\u7684\u6A19\u984C"
    },
    ["graph" /* Graph */]: {
      name: "\u5716\u8B5C",
      desc: "\u66FF\u63DB\u5716\u8B5C/\u672C\u5730\u5716\u8B5C\u4E2D\u986F\u793A\u7684\u6A19\u984C"
    },
    ["header" /* Header */]: {
      name: "\u9801\u9802\u6A19\u984C",
      desc: "\u66FF\u63DBTab\u6A19\u7C64\u9801\u9802\u90E8\u7684\u6A19\u984C\u4E26\u66F4\u65B0\u5B83\u5011"
    },
    ["bookmarks" /* Bookmarks */]: {
      name: "\u66F8\u7C3D",
      desc: "\u66FF\u63DB\u5167\u7F6E\u66F8\u7C3D\u63D2\u4EF6\u4E2D\u986F\u793A\u7684\u6A19\u984C"
    },
    ["search" /* Search */]: {
      name: "\u641C\u7D22",
      desc: "\u66FF\u63DB\u641C\u7D22\u9801\u9762\u4E2D\u986F\u793A\u7684\u6A19\u984C"
    },
    ["suggest" /* Suggest */]: {
      name: "\u5EFA\u8B70",
      desc: "\u66FF\u63DB\u5EFA\u8B70\u7A97\u53E3\u4E2D\u986F\u793A\u7684\u6A19\u984C"
    },
    ["tab" /* Tab */]: {
      name: "\u6A19\u7C64\u9801",
      desc: "\u66FF\u63DBTab\u6A19\u7C64\u9801\u4E2D\u986F\u793A\u7684\u6A19\u984C"
    },
    ["inlineTitle" /* InlineTitle */]: {
      name: "\u9801\u5167\u6A19\u984C",
      desc: "\u66FF\u63DB\u9801\u9762\u5167\u986F\u793A\u7684\u6A19\u984C"
    },
    ["canvas" /* Canvas */]: {
      name: "\u767D\u677F",
      desc: "\u66FF\u63DB\u767D\u677F\u4E2D\u986F\u793A\u7684\u6A19\u984C"
    },
    ["backlink" /* Backlink */]: {
      name: "\u53CD\u5411\u93C8\u63A5",
      desc: "\u66FF\u63DB\u53CD\u5411\u93C8\u63A5(\u93C8\u63A5\u63D0\u53CA)\u4E2D\u986F\u793A\u7684\u6A19\u984C"
    },
    ["noteLink" /* NoteLink */]: {
      name: "\u7B46\u8A18\u93C8\u63A5",
      desc: "\u66FF\u63DB\u7B46\u8A18\u4E2D\u7684\u5167\u90E8\u93C8\u63A5",
      strategy: {
        options: {
          all: "\u66FF\u63DB\u5168\u90E8\u93C8\u63A5",
          onlyEmpty: "\u53EA\u66FF\u63DB\u6C92\u6709\u5225\u540D\u7684\u93C8\u63A5"
        }
      },
      approval: {
        options: {
          showModal: "\u986F\u793A\u78BA\u8A8D\u7A97\u53E3",
          auto: "\u4F7F\u7528\u81EA\u52D5\u78BA\u8A8D"
        }
      }
    }
  },
  manage: "\u7BA1\u7406",
  template: {
    features: {
      name: "\u7279\u6027\u7BC4\u672C",
      desc: "\u6839\u64DA\u5404\u7279\u6027\u529F\u80FD\u7368\u7ACB\u7BA1\u7406\u7BC4\u672C"
    },
    main: "\u4E3B\u7BC4\u672C",
    fallback: "\u5099\u7528",
    placeholder: "\u8F38\u5165\u7BC4\u672C",
    common: {
      main: {
        name: "\u901A\u7528\u4E3B\u7BC4\u672C",
        desc: `\u8A2D\u7F6E yaml \u8DEF\u5F91\uFF0C\u8A72\u503C\u5C07\u7528\u65BC\u6A94\u6A19\u984C\u3002\u503C\u5FC5\u9808\u662F\u5B57\u4E32\u6216\u6578\u503C\u3002\u9084\u53EF\u4EE5\u4F7F\u7528\u985E\u4F3C\u7BC4\u672C\u8DEF\u5F91 "{{ }}"\u3002
                \u9084\u53EF\u4EE5\u4F7F\u7528 #\u6A19\u984C \u4F86\u4F7F\u7528\u4E00\u7D1A\u6A19\u984C\uFF0C\u6216 _ basename \u548C\u5176\u4ED6\u4FDD\u7559\u5B57\u3002
                \u67E5\u95B1 Readme \u77AD\u89E3\u66F4\u591A`
      },
      fallback: {
        name: "\u901A\u7528\u5099\u7528\u7BC4\u672C",
        desc: "\u4F5C\u70BA\u5099\u7528\u9078\u9805\u7684\u7BC4\u672C\uFF0C\u7576\u901A\u7528\u4E3B\u7BC4\u672C\u7121\u6CD5\u89E3\u6790\u51FA\u4F86\u6642\u4F7F\u7528"
      }
    },
    used: "\u901A\u7528\u7BC4\u672C\u5C07\u6703\u4F7F\u7528. \u503C\u70BA {{value}}",
    specific: "\u9019\u500B\u7279\u6027\u529F\u80FD\u5C07\u4F7F\u7528\u7576\u524D\u7BC4\u672C"
  },
  settings: "\u63D2\u4EF6\u8A2D\u7F6E",
  rule: {
    name: "\u898F\u5247",
    path: {
      name: "\u6A94\u8DEF\u5F91\u898F\u5247",
      black: {
        name: "\u9ED1\u540D\u55AE\u6A21\u5F0F",
        desc: "\u63D2\u4EF6\u5C07\u5FFD\u7565\u8DEF\u5F91\u4E2D\u7684\u6A94\uFF0C\u6BCF\u500B\u8DEF\u5F91\u5FC5\u9808\u4F7F\u7528\u5728\u65B0\u884C\u4E2D\u5BEB\u5165."
      },
      white: {
        name: "\u767D\u540D\u55AE\u6A21\u5F0F",
        desc: "\u63D2\u4EF6\u5C07\u8655\u7406\u8DEF\u5F91\u4E2D\u7684\u6A94\uFF0C\u6BCF\u500B\u8DEF\u5F91\u5FC5\u9808\u4F7F\u7528\u5728\u65B0\u884C\u4E2D\u5BEB\u5165."
      }
    },
    delimiter: {
      name: "\u5217\u8868\u503C",
      desc: "\u8A2D\u7F6E\u8655\u7406\u5217\u8868\u503C\u7684\u898F\u5247",
      first: "\u4F7F\u7528\u7B2C\u4E00\u500B\u503C",
      join: "\u7528\u5206\u9694\u7B26\u865F\u62FC\u63A5\u5168\u90E8",
      placeholder: {
        first: "\u6703\u63A1\u7528\u7B2C\u4E00\u500B\u503C",
        join: "\u8F38\u5165\u5206\u9694\u7B26\u865F"
      }
    }
  },
  features: "\u7279\u6027\u529F\u80FD",
  util: "\u5DE5\u5177",
  coffee: "\u5982\u679C\u60A8\u559C\u6B61\u6B64\u63D2\u4EF6\u4E26\u5E0C\u671B\u5E6B\u52A9/\u652F\u6301\u4F5C\u8005\u6301\u7E8C\u958B\u767C\uFF0C\u8ACB\u9EDE\u64CA\u4EE5\u4E0B\u6309\u9215:",
  debug_info: {
    title: "\u8ABF\u8A66\u8CC7\u8A0A",
    desc: "\u5728\u7D42\u7AEF\u4E2D\u986F\u793A\u8ABF\u8A66\u8CC7\u8A0A\u548C\u6355\u6349\u5230\u7684\u932F\u8AA4\u8CC7\u8A0A"
  },
  boot_delay: {
    title: "\u5EF6\u9072\u555F\u52D5\u52A0\u8F09",
    desc: "\u63D2\u4EF6\u5C07\u5728\u6307\u5B9A\u6642\u9593\uFF08\u6BEB\u79D2\uFF09\u5F8C\u52A0\u8F09"
  },
  disabled: "\u7981\u7528",
  processor: {
    name: "\u8655\u7406\u5668",
    desc: "\u4FEE\u6539\u89E3\u6790\u7684\u6A19\u984C",
    replace: {
      name: "\u66FF\u63DB",
      desc: "\u5C07\u57F7\u884C",
      pattern: {
        name: "\u6B63\u5247Pattern",
        desc: "\u5C07\u9996\u5148\u7528\u4F5C RegExp \u7684\u7B2C\u4E00\u500B\u53C3\u6578\uFF0C\u7136\u5F8C\u7528\u4F5C replace()\u7684\u7B2C\u4E00\u500B\u53C3\u6578"
      },
      flags: {
        name: "\u4FEE\u98FE\u7B26Flags",
        desc: "\u5C07\u7528\u4F5C\u65B0RegExp \u7684\u7B2C\u4E8C\u500B\u53C3\u6578"
      },
      replacement: {
        name: "\u66FF\u63DB\u503CReplacement",
        desc: "\u5C07\u7528\u4F5C replace() \u7684\u7B2C\u4E8C\u500B\u53C3\u6578"
      }
    },
    function: {
      name: "\u51FD\u6578",
      desc: "\u5C07\u600E\u6A23\u8D77\u4F5C\u7528:",
      valueDesc: "\u4F60\u5728\u6587\u672C\u5340\u8F38\u5165\u7684\u503C"
    }
  },
  command: {
    features: {
      reload: "\u91CD\u65B0\u52A0\u8F09\u7279\u6027\u529F\u80FD",
      disable: "\u7981\u7528\u7279\u6027\u529F\u80FD"
    }
  }
};
var zh_tw_default = zh_tw;

// src/i18n/ko/index.ts
var ko = {};
var ko_default = ko;

// src/i18n/it/index.ts
var it2 = {};
var it_default = it2;

// src/i18n/id/index.ts
var id = {};
var id_default = id;

// src/i18n/ro/index.ts
var ro = {};
var ro_default = ro;

// src/i18n/pl/index.ts
var pl = {};
var pl_default = pl;

// src/i18n/cs/index.ts
var cs = {};
var cs_default = cs;

// src/i18n/de/index.ts
var de = {};
var de_default = de;

// src/i18n/es/index.ts
var es = {};
var es_default = es;

// src/i18n/fr/index.ts
var fr = {};
var fr_default = fr;

// src/i18n/no/index.ts
var no = {};
var no_default = no;

// src/i18n/pr-br/index.ts
var pr_br = {};
var pr_br_default = pr_br;

// src/i18n/pt/index.ts
var pt2 = {};
var pt_default = pt2;

// src/i18n/ja/index.ts
var ja = {};
var ja_default = ja;

// src/i18n/da/index.ts
var da = {};
var da_default = da;

// src/i18n/uk/index.ts
var uk = {};
var uk_default = uk;

// src/i18n/sq/index.ts
var sq = {};
var sq_default = sq;

// src/i18n/th/index.ts
var th = {};
var th_default = th;

// src/i18n/fa/index.ts
var fa = {};
var fa_default = fa;

// src/i18n/tr/index.ts
var tr = {};
var tr_default = tr;

// src/i18n/nl/index.ts
var nl = {};
var nl_default = nl;

// src/i18n/am/index.ts
var am = {};
var am_default = am;

// src/i18n/ms/index.ts
var ms = {};
var ms_default = ms;

// src/i18n/Locale.ts
var defaultLocale = "en";
var locale = import_obsidian.moment.locale();
instance.init({
  lng: locale,
  fallbackLng: defaultLocale,
  resources: {
    en: { translation: en_default },
    ru: { translation: ru_default },
    zh: { translation: zh_default },
    "zh-TW": { translation: zh_tw_default },
    ko: { translation: ko_default },
    it: { translation: it_default },
    id: { translation: id_default },
    ro: { translation: ro_default },
    "pt-BR": { translation: pr_br_default },
    cs: { translation: cs_default },
    de: { translation: de_default },
    es: { translation: es_default },
    fr: { translation: fr_default },
    no: { translation: no_default },
    pl: { translation: pl_default },
    pt: { translation: pt_default },
    ja: { translation: ja_default },
    da: { translation: da_default },
    uk: { translation: uk_default },
    sq: { translation: sq_default },
    th: { translation: th_default },
    fa: { translation: fa_default },
    tr: { translation: tr_default },
    nl: { translation: nl_default },
    am: { translation: am_default },
    ms: { translation: ms_default }
  },
  nsSeparator: "|"
});
var resolved = instance.resolvedLanguage;
if (resolved !== locale) {
  if (instance.languages.includes(locale)) {
    console.warn(`Locale ${locale} empty. Resolved ${resolved}`);
  } else {
    console.warn(`Locale ${locale} does not exist. Resolved ${resolved}`);
  }
}
var t4 = (path, params) => {
  return instance.t(path, params);
};

// src/Settings/SettingsTab.ts
var SettingsTab = class {
  constructor(app, plugin, storage, dispatcher, factory) {
    this.storage = storage;
    this.dispatcher = dispatcher;
    this.factory = factory;
    const self2 = this;
    this.tab = new class extends import_obsidian2.PluginSettingTab {
      constructor() {
        super(...arguments);
        this.display = () => self2.display();
        this.hide = () => self2.hide();
      }
    }(app, plugin);
    this.updatePrevious();
    dispatcher.dispatch("settings.loaded", new Event({ settings: this.storage.collect() }));
  }
  get containerEl() {
    return this.getTab().containerEl;
  }
  get orderedKeys() {
    return ["templates", "processor", "rules", "features", "debug", "boot"];
  }
  getTab() {
    return this.tab;
  }
  display() {
    const { containerEl } = this;
    containerEl.empty();
    containerEl.createEl("h2", { text: t4("settings") });
    const builders = this.factory("main");
    for (const k3 of this.orderedKeys) {
      for (const builder of builders) {
        if (builder.support(k3)) {
          builder.build({
            name: k3,
            item: this.storage.get(k3),
            container: this.containerEl
          });
        }
      }
    }
    this.buildDonation();
  }
  hide() {
    this.dispatch();
  }
  buildDonation() {
    const div = this.containerEl.createEl("div");
    div.style.width = "75%";
    div.style.textAlign = "center";
    div.style.margin = "0 auto";
    const text = document.createElement("p");
    text.textContent = t4("coffee");
    div.appendChild(text);
    const anchor = document.createElement("a");
    anchor.setAttribute("href", "https://www.buymeacoffee.com/snezhig");
    anchor.appendChild(new DOMParser().parseFromString(coffee, "text/xml").documentElement);
    div.appendChild(anchor);
  }
  updatePrevious() {
    this.previous = JSON.parse(JSON.stringify(this.storage.collect()));
  }
  dispatch() {
    this.dispatcher.dispatch("settings:tab:close", null);
    const changed = ObjectHelper.compare(this.previous, this.storage.collect());
    if (Object.keys(changed).length === 0) {
      return;
    }
    this.dispatcher.dispatch(
      "settings:changed",
      new Event({
        old: this.previous,
        actual: this.storage.collect(),
        changed: ObjectHelper.compare(this.previous, this.storage.collect())
      })
    );
    this.updatePrevious();
  }
};
SettingsTab = __decorateClass([
  Kt(),
  __decorateParam(0, Yt(inversify_types_default["obsidian:app"])),
  __decorateParam(1, Yt(inversify_types_default["obsidian:plugin"])),
  __decorateParam(2, Yt(inversify_types_default["settings:storage"])),
  __decorateParam(3, Yt(inversify_types_default["event:dispatcher"])),
  __decorateParam(4, Yt(inversify_types_default["factory:settings:builder"]))
], SettingsTab);
var coffee = '<svg width="150" height="42" viewBox="0 0 545 153" fill="none" xmlns="http://www.w3.org/2000/svg">\n<path d="M0 24.48C0 10.9601 10.9601 0 24.48 0H520.2C533.72 0 544.68 10.9601 544.68 24.48V128.52C544.68 142.04 533.72 153 520.2 153H24.48C10.9601 153 0 142.04 0 128.52V24.48Z" fill="#FFDD00"/>\n<path d="M109.522 50.3178L109.455 50.2783L109.299 50.2308C109.362 50.2836 109.44 50.3142 109.522 50.3178Z" fill="#0D0C22"/>\n<path d="M110.507 57.3134L110.432 57.3344L110.507 57.3134Z" fill="#0D0C22"/>\n<path d="M109.549 50.3062C109.54 50.3051 109.532 50.3031 109.524 50.3003C109.523 50.3058 109.523 50.3113 109.524 50.3168C109.533 50.3156 109.541 50.3119 109.549 50.3062Z" fill="#0D0C22"/>\n<path d="M109.523 50.3205H109.536V50.3127L109.523 50.3205Z" fill="#0D0C22"/>\n<path d="M110.447 57.3006L110.56 57.2361L110.602 57.2123L110.64 57.1715C110.569 57.2025 110.503 57.2462 110.447 57.3006Z" fill="#0D0C22"/>\n<path d="M109.715 50.4713L109.604 50.3659L109.529 50.3251C109.57 50.3963 109.636 50.4488 109.715 50.4713Z" fill="#0D0C22"/>\n<path d="M81.8801 118.353C81.7916 118.391 81.7142 118.451 81.6548 118.527L81.7246 118.482C81.772 118.439 81.8392 118.387 81.8801 118.353Z" fill="#0D0C22"/>\n<path d="M98.0456 115.173C98.0456 115.073 97.9968 115.091 98.0087 115.447C98.0087 115.418 98.0206 115.389 98.0258 115.361C98.0324 115.298 98.0377 115.236 98.0456 115.173Z" fill="#0D0C22"/>\n<path d="M96.3761 118.353C96.2877 118.391 96.2103 118.451 96.1509 118.527L96.2207 118.482C96.2681 118.439 96.3353 118.387 96.3761 118.353Z" fill="#0D0C22"/>\n<path d="M70.4886 119.11C70.4215 119.052 70.3393 119.013 70.2515 118.999C70.3226 119.034 70.3937 119.068 70.4412 119.094L70.4886 119.11Z" fill="#0D0C22"/>\n<path d="M67.9304 116.657C67.92 116.553 67.8881 116.453 67.8369 116.362C67.8732 116.456 67.9035 116.553 67.9278 116.652L67.9304 116.657Z" fill="#0D0C22"/>\n<path d="M85.1368 72.7737C81.6195 74.2794 77.628 75.9866 72.4549 75.9866C70.2908 75.9823 68.1373 75.6854 66.0527 75.104L69.6306 111.838C69.7572 113.373 70.4567 114.805 71.59 115.848C72.7233 116.892 74.2076 117.471 75.7482 117.47C75.7482 117.47 80.8212 117.734 82.514 117.734C84.3358 117.734 89.7988 117.47 89.7988 117.47C91.3391 117.47 92.8231 116.891 93.9562 115.848C95.0892 114.804 95.7885 113.373 95.9151 111.838L99.7472 71.2456C98.0347 70.6607 96.3064 70.2721 94.358 70.2721C90.9883 70.2708 88.2733 71.4313 85.1368 72.7737Z" fill="white"/>\n<path d="M54.9844 57.1021L55.045 57.1587L55.0845 57.1824C55.0541 57.1522 55.0205 57.1252 54.9844 57.1021Z" fill="#0D0C22"/>\n<path d="M116.299 53.7119L115.761 50.9943C115.277 48.5559 114.18 46.2519 111.677 45.3706C110.875 45.0887 109.964 44.9675 109.349 44.384C108.734 43.8004 108.552 42.8941 108.41 42.0536C108.147 40.511 107.899 38.9671 107.629 37.4272C107.396 36.1033 107.211 34.616 106.604 33.4015C105.814 31.7706 104.174 30.8169 102.543 30.1859C101.707 29.8739 100.854 29.61 99.9884 29.3955C95.9139 28.3205 91.63 27.9253 87.4382 27.7001C82.407 27.4225 77.3623 27.5061 72.343 27.9504C68.6071 28.2902 64.6723 28.7013 61.1221 29.9935C59.8245 30.4665 58.4875 31.0342 57.5008 32.0367C56.2902 33.2684 55.895 35.1733 56.7789 36.7092C57.4073 37.8 58.4717 38.5706 59.6006 39.0804C61.0711 39.7373 62.6068 40.2371 64.1822 40.5716C68.5689 41.5412 73.1124 41.9219 77.5939 42.0839C82.561 42.2844 87.5362 42.1219 92.4796 41.5978C93.7021 41.4635 94.9224 41.3023 96.1405 41.1144C97.575 40.8944 98.4958 39.0185 98.073 37.7117C97.5671 36.1494 96.2077 35.5434 94.6703 35.7792C94.4438 35.8148 94.2185 35.8477 93.9919 35.8807L93.8286 35.9044C93.3078 35.9702 92.787 36.0317 92.2662 36.0888C91.1904 36.2047 90.112 36.2996 89.0309 36.3733C86.6097 36.5419 84.1818 36.6197 81.7553 36.6236C79.371 36.6236 76.9853 36.5564 74.6062 36.3997C73.5207 36.3285 72.4379 36.2381 71.3577 36.1283C70.8663 36.0769 70.3763 36.0229 69.8862 35.9623L69.4199 35.903L69.3185 35.8886L68.835 35.8187C67.847 35.6699 66.859 35.4986 65.8816 35.2918C65.783 35.2699 65.6947 35.2151 65.6315 35.1363C65.5683 35.0575 65.5338 34.9594 65.5338 34.8584C65.5338 34.7574 65.5683 34.6594 65.6315 34.5806C65.6947 34.5018 65.783 34.4469 65.8816 34.425H65.9C66.7471 34.2445 67.6007 34.0904 68.4569 33.956C68.7424 33.9113 69.0287 33.8673 69.3158 33.8243H69.3237C69.8599 33.7887 70.3987 33.6926 70.9322 33.6293C75.574 33.1465 80.2434 32.9819 84.9077 33.1367C87.1721 33.2025 89.4353 33.3356 91.6892 33.5648C92.174 33.6149 92.6562 33.6676 93.1383 33.7268C93.3227 33.7492 93.5085 33.7756 93.6942 33.798L94.0683 33.852C95.1591 34.0144 96.2441 34.2116 97.3234 34.4435C98.9227 34.7912 100.976 34.9045 101.688 36.6566C101.914 37.2125 102.017 37.8303 102.142 38.4139L102.302 39.1581C102.306 39.1715 102.309 39.1852 102.311 39.199C102.688 40.9554 103.065 42.7118 103.442 44.4683C103.47 44.598 103.471 44.7321 103.444 44.8621C103.418 44.9921 103.365 45.1153 103.289 45.2239C103.213 45.3326 103.115 45.4244 103.002 45.4936C102.889 45.5628 102.762 45.6079 102.631 45.6262H102.62L102.39 45.6578L102.162 45.6881C101.44 45.7821 100.717 45.8699 99.9936 45.9516C98.5683 46.114 97.1408 46.2546 95.711 46.3731C92.87 46.6094 90.0233 46.7644 87.1708 46.8381C85.7174 46.8768 84.2644 46.8948 82.8118 46.8921C77.0301 46.8876 71.2534 46.5516 65.5101 45.8857C64.8883 45.8119 64.2666 45.7329 63.6448 45.6525C64.1269 45.7145 63.2944 45.6051 63.1258 45.5814C62.7306 45.5261 62.3354 45.4686 61.9402 45.4088C60.6136 45.2099 59.295 44.9649 57.9711 44.7502C56.3705 44.4867 54.8398 44.6185 53.3921 45.4088C52.2037 46.0591 51.2419 47.0564 50.6349 48.2674C50.0105 49.5584 49.8248 50.964 49.5455 52.3511C49.2662 53.7383 48.8315 55.2308 48.9962 56.6548C49.3505 59.7281 51.4991 62.2258 54.5895 62.7843C57.4968 63.3112 60.42 63.7381 63.351 64.1016C74.8648 65.5118 86.4968 65.6805 98.0466 64.6049C98.9872 64.517 99.9265 64.4213 100.864 64.3177C101.157 64.2855 101.454 64.3192 101.732 64.4165C102.01 64.5137 102.263 64.6719 102.472 64.8795C102.681 65.0872 102.842 65.339 102.941 65.6165C103.04 65.894 103.076 66.1902 103.046 66.4834L102.753 69.3261C102.164 75.0705 101.575 80.8145 100.986 86.558C100.371 92.5896 99.7521 98.6208 99.1295 104.651C98.9538 106.35 98.7782 108.048 98.6025 109.746C98.4339 111.417 98.4102 113.142 98.0927 114.794C97.5922 117.391 95.8335 118.987 93.2674 119.57C90.9164 120.105 88.5148 120.386 86.1038 120.408C83.431 120.422 80.7594 120.304 78.0866 120.318C75.2333 120.334 71.7384 120.071 69.5358 117.947C67.6007 116.082 67.3333 113.161 67.0698 110.636C66.7185 107.293 66.3703 103.95 66.0252 100.607L64.0887 82.0212L62.8359 69.9953C62.8149 69.7964 62.7938 69.6001 62.774 69.3999C62.6239 67.9654 61.6082 66.5611 60.0077 66.6335C58.6376 66.6941 57.0806 67.8586 57.2413 69.3999L58.17 78.3155L60.0906 96.7581C60.6378 101.997 61.1836 107.236 61.7281 112.476C61.8335 113.48 61.9323 114.487 62.0429 115.49C62.6449 120.976 66.834 123.932 72.0216 124.764C75.0515 125.252 78.1551 125.352 81.2297 125.402C85.1711 125.465 89.1521 125.617 93.029 124.903C98.7738 123.849 103.084 120.013 103.699 114.062C103.875 112.345 104.051 110.626 104.226 108.908C104.81 103.224 105.393 97.5397 105.976 91.855L107.88 73.2807L108.754 64.7682C108.797 64.3461 108.976 63.9492 109.262 63.6363C109.549 63.3234 109.929 63.111 110.345 63.0307C111.988 62.7105 113.558 62.1639 114.727 60.9137C116.587 58.9232 116.957 56.3281 116.299 53.7119ZM54.5052 55.5483C54.5302 55.5364 54.4841 55.7511 54.4644 55.8513C54.4604 55.6998 54.4683 55.5654 54.5052 55.5483ZM54.6646 56.7813C54.6778 56.7721 54.7173 56.8248 54.7581 56.888C54.6962 56.83 54.6567 56.7866 54.6633 56.7813H54.6646ZM54.8214 56.9881C54.878 57.0843 54.9083 57.1449 54.8214 56.9881V56.9881ZM55.1362 57.2437H55.1441C55.1441 57.2529 55.1586 57.2621 55.1639 57.2713C55.1551 57.2612 55.1454 57.2519 55.1349 57.2437H55.1362ZM110.269 56.8616C109.679 57.4228 108.789 57.6837 107.911 57.8141C98.0572 59.2763 88.06 60.0166 78.0984 59.6899C70.9691 59.4462 63.9148 58.6545 56.8566 57.6573C56.165 57.5598 55.4155 57.4334 54.9399 56.9236C54.0441 55.9619 54.4841 54.0254 54.7173 52.8636C54.9307 51.7992 55.3391 50.3804 56.605 50.2289C58.581 49.9971 60.8758 50.8309 62.8307 51.1273C65.1843 51.4865 67.5467 51.7741 69.9179 51.9902C80.0375 52.9123 90.3271 52.7687 100.402 51.4198C102.238 51.173 104.068 50.8863 105.891 50.5596C107.516 50.2684 109.316 49.7218 110.298 51.404C110.971 52.55 111.06 54.0834 110.956 55.3783C110.924 55.9425 110.678 56.4732 110.267 56.8616H110.269Z" fill="#0D0C22"/>\n<path fill-rule="evenodd" clip-rule="evenodd" d="M170.036 84.2397C169.461 85.3378 168.67 86.2942 167.663 87.1057C166.656 87.9178 165.482 88.579 164.139 89.0881C162.797 89.5984 161.446 89.9408 160.088 90.1153C158.729 90.2905 157.41 90.2753 156.133 90.0674C154.854 89.8608 153.766 89.439 152.872 88.8014L153.88 78.3397C154.806 78.0216 155.972 77.6949 157.379 77.3604C158.785 77.0264 160.231 76.787 161.718 76.644C163.205 76.5004 164.61 76.5173 165.937 76.6919C167.263 76.867 168.31 77.2888 169.077 77.9579C169.493 78.3397 169.845 78.7537 170.132 79.1997C170.42 79.6458 170.595 80.1076 170.66 80.5852C170.819 81.9227 170.612 83.1409 170.036 84.2397ZM155.413 61.9545C156.084 61.5406 156.892 61.1739 157.834 60.8551C158.777 60.5376 159.744 60.3139 160.735 60.1867C161.725 60.06 162.692 60.043 163.636 60.1388C164.578 60.2345 165.41 60.497 166.129 60.9267C166.848 61.357 167.383 61.9782 167.735 62.7897C168.086 63.6024 168.182 64.6296 168.022 65.8714C167.895 66.8587 167.502 67.695 166.848 68.3793C166.193 69.0647 165.393 69.6374 164.451 70.0993C163.508 70.5617 162.509 70.9277 161.455 71.1974C160.399 71.4689 159.384 71.6683 158.41 71.795C157.435 71.9229 156.588 72.0029 155.869 72.0338C155.15 72.0659 154.678 72.0816 154.454 72.0816L155.413 61.9545ZM175.214 77.4798C174.703 76.3658 174.016 75.3864 173.153 74.5416C172.29 73.698 171.266 73.0853 170.084 72.7029C170.595 72.2889 171.099 71.6362 171.595 70.7441C172.09 69.8532 172.513 68.8811 172.865 67.8302C173.216 66.7787 173.457 65.7205 173.584 64.6533C173.711 63.5866 173.663 62.6709 173.441 61.906C172.896 59.9958 172.042 58.4988 170.875 57.4158C169.708 56.3334 168.35 55.5849 166.8 55.1704C165.249 54.7577 163.54 54.6692 161.67 54.908C159.8 55.1467 157.89 55.6164 155.941 56.317C155.941 56.1582 155.957 55.991 155.989 55.8158C156.02 55.6413 156.036 55.4576 156.036 55.2661C156.036 54.7886 155.797 54.3752 155.317 54.0243C154.838 53.674 154.287 53.4674 153.664 53.4031C153.04 53.3401 152.433 53.4746 151.841 53.8092C151.25 54.1437 150.842 54.7577 150.619 55.6479C150.363 58.5146 150.107 61.4927 149.852 64.5812C149.596 67.6708 149.324 70.792 149.037 73.9453C148.749 77.0979 148.461 80.227 148.174 83.3318C147.886 86.4372 147.598 89.4226 147.311 92.2886C147.407 93.1486 147.646 93.8177 148.03 94.2953C148.413 94.7734 148.861 95.0601 149.372 95.1553C149.883 95.251 150.419 95.1625 150.978 94.8922C151.537 94.6225 152.025 94.1516 152.441 93.4832C153.719 94.1838 155.158 94.6377 156.756 94.845C158.354 95.0516 159.975 95.0516 161.623 94.845C163.268 94.6377 164.89 94.248 166.488 93.6741C168.086 93.1013 169.541 92.3844 170.851 91.525C172.162 90.665 173.264 89.685 174.16 88.5869C175.054 87.4875 175.646 86.3014 175.933 85.0281C176.221 83.7221 176.301 82.4167 176.173 81.1106C176.045 79.8052 175.725 78.5955 175.214 77.4798Z" fill="#0D0C23"/>\n<path fill-rule="evenodd" clip-rule="evenodd" d="M221.989 102.702C221.814 103.753 221.565 104.86 221.246 106.023C220.926 107.184 220.551 108.244 220.12 109.2C219.688 110.155 219.209 110.926 218.682 111.516C218.154 112.105 217.586 112.352 216.979 112.257C216.5 112.192 216.196 111.89 216.069 111.349C215.94 110.807 215.94 110.138 216.069 109.343C216.196 108.546 216.443 107.646 216.811 106.643C217.179 105.64 217.627 104.644 218.154 103.658C218.682 102.67 219.281 101.723 219.952 100.815C220.623 99.9082 221.326 99.1512 222.061 98.5464C222.221 98.7373 222.293 99.2149 222.277 99.9797C222.26 100.744 222.165 101.652 221.989 102.702ZM238.243 81.9697C237.811 81.4921 237.284 81.2218 236.66 81.1576C236.037 81.0939 235.405 81.4442 234.767 82.2085C234.351 82.9727 233.823 83.7054 233.184 84.406C232.545 85.1072 231.882 85.7436 231.195 86.3169C230.507 86.8896 229.852 87.3841 229.229 87.7975C228.606 88.212 228.118 88.5144 227.767 88.7053C227.639 87.6866 227.566 86.5878 227.551 85.409C227.534 84.2308 227.559 83.0369 227.623 81.8266C227.718 80.1067 227.918 78.3715 228.222 76.6194C228.526 74.868 228.965 73.148 229.541 71.4595C229.541 70.5686 229.332 69.8438 228.917 69.2862C228.501 68.7293 227.998 68.3784 227.407 68.2353C226.815 68.0923 226.209 68.1717 225.585 68.4741C224.962 68.7771 224.427 69.3268 223.979 70.122C223.596 71.1735 223.156 72.3516 222.661 73.6571C222.165 74.9631 221.606 76.2928 220.983 77.6461C220.359 79.0006 219.664 80.3139 218.897 81.5873C218.13 82.8618 217.291 83.9927 216.38 84.9793C215.469 85.9666 214.478 86.7393 213.408 87.2963C212.336 87.8538 211.179 88.1005 209.932 88.0369C209.356 87.8775 208.94 87.4478 208.685 86.7466C208.429 86.0466 208.277 85.1702 208.23 84.1193C208.182 83.0684 208.23 81.9139 208.373 80.6557C208.517 79.3982 208.709 78.1479 208.949 76.9061C209.188 75.6637 209.452 74.4855 209.739 73.371C210.027 72.2565 210.298 71.3165 210.554 70.5523C210.938 69.6292 210.938 68.8559 210.554 68.2353C210.171 67.6141 209.644 67.2008 208.973 66.9929C208.302 66.7863 207.598 66.7947 206.863 67.0172C206.128 67.2402 205.6 67.7335 205.281 68.4977C204.737 69.8044 204.241 71.2686 203.794 72.8928C203.347 74.5171 202.987 76.1976 202.716 77.9328C202.444 79.6691 202.291 81.3891 202.26 83.0927C202.258 83.2036 202.263 83.309 202.263 83.4193C201.566 85.2708 200.902 86.6702 200.271 87.6066C199.456 88.8174 198.536 89.3429 197.514 89.1829C197.065 88.992 196.771 88.5465 196.627 87.8453C196.482 87.1453 196.435 86.2854 196.482 85.2654C196.531 84.2472 196.651 83.0927 196.842 81.8024C197.035 80.5127 197.273 79.1752 197.561 77.7897C197.849 76.4037 198.153 75.0116 198.472 73.6098C198.792 72.2086 199.079 70.8868 199.336 69.6444C199.304 68.5299 198.976 67.6784 198.352 67.0887C197.73 66.5002 196.858 66.2693 195.74 66.396C194.973 66.7147 194.405 67.1293 194.038 67.6384C193.67 68.1474 193.374 68.8008 193.151 69.5965C193.022 70.0111 192.831 70.8389 192.575 72.0813C192.319 73.3225 191.992 74.7486 191.592 76.3564C191.193 77.9655 190.721 79.6449 190.178 81.3963C189.635 83.1478 189.027 84.7333 188.357 86.1496C187.685 87.5666 186.95 88.7053 186.151 89.5653C185.352 90.4247 184.489 90.7756 183.562 90.6162C183.05 90.5205 182.723 89.995 182.579 89.0399C182.435 88.0841 182.412 86.9066 182.507 85.5048C182.603 84.1036 182.795 82.5666 183.082 80.8951C183.37 79.223 183.665 77.6388 183.969 76.1413C184.273 74.6449 184.553 73.3225 184.809 72.1765C185.064 71.0298 185.24 70.2656 185.336 69.8838C185.336 68.9602 185.127 68.2202 184.713 67.662C184.297 67.1056 183.794 66.7547 183.202 66.6111C182.61 66.4681 182.003 66.5475 181.381 66.8499C180.757 67.1529 180.222 67.7026 179.774 68.4977C179.614 69.3577 179.406 70.3535 179.151 71.4838C178.895 72.614 178.648 73.7765 178.408 74.971C178.168 76.1655 177.944 77.3358 177.737 78.4824C177.529 79.6291 177.377 80.6321 177.281 81.4921C177.217 82.1606 177.145 82.9812 177.066 83.9521C176.985 84.9242 176.945 85.9508 176.945 87.0332C176.945 88.1169 177.025 89.1914 177.186 90.258C177.345 91.3253 177.633 92.3047 178.048 93.1956C178.463 94.0877 179.047 94.8198 179.799 95.3931C180.549 95.9664 181.5 96.2846 182.651 96.3489C183.833 96.4119 184.864 96.3252 185.744 96.0858C186.622 95.847 187.421 95.4725 188.141 94.9628C188.86 94.4543 189.515 93.8489 190.107 93.1477C190.697 92.4477 191.281 91.6835 191.856 90.855C192.4 92.0659 193.103 93.0047 193.966 93.6737C194.829 94.3422 195.74 94.741 196.699 94.8677C197.657 94.9943 198.633 94.8604 199.624 94.4616C200.614 94.064 201.509 93.3871 202.308 92.4313C202.835 91.8453 203.331 91.1792 203.797 90.4429C203.995 90.7877 204.205 91.1204 204.442 91.4277C205.225 92.4477 206.288 93.1477 207.631 93.5301C209.069 93.9125 210.474 93.9768 211.849 93.7216C213.223 93.4671 214.534 93.0047 215.78 92.3362C217.027 91.6671 218.185 90.8635 219.257 89.9235C220.327 88.9841 221.262 88.0053 222.061 86.9854C222.029 87.7181 222.013 88.4114 222.013 89.0635C222.013 89.7168 221.997 90.4247 221.966 91.1895C220.367 92.3047 218.857 93.6422 217.435 95.2022C216.012 96.7622 214.765 98.4264 213.695 100.194C212.624 101.961 211.785 103.753 211.179 105.568C210.571 107.384 210.275 109.08 210.291 110.657C210.307 112.233 210.682 113.61 211.418 114.788C212.152 115.967 213.351 116.81 215.013 117.32C216.74 117.862 218.257 117.877 219.569 117.368C220.879 116.858 222.021 116.014 222.996 114.836C223.971 113.658 224.77 112.233 225.394 110.561C226.017 108.889 226.512 107.145 226.88 105.33C227.247 103.515 227.479 101.73 227.575 99.9797C227.671 98.2276 227.671 96.6664 227.575 95.2974C230.324 94.1513 232.577 92.7022 234.335 90.9501C236.093 89.1999 237.547 87.352 238.698 85.409C239.049 84.9314 239.169 84.3581 239.058 83.6896C238.945 83.0206 238.674 82.4472 238.243 81.9697Z" fill="#0D0C23"/>\n<path fill-rule="evenodd" clip-rule="evenodd" d="M298.724 78.9135C298.82 78.1814 298.964 77.4087 299.155 76.5966C299.347 75.7845 299.587 74.996 299.875 74.2318C300.162 73.4676 300.498 72.807 300.882 72.2494C301.265 71.6924 301.673 71.2943 302.104 71.0549C302.536 70.8167 302.974 70.8403 303.423 71.1264C303.902 71.4137 304.197 72.0185 304.31 72.9415C304.421 73.8663 304.31 74.853 303.974 75.9039C303.638 76.9554 303.039 77.942 302.176 78.8657C301.313 79.7899 300.146 80.3941 298.676 80.6808C298.612 80.236 298.628 79.6463 298.724 78.9135ZM315.336 80.8717C314.809 80.7135 314.306 80.6972 313.826 80.8244C313.347 80.9517 313.043 81.2862 312.916 81.8281C312.659 82.8468 312.251 83.8898 311.692 84.9565C311.133 86.0238 310.446 87.0346 309.632 87.9904C308.817 88.9455 307.897 89.7898 306.875 90.5219C305.851 91.2546 304.781 91.78 303.662 92.0982C302.543 92.4491 301.616 92.4885 300.882 92.2176C300.146 91.9479 299.563 91.4855 299.132 90.8328C298.7 90.1801 298.388 89.3916 298.197 88.468C298.005 87.5443 297.893 86.5892 297.861 85.6013C299.683 85.7292 301.305 85.4032 302.728 84.622C304.149 83.8426 305.356 82.8068 306.347 81.5171C307.337 80.2275 308.089 78.7784 308.6 77.1699C309.111 75.5621 309.399 73.9615 309.463 72.3688C309.495 70.8718 309.272 69.6064 308.792 68.5713C308.313 67.5367 307.665 66.7313 306.85 66.1586C306.036 65.5853 305.1 65.2507 304.046 65.1556C302.992 65.0598 301.92 65.2034 300.833 65.5853C299.522 66.0313 298.412 66.7555 297.501 67.7592C296.59 68.7622 295.831 69.9252 295.224 71.2464C294.617 72.5682 294.137 73.993 293.786 75.5215C293.434 77.0505 293.178 78.5554 293.019 80.0366C292.875 81.3656 292.798 82.6365 292.771 83.8632C292.702 84.0189 292.636 84.1686 292.563 84.3353C292.067 85.4668 291.491 86.5734 290.837 87.6558C290.182 88.7389 289.454 89.6467 288.656 90.3788C287.857 91.1116 287.026 91.3661 286.163 91.1431C285.651 91.0164 285.372 90.4261 285.324 89.3758C285.276 88.3243 285.331 87.0189 285.491 85.4583C285.651 83.8983 285.835 82.2093 286.043 80.3941C286.25 78.579 286.354 76.8439 286.354 75.1875C286.354 73.7542 286.082 72.3773 285.539 71.0549C284.995 69.7343 284.252 68.6349 283.31 67.7592C282.367 66.8828 281.272 66.3016 280.026 66.0156C278.779 65.7283 277.437 65.9198 275.999 66.5883C274.56 67.2574 273.417 68.1967 272.571 69.407C271.723 70.6179 270.948 71.8912 270.245 73.2288C269.989 72.2094 269.614 71.2628 269.118 70.3864C268.623 69.5107 268.016 68.7464 267.297 68.0931C266.577 67.441 265.769 66.9313 264.876 66.5646C263.981 66.1992 263.037 66.0156 262.046 66.0156C261.088 66.0156 260.201 66.1992 259.386 66.5646C258.571 66.9313 257.828 67.4004 257.156 67.9737C256.485 68.5476 255.878 69.1919 255.334 69.9088C254.791 70.6252 254.311 71.3343 253.896 72.0343C253.831 71.2064 253.76 70.4822 253.681 69.8603C253.6 69.2398 253.456 68.7143 253.249 68.2846C253.041 67.8543 252.746 67.5283 252.362 67.3052C251.978 67.0828 251.435 66.9707 250.732 66.9707C250.38 66.9707 250.028 67.0422 249.677 67.1852C249.325 67.3289 249.013 67.5283 248.742 67.7828C248.47 68.0386 248.263 68.3482 248.119 68.7143C247.975 69.0804 247.936 69.5028 247.999 69.9803C248.031 70.3312 248.119 70.7525 248.263 71.2464C248.406 71.7403 248.542 72.3858 248.67 73.1809C248.798 73.9773 248.902 74.9409 248.982 76.0712C249.062 77.2021 249.085 78.5875 249.054 80.2275C249.021 81.8681 248.902 83.7862 248.694 85.9837C248.486 88.1813 248.158 90.7291 247.711 93.6267C247.647 94.2957 247.903 94.8376 248.479 95.2515C249.054 95.6648 249.709 95.9036 250.444 95.9678C251.179 96.0315 251.875 95.9036 252.53 95.586C253.185 95.2666 253.561 94.7097 253.656 93.9139C253.752 92.417 253.936 90.8249 254.208 89.1364C254.479 87.4492 254.815 85.7771 255.215 84.1207C255.614 82.465 256.069 80.8887 256.581 79.3911C257.092 77.8942 257.66 76.573 258.283 75.4263C258.907 74.2797 259.554 73.3645 260.225 72.6797C260.896 71.9949 261.599 71.6524 262.335 71.6524C263.229 71.6524 263.924 72.0579 264.42 72.87C264.915 73.6827 265.266 74.7263 265.475 75.999C265.682 77.2736 265.778 78.6675 265.763 80.1796C265.746 81.6923 265.682 83.1492 265.571 84.5504C265.459 85.9522 265.331 87.2019 265.187 88.3007C265.043 89.3995 264.939 90.1564 264.876 90.5697C264.876 91.3025 265.155 91.8831 265.714 92.3134C266.273 92.743 266.896 92.9982 267.584 93.0776C268.272 93.1576 268.918 93.0297 269.526 92.6952C270.133 92.3606 270.485 91.7964 270.581 90.9994C270.9 88.7067 271.34 86.4062 271.899 84.0971C272.458 81.7881 273.098 79.7184 273.817 77.8869C274.536 76.0554 275.335 74.5585 276.214 73.3961C277.093 72.2343 278.028 71.6524 279.019 71.6524C279.53 71.6524 279.922 72.0033 280.193 72.7033C280.465 73.4039 280.601 74.3591 280.601 75.5694C280.601 76.4615 280.529 77.3772 280.386 78.3166C280.241 79.256 280.074 80.2275 279.882 81.2305C279.69 82.2341 279.522 83.2608 279.378 84.3117C279.235 85.3632 279.163 86.4613 279.163 87.608C279.163 88.4043 279.243 89.3279 279.403 90.3788C279.562 91.4291 279.865 92.4255 280.313 93.3642C280.761 94.3042 281.376 95.1 282.16 95.7527C282.943 96.4054 283.941 96.7321 285.155 96.7321C286.978 96.7321 288.591 96.3418 289.998 95.5618C291.404 94.7818 292.611 93.763 293.618 92.5049C293.67 92.4388 293.718 92.3685 293.769 92.3031C293.846 92.4891 293.914 92.6861 294.001 92.863C294.688 94.2642 295.623 95.3466 296.806 96.1115C297.988 96.8757 299.379 97.2975 300.978 97.3775C302.575 97.4563 304.317 97.1618 306.204 96.4933C307.609 95.9836 308.832 95.3466 309.871 94.5824C310.909 93.8182 311.844 92.8867 312.675 91.7879C313.507 90.6891 314.265 89.4231 314.953 87.9904C315.641 86.5565 316.335 84.9171 317.038 83.0692C317.166 82.5608 317.046 82.1068 316.679 81.7081C316.311 81.3105 315.864 81.0317 315.336 80.8717Z" fill="#0D0C23"/>\n<path fill-rule="evenodd" clip-rule="evenodd" d="M341.393 75.5432C341.233 76.4832 341.018 77.5189 340.746 78.6486C340.474 79.7795 340.131 80.9498 339.715 82.1601C339.3 83.3703 338.788 84.4612 338.181 85.4321C337.574 86.4042 336.878 87.1757 336.096 87.7491C335.312 88.3224 334.41 88.5612 333.387 88.4654C332.875 88.4024 332.483 88.0521 332.212 87.4145C331.94 86.7782 331.797 85.9655 331.78 84.9782C331.764 83.9915 331.852 82.9085 332.044 81.7298C332.236 80.5522 332.531 79.3971 332.932 78.2662C333.331 77.1365 333.818 76.0929 334.393 75.1371C334.969 74.182 335.632 73.4414 336.383 72.916C337.134 72.3905 337.958 72.1445 338.852 72.1754C339.747 72.2075 340.706 72.6529 341.729 73.5129C341.664 73.9275 341.553 74.6044 341.393 75.5432ZM358.437 79.1977C357.941 78.9431 357.43 78.888 356.903 79.031C356.376 79.174 356 79.6601 355.777 80.488C355.649 81.3801 355.361 82.4304 354.914 83.6406C354.466 84.8509 353.914 85.9982 353.26 87.08C352.604 88.163 351.853 89.063 351.006 89.7793C350.159 90.4963 349.256 90.823 348.298 90.7581C347.498 90.6951 346.938 90.289 346.62 89.5406C346.299 88.7921 346.132 87.8533 346.116 86.7218C346.099 85.5921 346.212 84.3182 346.451 82.9007C346.691 81.4837 346.979 80.0746 347.314 78.6722C347.65 77.2716 347.994 75.9256 348.346 74.6359C348.697 73.3463 348.984 72.2554 349.209 71.3639C349.464 70.5675 349.384 69.8912 348.969 69.333C348.553 68.7766 348.034 68.3778 347.411 68.1391C346.787 67.9003 346.155 67.8366 345.516 67.9481C344.877 68.0597 344.462 68.4021 344.27 68.9748C342.384 67.3506 340.57 66.4748 338.829 66.3476C337.086 66.2203 335.48 66.6027 334.01 67.4942C332.539 68.3857 331.237 69.6754 330.103 71.3639C328.968 73.0523 328.049 74.8911 327.345 76.8814C326.642 78.8716 326.203 80.9025 326.027 82.9722C325.851 85.0424 325.987 86.9297 326.435 88.6333C326.883 90.3369 327.673 91.7308 328.808 92.8126C329.942 93.8956 331.485 94.4375 333.435 94.4375C334.298 94.4375 335.129 94.2623 335.928 93.912C336.726 93.5611 337.462 93.1472 338.133 92.6696C338.804 92.192 339.395 91.6902 339.908 91.1648C340.418 90.6393 340.818 90.2018 341.106 89.8509C341.329 90.9975 341.697 91.9696 342.209 92.7654C342.719 93.5611 343.303 94.215 343.958 94.7235C344.613 95.2326 345.301 95.6071 346.02 95.8465C346.739 96.0853 347.435 96.2047 348.105 96.2047C349.608 96.2047 351.013 95.695 352.325 94.6756C353.635 93.6575 354.81 92.4066 355.849 90.926C356.887 89.4448 357.743 87.8848 358.413 86.2442C359.085 84.6043 359.532 83.1473 359.756 81.8728C359.98 81.3952 359.939 80.894 359.636 80.3686C359.332 79.8431 358.933 79.4534 358.437 79.1977Z" fill="#0D0C23"/>\n<path fill-rule="evenodd" clip-rule="evenodd" d="M444.738 105.571C444.467 106.653 444.043 107.57 443.467 108.318C442.892 109.066 442.173 109.456 441.31 109.489C440.767 109.52 440.351 109.233 440.063 108.629C439.776 108.023 439.576 107.243 439.464 106.288C439.352 105.332 439.304 104.265 439.32 103.087C439.336 101.909 439.384 100.746 439.464 99.5996C439.543 98.4536 439.64 97.3857 439.752 96.3991C439.863 95.4112 439.951 94.6482 440.015 94.1064C441.102 94.2336 442.006 94.7027 442.724 95.5154C443.443 96.3275 443.995 97.2906 444.378 98.4057C444.762 99.5202 444.985 100.723 445.05 102.012C445.113 103.302 445.009 104.488 444.738 105.571ZM427.382 105.571C427.111 106.653 426.687 107.57 426.112 108.318C425.537 109.066 424.817 109.456 423.954 109.489C423.411 109.52 422.996 109.233 422.708 108.629C422.42 108.023 422.22 107.243 422.109 106.288C421.996 105.332 421.948 104.265 421.965 103.087C421.98 101.909 422.028 100.746 422.109 99.5996C422.188 98.4536 422.284 97.3857 422.396 96.3991C422.508 95.4112 422.595 94.6482 422.66 94.1064C423.746 94.2336 424.65 94.7027 425.368 95.5154C426.088 96.3275 426.639 97.2906 427.023 98.4057C427.407 99.5202 427.63 100.723 427.694 102.012C427.757 103.302 427.653 104.488 427.382 105.571ZM409.572 78.4375C409.539 79.2011 409.467 79.8781 409.355 80.4672C409.243 81.0575 409.092 81.4308 408.9 81.5902C408.548 81.3987 408.116 80.906 407.605 80.109C407.094 79.3133 406.695 78.4127 406.406 77.4096C406.119 76.4066 406.03 75.42 406.143 74.4479C406.254 73.477 406.758 72.7212 407.653 72.1788C408.004 71.9879 408.308 72.0594 408.564 72.394C408.82 72.7285 409.027 73.2139 409.188 73.8509C409.347 74.4885 409.458 75.2206 409.523 76.0485C409.587 76.8769 409.603 77.6727 409.572 78.4375ZM405.328 87.9677C404.832 88.4925 404.28 88.9464 403.674 89.3289C403.066 89.7113 402.443 89.9979 401.804 90.1889C401.164 90.3804 400.589 90.4276 400.078 90.3319C398.64 90.0458 397.537 89.424 396.77 88.4689C396.003 87.5137 395.515 86.3913 395.308 85.1017C395.1 83.8114 395.123 82.4338 395.38 80.969C395.635 79.5042 396.066 78.143 396.674 76.8848C397.281 75.6266 398.017 74.5436 398.879 73.6364C399.742 72.7285 400.685 72.1637 401.708 71.94C401.324 73.5642 401.197 75.2448 401.324 76.98C401.452 78.7157 401.868 80.3478 402.571 81.8762C403.018 82.8011 403.554 83.6441 404.177 84.4083C404.801 85.1732 405.56 85.8259 406.455 86.3671C406.199 86.9089 405.823 87.4422 405.328 87.9677ZM458.378 78.9151C458.474 78.183 458.617 77.4096 458.81 76.5975C459.001 75.786 459.241 74.9976 459.528 74.2333C459.816 73.4685 460.152 72.8079 460.536 72.2509C460.92 71.694 461.326 71.2952 461.758 71.0564C462.19 70.8176 462.629 70.8413 463.076 71.1279C463.556 71.4152 463.851 72.02 463.963 72.943C464.075 73.8673 463.963 74.8539 463.628 75.9054C463.292 76.9563 462.693 77.9436 461.83 78.8666C460.968 79.7914 459.8 80.3957 458.33 80.6823C458.266 80.2369 458.282 79.6478 458.378 78.9151ZM477.7 78.9151C477.796 78.183 477.939 77.4096 478.131 76.5975C478.323 75.786 478.563 74.9976 478.851 74.2333C479.138 73.4685 479.473 72.8079 479.857 72.2509C480.241 71.694 480.649 71.2952 481.08 71.0564C481.512 70.8176 481.951 70.8413 482.398 71.1279C482.878 71.4152 483.173 72.02 483.285 72.943C483.397 73.8673 483.285 74.8539 482.95 75.9054C482.614 76.9563 482.015 77.9436 481.152 78.8666C480.289 79.7914 479.122 80.3957 477.652 80.6823C477.588 80.2369 477.604 79.6478 477.7 78.9151ZM495.655 81.7096C495.287 81.312 494.84 81.0332 494.313 80.8732C493.785 80.7144 493.282 80.6987 492.802 80.826C492.323 80.9532 492.018 81.2878 491.891 81.829C491.635 82.8484 491.228 83.8914 490.669 84.9574C490.109 86.0253 489.422 87.0362 488.607 87.9913C487.792 88.9464 486.873 89.7913 485.851 90.5234C484.827 91.2561 483.757 91.7816 482.639 92.0991C481.519 92.4506 480.592 92.49 479.857 92.2191C479.122 91.9488 478.539 91.487 478.107 90.8343C477.676 90.181 477.365 89.3931 477.172 88.4689C476.981 87.5459 476.868 86.5907 476.837 85.6029C478.659 85.7307 480.281 85.4047 481.703 84.6235C483.125 83.8435 484.332 82.8077 485.324 81.5181C486.314 80.229 487.065 78.7799 487.576 77.1715C488.087 75.563 488.375 73.963 488.44 72.3703C488.471 70.8734 488.247 69.6073 487.768 68.5722C487.289 67.5377 486.642 66.7328 485.827 66.1601C485.011 65.5862 484.077 65.2522 483.021 65.1565C481.967 65.0607 480.896 65.205 479.809 65.5862C478.498 66.0328 477.388 66.7571 476.478 67.7601C475.567 68.7637 474.807 69.9267 474.2 71.2473C473.592 72.5697 473.113 73.9939 472.761 75.523C472.409 77.0515 472.154 78.5569 471.995 80.0375C471.839 81.4744 471.755 82.8496 471.736 84.1659C471.615 84.4283 471.486 84.692 471.347 84.9574C470.787 86.0253 470.1 87.0362 469.285 87.9913C468.471 88.9464 467.551 89.7913 466.529 90.5234C465.506 91.2561 464.435 91.7816 463.317 92.0991C462.197 92.4506 461.271 92.49 460.536 92.2191C459.8 91.9488 459.217 91.487 458.786 90.8343C458.355 90.181 458.043 89.3931 457.851 88.4689C457.659 87.5459 457.547 86.5907 457.515 85.6029C459.337 85.7307 460.959 85.4047 462.382 84.6235C463.803 83.8435 465.01 82.8077 466.001 81.5181C466.992 80.229 467.743 78.7799 468.254 77.1715C468.765 75.563 469.054 73.963 469.117 72.3703C469.149 70.8734 468.926 69.6073 468.447 68.5722C467.967 67.5377 467.319 66.7328 466.504 66.1601C465.689 65.5862 464.755 65.2522 463.7 65.1565C462.645 65.0607 461.574 65.205 460.488 65.5862C459.176 66.0328 458.066 66.7571 457.156 67.7601C456.245 68.7637 455.485 69.9267 454.878 71.2473C454.271 72.5697 453.792 73.9939 453.44 75.523C453.088 77.0515 452.832 78.5569 452.673 80.0375C452.582 80.8726 452.522 81.6823 452.477 82.4774C452.168 82.7393 451.867 83.0029 451.546 83.2617C450.444 84.1538 449.284 84.9574 448.07 85.6744C446.855 86.3913 445.592 86.9804 444.283 87.4422C442.971 87.904 441.629 88.1828 440.255 88.278L443.228 56.5578C443.42 55.8887 443.324 55.3003 442.94 54.7906C442.557 54.2809 442.061 53.9306 441.454 53.7397C440.847 53.5482 440.199 53.5645 439.512 53.787C438.824 54.0106 438.258 54.5203 437.81 55.3154C437.586 56.5263 437.354 58.182 437.115 60.2838C436.875 62.3856 436.635 64.6789 436.396 67.1631C436.156 69.6473 435.916 72.2109 435.677 74.8539C435.437 77.4981 435.229 79.966 435.053 82.2587C435.045 82.3605 435.039 82.4526 435.031 82.5532C434.751 82.7896 434.48 83.0277 434.19 83.2617C433.088 84.1538 431.928 84.9574 430.714 85.6744C429.499 86.3913 428.237 86.9804 426.927 87.4422C425.616 87.904 424.273 88.1828 422.899 88.278L425.872 56.5578C426.064 55.8887 425.968 55.3003 425.585 54.7906C425.201 54.2809 424.705 53.9306 424.098 53.7397C423.491 53.5482 422.843 53.5645 422.156 53.787C421.469 54.0106 420.902 54.5203 420.454 55.3154C420.23 56.5263 419.999 58.182 419.76 60.2838C419.519 62.3856 419.28 64.6789 419.04 67.1631C418.8 69.6473 418.561 72.2109 418.321 74.8539C418.082 77.4981 417.873 79.966 417.698 82.2587C417.694 82.3047 417.691 82.3465 417.687 82.3926C417.185 82.6247 416.638 82.8284 416.043 82.9993C415.436 83.175 414.749 83.2786 413.982 83.3102C414.11 82.7362 414.213 82.0993 414.293 81.3987C414.373 80.6987 414.438 79.966 414.486 79.2011C414.534 78.4375 414.549 77.6727 414.534 76.9084C414.517 76.1436 414.477 75.4436 414.414 74.806C414.253 73.4376 413.958 72.1394 413.527 70.9128C413.095 69.6873 412.512 68.6607 411.777 67.8316C411.041 67.0037 410.123 66.4462 409.019 66.1601C407.917 65.8734 406.63 65.9686 405.161 66.4462C402.986 66.1601 401.029 66.3595 399.287 67.0437C397.545 67.7292 396.034 68.7237 394.756 70.0291C393.478 71.3358 392.431 72.8715 391.616 74.6394C390.801 76.4066 390.257 78.2224 389.986 80.0848C389.871 80.8744 389.815 81.6605 389.798 82.4447C389.303 83.4544 388.761 84.3368 388.164 85.0774C387.317 86.1283 386.438 86.9883 385.527 87.6568C384.616 88.3258 383.713 88.8355 382.819 89.1858C381.923 89.5367 381.124 89.7755 380.421 89.9022C379.59 90.0616 378.791 90.0779 378.024 89.9501C377.257 89.8234 376.553 89.4567 375.915 88.8513C375.403 88.4058 375.011 87.6889 374.74 86.7016C374.468 85.7144 374.309 84.5926 374.261 83.3338C374.213 82.0756 374.261 80.7617 374.404 79.3926C374.548 78.0236 374.795 76.7254 375.147 75.4994C375.499 74.2733 375.945 73.1746 376.49 72.2024C377.032 71.2322 377.672 70.5388 378.408 70.1249C378.822 70.1891 379.079 70.4352 379.175 70.8649C379.271 71.2952 379.294 71.8049 379.246 72.394C379.199 72.9836 379.127 73.5885 379.031 74.2091C378.935 74.8303 378.887 75.3485 378.887 75.7618C379.047 76.6218 379.358 77.2909 379.822 77.7684C380.285 78.246 380.805 78.5254 381.38 78.6042C381.955 78.6842 382.522 78.549 383.083 78.1981C383.641 77.8484 384.096 77.2909 384.449 76.526C384.48 76.5581 384.528 76.5739 384.592 76.5739L385.264 70.5073C385.455 69.6788 385.327 68.9467 384.88 68.3098C384.432 67.6728 383.841 67.3062 383.106 67.211C382.179 65.8734 380.924 65.165 379.342 65.085C377.76 65.0056 376.138 65.5231 374.476 66.6377C373.453 67.371 372.55 68.3813 371.767 69.671C370.983 70.9613 370.345 72.394 369.85 73.9703C369.353 75.5466 369.002 77.2115 368.795 78.963C368.587 80.7144 368.547 82.4187 368.674 84.0738C368.802 85.7307 369.098 87.2913 369.562 88.7555C370.025 90.221 370.672 91.447 371.504 92.4337C372.207 93.2937 373.005 93.9233 373.9 94.3215C374.795 94.7197 375.73 94.9658 376.705 95.0615C377.68 95.1567 378.647 95.1167 379.606 94.9421C380.565 94.7676 381.476 94.5209 382.339 94.2015C383.457 93.7882 384.609 93.2621 385.791 92.6252C386.973 91.9888 388.108 91.224 389.195 90.3319C389.767 89.8628 390.317 89.3513 390.849 88.8028C391.091 89.4016 391.362 89.981 391.688 90.5234C392.551 91.9561 393.717 93.1191 395.188 94.0106C396.657 94.9021 398.464 95.3312 400.605 95.3003C402.907 95.2682 405.032 94.6876 406.982 93.5567C408.932 92.427 410.53 90.7616 411.777 88.5646C413.644 88.5646 415.481 88.258 417.287 87.6489C417.272 87.8416 417.256 88.0446 417.242 88.2307C417.115 89.9186 417.05 91.0646 417.05 91.67C417.019 92.7209 416.947 94.0185 416.835 95.5627C416.723 97.1075 416.651 98.7318 416.619 100.435C416.588 102.139 416.651 103.859 416.811 105.595C416.971 107.33 417.306 108.907 417.818 110.325C418.328 111.741 419.055 112.944 419.999 113.932C420.941 114.918 422.18 115.508 423.715 115.699C425.345 115.921 426.751 115.635 427.934 114.839C429.116 114.042 430.075 112.952 430.811 111.567C431.546 110.181 432.064 108.581 432.369 106.766C432.672 104.95 432.76 103.127 432.633 101.295C432.504 99.4639 432.168 97.7366 431.625 96.113C431.082 94.4882 430.33 93.1506 429.372 92.0991C429.948 91.9409 430.634 91.6385 431.434 91.1919C432.232 90.7464 433.055 90.2446 433.903 89.687C434.111 89.5501 434.316 89.4058 434.524 89.2652C434.446 90.3937 434.406 91.1985 434.406 91.67C434.375 92.7209 434.303 94.0185 434.19 95.5627C434.079 97.1075 434.007 98.7318 433.975 100.435C433.943 102.139 434.007 103.859 434.167 105.595C434.326 107.33 434.662 108.907 435.173 110.325C435.684 111.741 436.412 112.944 437.354 113.932C438.297 114.918 439.536 115.508 441.071 115.699C442.7 115.921 444.106 115.635 445.289 114.839C446.472 114.042 447.431 112.952 448.166 111.567C448.901 110.181 449.42 108.581 449.724 106.766C450.028 104.95 450.115 103.127 449.988 101.295C449.86 99.4639 449.524 97.7366 448.982 96.113C448.437 94.4882 447.687 93.1506 446.727 92.0991C447.303 91.9409 447.99 91.6385 448.789 91.1919C449.588 90.7464 450.411 90.2446 451.259 89.687C451.699 89.3974 452.136 89.0986 452.573 88.7913C452.737 90.3488 453.091 91.7149 453.655 92.864C454.343 94.2658 455.277 95.3482 456.46 96.113C457.642 96.8766 459.033 97.299 460.632 97.3784C462.23 97.4572 463.971 97.1633 465.858 96.4942C467.264 95.9851 468.486 95.3482 469.525 94.5839C470.563 93.8191 471.498 92.8876 472.33 91.7894C472.378 91.7258 472.423 91.6567 472.47 91.5925C472.618 92.0385 472.782 92.467 472.977 92.864C473.665 94.2658 474.6 95.3482 475.782 96.113C476.964 96.8766 478.355 97.299 479.953 97.3784C481.551 97.4572 483.293 97.1633 485.179 96.4942C486.586 95.9851 487.808 95.3482 488.847 94.5839C489.885 93.8191 490.82 92.8876 491.652 91.7894C492.483 90.6901 493.241 89.424 493.929 87.9913C494.616 86.558 495.311 84.9186 496.015 83.0708C496.142 82.5617 496.022 82.1078 495.655 81.7096Z" fill="#0D0C23"/>\n</svg>';

// src/Storage/Storage.ts
var Storage = class {
  constructor(data) {
    this.set(data);
  }
  get(k3) {
    return this.item.get(k3);
  }
  set(value) {
    this.item = new StorageObjectItem(value);
  }
  collect() {
    return this.item.value();
  }
};
var create = (value) => {
  if (typeof value === "object" && !Array.isArray(value) && value !== null) {
    return new StorageObjectItem(value);
  } else {
    return new StoragePrimitiveItem(value);
  }
};
var StorageObjectItem = class {
  constructor(data) {
    this.items = null;
    this.set(data);
  }
  add(key, value) {
    this.items[key] = create(value);
    return this.get(key);
  }
  get(key) {
    return this.items[key];
  }
  set(value) {
    this.items = {};
    for (const [key, v3] of ObjectHelper.entries(value)) {
      this.items[key] = create(v3);
    }
  }
  value() {
    const data = {};
    for (const [k3, v3] of ObjectHelper.entries(this.items)) {
      data[k3] = v3.value();
    }
    return data;
  }
};
var StoragePrimitiveItem = class {
  constructor(data) {
    this.data = data;
  }
  value() {
    return this.data;
  }
  set(value) {
    this.data = value;
  }
};

// config/inversify.config.ts
var import_reflect_metadata2 = __toESM(require_Reflect());

// src/Creator/Template/Factory.ts
var Factory = class {
  constructor(pattern, factory) {
    this.pattern = pattern;
    this.factory = factory;
  }
  create(template) {
    const type = new RegExp(this.pattern).test(template) ? "composite" : "simple";
    const obj = this.factory(type);
    obj.setTemplate(template);
    return obj;
  }
};
Factory = __decorateClass([
  Kt(),
  __decorateParam(0, Yt(inversify_types_default["template:pattern"])),
  __decorateParam(1, Yt(inversify_types_default["factory:template:resolver"]))
], Factory);

// src/Creator/Template/Simple.ts
var Simple = class {
  constructor(factory) {
    this.factory = factory;
    this.placeholder = null;
  }
  getPlaceholders() {
    if (this.placeholder === null) {
      this.placeholder = this.factory.create(this.template);
    }
    return [this.placeholder];
  }
  getTemplate() {
    return this.template;
  }
  setTemplate(template) {
    this.template = template;
  }
};
Simple = __decorateClass([
  Kt(),
  __decorateParam(0, Yt(inversify_types_default["factory:placeholder"]))
], Simple);

// src/Creator/Template/Placeholders/AbstractPlaceholder.ts
var AbstractPlaceholder = class {
  getPlaceholder() {
    return this.placeholder;
  }
  setPlaceholder(placeholder) {
    this.placeholder = placeholder;
    return this;
  }
};
AbstractPlaceholder.META = "meta";
AbstractPlaceholder.BRACKETS = "brackets";
AbstractPlaceholder.HEADING = "heading";
AbstractPlaceholder.FILE = "file";
AbstractPlaceholder.LOGIC = "logic";
AbstractPlaceholder = __decorateClass([
  Kt()
], AbstractPlaceholder);

// src/Creator/Template/Placeholders/MetaPlaceholder.ts
var MetaPlaceholder = class extends AbstractPlaceholder {
  constructor(extractor, factory) {
    super();
    this.extractor = extractor;
    this.factory = factory;
  }
  makeValue(path) {
    return this.extractor.extract(this.placeholder, this.factory(path, "frontmatter"));
  }
};
MetaPlaceholder = __decorateClass([
  Kt(),
  __decorateParam(0, Yt(inversify_types_default["component:extractor"])),
  __decorateParam(1, Yt(inversify_types_default["factory:obsidian:meta"]))
], MetaPlaceholder);

// src/Components/Extractor/Exceptions/PathNotFoundException.ts
var PathNotFoundException = class extends Error {
};

// src/Components/Extractor/Exceptions/TypeNotSupportedException.ts
var TypeNotSupportedException = class extends Error {
};

// src/Creator/Creator.ts
var Creator = class {
  constructor(factory, logger) {
    this.factory = factory;
    this.logger = logger;
    this.cache = /* @__PURE__ */ new Map();
  }
  create(path, template) {
    var _a4, _b2;
    for (const placeholder of this.pull(template).getPlaceholders()) {
      let value = "";
      try {
        value = (_a4 = placeholder.makeValue(path)) != null ? _a4 : "";
      } catch (e2) {
        if (e2 instanceof PathNotFoundException || e2 instanceof TypeNotSupportedException) {
          this.logger.log(`Error by path: ${path}. ${e2.message}`);
        } else {
          throw e2;
        }
      }
      template = template.replace(placeholder.getPlaceholder(), value);
    }
    if ((_b2 = template == null ? void 0 : template.trim()) == null ? void 0 : _b2.length) {
      return template.trim();
    }
    return null;
  }
  pull(template) {
    const key = `template:${template}`;
    if (this.cache.has(key) === false) {
      this.logger.log(`Create template fo ${template}`);
      this.cache.set(key, this.factory.create(template));
    }
    return this.cache.get(key);
  }
};
Creator = __decorateClass([
  Kt(),
  __decorateParam(0, Yt(inversify_types_default["factory:creator:template"])),
  __decorateParam(1, Yt(inversify_types_default.logger)),
  __decorateParam(1, Ut("creator"))
], Creator);

// src/Creator/Template/Placeholders/Factory.ts
var Factory2 = class {
  constructor(factory) {
    this.factory = factory;
  }
  create(placeholder) {
    let type = AbstractPlaceholder.META;
    if (placeholder.startsWith("{{") && placeholder.endsWith("}}")) {
      type = AbstractPlaceholder.BRACKETS;
    } else if (placeholder.startsWith("_")) {
      type = AbstractPlaceholder.FILE;
    } else if (placeholder === "#heading") {
      type = AbstractPlaceholder.HEADING;
    } else if (placeholder.includes("|")) {
      type = AbstractPlaceholder.LOGIC;
    }
    return this.factory(type, placeholder).setPlaceholder(placeholder);
  }
};
Factory2 = __decorateClass([
  Kt(),
  __decorateParam(0, Yt(inversify_types_default["factory:placeholder:resolver"]))
], Factory2);

// src/Creator/Template/Composite.ts
var Composite = class {
  constructor(pattern, factory) {
    this.pattern = pattern;
    this.factory = factory;
    this.placeholders = [];
  }
  getPlaceholders() {
    if (this.placeholders.length === 0) {
      const parts = this.template.match(new RegExp(this.pattern, "g"));
      for (const part of parts) {
        const {
          groups: { placeholder }
        } = part.match(this.pattern);
        this.placeholders.push(this.factory.create(placeholder));
      }
    }
    return this.placeholders;
  }
  getTemplate() {
    return this.template;
  }
  setTemplate(template) {
    this.template = template;
  }
};
Composite = __decorateClass([
  Kt(),
  __decorateParam(0, Yt(inversify_types_default["template:pattern"])),
  __decorateParam(1, Yt(inversify_types_default["factory:placeholder"]))
], Composite);

// src/Creator/Template/Placeholders/BracketsPlaceholder.ts
var BracketsPlaceholder = class {
  constructor(factory) {
    this.factory = factory;
    this.leftSpace = "";
    this.rightSpace = "";
  }
  getPlaceholder() {
    return this.placeholder;
  }
  makeValue(path) {
    const value = this.child.makeValue(path);
    return value ? `${this.leftSpace}${value}${this.rightSpace}` : null;
  }
  setPlaceholder(placeholder) {
    this.placeholder = placeholder;
    const {
      groups: { left, key, right }
    } = this.placeholder.match(new RegExp(`^{{(?<left>\\s*)(?<key>.*?)(?<right>\\s*)}}$`));
    this.leftSpace = left != null ? left : "";
    this.rightSpace = right != null ? right : "";
    this.child = this.factory.create(key);
    return this;
  }
};
BracketsPlaceholder = __decorateClass([
  Kt(),
  __decorateParam(0, Yt(inversify_types_default["factory:placeholder"]))
], BracketsPlaceholder);

// src/Creator/Template/Placeholders/FilePlaceholder.ts
var FilePlaceholder = class extends AbstractPlaceholder {
  constructor(extractor, factory) {
    super();
    this.extractor = extractor;
    this.factory = factory;
  }
  makeValue(path) {
    return this.extractor.extract(this.path, this.factory(path));
  }
  setPlaceholder(placeholder) {
    this.path = placeholder.substring(1);
    return super.setPlaceholder(placeholder);
  }
};
FilePlaceholder = __decorateClass([
  __decorateParam(0, Yt(inversify_types_default["component:extractor"])),
  __decorateParam(1, Yt(inversify_types_default["factory:obsidian:file"]))
], FilePlaceholder);

// src/Creator/Template/Placeholders/HeadingPlaceholder.ts
var HeadingPlaceholder = class extends AbstractPlaceholder {
  constructor(factory) {
    super();
    this.factory = factory;
  }
  makeValue(path) {
    var _a4, _b2, _c;
    return (_c = (_b2 = (_a4 = this.factory(path, "headings")) == null ? void 0 : _a4[0]) == null ? void 0 : _b2.heading) != null ? _c : "";
  }
};
HeadingPlaceholder = __decorateClass([
  Kt(),
  __decorateParam(0, Yt(inversify_types_default["factory:obsidian:meta"]))
], HeadingPlaceholder);

// src/Creator/Template/Placeholders/LogicPlaceholder.ts
var LogicPlaceholder = class {
  constructor(factory) {
    this.factory = factory;
    this.DELIMITER = "|";
    this.children = [];
  }
  makeValue(path) {
    for (const child of this.children) {
      const value = child.makeValue(path);
      if (value) {
        return value;
      }
    }
  }
  setPlaceholder(placeholder) {
    this.placeholder = placeholder;
    for (const item of this.placeholder.split(this.DELIMITER)) {
      this.children.push(this.factory.create(item.trim()));
    }
    return this;
  }
  getPlaceholder() {
    return this.placeholder;
  }
};
LogicPlaceholder = __decorateClass([
  Kt(),
  __decorateParam(0, Yt(inversify_types_default["factory:placeholder"]))
], LogicPlaceholder);

// config/services/creator.config.ts
var creator_config_default = new kt((bind) => {
  bind(inversify_types_default["factory:creator:template"]).to(Factory);
  bind(inversify_types_default["factory:template:resolver"]).toAutoNamedFactory(
    inversify_types_default["creator:template"]
  );
  bind(inversify_types_default["creator:template"]).to(Simple).whenTargetNamed("simple");
  bind(inversify_types_default["creator:template"]).to(Composite).whenTargetNamed("composite");
  bind(inversify_types_default.placeholder).to(MetaPlaceholder).whenTargetNamed(AbstractPlaceholder.META);
  bind(inversify_types_default.placeholder).to(FilePlaceholder).whenTargetNamed(AbstractPlaceholder.FILE);
  bind(inversify_types_default.placeholder).to(BracketsPlaceholder).whenTargetNamed(AbstractPlaceholder.BRACKETS);
  bind(inversify_types_default.placeholder).to(HeadingPlaceholder).whenTargetNamed(AbstractPlaceholder.HEADING);
  bind(inversify_types_default.placeholder).to(LogicPlaceholder).whenTargetNamed(AbstractPlaceholder.LOGIC);
  bind(inversify_types_default["creator:creator"]).to(Creator);
  bind(inversify_types_default["factory:placeholder:resolver"]).toAutoNamedFactory(
    inversify_types_default.placeholder
  );
  bind(inversify_types_default["getter:delimiter"]).toDynamicValue((c3) => () => c3.container.get(inversify_types_default.delimiter));
  bind(inversify_types_default["factory:placeholder"]).to(Factory2).inSingletonScope();
});

// src/Feature/Explorer/ExplorerManager.ts
var import_obsidian3 = require("obsidian");

// src/Feature/AbstractFeature.ts
var AbstractFeature = class {
  static getId() {
    throw new Error("Static function 'getId()' is not implemented");
  }
};
AbstractFeature = __decorateClass([
  Kt()
], AbstractFeature);

// src/Feature/AbstractManager.ts
var AbstractManager = class extends AbstractFeature {
  disable() {
    this.isEnabled() && this.doDisable();
  }
  enable() {
    !this.isEnabled() && this.doEnable();
  }
  async update(path) {
    if (!this.isEnabled()) {
      return false;
    }
    return this.doUpdate(path);
  }
  async refresh() {
    if (this.isEnabled()) {
      return this.doRefresh();
    }
  }
  setResolver(resolver) {
    this.resolver = resolver;
  }
};
AbstractManager = __decorateClass([
  Kt()
], AbstractManager);

// src/Feature/Explorer/ExplorerViewUndefined.ts
var ExplorerViewUndefined = class extends Error {
};

// src/Feature/Explorer/ExplorerManager.ts
var ExplorerManager = class extends AbstractManager {
  constructor(facade, factory, sortFactory, dispatcher, logger) {
    super();
    this.facade = facade;
    this.factory = factory;
    this.dispatcher = dispatcher;
    this.logger = logger;
    this.explorerView = null;
    this.modified = /* @__PURE__ */ new WeakMap();
    this.state = "disabled" /* Disabled */;
    this.sort = null;
    this.sort = sortFactory();
  }
  static getId() {
    return "explorer" /* Explorer */;
  }
  setState(state) {
    this.state = state;
  }
  isState(state) {
    return this.state === state;
  }
  getId() {
    return "explorer" /* Explorer */;
  }
  isEnabled() {
    return this.state !== "disabled" /* Disabled */;
  }
  doDisable() {
    var _a4;
    if (this.explorerView) {
      this.restoreTitles();
      this.explorerView = null;
    }
    (_a4 = this.sort) == null ? void 0 : _a4.stop();
    this.setState("disabled" /* Disabled */);
  }
  tryEnable() {
    var _a4;
    (_a4 = this.sort) == null ? void 0 : _a4.start();
    this.setState("enabled" /* Enabled */);
    this.logger.log("Manager enabled");
  }
  subscribe() {
    const ref = this.dispatcher.addListener({
      name: "active:leaf:change",
      cb: () => {
        if (this.isEnabled() && this.getExplorerView()) {
          this.logger.log("Catch explorer view");
          this.tryEnable();
          this.doRefresh().catch(this.logger.log);
          this.dispatcher.removeListener(ref);
        } else if (!this.isEnabled()) {
          this.logger.log("Called when is not enabled");
          this.dispatcher.removeListener(ref);
        }
      }
    });
    this.setState("waitForActiveLeaf" /* WaitForActiveLeaf */);
  }
  doEnable() {
    if (!this.facade.isInternalPluginEnabled("file-explorer" /* FileExplorer */)) {
      this.logger.info(`internal plugin ${"file-explorer" /* FileExplorer */} is not enabled`);
      return;
    }
    if (!this.getExplorerView()) {
      this.logger.log("Could not get view. Subscribe and wait for active leaf");
      this.subscribe();
    } else {
      this.tryEnable();
    }
  }
  async doRefresh() {
    if (this.isState("enabled" /* Enabled */)) {
      return this.updateInternal(Object.values(this.getExplorerFileItems()));
    }
    return {};
  }
  async doUpdate(path) {
    if (this.isState("enabled" /* Enabled */)) {
      const item = this.getExplorerFileItems()[path];
      await this.updateInternal(item ? [item] : []);
      return !!item;
    }
    return false;
  }
  getExplorerView() {
    if (this.explorerView === null) {
      const leaves = this.facade.getLeavesOfType("file-explorer" /* FE */);
      if (leaves.length > 1) {
        throw new Error("There are some explorers' leaves");
      }
      const leaf = leaves == null ? void 0 : leaves[0];
      if (leaf === void 0 || leaf === null) {
        throw new ExplorerViewUndefined("Explorer leaf is undefined");
      }
      if (leaf.isVisible()) {
        this.explorerView = leaf.view;
      }
    }
    return this.explorerView;
  }
  getExplorerFileItems() {
    var _a4, _b2;
    return (_b2 = (_a4 = this.getExplorerView()) == null ? void 0 : _a4.fileItems) != null ? _b2 : {};
  }
  async updateInternal(items) {
    for (const i2 of items) {
      if (!(i2.file instanceof import_obsidian3.TFile)) {
        continue;
      }
      if (!this.modified.has(i2)) {
        this.modified.set(i2, this.factory(i2, this.resolver));
      }
      i2.updateTitle();
    }
    return {};
  }
  restoreTitles() {
    Object.values(this.explorerView.fileItems).map(this.restore.bind(this));
  }
  restore(item) {
    if (this.modified.has(item)) {
      this.modified.get(item).destroy();
      item.updateTitle();
      this.modified.delete(item);
      return true;
    }
    return false;
  }
};
ExplorerManager = __decorateClass([
  Kt(),
  __decorateParam(0, Yt(inversify_types_default["facade:obsidian"])),
  __decorateParam(1, Yt(inversify_types_default["feature:explorer:file_mutator:factory"])),
  __decorateParam(2, Yt(inversify_types_default["factory:feature:explorer:sort"])),
  __decorateParam(3, Yt(inversify_types_default["event:dispatcher"])),
  __decorateParam(4, Yt(inversify_types_default["logger"])),
  __decorateParam(4, Ut("explorer" /* Explorer */))
], ExplorerManager);

// src/Feature/FeatureComposer.ts
var import_obsidian4 = require("obsidian");
var FeatureComposer = class {
  constructor(factory, dispatcher, logger) {
    this.factory = factory;
    this.dispatcher = dispatcher;
    this.logger = logger;
    this.features = {};
  }
  get(id2) {
    var _a4;
    return (_a4 = this.features[id2]) != null ? _a4 : null;
  }
  getIds() {
    return Object.keys(this.features);
  }
  toggle(id2, state) {
    this.logger.log(`Feature - ${id2} toggle to ${state}`);
    const feature = this.features[id2];
    if (!state && !feature || state && (feature == null ? void 0 : feature.isEnabled())) {
      return;
    }
    if (!feature) {
      this.features[id2] = this.factory(id2);
      return this.toggle(id2, state);
    }
    try {
      feature[state ? "enable" : "disable"]();
    } catch (e2) {
      this.logger.log(`Feature - ${id2} failed to ${state ? "enable" : "disable"}: ${e2}`);
      console.error(`[front-matter-title] feature "${id2}" failed to ${state ? "enable" : "disable"}`, e2);
      if (state) {
        new import_obsidian4.Notice(
          `[Front Matter Title] Feature "${id2}" was disabled due to an error. See console for details.`
        );
        try {
          feature.disable();
        } catch (_3) {
        }
        delete this.features[id2];
      }
      return;
    }
    this.logger.log(`Feature - ${feature.getId()}. State: ${state}`);
    if (!state) {
      delete this.features[id2];
    } else {
      this.dispatcher.dispatch("feature:enable", new Event({ feature }));
    }
  }
  disableAll() {
    for (const feature of Object.values(this.features)) {
      this.logger.log(`Disable feature ${feature.getId()}`);
      try {
        feature.disable();
      } catch (e2) {
        console.error(`[front-matter-title] feature "${feature.getId()}" failed to disable`, e2);
      }
    }
    this.features = {};
  }
};
FeatureComposer = __decorateClass([
  Kt(),
  __decorateParam(0, Yt(inversify_types_default["factory:feature"])),
  __decorateParam(1, Yt(inversify_types_default["event:dispatcher"])),
  __decorateParam(2, Yt(inversify_types_default.logger)),
  __decorateParam(2, Ut("composer:feature"))
], FeatureComposer);

// src/Feature/Explorer/ExplorerSort.ts
var import_obsidian5 = require("obsidian");
var ExplorerSort = class {
  constructor(logger, facade, dispatcher, service, replacerFactory) {
    this.logger = logger;
    this.facade = facade;
    this.dispatcher = dispatcher;
    this.replacerFactory = replacerFactory;
    this.started = false;
    this.refs = [];
    this.resolver = service.createResolver("explorer" /* Explorer */);
    const trigger = (0, import_obsidian5.debounce)(this.onManagerUpdate.bind(this), 1e3);
    this.cb = (e2) => {
      if (e2.id === "explorer" /* Explorer */ && (e2.result === true || e2.result === void 0)) {
        trigger();
      }
    };
  }
  start() {
    if (this.isStarted()) {
      return;
    }
    if (!this.view && (this.initView(), !this.view)) {
      throw new ExplorerViewUndefined();
    }
    this.refs.push(this.dispatcher.addListener({ name: "manager:refresh", cb: (e2) => this.cb({ id: e2.get().id }) }));
    this.refs.push(
      this.dispatcher.addListener({
        name: "manager:update",
        cb: (e2) => this.cb(e2.get())
      })
    );
    this.started = true;
    this.tryToReplaceOriginalSort();
    this.logger.log("enabled");
  }
  stop() {
    var _a4, _b2;
    this.started = false;
    this.refs.forEach((e2) => this.dispatcher.removeListener(e2));
    this.refs = [];
    (_a4 = this.replacer) == null ? void 0 : _a4.disable();
    (_b2 = this.view) == null ? void 0 : _b2.requestSort();
    this.logger.log("disabled");
  }
  isStarted() {
    return this.started;
  }
  onManagerUpdate() {
    this.logger.log("Try to request sort by event");
    if (!this.isStarted()) {
      this.logger.log("Skipped because feature is not enabled");
      return;
    }
    this.view.requestSort();
  }
  initView() {
    var _a4, _b2;
    this.view = (_b2 = (_a4 = this.facade.getViewsOfType("file-explorer" /* FE */)) == null ? void 0 : _a4[0]) != null ? _b2 : null;
  }
  tryToReplaceOriginalSort() {
    if (!this.isStarted()) {
      return;
    }
    if (this.replacer) {
      this.logger.log(`Replacer is exist already. State is ${this.replacer.isEnabled()}`);
      if (!this.replacer.isEnabled()) {
        this.logger.log("Enable replacer");
        this.replacer.enable();
      }
      return;
    }
    this.logger.log("Init replacer");
    this.replacer = this.replacerFactory(
      Object.getPrototypeOf(this.view),
      "getSortedFolderItems",
      this,
      // Regular function (not an arrow) so `this` is the file-explorer view
      // at call time. For unsupported sort orders (e.g. by created/modified
      // time) the feature returns null and we defer to Obsidian's native
      // sort, forwarding the original args (the folder) AND the view `this`
      // — otherwise native getSortedFolderItems reads this.fileItems on the
      // wrong object and time-sort breaks (#262).
      function(feature, defaultArgs, vanilla) {
        var _a4;
        return (_a4 = feature.getSortedFolderItems(defaultArgs == null ? void 0 : defaultArgs[0])) != null ? _a4 : vanilla.apply(this, defaultArgs);
      }
    );
    if (!this.replacer) {
      this.logger.log("Could not patch getSortedFolderItems (Obsidian API changed). Sort feature degraded.");
      return;
    }
    this.replacer.enable();
  }
  isSortSupported(sort) {
    return ["alphabeticalReverse", "alphabetical"].includes(sort);
  }
  isAsc(order) {
    return order === "alphabetical";
  }
  getSortedFolderItems(folder) {
    var _a4;
    if (!((_a4 = this.view) == null ? void 0 : _a4.fileItems)) {
      this.logger.log("Explorer view has no fileItems (Obsidian API changed). Skipped.");
      return null;
    }
    const sortOrder = this.view.sortOrder;
    if (!this.isSortSupported(sortOrder)) {
      this.logger.log(`Sort is ${sortOrder}. Skipped.`);
      return;
    }
    if (!(folder instanceof import_obsidian5.TFolder)) {
      this.logger.log("File is not TFolder. Why? Skipped.");
      return;
    }
    const children = folder.children.slice();
    const result = [];
    children.sort((a3, b3) => {
      var _a5, _b2;
      const i2 = a3 instanceof import_obsidian5.TFolder;
      const r3 = b3 instanceof import_obsidian5.TFolder;
      let k3;
      if (i2 && !r3) {
        k3 = -1;
      } else if (!i2 && r3) {
        k3 = 1;
      } else {
        const at2 = i2 ? a3.name : (_a5 = this.resolver.resolve(a3.path)) != null ? _a5 : a3.name;
        const bt2 = r3 ? b3.name : (_b2 = this.resolver.resolve(b3.path)) != null ? _b2 : b3.name;
        k3 = at2.localeCompare(bt2);
        k3 = this.isAsc(sortOrder) ? k3 : -k3;
      }
      return k3;
    });
    for (const child of children) {
      const f3 = this.view.fileItems[child.path];
      f3 && result.push(f3);
    }
    return result;
  }
};
ExplorerSort = __decorateClass([
  Kt(),
  __decorateParam(0, Yt(inversify_types_default.logger)),
  __decorateParam(0, Ut("explorer:feature:sort")),
  __decorateParam(1, Yt(inversify_types_default["facade:obsidian"])),
  __decorateParam(2, Yt(inversify_types_default["event:dispatcher"])),
  __decorateParam(3, Yt(inversify_types_default["feature:service"])),
  __decorateParam(4, Yt(inversify_types_default["factory:replacer"]))
], ExplorerSort);

// src/Feature/ManagerComposer.ts
var ManagerComposer = class {
  constructor(features, logger, dispatcher) {
    this.features = features;
    this.logger = logger;
    this.dispatcher = dispatcher;
    this.ids = Managers;
  }
  async update(path, id2 = null) {
    const ids = id2 ? [id2] : this.ids;
    const result = {};
    const promises = [];
    for (const i2 of ids) {
      const manager = this.features.get(i2);
      if (manager) {
        this.logger.log(`Run update for [${path}] on [${manager.getId()}]`);
        promises.push(
          manager.update(path).then((r3) => {
            result[i2] = r3;
            this.dispatcher.dispatch("manager:update", new Event({ id: i2, result: r3, path }));
          })
        );
      }
    }
    await Promise.all(promises);
    return result;
  }
  async refresh(id2 = null) {
    this.logger.log("refresh");
    const ids = id2 ? [id2] : this.ids;
    const promises = [];
    for (const i2 of ids) {
      const manager = this.features.get(i2);
      if (manager) {
        promises.push(
          manager.refresh().then(() => this.dispatcher.dispatch("manager:refresh", new Event({ id: i2 })))
        );
      }
    }
    await Promise.all(promises);
  }
};
ManagerComposer = __decorateClass([
  Kt(),
  __decorateParam(0, Yt(inversify_types_default["feature:composer"])),
  __decorateParam(1, Yt(inversify_types_default.logger)),
  __decorateParam(1, Ut("composer:manager")),
  __decorateParam(2, Yt(inversify_types_default["event:dispatcher"]))
], ManagerComposer);

// src/Feature/Search/SearchFeature.ts
var SearchManager = class extends AbstractFeature {
  constructor(facade, logger, service, featureService, dispatcher) {
    super();
    this.facade = facade;
    this.logger = logger;
    this.service = service;
    this.dispatcher = dispatcher;
    this.state = 1 /* Disabled */;
    this.dom = null;
    this.resolver = featureService.createResolver(this.getId());
  }
  static getId() {
    return "search" /* Search */;
  }
  disable() {
    this.dom = null;
    this.service.destroyByTag(this.getId());
    this.setState(1 /* Disabled */);
  }
  enable() {
    if (!this.facade.isInternalPluginEnabled("global-search" /* Search */)) {
      this.logger.info(`internal plugin ${"global-search" /* Search */} is not enabled`);
      return;
    }
    if (this.isLeafActive()) {
      this.tryEnable();
    } else {
      this.waitForLeafActivation();
    }
  }
  setState(state) {
    this.logger.info(`Change state from ${this.state} to ${state}`);
    this.state = state;
  }
  tryEnable() {
    const dom = this.getSearchDom();
    if (!dom) {
      throw new Error(`View of ${"search" /* S */} not found`);
    }
    this.service.wrapDom(dom, this.resolver, this.getId());
    this.setState(0 /* Enabled */);
  }
  waitForLeafActivation() {
    const ref = this.dispatcher.addListener({
      name: "active:leaf:change",
      cb: () => {
        this.logger.debug("called active:leaf:change callback");
        if (!this.isEnabled()) {
          this.dispatcher.removeListener(ref);
        } else if (this.isLeafActive()) {
          this.dispatcher.removeListener(ref);
          this.tryEnable();
        }
      }
    });
    this.setState(2 /* WaitForActiveLeaf */);
  }
  getId() {
    return SearchManager.getId();
  }
  isEnabled() {
    return this.state !== 1 /* Disabled */;
  }
  isLeafActive() {
    var _a4, _b2;
    return (_b2 = (_a4 = this.facade.getLeavesOfType("search" /* S */)) == null ? void 0 : _a4[0]) == null ? void 0 : _b2.isVisible();
  }
  getView() {
    var _a4, _b2;
    return (_b2 = (_a4 = this.facade.getViewsOfType("search" /* S */)) == null ? void 0 : _a4[0]) != null ? _b2 : null;
  }
  getSearchDom() {
    var _a4, _b2;
    if (!this.dom) {
      this.dom = (_b2 = (_a4 = this.getView()) == null ? void 0 : _a4.dom) != null ? _b2 : null;
    }
    return this.dom;
  }
};
SearchManager = __decorateClass([
  __decorateParam(0, Yt(inversify_types_default["facade:obsidian"])),
  __decorateParam(1, Yt(inversify_types_default.logger)),
  __decorateParam(1, Ut("manager:search")),
  __decorateParam(2, Yt(inversify_types_default["service:search:dom:wrapper"])),
  __decorateParam(3, Yt(inversify_types_default["feature:service"])),
  __decorateParam(4, Yt(inversify_types_default["event:dispatcher"]))
], SearchManager);

// src/Utils/FunctionReplacer.ts
var FunctionReplacer = class _FunctionReplacer {
  constructor(target, method, args, implementation) {
    this.target = target;
    this.method = method;
    this.args = args;
    this.implementation = implementation;
    this.vanilla = null;
    this.valid();
  }
  static create(target, method, args, implementation) {
    return new _FunctionReplacer(target, method, args, implementation);
  }
  /**
   * Fail-soft variant of {@link create}: returns null instead of throwing when
   * the target or method is missing (e.g. an Obsidian internal API changed),
   * so a single drifted API degrades one feature instead of crashing boot.
   */
  static tryCreate(target, method, args, implementation) {
    try {
      return new _FunctionReplacer(target, method, args, implementation);
    } catch (e2) {
      console.error(`[front-matter-title] cannot patch "${String(method)}", feature degraded:`, e2);
      return null;
    }
  }
  getTarget() {
    return this.target;
  }
  enable() {
    if (this.vanilla !== null) {
      return false;
    }
    const self2 = this;
    this.vanilla = this.target[this.method];
    this.target[this.method] = function(...args) {
      return self2.implementation.call(this, self2.args, args, self2.vanilla);
    };
    return true;
  }
  disable() {
    if (this.vanilla !== null) {
      this.target[this.method] = this.vanilla;
      this.vanilla = null;
    }
  }
  isEnabled() {
    return this.vanilla !== null;
  }
  valid() {
    if (typeof this.target[this.method] !== "function") {
      throw new Error(`Method ${this.method} is not a function`);
    }
  }
};

// src/Feature/Tab/TabManager.ts
var TabManager = class extends AbstractManager {
  constructor(facade, dispatcher, factory) {
    super();
    this.facade = facade;
    this.dispatcher = dispatcher;
    this.enabled = false;
    this.callback = null;
    this.replacer = null;
    this.replacerDisplayText = null;
    this.callback = () => {
      const file = factory();
      file && this.update(file.path);
    };
  }
  static getId() {
    return "tab" /* Tab */;
  }
  getId() {
    return TabManager.getId();
  }
  isEnabled() {
    return this.enabled;
  }
  async doDisable() {
    var _a4, _b2;
    this.dispatcher.removeListener(this.ref);
    (_a4 = this.replacer) == null ? void 0 : _a4.disable();
    (_b2 = this.replacerDisplayText) == null ? void 0 : _b2.disable();
    this.ref = null;
    this.reset();
    this.enabled = false;
  }
  async doEnable() {
    this.enabled = true;
    this.initReplacer();
    this.ref = this.dispatcher.addListener({ name: "layout:change", cb: this.callback });
    return;
  }
  async doRefresh() {
    return this.innerUpdate();
  }
  async doUpdate(path) {
    const result = await this.innerUpdate(path);
    return result[path] === true;
  }
  initReplacer() {
    var _a4, _b2;
    const leaf = this.facade.getActiveLeaf();
    if (!leaf) {
      return;
    }
    const proto = Object.getPrototypeOf(leaf);
    this.replacer = FunctionReplacer.tryCreate(proto, "setPinned", this, function(self2, [pinned], vanilla) {
      var _a5, _b3;
      const result = vanilla.call(this, pinned);
      if (((_a5 = this == null ? void 0 : this.view) == null ? void 0 : _a5.getViewType()) === "markdown" /* MD */ && ((_b3 = this.view) == null ? void 0 : _b3.file)) {
        self2.innerUpdate(this.view.file.path);
      }
      return result;
    });
    (_a4 = this.replacer) == null ? void 0 : _a4.enable();
    this.replacerDisplayText = FunctionReplacer.tryCreate(
      proto,
      "getDisplayText",
      this,
      function(self2, _args, vanilla) {
        var _a5, _b3, _c, _d, _e3, _f, _g, _h;
        const filePath = (_g = (_f = (_b3 = (_a5 = this == null ? void 0 : this.view) == null ? void 0 : _a5.file) == null ? void 0 : _b3.path) != null ? _f : ((_e3 = (_d = (_c = this == null ? void 0 : this.view) == null ? void 0 : _c.getState) == null ? void 0 : _d.call(_c)) != null ? _e3 : {}).file) != null ? _g : null;
        return filePath ? (_h = self2.resolver.resolve(filePath)) != null ? _h : vanilla.call(this) : vanilla.call(this);
      }
    );
    (_b2 = this.replacerDisplayText) == null ? void 0 : _b2.enable();
  }
  reset() {
    var _a4;
    const leaves = this.facade.getLeavesOfType("markdown");
    for (const leaf of leaves) {
      const file = (_a4 = leaf.view) == null ? void 0 : _a4.file;
      if (file && leaf.tabHeaderInnerTitleEl) {
        leaf.tabHeaderInnerTitleEl.setText(file.basename);
      }
    }
  }
  async innerUpdate(path = null) {
    var _a4, _b2;
    const leaves = this.facade.getLeavesOfType("markdown");
    const result = {};
    for (const leaf of leaves) {
      const filePath = (_b2 = (_a4 = leaf.view) == null ? void 0 : _a4.getState()) == null ? void 0 : _b2.file;
      if (!filePath || path && path !== filePath) {
        continue;
      }
      result[filePath] = false;
      if (!leaf.tabHeaderInnerTitleEl) {
        continue;
      }
      const title = filePath ? this.resolver.resolve(filePath) : null;
      if (title && title !== leaf.tabHeaderInnerTitleEl.getText()) {
        leaf.tabHeaderInnerTitleEl.setText(title);
        result[filePath] = true;
      }
    }
    return result;
  }
};
TabManager = __decorateClass([
  Kt(),
  __decorateParam(0, Yt(inversify_types_default["facade:obsidian"])),
  __decorateParam(1, Yt(inversify_types_default["event:dispatcher"])),
  __decorateParam(2, Yt(inversify_types_default["factory:obsidian:active:file"]))
], TabManager);

// src/Feature/Alias/Alias.ts
var Alias = class {
  constructor(cache) {
    this.cache = cache;
    this.changed = false;
    this.original = void 0;
  }
  getKey() {
    for (const key of Object.keys(this.cache)) {
      if (this.getPossibleKeys().includes(key)) {
        return key;
      }
    }
    return null;
  }
  getValue() {
    var _a4;
    const val = (_a4 = this.cache[this.getKey()]) != null ? _a4 : null;
    return Array.isArray(val) ? [...val] : val;
  }
  setValue(alias) {
    if (this.original === void 0) {
      const value = this.getValue();
      this.original = {
        value: Array.isArray(value) ? [...value] : value,
        key: this.getKey()
      };
    }
    this.modify(alias);
    this.changed = true;
  }
  isChanged() {
    return this.changed;
  }
  restore() {
    const key = this.getKey();
    if (this.original.key === null && key) {
      delete this.cache[key];
    } else if (this.original.key) {
      this.modify(this.original.value);
    }
    this.changed = false;
  }
  getPossibleKeys() {
    return ["aliases", "alias"];
  }
  modify(alias) {
    var _a4;
    const key = (_a4 = this.getKey()) != null ? _a4 : this.getPossibleKeys()[0];
    this.cache[key] = alias;
  }
};

// src/Feature/Alias/AliasFeature.ts
var AliasFeature = class extends AbstractFeature {
  constructor(strategyFactory, validatorFactory, logger, factory, dispatcher, config) {
    super();
    this.strategyFactory = strategyFactory;
    this.validatorFactory = validatorFactory;
    this.logger = logger;
    this.factory = factory;
    this.dispatcher = dispatcher;
    this.config = config;
    this.enabled = false;
    this.strategy = null;
    this.validator = null;
    this.items = {};
    this.ref = null;
  }
  static getId() {
    return "alias" /* Alias */;
  }
  disable() {
    this.reset();
    this.dispatcher.removeListener(this.ref);
    this.ref = null;
    this.enabled = false;
  }
  enable() {
    this.ref = this.dispatcher.addListener({
      name: "metadata:cache:changed",
      cb: (e2) => this.update(e2.get().path, e2.get().cache)
    });
    this.setValidator(this.config.getValidator());
    this.setStrategy(this.config.getStrategy());
    this.enabled = true;
    this.refresh().catch(console.error);
  }
  getId() {
    return AliasFeature.getId();
  }
  isEnabled() {
    return this.enabled;
  }
  setValidator(type) {
    this.validator = this.validatorFactory(type);
    this.logger.log(`Set validator [${type}]. Status: ${this.validator !== null}`);
  }
  setStrategy(type) {
    this.strategy = this.strategyFactory(type);
    this.logger.log(`Set strategy [${type}]. Status: ${this.strategy !== null}`);
  }
  process(frontmatter, path) {
    const alias = new Alias(frontmatter);
    this.strategy.process(alias, path);
    const result = alias.isChanged();
    if (result) {
      this.items[path] = alias;
    }
    return result;
  }
  reset() {
    for (const alias of Object.values(this.items)) {
      alias.restore();
    }
    this.items = {};
  }
  async update(path, metadata = null) {
    if (this.validator.validate(metadata)) {
      this.process(metadata.frontmatter, path);
    }
  }
  async refresh() {
    const cache = this.factory();
    const promises = [];
    for (const path of cache.getCachedFiles()) {
      promises.push(this.update(path, cache.getCache(path)));
    }
    await Promise.all(promises);
  }
};
AliasFeature = __decorateClass([
  Kt(),
  __decorateParam(0, Yt(inversify_types_default["factory:alias:modifier:strategy"])),
  __decorateParam(1, Yt(inversify_types_default["factory:alias:modifier:validator"])),
  __decorateParam(2, Yt(inversify_types_default.logger)),
  __decorateParam(2, Ut("alias:modifier")),
  __decorateParam(3, Yt(inversify_types_default["factory:metadata:cache"])),
  __decorateParam(4, Yt(inversify_types_default["event:dispatcher"])),
  __decorateParam(5, Yt(inversify_types_default["feature:alias:config"]))
], AliasFeature);

// src/Feature/Alias/Strategy/AbstractStrategy.ts
var AbstractStrategy = class {
  constructor(service) {
    this.service = service;
    this.resolver = service.createResolver("alias" /* Alias */);
  }
  process(alias, path) {
    const title = this.resolver.resolve(path);
    if (title) {
      this.modify(alias, title);
    }
  }
};
AbstractStrategy = __decorateClass([
  Kt(),
  __decorateParam(0, Yt(inversify_types_default["feature:service"]))
], AbstractStrategy);

// src/Feature/Alias/Strategy/ReplaceStrategy.ts
var ReplaceStrategy = class extends AbstractStrategy {
  modify(alias, title) {
    alias.setValue(title);
  }
};

// src/Feature/Alias/Strategy/EnsureStrategy.ts
var EnsureStrategy = class extends ReplaceStrategy {
  process(alias, path) {
    if (alias.getValue() === null) {
      super.process(alias, path);
    }
  }
};

// src/Feature/Alias/Strategy/AdjustStrategy.ts
var AdjustStrategy = class extends AbstractStrategy {
  modify(alias, title) {
    var _a4;
    let value = (_a4 = alias.getValue()) != null ? _a4 : [];
    if (!Array.isArray(value)) {
      value = [value];
    }
    value.push(title);
    alias.setValue(value);
  }
};

// src/Feature/Suggest/SuggestFeature.ts
var SuggestFeature = class extends AbstractFeature {
  constructor(chooser, service) {
    super();
    this.replacer = null;
    this.resolver = service.createResolver(this.getId());
    this.createChooserReplacer(chooser);
  }
  static getId() {
    return "suggest" /* Suggest */;
  }
  isEnabled() {
    return this.state;
  }
  enable() {
    var _a4;
    if (this.isEnabled()) {
      return;
    }
    (_a4 = this.replacer) == null ? void 0 : _a4.enable();
    this.state = true;
  }
  disable() {
    var _a4;
    (_a4 = this.replacer) == null ? void 0 : _a4.disable();
    this.state = false;
  }
  getId() {
    return SuggestFeature.getId();
  }
  createChooserReplacer(chooser) {
    var _a4, _b2;
    if (typeof ((_a4 = chooser == null ? void 0 : chooser.prototype) == null ? void 0 : _a4.setSuggestions) !== "function") {
      console.error("[front-matter-title] Chooser.setSuggestions not found, Suggest feature degraded.");
      return;
    }
    this.replacer = FunctionReplacer.tryCreate(
      chooser.prototype,
      "setSuggestions",
      this,
      function(self2, args, vanilla) {
        if (Array.isArray(args == null ? void 0 : args[0])) {
          args[0] = self2.modifySuggestions(args[0]);
        }
        return vanilla.call(this, ...args);
      }
    );
    (_b2 = this.replacer) == null ? void 0 : _b2.enable();
  }
  modifySuggestions(items) {
    const aliases = /* @__PURE__ */ new Set();
    for (const item of items) {
      if (!(item == null ? void 0 : item.file)) {
        continue;
      }
      aliases.add(item.alias + item.file.path);
      if (!item || !item.type || item.type !== "file") {
        continue;
      }
      const alias = this.resolver.resolve(item.file.path);
      const value = alias + item.file.path;
      if (alias && !aliases.has(value)) {
        item.alias = alias;
        item.type = "alias";
        aliases.add(value);
      }
    }
    return items;
  }
};
SuggestFeature = __decorateClass([
  Kt(),
  __decorateParam(0, Yt(inversify_types_default["newable:obsidian:chooser"])),
  __decorateParam(1, Yt(inversify_types_default["feature:service"]))
], SuggestFeature);

// src/Feature/Bookmarks/BookmarksManager.ts
var BookmarksManager = class extends AbstractFeature {
  constructor(facade, service, dispatcher, logger) {
    super();
    this.facade = facade;
    this.dispatcher = dispatcher;
    this.logger = logger;
    this.state = "disabled" /* Disabled */;
    this.view = null;
    this.ref = null;
    this.metaRef = null;
    this.activeLeafChangeRef = null;
    this.requestUpdate = () => setTimeout(this.onChanged.bind(this), 100);
    this.resolver = service.createResolver(this.getId());
  }
  setState(state) {
    this.logger.log(`State changed from ${this.state} to ${state}`);
    this.state = state;
  }
  static getId() {
    return "bookmarks" /* Bookmarks */;
  }
  enable() {
    this.tryEnable(true);
  }
  disable() {
    this.logger.log("disable");
    if (this.isEnabled()) {
      this.unsubscribe();
      this.view = null;
      this.setState("disabled" /* Disabled */);
    }
  }
  getId() {
    return BookmarksManager.getId();
  }
  isEnabled() {
    return this.state !== "disabled" /* Disabled */;
  }
  tryEnable(subscribeOnActive = false) {
    var _a4;
    const leaf = (_a4 = this.facade.getLeavesOfType("bookmarks" /* Bookmarks */)) == null ? void 0 : _a4[0];
    if (leaf.isVisible()) {
      if (this.state !== "enabled" /* Enabled */ && this.initView() && this.subscribe()) {
        this.setState("enabled" /* Enabled */);
        this.requestUpdate();
      }
      if (this.activeLeafChangeRef) {
        this.dispatcher.removeListener(this.activeLeafChangeRef);
        this.activeLeafChangeRef = null;
      }
    } else if (subscribeOnActive) {
      this.setState("awaiting" /* Awaiting */);
      this.activeLeafChangeRef = this.dispatcher.addListener({
        name: "active:leaf:change",
        cb: () => this.tryEnable()
      });
    }
  }
  initView() {
    var _a4, _b2;
    const view = (_b2 = (_a4 = this.facade.getViewsOfType("bookmarks" /* Bookmarks */)) == null ? void 0 : _a4[0]) != null ? _b2 : null;
    if (view === null) {
      this.logger.log("Could not find a view of bookmarks type");
      return false;
    }
    this.view = view;
    return true;
  }
  subscribe() {
    var _a4;
    const plugin = (_a4 = this.view.plugin) != null ? _a4 : null;
    if (plugin === null) {
      this.logger.log("Leaf does not have a plugin");
      return false;
    }
    this.ref = plugin.on("changed", () => {
      this.logger.log("Triggered by plugin event");
      this.requestUpdate();
    });
    this.metaRef = this.dispatcher.addListener({
      name: "metadata:cache:changed",
      cb: this.requestUpdate.bind(this)
    });
    return true;
  }
  unsubscribe() {
    if (this.metaRef) {
      this.dispatcher.removeListener(this.metaRef);
    }
    if (this.activeLeafChangeRef) {
      this.dispatcher.removeListener(this.activeLeafChangeRef);
    }
    if (this.state === "enabled" /* Enabled */) {
      this.view.plugin.offref(this.ref);
      this.view.plugin.trigger("changed");
    }
  }
  onChanged(path = null) {
    var _a4;
    if (this.state !== "enabled" /* Enabled */) {
      this.logger.log(`Called onChanged when is ${this.state} state`);
      return;
    }
    if (!this.view.plugin) {
      return;
    }
    const items = this.collectFileItems(this.view.plugin.items);
    const itemDoms = this.view.itemDoms;
    const result = {};
    for (const item of items) {
      const titleEl = (_a4 = itemDoms.get(item)) == null ? void 0 : _a4.titleEl;
      result[item.path] = false;
      if (titleEl && item.type === "file" && (item.title === "" || (item == null ? void 0 : item.title) === void 0) && (!path || item.path === path)) {
        this.process(titleEl, item.path).catch(console.error);
        result[item.path] = true;
      }
    }
    return result;
  }
  collectFileItems(items, acc = []) {
    for (const item of items != null ? items : []) {
      if ((item == null ? void 0 : item.type) === "group" && Array.isArray(item.items)) {
        this.collectFileItems(item.items, acc);
      } else if ((item == null ? void 0 : item.type) === "file") {
        acc.push(item);
      }
    }
    return acc;
  }
  async process(div, path) {
    var _a4;
    const title = (_a4 = this.resolver.resolve(path)) != null ? _a4 : div.getText();
    if (div.getText() !== title) {
      div.setText(title);
    }
  }
};
BookmarksManager = __decorateClass([
  Kt(),
  __decorateParam(0, Yt(inversify_types_default["facade:obsidian"])),
  __decorateParam(1, Yt(inversify_types_default["feature:service"])),
  __decorateParam(2, Yt(inversify_types_default["event:dispatcher"])),
  __decorateParam(3, Yt(inversify_types_default.logger)),
  __decorateParam(3, Ut("manager:bookmarks"))
], BookmarksManager);

// src/Feature/Graph/GraphManager.ts
var GraphManager = class extends AbstractManager {
  constructor(dispatcher, facade, factory, logger) {
    super();
    this.dispatcher = dispatcher;
    this.facade = facade;
    this.factory = factory;
    this.logger = logger;
    this.enabled = false;
    this.ref = null;
    this.replacement = null;
  }
  static getId() {
    return "graph" /* Graph */;
  }
  getId() {
    return GraphManager.getId();
  }
  isEnabled() {
    return this.enabled;
  }
  doDisable() {
    var _a4;
    this.unbind();
    (_a4 = this.replacement) == null ? void 0 : _a4.disable();
    this.enabled = false;
  }
  doEnable() {
    if (!this.initReplacement()) {
      this.bind();
    }
    this.enabled = true;
  }
  doRefresh() {
    var _a4;
    for (const view of this.getViews()) {
      (_a4 = view.renderer) == null ? void 0 : _a4.onIframeLoad();
    }
    return Promise.resolve();
  }
  async doUpdate(path) {
    var _a4, _b2;
    let result = false;
    for (const view of this.getViews()) {
      for (const node of (_b2 = (_a4 = view.renderer) == null ? void 0 : _a4.nodes) != null ? _b2 : []) {
        if (node.id === path) {
          result = true;
          view.renderer.onIframeLoad();
          break;
        }
      }
    }
    return result;
  }
  unbind() {
    if (this.ref) {
      this.logger.log("unbind");
      this.dispatcher.removeListener(this.ref);
      this.ref = null;
    }
  }
  bind() {
    this.logger.log("bind");
    this.ref = this.dispatcher.addListener({
      name: "layout:change",
      cb: () => {
        this.initReplacement() && this.unbind();
      }
    });
  }
  initReplacement() {
    const node = this.getFirstNode();
    if (node) {
      this.replacement = this.factory(
        Object.getPrototypeOf(node),
        "getDisplayText",
        this,
        function(self2, defaultArgs, vanilla) {
          const text = self2.resolver.resolve(this.id);
          return text != null ? text : vanilla.call(this, ...defaultArgs);
        }
      );
      if (!this.replacement) {
        return false;
      }
      this.replacement.enable();
      this.refresh().catch(console.error);
      return true;
    } else if (this.getViews().length) {
      this.runBackgroundInit();
      return true;
    }
    return false;
  }
  runBackgroundInit() {
    setTimeout(async () => {
      this.logger.log("init by backgroundInit");
      this.initReplacement();
    }, 200);
  }
  getFirstNode() {
    var _a4, _b2, _c;
    for (const view of this.getViews()) {
      const node = (_c = ((_b2 = (_a4 = view.renderer) == null ? void 0 : _a4.nodes) != null ? _b2 : []).first()) != null ? _c : null;
      if (node) {
        return node;
      }
    }
    return null;
  }
  getViews() {
    return [...this.facade.getViewsOfType("graph" /* G */), ...this.facade.getViewsOfType("localgraph" /* LG */)];
  }
};
GraphManager = __decorateClass([
  Kt(),
  __decorateParam(0, Yt(inversify_types_default["event:dispatcher"])),
  __decorateParam(1, Yt(inversify_types_default["facade:obsidian"])),
  __decorateParam(2, Yt(inversify_types_default["factory:replacer"])),
  __decorateParam(3, Yt(inversify_types_default.logger)),
  __decorateParam(3, Ut("manager:graph"))
], GraphManager);

// src/Feature/MarkdownHeader/MarkdownHeaderManager.ts
var MarkdownHeaderManager = class extends AbstractManager {
  constructor(dispatcher, facade, logger) {
    super();
    this.dispatcher = dispatcher;
    this.facade = facade;
    this.logger = logger;
    this.ref = null;
    this.enabled = false;
  }
  static getId() {
    return "header" /* Header */;
  }
  getId() {
    return MarkdownHeaderManager.getId();
  }
  isEnabled() {
    return this.enabled;
  }
  doDisable() {
    this.dispatcher.removeListener(this.ref);
    this.facade.getViewsOfType("markdown" /* MD */).forEach(this.revert.bind(this));
    this.enabled = false;
  }
  doEnable() {
    this.ref = this.dispatcher.addListener({
      name: "layout:change",
      cb: () => this.refresh().catch(console.error)
    });
    this.enabled = true;
  }
  async doRefresh() {
    await this.innerUpdate();
    return Promise.resolve({});
  }
  async doUpdate(path) {
    return this.innerUpdate(path);
  }
  async innerUpdate(path = null) {
    const views = this.facade.getViewsOfType("markdown" /* MD */);
    let updated = false;
    for (const view of views) {
      if (view.file && (!path || view.file.path === path)) {
        const title = this.resolver.resolve(view.file.path);
        this.setTitle(view, title);
        updated = true;
      }
    }
    return updated;
  }
  setTitle(view, title) {
    this.logger.log(`Set title "${title != null ? title : " "}" for ${view.file.path}`);
    const container = view.titleContainerEl;
    if (!title) {
      return this.revert(view);
    }
    let el = this.findExistingFakeEl(container);
    if (title && el && el.innerText === title && !el.hidden) {
      return this.logger.log(`Set title "${title}" for ${view.file.path} is skipped`);
    }
    el = el != null ? el : this.createFakeEl(title, view);
    el.innerText = title;
    el.hidden = false;
    view.titleEl.hidden = true;
  }
  revert(view) {
    if (!view.file) {
      return;
    }
    const container = view.titleContainerEl;
    const el = this.findExistingFakeEl(container);
    if (el) {
      container.removeChild(el);
    }
    view.titleEl.hidden = false;
    return;
  }
  findExistingFakeEl(container) {
    for (const i2 of Array.from(container.children)) {
      if (i2.hasAttribute("data-ofmt") && i2 instanceof HTMLDivElement) {
        return i2;
      }
    }
    return null;
  }
  createFakeEl(title, view) {
    const el = document.createElement("div");
    el.className = "view-header-title";
    el.dataset["ofmt"] = "true";
    el.innerText = title;
    el.hidden = true;
    el.onclick = () => {
      el.hidden = true;
      view.titleEl.hidden = false;
      view.titleEl.focus();
      view.titleEl.onblur = () => {
        view.titleEl.hidden = true;
        el.hidden = false;
      };
    };
    view.titleContainerEl.appendChild(el);
    return el;
  }
};
MarkdownHeaderManager = __decorateClass([
  Kt(),
  __decorateParam(0, Yt(inversify_types_default["event:dispatcher"])),
  __decorateParam(1, Yt(inversify_types_default["facade:obsidian"])),
  __decorateParam(2, Yt(inversify_types_default.logger)),
  __decorateParam(2, Ut(`manager:${MarkdownHeaderManager.getId()}`))
], MarkdownHeaderManager);

// src/Feature/Alias/Validator.ts
var ValidatorRequired = class {
  validate(metadata) {
    return metadata.frontmatter ? true : false;
  }
};
ValidatorRequired = __decorateClass([
  Kt()
], ValidatorRequired);
var ValidatorAuto = class {
  validate(metadata) {
    if (!metadata.frontmatter) {
      metadata.frontmatter = {
        position: {
          end: {
            col: 0,
            line: 0,
            offset: 0
          },
          start: {
            col: 0,
            line: 0,
            offset: 0
          }
        }
      };
    }
    return true;
  }
};
ValidatorAuto = __decorateClass([
  Kt()
], ValidatorAuto);

// src/Feature/InlineTitle/InlineTitleManager.ts
var InlineTitleManager = class extends AbstractManager {
  constructor(dispatcher, facade, logger, fakeTitleElementService) {
    super();
    this.dispatcher = dispatcher;
    this.facade = facade;
    this.logger = logger;
    this.fakeTitleElementService = fakeTitleElementService;
    this.ref = null;
    this.enabled = false;
  }
  static getId() {
    return "inlineTitle" /* InlineTitle */;
  }
  getId() {
    return InlineTitleManager.getId();
  }
  isEnabled() {
    return this.enabled;
  }
  doDisable() {
    this.dispatcher.removeListener(this.ref);
    this.fakeTitleElementService.removeAll();
    this.enabled = false;
  }
  doEnable() {
    this.ref = this.dispatcher.addListener({
      name: "layout:change",
      cb: () => this.refresh().catch(console.error)
    });
    this.enabled = true;
  }
  async doRefresh() {
    await this.innerUpdate();
    return Promise.resolve({});
  }
  async doUpdate(path) {
    return this.innerUpdate(path);
  }
  async resolve(path) {
    return this.resolver.resolve(path);
  }
  async innerUpdate(path = null) {
    const views = this.facade.getViewsOfType("markdown" /* MD */);
    const promises = [];
    const ids = [];
    for (const view of views) {
      if (view.file && (!path || view.file.path === path)) {
        promises.push(
          this.resolve(view.file.path).then(
            (title) => title ? ids.push(this.setTitle(view, title)) : this.resetTitle(this.getTitleElId(view))
          ).catch(console.error)
        );
      }
    }
    await Promise.all(promises);
    this.fakeTitleElementService.removeExcept(ids);
    return promises.length > 0;
  }
  resetTitle(id2) {
    this.fakeTitleElementService.remove(id2);
  }
  setTitle(view, title) {
    this.logger.log(`Set inline title "${title != null ? title : " "}" for ${view.file.path}`);
    const id2 = this.getTitleElId(view);
    const original = view.inlineTitleEl;
    const { created } = this.fakeTitleElementService.getOrCreate({ original, title, id: id2, events: ["click"] });
    if (created && !original.isActiveElement()) {
      this.fakeTitleElementService.setVisible(id2, true);
    }
    return id2;
  }
  getTitleElId(view) {
    return `${view.getState().mode}-${this.getId()}-${view.leaf.id}`;
  }
};
InlineTitleManager = __decorateClass([
  Kt(),
  __decorateParam(0, Yt(inversify_types_default["event:dispatcher"])),
  __decorateParam(1, Yt(inversify_types_default["facade:obsidian"])),
  __decorateParam(2, Yt(inversify_types_default.logger)),
  __decorateParam(2, Ut(`manager:${InlineTitleManager.getId()}`)),
  __decorateParam(3, Yt(inversify_types_default["service:fake_title_element"]))
], InlineTitleManager);

// src/Feature/Canvas/CanvasManager.ts
var import_obsidian6 = require("obsidian");
var CanvasManager = class extends AbstractManager {
  constructor(dispatcher, facade, logger, fakeTitleElementService) {
    super();
    this.dispatcher = dispatcher;
    this.facade = facade;
    this.logger = logger;
    this.fakeTitleElementService = fakeTitleElementService;
    this.ref = null;
    this.enabled = false;
    this.queue = /* @__PURE__ */ new Set();
    this.updateByRequestFrame = (0, import_obsidian6.debounce)(
      () => {
        this.logger.log(`updateByRequestFrame`, this.queue);
        Array.from(this.queue.values()).forEach((e2) => this.innerUpdate(e2) && this.queue.delete(e2));
      },
      1e3,
      true
    );
  }
  static getId() {
    return "canvas" /* Canvas */;
  }
  getId() {
    return CanvasManager.getId();
  }
  isEnabled() {
    return this.enabled;
  }
  doDisable() {
    this.dispatcher.removeListener(this.ref);
    this.fakeTitleElementService.removeAll();
    this.enabled = false;
  }
  doEnable() {
    this.ref = this.dispatcher.addListener({
      name: "layout:change",
      cb: () => this.refresh().catch(console.error)
    });
    this.enabled = true;
  }
  async doRefresh() {
    await this.innerUpdate();
    return Promise.resolve({});
  }
  async doUpdate(path) {
    return this.innerUpdate(path);
  }
  async resolve(path) {
    return this.resolver.resolve(path);
  }
  async innerUpdate(path = null) {
    const promises = [];
    this.logger.log(`inner update "${path}"`);
    const canvasViews = this.facade.getViewsOfType("canvas" /* CV */);
    for (const view of canvasViews) {
      if (!view.file) {
        continue;
      }
      const currentPath = view.file.path;
      const canvas = view.canvas;
      for (const node of canvas.nodes.values()) {
        if (!node.filePath || path && path !== node.filePath && path !== currentPath) {
          continue;
        }
        promises.push(
          this.resolve(node.filePath).then((title) => {
            if (title) {
              this.setNodeTitle(node, title);
            } else {
              this.restoreNodeTitle(node);
            }
          })
        );
      }
      if (!canvas.requestFrame._originalFunc) {
        const originalFunc = canvas.requestFrame;
        const manager = this;
        canvas.requestFrame = function(...args) {
          canvas.requestFrame._originalFunc.apply(this, args);
          if (manager.enabled) {
            manager.queue.add(currentPath);
            manager.updateByRequestFrame();
          } else {
            canvas.requestFrame = canvas.requestFrame._originalFunc;
          }
        };
        canvas.requestFrame._originalFunc = originalFunc;
      }
    }
    await Promise.all(promises);
    return promises.length > 0;
  }
  restoreNodeTitle(node) {
    const ids = this.makeFakeElementIds(node.canvas.view.file.path, node.filePath);
    this.fakeTitleElementService.remove(ids.label);
    this.fakeTitleElementService.remove(ids.inline);
  }
  async setNodeTitle(node, title) {
    var _a4;
    do {
      await new Promise((resolve) => setTimeout(resolve, 200));
    } while (!node.initialized);
    const view = node.canvas.view;
    this.logger.log(`Set canvas title "${title}" for canvas "${view.file.path}" and node "${node.filePath}"`);
    const ids = this.makeFakeElementIds(view.file.path, node.filePath);
    const label = this.fakeTitleElementService.getOrCreate({
      original: node.labelEl,
      title,
      id: ids.label,
      events: ["hover"]
    });
    if (label == null ? void 0 : label.created) {
      this.fakeTitleElementService.setVisible(ids.label, true);
    }
    const inlineTitleEl = (_a4 = node.contentEl) == null ? void 0 : _a4.querySelector(".inline-title");
    const inline = this.fakeTitleElementService.getOrCreate({
      original: inlineTitleEl,
      title,
      id: ids.inline,
      events: ["click"]
    });
    if (inline == null ? void 0 : inline.created) {
      this.fakeTitleElementService.setVisible(ids.inline, true);
    }
    if (node.placeholderEl.getText() !== title) {
      node.placeholderEl.setText(title);
    }
  }
  makeFakeElementIds(canvasPath, nodePath) {
    const prefix = `${canvasPath}-${nodePath}`;
    return {
      label: `${prefix}-label`,
      inline: `${prefix}-inline`
    };
  }
};
CanvasManager = __decorateClass([
  Kt(),
  __decorateParam(0, Yt(inversify_types_default["event:dispatcher"])),
  __decorateParam(1, Yt(inversify_types_default["facade:obsidian"])),
  __decorateParam(2, Yt(inversify_types_default.logger)),
  __decorateParam(2, Ut(`manager:${CanvasManager.getId()}`)),
  __decorateParam(3, Yt(inversify_types_default["service:fake_title_element"]))
], CanvasManager);

// src/Utils/FeatureHelper.ts
var FeatureHelper = class {
  getOrderedFeatures() {
    return [
      "alias" /* Alias */,
      "explorer" /* Explorer */,
      "graph" /* Graph */,
      "header" /* Header */,
      "bookmarks" /* Bookmarks */,
      "search" /* Search */,
      "suggest" /* Suggest */,
      "tab" /* Tab */,
      "inlineTitle" /* InlineTitle */,
      "canvas" /* Canvas */,
      "backlink" /* Backlink */,
      "noteLink" /* NoteLink */,
      "windowFrame" /* WindowFrame */
    ];
  }
  getName(feature) {
    switch (feature) {
      case "alias" /* Alias */:
        return t4("feature.alias.name");
      case "explorer" /* Explorer */:
        return t4("feature.explorer.name");
      case "graph" /* Graph */:
        return t4("feature.graph.name");
      case "header" /* Header */:
        return t4("feature.header.name");
      case "bookmarks" /* Bookmarks */:
        return t4("feature.bookmarks.name");
      case "search" /* Search */:
        return t4("feature.search.name");
      case "suggest" /* Suggest */:
        return t4("feature.suggest.name");
      case "tab" /* Tab */:
        return t4("feature.tab.name");
      case "inlineTitle" /* InlineTitle */:
        return t4("feature.inlineTitle.name");
      case "canvas" /* Canvas */:
        return t4("feature.canvas.name");
      case "backlink" /* Backlink */:
        return t4("feature.backlink.name");
      case "noteLink" /* NoteLink */:
        return t4("feature.noteLink.name");
      case "windowFrame" /* WindowFrame */:
        return t4("feature.windowFrame.name");
    }
  }
  getDescription(feature) {
    switch (feature) {
      case "alias" /* Alias */:
        return t4("feature.alias.desc");
      case "explorer" /* Explorer */:
        return t4("feature.explorer.desc");
      case "graph" /* Graph */:
        return t4("feature.graph.desc");
      case "header" /* Header */:
        return t4("feature.header.desc");
      case "bookmarks" /* Bookmarks */:
        return t4("feature.bookmarks.desc");
      case "search" /* Search */:
        return t4("feature.search.desc");
      case "suggest" /* Suggest */:
        return t4("feature.suggest.desc");
      case "tab" /* Tab */:
        return t4("feature.tab.desc");
      case "inlineTitle" /* InlineTitle */:
        return t4("feature.inlineTitle.desc");
      case "canvas" /* Canvas */:
        return t4("feature.canvas.desc");
      case "backlink" /* Backlink */:
        return t4("feature.backlink.desc");
      case "noteLink" /* NoteLink */:
        return t4("feature.noteLink.desc");
      case "windowFrame" /* WindowFrame */:
        return t4("feature.windowFrame.desc");
    }
  }
  getDocSection(feature) {
    switch (feature) {
      case "noteLink" /* NoteLink */:
        return "Features/NoteLink.md";
      default:
        return null;
    }
  }
};
FeatureHelper = __decorateClass([
  Kt()
], FeatureHelper);

// src/Feature/FeatureService.ts
var FeatureService = class {
  constructor(service) {
    this.service = service;
  }
  createResolver(name) {
    const main = this.service.createNamed(`${name}:main`);
    const fallback = this.service.createNamed(`${name}:fallback`);
    return {
      resolve(path) {
        var _a4;
        return (_a4 = main.resolve(path)) != null ? _a4 : fallback.resolve(path);
      }
    };
  }
};
FeatureService = __decorateClass([
  Kt(),
  __decorateParam(0, Yt(inversify_types_default["resolver:service"]))
], FeatureService);

// src/Feature/Backlink/BacklinkFeature.ts
var BacklinkFeature = class extends AbstractFeature {
  constructor(facade, logger, dispatcher, service, mutateService) {
    super();
    this.facade = facade;
    this.logger = logger;
    this.dispatcher = dispatcher;
    this.mutateService = mutateService;
    this.enabled = false;
    this.refs = [];
    this.dProcess = null;
    this.timer = null;
    this.isBacklinkWrapped = false;
    this.resolver = service.createResolver(this.getId());
    this.dProcess = () => {
      if (this.timer) {
        clearTimeout(this.timer);
      }
      this.timer = window.setTimeout(this.process.bind(this), 50);
    };
  }
  static getId() {
    return "backlink" /* Backlink */;
  }
  isEnabled() {
    return this.enabled;
  }
  enable() {
    this.enabled = this.facade.isInternalPluginEnabled("backlink" /* Backlink */);
    this.logger.log(`Manager state is ${this.enabled}`);
    if (!this.enabled) {
      return;
    }
    this.refs = [
      this.dispatcher.addListener({
        name: "metadata:cache:changed",
        cb: this.dProcess.bind(this)
      }),
      this.dispatcher.addListener({
        name: "layout:change",
        cb: this.dProcess.bind(this)
      })
    ];
    this.process();
  }
  disable() {
    this.enabled = false;
    if (this.refs.length) {
      this.refs.forEach((e2) => this.dispatcher.removeListener(e2));
      this.refs = [];
      this.mutateService.destroyByTag(this.getId());
    }
  }
  getId() {
    return BacklinkFeature.getId();
  }
  process() {
    var _a4;
    this.logger.log("process");
    this.processBacklinkLayout();
    for (const view of this.facade.getViewsOfType("markdown" /* MD */)) {
      const dom = (_a4 = view == null ? void 0 : view.backlinks) == null ? void 0 : _a4.backlinkDom;
      if (dom) {
        this.mutateService.wrapDom(dom, this.resolver, this.getId());
      }
    }
  }
  processBacklinkLayout() {
    var _a4, _b2, _c;
    if (this.isBacklinkWrapped) {
      return;
    }
    const view = (_a4 = this.facade.getViewsOfType("backlink" /* BL */)[0]) != null ? _a4 : null;
    if ((_b2 = view == null ? void 0 : view.backlink) == null ? void 0 : _b2.backlinkDom) {
      this.mutateService.wrapDom(view.backlink.backlinkDom, this.resolver, this.getId());
    }
    if ((_c = view == null ? void 0 : view.backlink) == null ? void 0 : _c.unlinkedDom) {
      this.mutateService.wrapDom(view.backlink.unlinkedDom, this.resolver, this.getId());
    }
    this.isBacklinkWrapped = true;
  }
};
BacklinkFeature = __decorateClass([
  Kt(),
  __decorateParam(0, Yt(inversify_types_default["facade:obsidian"])),
  __decorateParam(1, Yt(inversify_types_default.logger)),
  __decorateParam(1, Ut("backlink")),
  __decorateParam(2, Yt(inversify_types_default["event:dispatcher"])),
  __decorateParam(3, Yt(inversify_types_default["feature:service"])),
  __decorateParam(4, Yt(inversify_types_default["service:search:dom:wrapper"]))
], BacklinkFeature);

// src/Feature/NoteLink/NoteLinkFeature.ts
var import_obsidian7 = require("obsidian");
var NoteLinkFeature = class extends AbstractFeature {
  constructor(featureService, service, dispatcher, approve, facade, config) {
    super();
    this.service = service;
    this.dispatcher = dispatcher;
    this.approve = approve;
    this.facade = facade;
    this.config = config;
    this.enabled = false;
    this.refs = [];
    this.resolver = featureService.createResolver(this.getId());
  }
  static getId() {
    return "noteLink" /* NoteLink */;
  }
  disable() {
    this.refs.forEach((e2) => this.dispatcher.removeListener(e2));
    this.refs = [];
    this.enabled = false;
  }
  enable() {
    this.refs.push(
      this.dispatcher.addListener({
        name: "metadata:cache:changed",
        cb: (e2) => this.requestApprove(e2.get().path)
      })
    );
    this.enabled = true;
  }
  getId() {
    return NoteLinkFeature.getId();
  }
  isEnabled() {
    return this.enabled;
  }
  shouldChangeLink(link, destTitle) {
    if (!destTitle || destTitle === link.alias) {
      return false;
    }
    const hasAlias = /.+\|.+/.test(link.original);
    return this.config.strategy === "all" /* All */ || !hasAlias;
  }
  async requestApprove(path) {
    const links = this.service.getNoteLinks(path);
    const changes = [];
    for (const link of links) {
      const title = this.resolver.resolve(link.dest);
      if (this.shouldChangeLink(link, title)) {
        changes.push({
          original: link.original,
          replace: `[[${link.link}|${title}]]`
        });
      }
    }
    if (changes.length === 0) {
      return;
    }
    const exec = this.config.approval ? await this.approve.request(path, changes) : true;
    if (exec) {
      await this.executeChanges(path, changes);
    }
  }
  async executeChanges(path, changes) {
    const file = this.facade.getTFile(path);
    let content = file instanceof import_obsidian7.TFile ? await this.facade.getFileContent(file) : null;
    if (!content) {
      return;
    }
    for (const { original, replace } of changes) {
      content = content.replace(original, replace);
    }
    await this.facade.modifyFile(file, content).catch(console.error);
  }
};
NoteLinkFeature = __decorateClass([
  __decorateParam(0, Yt(inversify_types_default["feature:service"])),
  __decorateParam(1, Yt(inversify_types_default["service:note:link"])),
  __decorateParam(2, Yt(inversify_types_default["event:dispatcher"])),
  __decorateParam(3, Yt(inversify_types_default["feature:notelink:approve"])),
  __decorateParam(4, Yt(inversify_types_default["facade:obsidian"])),
  __decorateParam(5, Yt(inversify_types_default["feature:config"])),
  __decorateParam(5, Ut("noteLink" /* NoteLink */))
], NoteLinkFeature);

// src/Feature/NoteLink/NoteLinkApprove.ts
var NoteLinkApprove = class {
  constructor(factory) {
    this.factory = factory;
  }
  async request(path, changes) {
    return new Promise((r3) => {
      const modal = this.factory();
      let approved = false;
      modal.onClose = () => r3(approved);
      const { contentEl } = modal;
      contentEl.setText(`Approve changes for ${path}`);
      contentEl.createEl(
        "ul",
        null,
        (ul) => changes.forEach((e2) => ul.createEl("li", { text: `${e2.original} => ${e2.replace}` }))
      );
      const btnContainer = contentEl.createDiv("modal-button-container");
      btnContainer.createEl("button", { cls: "mod-cta", text: t4("apply") }).addEventListener("click", () => {
        approved = true;
        modal.close();
      });
      btnContainer.createEl("button", { text: t4("cancel") }).addEventListener("click", () => modal.close());
      modal.open();
    });
  }
};
NoteLinkApprove = __decorateClass([
  Kt(),
  __decorateParam(0, Yt(inversify_types_default["factory:obsidian:modal"]))
], NoteLinkApprove);

// src/Feature/Alias/AliasConfig.ts
var AliasConfig = class {
  constructor(storage) {
    this.storage = storage;
  }
  get config() {
    return this.storage.get("features").get("alias" /* Alias */);
  }
  getStrategy() {
    return this.config.get("strategy").value();
  }
  getValidator() {
    return this.config.get("validator").value();
  }
};
AliasConfig = __decorateClass([
  Kt(),
  __decorateParam(0, Yt(inversify_types_default["settings:storage"]))
], AliasConfig);

// src/Feature/Explorer/ExplorerFileItemMutator.ts
var ExplorerFileItemMutator = class {
  constructor(item, resolver) {
    this.item = item;
    this.resolver = resolver;
    item.updateTitle = this.updateTitle.bind(this);
    item.startRename = this.startRename.bind(this);
  }
  destroy() {
    this.item.updateTitle = this.getProto().updateTitle;
    this.item.startRename = this.getProto().startRename;
  }
  getProto() {
    return Object.getPrototypeOf(this.item);
  }
  updateTitle() {
    this.getProto().updateTitle.call(this.item);
    const title = this.resolver.resolve(this.item.file.path);
    (title == null ? void 0 : title.length) > 0 && this.item.innerEl.setText(title);
  }
  startRename() {
    this.item.innerEl.setText(this.item.getTitle());
    return this.getProto().startRename.call(this.item);
  }
};
ExplorerFileItemMutator = __decorateClass([
  Kt()
], ExplorerFileItemMutator);

// src/Feature/WindowFrame/WindowFrameFeature.ts
var import_obsidian8 = require("obsidian");
var WindowFrameFeature = class {
  constructor(facade, featureService, dispatcher) {
    this.facade = facade;
    this.dispatcher = dispatcher;
    this.vanillaUpdateTitle = null;
    this.enabled = false;
    this.ref = null;
    this.resolver = featureService.createResolver(this.getId());
    this.ref = dispatcher.addListener({ name: "metadata:cache:changed", cb: this.onCacheChanged.bind(this) });
  }
  static getId() {
    return "windowFrame" /* WindowFrame */;
  }
  onCacheChanged(e2) {
    var _a4;
    if (((_a4 = this.facade.getActiveFile()) == null ? void 0 : _a4.path) === e2.get().path) {
      this.updateTitle();
    }
  }
  updateTitle() {
    var _a4;
    const e2 = this.facade.getMostRecentLeaf(this.facade.getActiveLeaf().getContainer());
    let text;
    if (e2.view instanceof import_obsidian8.EditableFileView && e2.getViewState().state.file) {
      text = this.resolver.resolve(e2.getViewState().state.file);
    } else {
      text = (_a4 = e2 == null ? void 0 : e2.getDisplayText().trim()) != null ? _a4 : "";
    }
    document.title = this.facade.getAppTitle(text);
  }
  disable() {
    this.dispatcher.removeListener(this.ref);
    this.facade.getWorkspace().updateTitle = this.vanillaUpdateTitle;
    this.facade.getWorkspace().updateTitle();
    this.enabled = false;
  }
  enable() {
    this.vanillaUpdateTitle = this.facade.getWorkspace().updateTitle;
    this.facade.getWorkspace().updateTitle = this.updateTitle.bind(this);
    this.facade.getWorkspace().updateTitle();
    this.enabled = true;
  }
  isEnabled() {
    return this.enabled;
  }
  getId() {
    return "windowFrame" /* WindowFrame */;
  }
};
WindowFrameFeature = __decorateClass([
  Kt(),
  __decorateParam(0, Yt(inversify_types_default["facade:obsidian"])),
  __decorateParam(1, Yt(inversify_types_default["feature:service"])),
  __decorateParam(2, Yt(inversify_types_default["event:dispatcher"]))
], WindowFrameFeature);

// config/services/feature.config.ts
var feature_config_default = (container) => {
  container.bind(inversify_types_default["feature:service"]).to(FeatureService).inSingletonScope();
  container.bind(inversify_types_default["feature:composer"]).to(FeatureComposer).inSingletonScope();
  container.bind(inversify_types_default["manager:composer"]).to(ManagerComposer).inSingletonScope();
  container.bind(inversify_types_default["feature:helper"]).to(FeatureHelper).inSingletonScope();
  container.bind(inversify_types_default["factory:feature"]).toAutoNamedFactory(inversify_types_default.feature).onActivation((c3, i2) => (name) => {
    const feature = i2(name);
    if (feature instanceof AbstractManager) {
      const service = c3.container.get(inversify_types_default["feature:service"]);
      feature.setResolver(service.createResolver(feature.getId()));
    }
    return feature;
  });
  container.bind(inversify_types_default.feature).to(AliasFeature).whenTargetNamed(AliasFeature.getId());
  container.bind(inversify_types_default.feature).to(WindowFrameFeature).whenTargetNamed(WindowFrameFeature.getId());
  container.bind(inversify_types_default.feature).to(ExplorerManager).whenTargetNamed(ExplorerManager.getId());
  container.bind(inversify_types_default.feature).to(SearchManager).whenTargetNamed(SearchManager.getId());
  container.bind(inversify_types_default.feature).to(BookmarksManager).whenTargetNamed(BookmarksManager.getId());
  container.bind(inversify_types_default.feature).to(TabManager).whenTargetNamed(TabManager.getId());
  container.bind(inversify_types_default.feature).to(SuggestFeature).whenTargetNamed(SuggestFeature.getId());
  container.bind(inversify_types_default.feature).to(GraphManager).whenTargetNamed(GraphManager.getId());
  container.bind(inversify_types_default.feature).to(MarkdownHeaderManager).whenTargetNamed(MarkdownHeaderManager.getId());
  container.bind(inversify_types_default.feature).to(BacklinkFeature).whenTargetNamed(BacklinkFeature.getId());
  container.bind(inversify_types_default.feature).to(NoteLinkFeature).whenTargetNamed(NoteLinkFeature.getId());
  container.bind(inversify_types_default.feature).to(InlineTitleManager).whenTargetNamed(InlineTitleManager.getId());
  container.bind(inversify_types_default.feature).to(CanvasManager).whenTargetNamed(CanvasManager.getId());
  container.bind(inversify_types_default["factory:alias:modifier:strategy"]).toAutoNamedFactory(inversify_types_default["alias:modifier:strategy"]);
  container.bind(inversify_types_default["alias:modifier:strategy"]).to(EnsureStrategy).whenTargetNamed("ensure" /* Ensure */);
  container.bind(inversify_types_default["alias:modifier:strategy"]).to(AdjustStrategy).whenTargetNamed("adjust" /* Adjust */);
  container.bind(inversify_types_default["alias:modifier:strategy"]).to(ReplaceStrategy).whenTargetNamed("replace" /* Replace */);
  container.bind(inversify_types_default["feature:alias:config"]).to(AliasConfig);
  container.bind(inversify_types_default["factory:alias:modifier:validator"]).toAutoNamedFactory(inversify_types_default["alias:modifier:validator"]);
  container.bind(inversify_types_default["alias:modifier:validator"]).to(ValidatorAuto).whenTargetNamed("fa" /* FrontmatterAuto */);
  container.bind(inversify_types_default["alias:modifier:validator"]).to(ValidatorRequired).whenTargetNamed("fr" /* FrontmatterRequired */);
  container.bind(inversify_types_default["feature:notelink:approve"]).to(NoteLinkApprove);
  container.bind(inversify_types_default["feature:config"]).toDynamicValue((c3) => {
    const feature = c3.currentRequest.target.getNamedTag().value;
    return c3.container.get(inversify_types_default["settings:storage"]).get("features").get(feature).value();
  }).when(() => true);
  container.bind(inversify_types_default["feature:explorer:file_mutator:factory"]).toFunction(
    (item, resolver) => new ExplorerFileItemMutator(item, resolver)
  );
  container.bind(inversify_types_default["feature:explorer:sort"]).to(ExplorerSort);
  container.bind(inversify_types_default["factory:feature:explorer:sort"]).toFunction(() => {
    const enabled = container.get(inversify_types_default["settings:storage"]).get("features").get("explorer" /* Explorer */).get("sort").value();
    return enabled ? container.get(inversify_types_default["feature:explorer:sort"]) : null;
  });
};

// src/Settings/FeatureBuilder/AbstractBuilder.ts
var import_obsidian9 = require("obsidian");
var AbstractBuilder = class {
  constructor(storage, factory) {
    this.storage = storage;
    this.factory = factory;
  }
  setContext(context) {
    this.context = context;
  }
  build(options) {
    this.options = options;
    this.id = options.id;
    this.config = options.config;
    this.doBuild();
  }
  doBuild() {
  }
  dispatchChanges() {
    this.context.getDispatcher().dispatch("settings:tab:feature:changed", new Event({ id: this.options.id, value: this.config }));
  }
  buildEnable() {
    var _a4;
    const fragment = ((_a4 = this.options.doc) == null ? void 0 : _a4.link) ? createFragment(
      (e2) => e2.createEl("a", {
        text: this.options.name,
        href: this.options.doc.link
      })
    ) : this.options.name;
    return new import_obsidian9.Setting(this.context.getContainer()).setName(fragment).setDesc(this.options.desc).addButton((b3) => {
      this.manageButton = b3.setButtonText(t4("manage")).onClick(() => {
        const modal = this.factory();
        modal.titleEl.setText(this.options.name);
        this.onModalShow(modal);
        modal.open();
      });
      this.toggleManageButtonVisible();
    }).addToggle(
      (e2) => e2.setValue(this.options.config.enabled).onChange((v3) => {
        this.options.config.enabled = v3;
        this.toggleManageButtonVisible();
        this.dispatchChanges();
      })
    );
  }
  buildTemplates(el) {
    el.createEl("h6", { text: "Templates" });
    const templateStorage = this.storage.get("templates");
    for (const type of ["main", "fallback"]) {
      const tStorage = this.storage.get("features").get(this.id).get("templates").get(type);
      const setting = new import_obsidian9.Setting(el).setName(t4(`template.${type}`)).setDesc(this.getDesc(type, tStorage.value())).addText((e2) => {
        var _a4;
        e2.setValue((_a4 = tStorage.value()) != null ? _a4 : "").setPlaceholder(templateStorage.get("common").get(type).value()).onChange((v3) => {
          setting.setDesc(this.getDesc(type, v3));
          this.config.templates[type] = v3;
          this.dispatchChanges();
        });
      });
    }
  }
  toggleManageButtonVisible() {
    this.manageButton.buttonEl.toggleVisibility(this.config.enabled);
  }
  getDesc(type, value) {
    if (value) {
      return t4("template.specific");
    } else {
      return t4("template.used", { value: this.storage.get("templates").get("common").get(type).value() });
    }
  }
};
AbstractBuilder = __decorateClass([
  Kt(),
  __decorateParam(0, Yt(inversify_types_default["settings:storage"])),
  __decorateParam(1, Yt(inversify_types_default["factory:obsidian:modal"]))
], AbstractBuilder);

// src/Settings/FeatureBuilder/DefaultBuilder.ts
var DefaultBuilder = class extends AbstractBuilder {
  doBuild() {
    this.buildEnable();
  }
  onModalShow(modal) {
    this.buildTemplates(modal.contentEl);
  }
};
DefaultBuilder = __decorateClass([
  Kt()
], DefaultBuilder);

// src/Settings/FeatureBuilder/AliasBuilder.ts
var import_obsidian10 = require("obsidian");
var AliasBuilder = class extends AbstractBuilder {
  doBuild() {
    this.buildEnable();
    this.extraSettingContainerEl = this.context.getContainer().createDiv();
  }
  onModalShow(modal) {
    this.buildValidatorDropdown(modal.contentEl);
    this.buildStrategyDropdown(modal.contentEl);
    this.buildTemplates(modal.contentEl);
  }
  buildValidatorDropdown(el) {
    const value = this.config.validator;
    const s3 = new import_obsidian10.Setting(el).setName(t4("strategy"));
    this.validatorDropdown = new import_obsidian10.DropdownComponent(s3.controlEl).addOptions({
      ["fa" /* FrontmatterAuto */]: t4("feature.alias.validator.auto.name"),
      ["fr" /* FrontmatterRequired */]: t4("feature.alias.validator.required.name")
    }).setValue(value ? value : "fr" /* FrontmatterRequired */).onChange((v3) => {
      this.actualizeValidatorDesc(s3, v3);
      this.config.validator = v3;
      this.dispatchChanges();
    });
    this.actualizeValidatorDesc(s3, this.validatorDropdown.getValue());
  }
  actualizeValidatorDesc(setting, value) {
    let text = "";
    switch (value) {
      case "fa" /* FrontmatterAuto */: {
        text = t4("feature.alias.validator.auto.desc");
        break;
      }
      case "fr" /* FrontmatterRequired */: {
        text = t4("feature.alias.validator.required.desc");
        break;
      }
    }
    setting.setDesc(text);
  }
  buildStrategyDropdown(el) {
    const value = this.config.strategy;
    const s3 = new import_obsidian10.Setting(el).setName(t4("strategy"));
    this.strategyDropdown = new import_obsidian10.DropdownComponent(s3.controlEl).addOptions({
      ["ensure" /* Ensure */]: t4("feature.alias.strategy.ensure.name"),
      ["adjust" /* Adjust */]: t4("feature.alias.strategy.adjust.name"),
      ["replace" /* Replace */]: t4("feature.alias.strategy.replace.name")
    }).setValue(value ? value : "ensure" /* Ensure */).onChange((v3) => {
      this.actualizeStrategyDesc(s3, v3);
      this.options.config.strategy = v3;
      this.dispatchChanges();
    });
    this.actualizeStrategyDesc(s3, this.strategyDropdown.getValue());
  }
  actualizeStrategyDesc(setting, value) {
    let desc = "";
    switch (value) {
      case "ensure" /* Ensure */:
        desc = t4("feature.alias.strategy.ensure.desc");
        break;
      case "replace" /* Replace */:
        desc = t4("feature.alias.strategy.replace.desc");
        break;
      case "adjust" /* Adjust */:
        desc = t4("feature.alias.strategy.adjust.desc");
        break;
    }
    setting.setDesc(desc);
  }
};

// src/Settings/SettingBuilders/AbstractBuilder.ts
var AbstractBuilder2 = class {
  constructor() {
    this.item = null;
    this.container = null;
    this.name = null;
  }
  build({ name, container, item }) {
    this.name = name;
    this.item = item;
    this.container = container;
    this.doBuild();
  }
};
AbstractBuilder2 = __decorateClass([
  Kt()
], AbstractBuilder2);

// src/Settings/SettingBuilders/Templates/TemplatesBuilder.ts
var import_obsidian11 = require("obsidian");
var TemplatesBuilder = class extends AbstractBuilder2 {
  constructor(factory, helper) {
    super();
    this.factory = factory;
    this.helper = helper;
  }
  support(k3) {
    return k3 === "templates";
  }
  doBuild() {
    this.buildTemplate();
    this.buildFallbackTemplate();
  }
  createDocFragment(text, section = null) {
    return createFragment(
      (e2) => e2.createEl("a", {
        href: GITHUB_DOCS + "Templates.md" + (section ? `#${section}` : ""),
        text
      })
    );
  }
  buildTemplate() {
    new import_obsidian11.Setting(this.container).setName(this.createDocFragment(t4("template.common.main.name"))).setDesc(t4("template.common.main.desc")).addText(
      (text) => {
        var _a4;
        return text.setPlaceholder(t4("template.placeholder")).setValue((_a4 = this.item.get("common").get("main").value()) != null ? _a4 : "").onChange(async (value) => this.item.get("common").get("main").set(value));
      }
    );
  }
  buildFallbackTemplate() {
    new import_obsidian11.Setting(this.container).setName(this.createDocFragment(t4("template.common.fallback.name"))).setDesc(t4("template.common.fallback.desc")).addText(
      (text) => {
        var _a4;
        return text.setPlaceholder(t4("template.placeholder")).setValue((_a4 = this.item.get("common").get("fallback").value()) != null ? _a4 : "").onChange(async (value) => this.item.get("common").get("fallback").set(value));
      }
    );
  }
};
TemplatesBuilder = __decorateClass([
  Kt(),
  __decorateParam(0, Yt(inversify_types_default["factory:obsidian:modal"])),
  __decorateParam(1, Yt(inversify_types_default["feature:helper"]))
], TemplatesBuilder);

// src/Settings/SettingBuilders/Rules/RulesBuilder.ts
var RulesBuiler = class extends AbstractBuilder2 {
  constructor(factory) {
    super();
    this.factory = factory;
  }
  get orderedKeys() {
    return ["paths", "delimiter"];
  }
  doBuild() {
    this.container.createEl("h4", { text: t4("rule.name") });
    const builders = this.factory("rules");
    for (const key of this.orderedKeys) {
      for (const builder of builders) {
        builder.support(key) && builder.build({
          name: key,
          container: this.container,
          item: this.item.get(key)
        });
      }
    }
  }
  support(k3) {
    return k3 === "rules";
  }
};
RulesBuiler = __decorateClass([
  Kt(),
  __decorateParam(0, Yt(inversify_types_default["factory:settings:builder"]))
], RulesBuiler);

// src/Settings/SettingBuilders/Features/FeaturesBuilder.ts
var FeaturesBuilder = class extends AbstractBuilder2 {
  constructor(builderFactory, dispatcher, helper) {
    super();
    this.builderFactory = builderFactory;
    this.dispatcher = dispatcher;
    this.helper = helper;
    this.ref = null;
  }
  support(k3) {
    return k3 === "features";
  }
  doBuild() {
    var _a4;
    this.bind();
    this.container.createEl("h4", { text: t4("features") });
    const data = this.helper.getOrderedFeatures().map((feature) => ({
      desc: this.helper.getDescription(feature),
      feature,
      name: this.helper.getName(feature),
      doc: {
        link: this.helper.getDocSection(feature) ? `${GITHUB_DOCS}/${this.helper.getDocSection(feature)}` : null
      }
    }));
    for (const item of data) {
      const builder = (_a4 = this.builderFactory(item.feature)) != null ? _a4 : this.builderFactory("default");
      const settings = this.item.get(item.feature).value();
      builder.setContext({
        getContainer: () => this.container,
        getSettings: () => this.item.value(),
        getDispatcher: () => this.dispatcher
      });
      builder.build({ id: item.feature, desc: item.desc, name: item.name, config: settings, doc: item.doc });
    }
  }
  bind() {
    this.ref = this.dispatcher.addListener({
      name: "settings:tab:feature:changed",
      cb: (e2) => this.item.get(e2.get().id).set(e2.get().value)
    });
    this.dispatcher.addListener({
      name: "settings:tab:close",
      cb: () => this.dispatcher.removeListener(this.ref),
      once: true
    });
  }
};
FeaturesBuilder = __decorateClass([
  Kt(),
  __decorateParam(0, Yt(inversify_types_default["factory:settings:feature:builder"])),
  __decorateParam(1, Yt(inversify_types_default["event:dispatcher"])),
  __decorateParam(2, Yt(inversify_types_default["feature:helper"]))
], FeaturesBuilder);

// src/Settings/SettingBuilders/Util/UtilBuilder.ts
var import_obsidian12 = require("obsidian");
var UtilBuilder = class {
  constructor() {
    this.isTitleBuild = false;
  }
  build({ name, item, container }) {
    this.buildTitle(container);
    switch (name) {
      case "boot": {
        this.buildBoot(item, container);
        break;
      }
      case "debug": {
        this.buildDebug(item, container);
        break;
      }
    }
  }
  support(k3) {
    return ["boot", "debug"].includes(k3);
  }
  buildTitle(container) {
    if (!this.isTitleBuild) {
      container.createEl("h4", { text: t4("util") });
      this.isTitleBuild = true;
    }
  }
  buildDebug(item, container) {
    new import_obsidian12.Setting(container).setName(t4("debug_info.title")).setDesc(t4("debug_info.desc")).addToggle((e2) => e2.setValue(item.value()).onChange((e3) => item.set(e3)));
  }
  buildBoot(item, container) {
    new import_obsidian12.Setting(container).setName(t4("boot_delay.title")).setDesc(t4("boot_delay.desc")).addText(
      (e2) => e2.setValue(item.get("delay").value().toString()).onChange((s3) => {
        const v3 = !isNaN(parseInt(s3)) ? parseInt(s3) : 0;
        e2.setValue(v3.toString());
        item.get("delay").set(v3);
      })
    );
    new import_obsidian12.Setting(container).setName(t4("boot_background.title")).setDesc(t4("boot_background.desc")).addToggle(
      (e2) => e2.setValue(item.get("background").value()).onChange((e3) => {
        item.get("background").set(e3);
      })
    );
  }
};
UtilBuilder = __decorateClass([
  Kt()
], UtilBuilder);

// src/Settings/SettingBuilders/Rules/RulesDelimiterBuilder.ts
var import_obsidian13 = require("obsidian");
var RulesDelimiterBuilder = class extends AbstractBuilder2 {
  constructor() {
    super(...arguments);
    this.setting = null;
    this.text = null;
  }
  support(k3) {
    return k3 === "delimiter";
  }
  doBuild() {
    this.setting = new import_obsidian13.Setting(this.container).setName(t4("rule.delimiter.name")).setDesc(t4("rule.delimiter.desc"));
    this.buildDropdown();
    this.buildText();
  }
  buildDropdown() {
    const enabled = this.item.get("enabled");
    new import_obsidian13.DropdownComponent(this.setting.controlEl).addOptions({ N: t4("rule.delimiter.first"), Y: t4("rule.delimiter.join") }).setValue(enabled.value() ? "Y" : "N").onChange((e2) => {
      enabled.set(e2 === "Y");
      this.text.setValue("").setPlaceholder(this.getPlaceholder()).setDisabled(e2 === "N").onChanged();
    });
  }
  buildText() {
    const v3 = this.item.get("value");
    this.text = new import_obsidian13.TextComponent(this.setting.controlEl).setValue(v3.value()).setDisabled(!this.isEnabled()).setPlaceholder(this.getPlaceholder()).onChange((e2) => v3.set(e2));
  }
  getPlaceholder() {
    return this.isEnabled() ? t4("rule.delimiter.placeholder.join") : t4("rule.delimiter.placeholder.first");
  }
  isEnabled() {
    return this.item.get("enabled").value();
  }
};
RulesDelimiterBuilder = __decorateClass([
  Kt()
], RulesDelimiterBuilder);

// src/Settings/SettingBuilders/Rules/RulesPathsBuilder.ts
var import_obsidian14 = require("obsidian");
var RulesPathsBuilder = class extends AbstractBuilder2 {
  constructor() {
    super(...arguments);
    this.settings = null;
  }
  doBuild() {
    this.settings = new import_obsidian14.Setting(this.container).setName(t4("rule.path.name")).addDropdown(
      (e2) => e2.addOptions({ white: t4("rule.path.white.name"), black: t4("rule.path.black.name") }).setValue(this.item.get("mode").value()).onChange((e3) => {
        this.item.get("mode").set(e3);
        this.updateDesc();
      })
    ).addTextArea(
      (e2) => e2.setValue(this.item.get("values").value().join("\n")).onChange((e3) => this.item.get("values").set(e3.split("\n").filter((e4) => e4)))
    );
    this.updateDesc();
  }
  support(k3) {
    return k3 === "paths";
  }
  updateDesc() {
    const descriptions = {
      white: t4("rule.path.white.desc"),
      black: t4("rule.path.black.desc")
    };
    this.settings.setDesc(descriptions[this.item.get("mode").value()]);
  }
};
RulesPathsBuilder = __decorateClass([
  Kt()
], RulesPathsBuilder);

// src/Settings/SettingBuilders/Processor/ProcessorBuilder.ts
var import_obsidian15 = require("obsidian");

// src/Components/Processor/ProcessorUtils.ts
var ProcessorTypes = /* @__PURE__ */ ((ProcessorTypes2) => {
  ProcessorTypes2["Function"] = "function";
  ProcessorTypes2["FunctionV2"] = "functionV2";
  ProcessorTypes2["Replace"] = "replace";
  return ProcessorTypes2;
})(ProcessorTypes || {});

// src/Settings/SettingBuilders/Processor/ProcessorBuilder.ts
var ProcessorBuilder = class extends AbstractBuilder2 {
  constructor() {
    super(...arguments);
    this.lastArgs = {};
  }
  support(k3) {
    return k3 === "processor";
  }
  doBuild() {
    const fragment = createFragment(
      (e2) => e2.createEl("a", {
        text: t4("processor.name"),
        href: GITHUB_DOCS + "Processor.md"
      })
    );
    this.setting = new import_obsidian15.Setting(this.container).setName(fragment);
    this.buildDynamic();
  }
  updateDesc() {
    const fragment = createFragment();
    fragment.appendText(t4("processor.desc"));
    let additional = [];
    switch (this.item.get("type").value()) {
      case "replace" /* Replace */:
        additional = [
          "",
          t4("processor.replace.desc"),
          "title.replace(",
          createSpan(
            "",
            (e2) => e2.innerHTML = "&emsp;new RegExp(<br>&emsp;&emsp;#pattern#, <br>&emsp;&emsp;#flags#<br>&emsp;),"
          ),
          createSpan("", (e2) => e2.innerHTML = "&emsp;#replacement#"),
          ")"
        ];
        break;
      case "function" /* Function */:
        additional = [
          "",
          t4("processor.function.desc"),
          `const value = new Function('title', #${t4("processor.function.valueDesc")}#)`
        ];
        break;
      case "functionV2" /* FunctionV2 */:
        additional = [
          "",
          t4("processor.functionV2.desc"),
          `const value = new Function('obj', #${t4("processor.functionV2.valueDesc")}#)`
        ];
        break;
    }
    additional.forEach((e2) => {
      fragment.appendChild(createEl("br"));
      typeof e2 === "string" ? fragment.appendText(e2) : fragment.appendChild(e2);
    });
    this.setting.setDesc(fragment);
  }
  buildDynamic() {
    this.updateDesc();
    this.setting.addDropdown(
      (c3) => {
        var _a4;
        return c3.addOptions({
          "": t4("disabled"),
          ["replace" /* Replace */]: t4("processor.replace.name"),
          ["function" /* Function */]: t4("processor.function.name"),
          ["functionV2" /* FunctionV2 */]: t4("processor.functionV2.name")
        }).setValue((_a4 = this.item.get("type").value()) != null ? _a4 : "").onChange((v3) => {
          var _a5, _b2;
          const value = v3 === "" ? null : v3;
          const type = this.item.get("type").value();
          this.lastArgs[type] = [...this.item.get("args").value()];
          this.item.get("type").set(value);
          this.item.get("args").set([...(_b2 = (_a5 = this.lastArgs) == null ? void 0 : _a5[value]) != null ? _b2 : []]);
          this.setting.controlEl.innerHTML = "";
          this.buildDynamic();
        });
      }
    );
    this.setAlignItemsMode("center");
    switch (this.item.get("type").value()) {
      case "function" /* Function */:
        this.setAlignItemsMode("start");
        return this.buildFunction();
      case "functionV2" /* FunctionV2 */:
        this.setAlignItemsMode("start");
        return this.buildFunction();
      case "replace" /* Replace */:
        this.setAlignItemsMode("start");
        return this.buildReplace();
    }
  }
  setAlignItemsMode(mode) {
    this.setting.settingEl.style.alignItems = mode;
    this.setting.controlEl.style.alignItems = mode;
  }
  buildReplace() {
    const items = [
      {
        name: t4("processor.replace.pattern.name"),
        desc: t4("processor.replace.pattern.desc")
      },
      { name: t4("processor.replace.flags.name"), desc: t4("processor.replace.flags.desc") },
      { name: t4("processor.replace.replacement.name"), desc: t4("processor.replace.replacement.desc") }
    ];
    const container = createDiv({ attr: { style: "margin-left: 20px" } });
    let margin = "10px";
    const value = this.item.get("args").value();
    for (const [i2, item] of items.entries()) {
      const c3 = createDiv({
        attr: { style: `margin-bottom: ${margin}` }
      });
      margin = "0px";
      c3.appendChild(
        createDiv(
          {
            attr: { style: "display: flex; align-items: center; justify-content: space-between;" }
          },
          (e2) => {
            var _a4;
            e2.appendChild(
              createDiv({ attr: { style: "margin-right: 10px" } }, (e3) => e3.appendText(`${item.name}:`))
            );
            new import_obsidian15.TextComponent(e2).setValue((_a4 = value == null ? void 0 : value[i2]) != null ? _a4 : "").onChange((e3) => value.splice(i2, 1, e3)).onChanged();
          }
        )
      );
      c3.appendChild(
        createDiv(
          { cls: "setting-item-description", attr: { style: "text-align: left" } },
          (e2) => e2.appendText(item.desc)
        )
      );
      container.appendChild(c3);
    }
    this.setting.controlEl.prepend(container);
  }
  buildFunction() {
    this.setting.addTextArea(
      (e2) => e2.setValue(this.item.get("args").value().join()).onChange((e3) => this.item.get("args").set([e3]))
    );
  }
};

// src/Settings/FeatureBuilder/NoteLinkBuilder.ts
var import_obsidian16 = require("obsidian");
var NoteLinkBuilder = class extends AbstractBuilder {
  doBuild() {
    this.buildEnable();
  }
  onModalShow(modal) {
    this.buildStrategy(modal.contentEl);
    this.buildApproval(modal.contentEl);
    this.buildTemplates(modal.contentEl);
  }
  buildStrategy(el) {
    new import_obsidian16.Setting(el).setName(t4("feature.noteLink.strategy.name")).setDesc(t4("feature.noteLink.strategy.desc")).addDropdown((e2) => {
      var _a4;
      e2.addOptions({
        ["all" /* All */]: t4("feature.noteLink.strategy.options.all"),
        ["onlyEmpty" /* OnlyEmpty */]: t4("feature.noteLink.strategy.options.onlyEmpty")
      }).setValue((_a4 = this.config.strategy) != null ? _a4 : "onlyEmpty" /* OnlyEmpty */).onChange((v3) => {
        this.config.strategy = v3;
        this.dispatchChanges();
      });
    });
  }
  buildApproval(el) {
    new import_obsidian16.Setting(el).setName(t4("feature.noteLink.approval.name")).setDesc(t4("feature.noteLink.approval.desc")).addDropdown((e2) => {
      e2.addOptions({
        Y: t4("feature.noteLink.approval.options.showModal"),
        N: t4("feature.noteLink.approval.options.auto")
      }).setValue(this.config.approval ? "Y" : "N").onChange((v3) => {
        this.config.approval = v3 === "Y";
        this.dispatchChanges();
      });
    });
  }
};

// src/Settings/FeatureBuilder/ExplorerBuilder.ts
var import_obsidian17 = require("obsidian");
var ExplorerBuilder = class extends AbstractBuilder {
  doBuild() {
    this.buildEnable();
  }
  onModalShow(modal) {
    const el = modal.contentEl;
    this.buildSort(el);
    this.buildTemplates(el);
  }
  buildSort(el) {
    new import_obsidian17.Setting(el).setName(t4("feature.explorer.sort.name")).setDesc(t4("feature.explorer.sort.desc")).addToggle((e2) => {
      e2.setValue(this.config.sort).onChange((v3) => {
        this.config.sort = v3;
        this.dispatchChanges();
      });
    });
  }
};

// config/services/settings.config.ts
var settings_config_default = (c3) => {
  c3.bind(inversify_types_default["settings:feature:builder"]).to(DefaultBuilder).whenTargetNamed("default");
  c3.bind(inversify_types_default["settings:feature:builder"]).to(AliasBuilder).whenTargetNamed("alias" /* Alias */);
  c3.bind(inversify_types_default["settings:feature:builder"]).to(ExplorerBuilder).whenTargetNamed("explorer" /* Explorer */);
  c3.bind(inversify_types_default["settings:feature:builder"]).to(NoteLinkBuilder).whenTargetNamed("noteLink" /* NoteLink */);
  c3.bind(inversify_types_default["factory:settings:feature:builder"]).toFunction(
    (name) => c3.isBoundNamed(inversify_types_default["settings:feature:builder"], name) ? c3.getNamed(inversify_types_default["settings:feature:builder"], name) : null
  );
  c3.bind(inversify_types_default["settings:builder"]).to(TemplatesBuilder).whenTargetNamed("main");
  c3.bind(inversify_types_default["settings:builder"]).to(FeaturesBuilder).whenTargetNamed("main");
  c3.bind(inversify_types_default["settings:builder"]).to(UtilBuilder).whenTargetNamed("main");
  c3.bind(inversify_types_default["settings:builder"]).to(RulesBuiler).whenTargetNamed("main");
  c3.bind(inversify_types_default["settings:builder"]).to(ProcessorBuilder).whenTargetNamed("main");
  c3.bind(inversify_types_default["settings:builder"]).to(RulesDelimiterBuilder).whenTargetNamed("rules");
  c3.bind(inversify_types_default["settings:builder"]).to(RulesPathsBuilder).whenTargetNamed("rules");
  c3.bind(inversify_types_default["factory:settings:builder"]).toFunction(
    (name) => c3.getAllNamed(inversify_types_default["settings:builder"], name)
  );
};

// src/Components/Processor/ReplaceProcessor.ts
var ReplaceProcessor = class {
  constructor(args) {
    var _a4, _b2;
    try {
      this.pattern = new RegExp((_a4 = args == null ? void 0 : args[0]) != null ? _a4 : null, args == null ? void 0 : args[1]);
      this.replacement = (_b2 = args == null ? void 0 : args[2]) != null ? _b2 : null;
    } catch (e2) {
      console.error(e2);
    }
  }
  process(value) {
    return value.replace(this.pattern, this.replacement);
  }
  getType() {
    return "replace" /* Replace */;
  }
};
ReplaceProcessor = __decorateClass([
  Kt(),
  __decorateParam(0, Yt(inversify_types_default["processor:args"]))
], ReplaceProcessor);

// src/Components/Processor/FunctionProcessor.ts
var FunctionProcessor = class {
  constructor(args) {
    this.func = (args == null ? void 0 : args[0]) ? new Function("title", args[0]) : null;
  }
  process(value) {
    var _a4, _b2;
    return (_b2 = (_a4 = this.func) == null ? void 0 : _a4.call(this, value)) != null ? _b2 : value;
  }
  getType() {
    return "function" /* Function */;
  }
};
FunctionProcessor = __decorateClass([
  Kt(),
  __decorateParam(0, Yt(inversify_types_default["processor:args"]))
], FunctionProcessor);

// src/Components/Processor/FunctionV2Processor.ts
var FunctionV2Processor = class {
  constructor(args, facade) {
    this.facade = facade;
    this.func = (args == null ? void 0 : args[0]) ? new Function("obj", args[0]) : null;
  }
  process(value) {
    var _a4, _b2;
    const obj = JSON.parse(value);
    obj.file = this.facade.getTFile(obj.path);
    return (_b2 = (_a4 = this.func) == null ? void 0 : _a4.call(this, obj)) != null ? _b2 : obj.title;
  }
  getType() {
    return "functionV2" /* FunctionV2 */;
  }
};
FunctionV2Processor = __decorateClass([
  Kt(),
  __decorateParam(0, Yt(inversify_types_default["processor:args"])),
  __decorateParam(1, Yt(inversify_types_default["facade:obsidian"]))
], FunctionV2Processor);

// src/Components/Processor/ProcessorArgumentTransformer.ts
var ProcessorArgumentTransformer = class {
  transform(event, type) {
    switch (type) {
      case "function" /* Function */:
      case "replace" /* Replace */:
        return event.get().value;
      case "functionV2" /* FunctionV2 */:
        return JSON.stringify({
          path: event.get().path,
          title: event.get().value,
          file: null
        });
    }
  }
};
ProcessorArgumentTransformer = __decorateClass([
  Kt()
], ProcessorArgumentTransformer);

// config/services/processors.config.ts
var processors_config_default = new kt((bind) => {
  bind(inversify_types_default.processor).to(ReplaceProcessor).whenTargetNamed("replace" /* Replace */);
  bind(inversify_types_default.processor).to(FunctionProcessor).whenTargetNamed("function" /* Function */);
  bind(inversify_types_default.processor).to(FunctionV2Processor).whenTargetNamed("functionV2" /* FunctionV2 */);
  bind(inversify_types_default["processor:args"]).toDynamicValue(
    (c3) => c3.container.get(inversify_types_default["settings:storage"]).get("processor").get("args").value()
  );
  bind(inversify_types_default["processor:factory"]).toAutoNamedFactory(inversify_types_default.processor);
  bind(inversify_types_default["processor:argument.transformer"]).to(ProcessorArgumentTransformer);
});

// src/Resolver/ResolverService.ts
var ResolverService = class {
  constructor(factory, templateFactory, logger) {
    this.factory = factory;
    this.templateFactory = templateFactory;
    this.logger = logger;
    this.resolvers = /* @__PURE__ */ new Map();
  }
  create(template) {
    this.logger.log(`Create "${template}" template`);
    return this.getOrCreate(template);
  }
  createNamed(name) {
    this.logger.log(`Create named "${name}" template`);
    return this.create(this.templateFactory(name));
  }
  getOrCreate(template) {
    if (!this.resolvers.has(template)) {
      this.logger.log(`Create resolver for "${template}" template`);
      const resolver = this.factory();
      resolver.setTemplate(template);
      this.resolvers.set(template, resolver);
    }
    return this.resolvers.get(template);
  }
};
ResolverService = __decorateClass([
  Kt(),
  __decorateParam(0, Yt(inversify_types_default["factory:resolver:resolver"])),
  __decorateParam(1, Yt(inversify_types_default["factory:resolver:template"])),
  __decorateParam(2, Yt(inversify_types_default.logger)),
  __decorateParam(2, Ut("resolver:service"))
], ResolverService);

// src/Resolver/ResolverCachedProxy.ts
var ResolverCachedProxy = class {
  constructor(resolver, cache, dispatcher) {
    this.resolver = resolver;
    this.cache = cache;
    this.dispatcher = dispatcher;
    dispatcher.addListener({ name: "resolver:clear", cb: () => this.cache.clear(), sort: 0 });
    dispatcher.addListener({ name: "metadata:cache:changed", cb: (e2) => this.handleDelete(e2.get().path), sort: 0 });
    dispatcher.addListener({
      name: "file:rename",
      cb: (e2) => {
        this.cache.delete(e2.get().old);
        this.cache.save(this.cache.getItem(e2.get().actual).set(null));
        this.handleDelete(e2.get().actual);
      }
    });
  }
  resolve(path) {
    return this.get(path);
  }
  setTemplate(template) {
    this.resolver.setTemplate(template);
  }
  handleDelete(path) {
    const item = this.cache.getItem(path);
    if (!item.isHit()) {
      return;
    }
    const old = item.get();
    this.actualize(item);
    if (old !== item.get()) {
      this.dispatcher.dispatch("resolver:unresolved", new Event({ path }));
    }
  }
  actualize(item) {
    const title = this.resolver.resolve(item.getKey());
    this.cache.save(item.set(title));
  }
  get(path) {
    var _a4;
    const item = this.cache.getItem(path);
    if (item.isHit() === false) {
      this.actualize(item);
    }
    return (_a4 = item.get()) != null ? _a4 : null;
  }
};
ResolverCachedProxy = __decorateClass([
  Kt(),
  __decorateParam(0, Yt(inversify_types_default.resolver)),
  __decorateParam(0, Ut("original")),
  __decorateParam(1, Yt(inversify_types_default.cache)),
  __decorateParam(2, Yt(inversify_types_default["event:dispatcher"]))
], ResolverCachedProxy);

// src/Resolver/Resolver.ts
var Resolver = class {
  constructor(filters, creator, dispatcher) {
    this.filters = filters;
    this.creator = creator;
    this.dispatcher = dispatcher;
    this.template = "";
  }
  setTemplate(template) {
    this.template = template;
  }
  resolve(path) {
    return this.valid(path) ? this.get(path) : null;
  }
  get(path) {
    var _a4;
    try {
      return (_a4 = this.dispatch(this.creator.create(path, this.template), path)) != null ? _a4 : null;
    } catch (e2) {
      console.error(`Error by path ${path}`, e2);
    }
    return null;
  }
  dispatch(title, path) {
    var _a4;
    const event = new Event({
      value: title,
      modify(v3) {
        this.value = v3;
      },
      path
    });
    this.dispatcher.dispatch("resolver:resolved", event);
    return (_a4 = event.get().value) != null ? _a4 : null;
  }
  valid(path) {
    for (const filter of this.filters) {
      if (filter.check(path) === false) {
        return false;
      }
    }
    return true;
  }
};
Resolver = __decorateClass([
  Kt(),
  __decorateParam(0, Qt(inversify_types_default.filter)),
  __decorateParam(1, Yt(inversify_types_default["creator:creator"])),
  __decorateParam(2, Yt(inversify_types_default["event:dispatcher"]))
], Resolver);

// config/services/resolver.config.ts
var resolver_config_default = new kt((bind) => {
  bind(inversify_types_default["factory:resolver:template"]).toFactory((c3) => (value) => {
    var _a4, _b2;
    const storage = c3.container.get(inversify_types_default["settings:storage"]);
    const [name, type] = value.split(":");
    const targetTemplates = (_a4 = storage.get("features").get(name)) == null ? void 0 : _a4.get("templates");
    const template = (_b2 = targetTemplates == null ? void 0 : targetTemplates.get(type)) == null ? void 0 : _b2.value();
    return template ? template : storage.get("templates").get("common").get(type).value();
  });
  bind(inversify_types_default.resolver).to(ResolverCachedProxy).whenTargetIsDefault();
  bind(inversify_types_default.resolver).to(Resolver).whenTargetNamed("original");
  bind(inversify_types_default["resolver:service"]).to(ResolverService).inSingletonScope();
  bind(
    inversify_types_default["factory:resolver:resolver"]
  ).toFactory((c3) => () => c3.container.get(inversify_types_default.resolver));
});

// src/Components/ApiAdapter/Api.ts
var Api = class {
  constructor(factory, dispatcher, composer) {
    this.factory = factory;
    this.dispatcher = dispatcher;
    this.composer = composer;
  }
  getResolverFactory() {
    return this.factory;
  }
  getEventDispatcher() {
    return this.dispatcher;
  }
  getEnabledFeatures() {
    return this.composer.getIds();
  }
};
Api = __decorateClass([
  Kt(),
  __decorateParam(0, Yt(inversify_types_default["feature:service"])),
  __decorateParam(1, Yt(inversify_types_default["event:dispatcher"])),
  __decorateParam(2, Yt(inversify_types_default["feature:composer"]))
], Api);

// src/Components/ApiAdapter/Defer.ts
var DeferPluginReady = 2;
var DeferFeaturesReady = 4;
var Defer = class {
  constructor(factory) {
    this.factory = factory;
    this.state = 0;
    this.promises = {
      plugin: null,
      managers: null
    };
    this.resolves = {
      plugin: null,
      managers: null
    };
    this.promises.plugin = new Promise((r3) => this.resolves.plugin = r3);
    this.promises.managers = new Promise((r3) => this.resolves.managers = r3);
  }
  setFlag(flag) {
    if (!(this.state & flag)) {
      this.state = this.state | flag;
      this.processState();
    }
  }
  async awaitPlugin() {
    return await this.promises.plugin;
  }
  async awaitFeatures() {
    return this.promises.managers;
  }
  isPluginReady() {
    return (this.state & DeferPluginReady) !== 0;
  }
  isFeaturesReady() {
    return (this.state & DeferFeaturesReady) !== 0;
  }
  getApi() {
    return this.isPluginReady() ? this.factory() : null;
  }
  processState() {
    if (this.isPluginReady()) {
      this.resolves.plugin();
      if (this.isFeaturesReady()) {
        this.resolves.managers();
      }
    }
  }
};
Defer = __decorateClass([
  Kt(),
  __decorateParam(0, Yt(inversify_types_default["factory:api"]))
], Defer);

// config/services/api.adapter.config.ts
var api_adapter_config_default = new kt((bind) => {
  bind(inversify_types_default.api).to(Api).inSingletonScope();
  bind(inversify_types_default["factory:api"]).toFactory((c3) => () => c3.container.get(inversify_types_default.api));
  bind(inversify_types_default.defer).to(Defer).inSingletonScope();
});

// src/Filters/ExtensionFilter.ts
var ExtensionFilter = class {
  check(path) {
    return /.*\.md$/.test(path);
  }
};
ExtensionFilter = __decorateClass([
  Kt()
], ExtensionFilter);

// src/Filters/PathListFilter.ts
var PathListFilter = class {
  constructor(list) {
    this.list = list;
  }
  check(path) {
    return this.list.isFileAllowed(path);
  }
};
PathListFilter = __decorateClass([
  Kt(),
  __decorateParam(0, Yt(inversify_types_default["component:black_white_list"]))
], PathListFilter);

// src/Components/BlackWhiteList/BlackWhiteList.ts
var BlackWhiteList = class {
  constructor() {
    this.mode = "black";
    this.list = [];
  }
  get default() {
    return this.mode === "black";
  }
  isFileAllowed(path) {
    if (this.list.length === 0) {
      return true;
    }
    for (const item of this.list) {
      if (path.startsWith(item)) {
        return this.mode === "white";
      }
    }
    return this.default;
  }
  setMode(mode) {
    this.mode = mode;
  }
  setList(list) {
    this.list = [...list];
  }
};
BlackWhiteList = __decorateClass([
  Kt()
], BlackWhiteList);

// src/Components/Cache/CacheItem.ts
var CacheItem = class {
  constructor(_isHit, value, key) {
    this._isHit = _isHit;
    this.value = value;
    this.key = key;
  }
  get() {
    return this.value;
  }
  isHit() {
    return this._isHit;
  }
  set(value) {
    this.value = value;
    return this;
  }
  getKey() {
    return this.key;
  }
};

// src/Components/Cache/Cache.ts
var Cache = class {
  constructor() {
    this.pool = /* @__PURE__ */ new Map();
    this.serialized = /* @__PURE__ */ new Map();
  }
  clear() {
    this.pool.clear();
    this.serialized.clear();
  }
  delete(key) {
    this.pool.delete(key);
    this.serialized.delete(key);
  }
  getItem(key) {
    const isHit = this.pool.has(key) || this.serialized.has(key);
    const value = isHit ? this.resolve(key) : null;
    return new CacheItem(isHit, value, key);
  }
  save(item) {
    const key = item.getKey();
    const value = item.get();
    this.pool.delete(key);
    this.serialized.delete(key);
    if (this.isObject(value)) {
      if (!this.canBeSerialized(value)) {
        throw new Error("Object with functions can not be serialized");
      }
      this.serialized.set(key, JSON.stringify(value));
    } else {
      this.pool.set(key, value);
    }
  }
  resolve(key) {
    if (this.pool.has(key)) {
      return this.pool.get(key);
    }
    return JSON.parse(this.serialized.get(key));
  }
  isObject(value) {
    return typeof value === "object" && ![null, void 0].includes(value);
  }
  canBeSerialized(object) {
    return !ObjectHelper.values(object).some(
      (e2) => this.isObject(e2) ? !this.canBeSerialized(e2) : typeof e2 === "function"
    );
  }
};
Cache = __decorateClass([
  Kt()
], Cache);

// src/Components/Extractor/Extractor.ts
var Extractor = class {
  constructor(strategies) {
    this.strategies = strategies;
  }
  /**
   * @throws {PathNotFoundException}
   * @throws {TypeNotSupportedException}
   * @param path
   * @param obj
   */
  extract(path, obj) {
    const parts = path.split(".");
    let part;
    let extracted = obj;
    while (part = parts.shift()) {
      extracted = this.extractInternal(part, extracted);
    }
    let strategy = null;
    let type = extracted === null ? "null" : typeof extracted;
    type = type === "object" && Array.isArray(extracted) ? "array" : type;
    for (const item of this.strategies) {
      if (item.support(type)) {
        strategy = item;
        break;
      }
    }
    if (strategy === null) {
      throw new TypeNotSupportedException(`Type ${type} is not supported`);
    }
    return strategy.process(extracted);
  }
  /**
   * @throws {PathNotFoundException}
   * @param key
   * @param obj
   * @private
   */
  extractInternal(key, obj) {
    if (obj === void 0 || (obj == null ? void 0 : obj[key]) === void 0) {
      throw new PathNotFoundException(`Key ${key} not found`);
    }
    return obj[key];
  }
};
Extractor = __decorateClass([
  Kt(),
  __decorateParam(0, Qt(inversify_types_default["component:extractor:strategy"]))
], Extractor);

// src/Components/Extractor/LiteralStrategy.ts
var LiteralStrategy = class {
  support(type) {
    return ["number", "string"].includes(type);
  }
  process(v3) {
    return v3.toString();
  }
};
LiteralStrategy = __decorateClass([
  Kt()
], LiteralStrategy);

// src/Components/Extractor/ArrayStrategy.ts
var ArrayStrategy = class {
  constructor(delimiterGetter) {
    this.delimiterGetter = delimiterGetter;
  }
  process(v3) {
    if (v3.length === 0) {
      return null;
    }
    const delimiter = this.delimiterGetter();
    return !delimiter.enabled ? v3[0] : v3.join(delimiter.value);
  }
  support(type) {
    return type === "array";
  }
};
ArrayStrategy = __decorateClass([
  Kt(),
  __decorateParam(0, Yt(inversify_types_default["getter:delimiter"]))
], ArrayStrategy);

// src/Components/Extractor/NullStrategy.ts
var NullStrategy = class {
  // eslint-stop-next-line @typescript-eslint/no-unused-vars
  process() {
    return null;
  }
  support(type) {
    return type === "null";
  }
};
NullStrategy = __decorateClass([
  Kt()
], NullStrategy);

// src/Components/Debug/Debugger.ts
var Debugger = class {
  constructor(name, enabled) {
    this.name = name;
    this.enabled = enabled;
  }
  info(...value) {
    this.log(...value);
  }
  debug(...value) {
    this.log(...value);
  }
  warn(...value) {
    this.log(...value);
  }
  log(...value) {
    if (this.enabled()) {
      console.debug(`[${this.name}]`, ...value);
    }
  }
};
Debugger = __decorateClass([
  Kt()
], Debugger);

// src/Components/Debug/LoggerComposer.ts
var LoggerComposer = class {
  constructor() {
    this.loggers = /* @__PURE__ */ new Map();
    this.enabled = false;
  }
  create(name) {
    if (!this.loggers.has(name)) {
      this.loggers.set(name, new Debugger(name, this.isEnabled.bind(this)));
    }
    return this.loggers.get(name);
  }
  enable() {
    this.enabled = true;
  }
  disable() {
    this.enabled = false;
  }
  isEnabled() {
    return this.enabled;
  }
};
LoggerComposer = __decorateClass([
  Kt()
], LoggerComposer);

// src/Utils/FileNoteLinkService.ts
var import_obsidian18 = require("obsidian");
var FileNoteLinkService = class {
  constructor(facade) {
    this.facade = facade;
    this.REGEXP = /^\[\[.+\]\]$/;
  }
  getNoteLinks(path) {
    var _a4;
    const links = this.facade.getFileLinksCache(path);
    const result = [];
    for (const link of links) {
      if (!this.REGEXP.test(link.original)) {
        continue;
      }
      const file = this.facade.getFirstLinkpathDest(link.link, path);
      if (file instanceof import_obsidian18.TFile === false) {
        continue;
      }
      result.push({
        dest: file.path,
        link: link.link,
        original: link.original,
        alias: (_a4 = link == null ? void 0 : link.displayText) != null ? _a4 : null
      });
    }
    return result;
  }
};
FileNoteLinkService = __decorateClass([
  Kt(),
  __decorateParam(0, Yt(inversify_types_default["facade:obsidian"]))
], FileNoteLinkService);

// src/Components/EventDispatcher/EventDispatcher.ts
var EventDispatcher = class {
  constructor(logger) {
    this.logger = logger;
    this.events = /* @__PURE__ */ new Map();
  }
  addListener({ name, cb, sort = null, once = false }) {
    var _a4, _b2, _c;
    const ref = { getName: () => name };
    const events = (_a4 = this.events.get(name)) != null ? _a4 : [];
    sort = sort != null ? sort : ((_c = (_b2 = events[0]) == null ? void 0 : _b2.sort) != null ? _c : 0) + 1;
    const event = { cb, sort, ref, once };
    events.push(event);
    events.sort((a3, b3) => a3.sort - b3.sort);
    this.logger.log(`Added listener: ${name.toString()}`, event);
    this.events.set(name, events);
    return ref;
  }
  dispatch(name, e2) {
    var _a4, _b2;
    this.logger.log(name);
    let shift = 0;
    for (const [i2, item] of [...(_b2 = (_a4 = this.events.get(name)) == null ? void 0 : _a4.entries()) != null ? _b2 : []]) {
      item.cb(e2);
      if (item.once) {
        this.events.get(name).splice(i2 - shift++, 1);
      }
    }
  }
  removeListener(ref) {
    for (const [i2, item] of this.events.get(ref.getName()).entries()) {
      if (item.ref === ref) {
        this.events.get(ref.getName()).splice(i2, 1);
        this.logger.log(`Removed listener: ${ref.getName().toString()}`);
        return;
      }
    }
  }
};
EventDispatcher = __decorateClass([
  Kt(),
  __decorateParam(0, Yt(inversify_types_default.logger)),
  __decorateParam(0, Ut("event:dispatcher"))
], EventDispatcher);

// src/Components/BlackWhiteList/BlackWhiteListListener.ts
var BlackWhiteListListener = class {
  constructor(component, dispatcher) {
    this.component = component;
    this.dispatcher = dispatcher;
    this.ref = null;
  }
  bind() {
    this.ref = this.dispatcher.addListener({ name: "settings:changed", cb: (e2) => this.handle(e2.get()) });
    this.dispatcher.addListener({
      name: "settings.loaded",
      once: true,
      cb: (e2) => {
        this.component.setMode(e2.get().settings.rules.paths.mode);
        this.component.setList(e2.get().settings.rules.paths.values);
      }
    });
  }
  unbind() {
    this.dispatcher.removeListener(this.ref);
    this.ref = null;
  }
  handle({ changed, actual }) {
    var _a4, _b2, _c, _d;
    if ((_b2 = (_a4 = changed == null ? void 0 : changed.rules) == null ? void 0 : _a4.paths) == null ? void 0 : _b2.values) {
      this.component.setList(actual.rules.paths.values);
    }
    if ((_d = (_c = changed == null ? void 0 : changed.rules) == null ? void 0 : _c.paths) == null ? void 0 : _d.mode) {
      this.component.setMode(actual.rules.paths.mode);
    }
  }
};
BlackWhiteListListener = __decorateClass([
  Kt(),
  __decorateParam(0, Yt(inversify_types_default["component:black_white_list"])),
  __decorateParam(1, Yt(inversify_types_default["event:dispatcher"]))
], BlackWhiteListListener);

// src/Components/Processor/ProccessorListener.ts
var ProcessorListener = class {
  constructor(dispatcher, factory, transformer) {
    this.dispatcher = dispatcher;
    this.factory = factory;
    this.transformer = transformer;
    this.changedRef = null;
    this.resolvedRef = null;
    this.processor = null;
  }
  bind() {
    this.changedRef = this.dispatcher.addListener({ name: "settings:changed", cb: (e2) => this.make(e2.get().actual) });
    this.dispatcher.addListener({ name: "settings.loaded", cb: (e2) => this.make(e2.get().settings), once: true });
  }
  unbind() {
    this.dispatcher.removeListener(this.changedRef);
    this.changedRef = null;
    this.disable();
  }
  make(actual) {
    const { type, args } = actual.processor;
    if (Object.values(ProcessorTypes).includes(type)) {
      this.enable(type, args);
    } else {
      this.disable();
    }
  }
  disable() {
    if (this.resolvedRef) {
      this.dispatcher.removeListener(this.resolvedRef);
      this.resolvedRef = null;
    }
    this.processor = null;
  }
  enable(type, args) {
    this.processor = this.factory(type, args);
    if (this.resolvedRef === null) {
      this.resolvedRef = this.dispatcher.addListener({
        name: "resolver:resolved",
        cb: this.handleResolved.bind(this)
      });
    }
  }
  handleResolved(event) {
    var _a4;
    const obj = event.get();
    if (typeof obj.value !== "string") {
      return;
    }
    if (this.processor === null) {
      return;
    }
    const arg = this.transformer.transform(event, this.processor.getType());
    const value = (_a4 = this.processor.process(arg)) != null ? _a4 : null;
    if (value) {
      obj.modify(value);
    }
  }
};
ProcessorListener = __decorateClass([
  Kt(),
  __decorateParam(0, Yt(inversify_types_default["event:dispatcher"])),
  __decorateParam(1, Yt(inversify_types_default["processor:factory"])),
  __decorateParam(2, Yt(inversify_types_default["processor:argument.transformer"]))
], ProcessorListener);

// src/Utils/FakeTitleElementService.ts
var FakeTitleElementService = class {
  constructor() {
    this.attr = {
      fake: "ofmt-fake-id",
      original: "ofmt-original-id"
    };
    this.elements = /* @__PURE__ */ new Map();
    //TODO: add logs for events
    this.events = {
      click: (e2) => {
        const id2 = this.extractId(e2.target);
        this.setVisible(id2, false).getOriginal(id2).focus();
      },
      blur: (e2) => this.setVisible(this.extractId(e2.target), true),
      hover: (e2) => this.setVisible(this.extractId(e2.target), false),
      out: (e2) => this.setVisible(this.extractId(e2.target), true)
    };
  }
  removeExcept(id2) {
    const ids = Array.isArray(id2) ? id2 : [id2];
    for (const [key, { fake }] of this.elements.entries()) {
      if (!ids.includes(key)) {
        fake.remove();
        this.elements.delete(key);
      }
    }
  }
  remove(id2) {
    if (!this.has(id2)) {
      return;
    }
    this.setVisible(id2, false);
    const { fake, events = [], original } = this.elements.get(id2);
    events.forEach((e2) => {
      if (e2 === "click") {
        original.removeEventListener("click", this.events.blur);
      }
      if (e2 === "hover") {
        original.removeEventListener("mouseout", this.events.out);
      }
    });
    original.removeAttribute(this.attr.original);
    fake.remove();
    this.elements.delete(id2);
  }
  removeAll() {
    Array.from(this.elements.keys()).forEach((e2) => this.remove(e2));
  }
  getOrCreate({
    original,
    title,
    id: id2,
    events = []
  }) {
    const container = original == null ? void 0 : original.parentElement;
    if (!container) {
      return;
    }
    let element = this.find(container, id2);
    if (element) {
      element.setText(title);
      return { created: false, element };
    }
    element = document.createElement(original.tagName);
    element.className = original.className;
    element.setText(title);
    element.setAttribute(this.attr.fake, id2);
    element.tabIndex = -1;
    original.setAttribute(this.attr.original, id2);
    this.elements.set(id2, { original, fake: element, events: [...events] });
    if (events.contains("click")) {
      this.bindClick(id2);
    }
    if (events.contains("hover")) {
      this.bindHover(id2);
    }
    container.insertBefore(element, original);
    this.hide(element);
    return { created: true, element };
  }
  setVisible(id2, visible) {
    if (this.elements.has(id2)) {
      const { fake, original } = this.elements.get(id2);
      visible ? this.hide(original) : this.show(original);
      visible ? this.show(fake) : this.hide(fake);
    }
    return this;
  }
  // Hide via an inline `display:none !important` instead of the `hidden`
  // attribute. The UA rule `[hidden]{display:none}` has the lowest priority,
  // so any theme/snippet with a `.inline-title{display:…}` rule un-hides the
  // original title and the filename shows alongside our title (#280, #234,
  // #123). An important inline declaration wins the cascade against any
  // author selector, keeping exactly one title visible.
  hide(el) {
    el.style.setProperty("display", "none", "important");
  }
  show(el) {
    el.style.removeProperty("display");
  }
  extractId(e2) {
    var _a4;
    if (e2 instanceof HTMLElement) {
      return (_a4 = e2.getAttribute(this.attr.fake)) != null ? _a4 : e2.getAttribute(this.attr.original);
    }
    return null;
  }
  getOriginal(id2) {
    var _a4, _b2;
    return (_b2 = (_a4 = this.elements.get(id2)) == null ? void 0 : _a4.original) != null ? _b2 : null;
  }
  has(id2) {
    return this.elements.has(id2);
  }
  bindClick(id2) {
    if (this.has(id2)) {
      const { fake, original } = this.elements.get(id2);
      fake.addEventListener("click", this.events.click);
      fake.addEventListener("focus", this.events.click);
      original.addEventListener("blur", this.events.blur);
    }
  }
  bindHover(id2) {
    if (this.has(id2)) {
      const { fake, original } = this.elements.get(id2);
      fake.addEventListener("mouseover", this.events.hover);
      original.addEventListener("mouseout", this.events.out);
    }
  }
  find(container, id2) {
    for (const node of Array.from(container.children)) {
      const cId = node.getAttribute(this.attr.fake);
      if (id2 === cId && node instanceof HTMLElement) {
        return node;
      }
    }
    return null;
  }
};
FakeTitleElementService = __decorateClass([
  Kt()
], FakeTitleElementService);

// src/Components/Extractor/BooleanStrategy.ts
var BooleanStrategy = class {
  process(v3) {
    return v3 ? "true" : "false";
  }
  support(type) {
    return "boolean" === type;
  }
};
BooleanStrategy = __decorateClass([
  Kt()
], BooleanStrategy);

// src/Utils/ResultDomWrapper.ts
var ResultDomWrapper = class {
  constructor(resolver = null, dom) {
    this.resolver = resolver;
    this.replacer = null;
    this.replacer = FunctionReplacer.tryCreate(dom, "addResult", this, (self2, defaultArgs, vanilla) => {
      const c3 = vanilla.call(dom, ...defaultArgs);
      const file = defaultArgs[0];
      self2.processLookupItem(file, c3);
      return c3;
    });
    if (!this.replacer) {
      return;
    }
    this.replacer.enable();
    this.process();
  }
  disable() {
    var _a4;
    (_a4 = this.replacer) == null ? void 0 : _a4.disable();
    this.replacer = null;
  }
  process() {
    var _a4, _b2, _c;
    const lookup = (_c = (_b2 = (_a4 = this.replacer) == null ? void 0 : _a4.getTarget()) == null ? void 0 : _b2.resultDomLookup) != null ? _c : /* @__PURE__ */ new Map();
    for (const [file, item] of lookup) {
      this.processLookupItem(file, item);
    }
  }
  processLookupItem(file, item) {
    var _a4, _b2, _c, _d;
    const restore = !((_a4 = this == null ? void 0 : this.replacer) == null ? void 0 : _a4.isEnabled());
    if (file.extension !== "md") {
      return;
    }
    const node = (_c = (_b2 = item == null ? void 0 : item.containerEl) == null ? void 0 : _b2.firstElementChild) == null ? void 0 : _c.find(".tree-item-inner");
    if (!node) {
      return;
    }
    const text = (_d = restore ? null : this.resolver.resolve(file.path)) != null ? _d : file.basename;
    if (node.getText() !== text) {
      node.setText(text);
    }
  }
};

// src/Utils/SearchDomWrapperService.ts
var SearchDomWrapperService = class {
  constructor(logger) {
    this.logger = logger;
    this.entities = /* @__PURE__ */ new Map();
    this.timer = null;
  }
  wrapDom(dom, resolver, tag) {
    if (!this.entities.has(tag)) {
      this.entities.set(tag, /* @__PURE__ */ new Map());
    }
    const tagged = this.entities.get(tag);
    if (!tagged.has(dom)) {
      const wrapper = new ResultDomWrapper(resolver, dom);
      tagged.set(dom, wrapper);
      this.logger.log(`Entity created`);
      this.runTimer();
    }
  }
  destroyByTag(tag) {
    if (!this.entities.has(tag)) {
      return;
    }
    for (const [dom, wrapper] of this.entities.get(tag)) {
      wrapper.disable();
      this.entities.get(tag).delete(dom);
    }
  }
  runTimer() {
    if (this.timer) {
      clearTimeout(this.timer);
    }
    this.timer = window.setTimeout(this.selfClean.bind(this), 1e4);
  }
  selfClean() {
    this.timer = null;
    for (const [tag, entities] of this.entities) {
      this.logger.log(`Tag ${tag} has ${entities.size} entities before self-clean`);
      for (const [dom, wrapper] of entities) {
        if (!document.body.contains(dom.el)) {
          wrapper.disable();
          this.logger.log(`Dom from ${tag} tag has been removed`);
          this.entities.get(tag).delete(dom);
        }
      }
      this.logger.log(`Tag ${tag} has ${entities.size} entities after self-clean`);
    }
  }
};
SearchDomWrapperService = __decorateClass([
  Kt(),
  __decorateParam(0, Yt(inversify_types_default.logger)),
  __decorateParam(0, Ut(SearchDomWrapperService.name))
], SearchDomWrapperService);

// src/Components/Delayer/Delayer.ts
var Delayer = class {
  delay(fn, time) {
    return setTimeout(fn, time);
  }
  clear(id2) {
    clearTimeout(id2);
  }
};
Delayer = __decorateClass([
  Kt()
], Delayer);

// config/inversify.config.ts
var Container = new Pt();
Container.bind(inversify_types_default["event:dispatcher"]).to(EventDispatcher).inSingletonScope();
Container.bind(inversify_types_default["template:pattern"]).toConstantValue("(?<placeholder>{{[^{}]+?}})");
Container.bind(inversify_types_default.filter).to(ExtensionFilter);
Container.bind(inversify_types_default.filter).to(PathListFilter);
Container.bind(inversify_types_default["component:black_white_list"]).to(BlackWhiteList).inSingletonScope();
Container.bind(inversify_types_default.cache).to(Cache);
Container.bind(inversify_types_default["component:extractor"]).to(Extractor);
Container.bind(inversify_types_default["component:extractor:strategy"]).to(LiteralStrategy);
Container.bind(inversify_types_default["component:extractor:strategy"]).to(ArrayStrategy);
Container.bind(inversify_types_default["component:extractor:strategy"]).to(NullStrategy);
Container.bind(inversify_types_default["component:extractor:strategy"]).to(BooleanStrategy);
Container.bind(inversify_types_default["logger:composer"]).to(LoggerComposer).inSingletonScope();
Container.bind(inversify_types_default.delayer).to(Delayer);
Container.bind(inversify_types_default.logger).toDynamicValue((context) => {
  return context.container.get(inversify_types_default["logger:composer"]).create(String(context.currentRequest.target.getNamedTag().value));
}).when(() => true);
Container.bind(inversify_types_default["service:note:link"]).to(FileNoteLinkService).inSingletonScope();
Container.bind(inversify_types_default["service:fake_title_element"]).to(FakeTitleElementService);
Container.bind(inversify_types_default.listener).to(BlackWhiteListListener).whenTargetNamed("unnamed");
Container.bind(inversify_types_default.listener).to(ProcessorListener).whenTargetNamed("unnamed");
Container.load(creator_config_default);
feature_config_default(Container);
settings_config_default(Container);
Container.load(processors_config_default);
Container.load(resolver_config_default);
Container.load(api_adapter_config_default);
Container.bind(inversify_types_default["factory:replacer"]).toFunction(
  (t5, m3, a3, i2) => FunctionReplacer.tryCreate(t5, m3, a3, i2)
);
Container.bind(inversify_types_default["service:search:dom:wrapper"]).to(SearchDomWrapperService).inSingletonScope();
var inversify_config_default = Container;

// src/App.ts
var App2 = class {
  constructor() {
    this.container = inversify_config_default;
    this.bind();
  }
  bind() {
    const dispatcher = this.container.get(inversify_types_default["event:dispatcher"]);
    dispatcher.addListener({
      name: "settings:changed",
      cb: (e2) => this.onSettingsChanged(e2.get())
    });
    dispatcher.addListener({
      name: "settings.loaded",
      once: true,
      cb: (e2) => this.init(e2.get().settings)
    });
  }
  init(settings) {
    this.container.bind(inversify_types_default.delimiter).toConstantValue(settings.rules.delimiter);
    this.container.get(inversify_types_default["logger:composer"])[settings.debug ? "enable" : "disable"]();
  }
  onSettingsChanged({ actual, changed }) {
    var _a4;
    if ((_a4 = changed == null ? void 0 : changed.rules) == null ? void 0 : _a4.delimiter) {
      this.container.rebind(inversify_types_default.delimiter).toConstantValue(actual.rules.delimiter);
    }
    if (changed.debug) {
      this.container.get(inversify_types_default["logger:composer"])[actual.debug ? "enable" : "disable"]();
    }
    const dispatcher = this.container.get(inversify_types_default["event:dispatcher"]);
    dispatcher.dispatch("resolver:clear", null);
  }
};
App2 = __decorateClass([
  Kt()
], App2);

// src/Utils/PluginHelper.ts
var PluginHelper = class {
  static compareVersion(version1, comparator, version2) {
    const left = version1.split(".").map(Number);
    const right = version2.split(".").map(Number);
    for (let i2 = 0; i2 < 3; ++i2) {
      if (left[i2] > right[i2]) {
        return comparator === ">" || comparator === ">=" || comparator === "!=";
      } else if (left[i2] < right[i2]) {
        return comparator === "<" || comparator === "<=" || comparator === "!=";
      }
    }
    return comparator === "=" || comparator === ">=" || comparator === "<=";
  }
  /**
   * Whether to show the "what's new" notice: only on an actual update (a known
   * previous version that differs from the current one), never on first install,
   * and only when there are release notes to show (`hasNotes`) — technical
   * patches without a CHANGELOG section show nothing.
   */
  static shouldShowReleaseNotice(previous, current, hasNotes) {
    return hasNotes && previous !== null && previous !== current;
  }
  static createDefaultSettings() {
    return {
      version: PLUGIN_VERSION,
      templates: {
        common: { main: "title", fallback: "fallback_title" }
      },
      rules: {
        paths: {
          mode: "black",
          values: []
        },
        delimiter: {
          enabled: false,
          value: ""
        }
      },
      debug: false,
      boot: {
        delay: 1e3,
        background: true
      },
      features: {
        ["alias" /* Alias */]: {
          enabled: false,
          strategy: "ensure" /* Ensure */,
          validator: "fr" /* FrontmatterRequired */,
          templates: { main: "", fallback: "" }
        },
        ["explorer" /* Explorer */]: { enabled: false, sort: false, templates: { main: "", fallback: "" } },
        ["tab" /* Tab */]: { enabled: false, templates: { main: "", fallback: "" } },
        ["header" /* Header */]: { enabled: false, templates: { main: "", fallback: "" } },
        ["graph" /* Graph */]: { enabled: false, templates: { main: "", fallback: "" } },
        ["bookmarks" /* Bookmarks */]: { enabled: false, templates: { main: "", fallback: "" } },
        ["search" /* Search */]: { enabled: false, templates: { main: "", fallback: "" } },
        ["suggest" /* Suggest */]: { enabled: false, templates: { main: "", fallback: "" } },
        ["inlineTitle" /* InlineTitle */]: { enabled: false, templates: { main: "", fallback: "" } },
        ["canvas" /* Canvas */]: { enabled: false, templates: { main: "", fallback: "" } },
        ["backlink" /* Backlink */]: { enabled: false, templates: { main: "", fallback: "" } },
        ["windowFrame" /* WindowFrame */]: { enabled: false, templates: { main: "", fallback: "" } },
        ["noteLink" /* NoteLink */]: {
          enabled: false,
          strategy: "onlyEmpty" /* OnlyEmpty */,
          approval: true,
          templates: { main: "", fallback: "" }
        }
      },
      processor: {
        args: [],
        type: null
      }
    };
  }
};

// src/Obsidian/ObsidianFacade.ts
var import_obsidian19 = require("obsidian");
var ObsidianFacade = class {
  constructor(app) {
    this.app = app;
  }
  isInternalPluginEnabled(id2) {
    return this.app.internalPlugins.getEnabledPluginById(id2) !== null;
  }
  getTFile(path) {
    const file = this.app.vault.getAbstractFileByPath(path);
    return file instanceof import_obsidian19.TFile ? file : null;
  }
  getFileContent(file) {
    return this.app.vault.cachedRead(file);
  }
  modifyFile(file, c3) {
    return this.app.vault.modify(file, c3);
  }
  getFileCache(path) {
    return this.app.metadataCache.getCache(path);
  }
  getFileLinksCache(path) {
    var _a4, _b2;
    return (_b2 = (_a4 = this.getFileCache(path)) == null ? void 0 : _a4.links) != null ? _b2 : [];
  }
  getLeavesOfType(type) {
    return this.app.workspace.getLeavesOfType(type);
  }
  getViewsOfType(type) {
    return this.getLeavesOfType(type).map((e2) => e2.view);
  }
  getFirstLinkpathDest(linkpath, from) {
    return this.app.metadataCache.getFirstLinkpathDest(linkpath, from);
  }
  getActiveLeaf() {
    var _a4, _b2;
    return (_b2 = (_a4 = this.app.workspace) == null ? void 0 : _a4.activeLeaf) != null ? _b2 : null;
  }
  getActiveFile() {
    var _a4, _b2;
    return (_b2 = (_a4 = this.app.workspace) == null ? void 0 : _a4.getActiveFile()) != null ? _b2 : null;
  }
  getWorkspace() {
    return this.app.workspace;
  }
  getMostRecentLeaf(root) {
    return this.app.workspace.getMostRecentLeaf(root);
  }
  getAppTitle(text = null) {
    return this.app.getAppTitle(text);
  }
};

// src/Migrator/Migrator.ts
var Migrator = class {
  constructor(config, dispatcher) {
    this.config = config;
    this.dispatcher = dispatcher;
  }
  migrate(version) {
    if (this.config.version === version) {
      return this.config;
    }
    const currentVersion = this.config.version;
    const versions = Object.getOwnPropertyNames(Object.getPrototypeOf(this)).filter((e2) => e2.startsWith("v")).map((e2) => e2.replace("v", "").replaceAll("_", ".")).filter((e2) => PluginHelper.compareVersion(e2, ">", currentVersion)).sort((a3, b3) => {
      if (PluginHelper.compareVersion(a3, "<", b3)) {
        return -1;
      } else if (PluginHelper.compareVersion(a3, ">", b3)) {
        return 1;
      }
      return 0;
    });
    for (const version2 of versions) {
      const method = "v" + version2.replaceAll(".", "_");
      console.log(`Migrate from ${currentVersion} to ${version2} by ${method}`);
      this[method]();
    }
    this.dispatcher.dispatch("migrator:migrated", new Event({ from: currentVersion, to: version }));
    return this.config;
  }
  v3_8_0() {
    const features = Object.values(Feature);
    delete this.config.features["explorer:sort"];
    for (const [k3, v3] of Object.entries(this.config.templates)) {
      if (features.contains(k3)) {
        this.config.features[k3].templates = v3;
        delete this.config.templates[k3];
      }
    }
    return;
  }
  v3_8_3() {
    var _a4, _b2, _c, _d, _e3, _f, _g;
    if (Array.isArray((_a4 = this.config) == null ? void 0 : _a4.templates)) {
      this.config.templates = {
        common: {
          main: (_d = (_c = (_b2 = this.config) == null ? void 0 : _b2.templates) == null ? void 0 : _c[0]) != null ? _d : PluginHelper.createDefaultSettings().templates.common.main,
          fallback: (_g = (_f = (_e3 = this.config) == null ? void 0 : _e3.templates) == null ? void 0 : _f[1]) != null ? _g : ""
        }
      };
    }
  }
  v3_9_0() {
    if (this.config.features.starred) {
      delete this.config.features.starred;
    }
  }
};

// src/Modal/ReleaseNoticeModal.ts
var import_obsidian20 = require("obsidian");
var ISSUES_URL = "https://github.com/snezhig/obsidian-front-matter-title/issues";
var ReleaseNoticeModal = class extends import_obsidian20.Modal {
  constructor(app, version) {
    super(app);
    this.version = version;
    this.component = new import_obsidian20.Component();
  }
  onOpen() {
    const { contentEl, titleEl } = this;
    titleEl.setText(`What's new in Front Matter Title ${this.version}`);
    const whatsNew = typeof PLUGIN_WHATS_NEW === "string" ? PLUGIN_WHATS_NEW.trim() : "";
    this.component.load();
    const body = contentEl.createDiv();
    import_obsidian20.MarkdownRenderer.render(this.app, whatsNew || `Updated to ${this.version}.`, body, "", this.component);
    contentEl.createEl("hr");
    contentEl.createEl("p", {
      text: "This plugin is back to a working state and is maintained based on demand from the community. If there's a feature you need or a bug that matters to you, please open an issue on GitHub \u2014 that's how priorities get set."
    });
    const buttons = contentEl.createDiv({ cls: "modal-button-container" });
    const issues = buttons.createEl("button", { text: "Open an issue on GitHub", cls: "mod-cta" });
    issues.addEventListener("click", () => window.open(ISSUES_URL));
    const close = buttons.createEl("button", { text: "Close" });
    close.addEventListener("click", () => this.close());
  }
  onClose() {
    this.component.unload();
    this.contentEl.empty();
  }
};

// main.ts
var MetaTitlePlugin = class extends import_obsidian21.Plugin {
  constructor() {
    super(...arguments);
    this.container = inversify_config_default;
    this.previousVersion = null;
  }
  getDefer() {
    return this.container.get(inversify_types_default.defer);
  }
  async onload() {
    this.bindServices();
    this.dispatcher = this.container.get(inversify_types_default["event:dispatcher"]);
    this.logger = this.container.getNamed(inversify_types_default.logger, "main");
    this.app.workspace.on("layout-change", () => this.dispatcher.dispatch("layout:change", null));
    this.app.workspace.on("active-leaf-change", () => this.dispatcher.dispatch("active:leaf:change", null));
    this.app.workspace.on("file-open", (file) => this.dispatcher.dispatch("file:open", new Event(file)));
    new App2();
    this.container.getAllNamed(inversify_types_default.listener, "unnamed").map((e2) => e2.bind());
    await this.loadSettings();
    this.app.workspace.onLayoutReady(() => {
      this.getDefer().setFlag(DeferPluginReady);
      this.maybeShowReleaseNotice();
    });
    await this.delay();
  }
  maybeShowReleaseNotice() {
    const hasNotes = typeof PLUGIN_WHATS_NEW === "string" && PLUGIN_WHATS_NEW.trim().length > 0;
    if (!PluginHelper.shouldShowReleaseNotice(this.previousVersion, PLUGIN_VERSION, hasNotes)) {
      return;
    }
    new ReleaseNoticeModal(this.app, PLUGIN_VERSION).open();
    this.saveData(this.storage.collect()).catch((e2) => {
      var _a4;
      return (_a4 = this.logger) == null ? void 0 : _a4.log(e2);
    });
  }
  onunload() {
    this.fc.disableAll();
  }
  async loadSettings() {
    var _a4;
    const loaded = await this.loadData();
    this.previousVersion = loaded ? (_a4 = loaded.version) != null ? _a4 : "0.0.0" : null;
    if (loaded && !loaded.version) {
      loaded.version = "0.0.0";
    }
    let data = ObjectHelper.fillFrom(PluginHelper.createDefaultSettings(), loaded != null ? loaded : {});
    data = new Migrator(data, this.dispatcher).migrate(PLUGIN_VERSION);
    data.version = PLUGIN_VERSION;
    this.storage = new Storage(data);
    this.container.bind(inversify_types_default["settings:storage"]).toConstantValue(this.storage);
    this.addSettingTab(this.container.resolve(SettingsTab).getTab());
  }
  async onSettingsChange(settings) {
    await this.saveData(settings);
    this.reloadFeatures();
    await this.mc.refresh();
  }
  async delay() {
    const delay = this.storage.get("boot").get("delay").value();
    const background = this.storage.get("boot").get("background").value();
    const debug = this.storage.get("debug").value();
    this.logger.log(`Plugin manual delay ${delay}`);
    let promise = new Promise(
      (r3) => setTimeout(() => {
        this.fc = inversify_config_default.get(inversify_types_default["feature:composer"]);
        this.mc = inversify_config_default.get(inversify_types_default["manager:composer"]);
        this.bind();
        this.registerCommands();
        r3();
      }, delay)
    );
    if (delay > 0 && debug) {
      new import_obsidian21.Notice(`[${this.manifest.name}]
Will be loaded in ${delay}ms. Background: ${background}`);
      promise = promise.then(() => {
        new import_obsidian21.Notice(`[${this.manifest.name}]
Loaded. Background: ${background}`);
      });
    }
    return background ? Promise.resolve() : promise;
  }
  bindServices() {
    inversify_config_default.bind(inversify_types_default["factory:obsidian:file"]).toFactory(
      () => (path) => this.app.vault.getAbstractFileByPath(path)
    );
    inversify_config_default.bind(inversify_types_default["factory:obsidian:meta"]).toFactory(
      () => (path, type) => {
        var _a4;
        return (_a4 = this.app.metadataCache.getCache(path)) == null ? void 0 : _a4[type];
      }
    );
    inversify_config_default.bind(inversify_types_default["facade:obsidian"]).toConstantValue(new ObsidianFacade(this.app));
    inversify_config_default.bind(inversify_types_default["factory:metadata:cache"]).toFunction(() => this.app.metadataCache);
    inversify_config_default.bind(inversify_types_default["obsidian:app"]).toConstantValue(this.app);
    inversify_config_default.bind(inversify_types_default["obsidian:plugin"]).toConstantValue(this);
    inversify_config_default.bind(inversify_types_default["newable:obsidian:chooser"]).toConstructor(
      //@ts-ignore
      Object.getPrototypeOf(this.app.workspace.editorSuggest.suggests[0].suggestions).constructor
    );
    inversify_config_default.bind(inversify_types_default["factory:obsidian:active:file"]).toFunction(() => this.app.workspace.getActiveFile());
    inversify_config_default.bind(inversify_types_default["factory:obsidian:modal"]).toFunction(() => new import_obsidian21.Modal(this.app));
  }
  bind() {
    this.registerEvent(
      this.app.metadataCache.on(
        "changed",
        (file, _3, cache) => this.dispatcher.dispatch("metadata:cache:changed", new Event({ path: file.path, cache }))
      )
    );
    this.app.workspace.onLayoutReady(async () => {
      this.registerEvent(
        this.app.vault.on(
          "rename",
          ({ path: actual }, old) => this.dispatcher.dispatch("file:rename", new Event({ old, actual }))
        )
      );
      this.registerEvent(
        // after workspace.onLayoutReady, "create" is only called for new files.
        // (it's called for *all* files on startup, before layout is ready.)
        this.app.vault.on("create", ({ path }) => this.mc.update(path).catch(console.error))
      );
      await new Promise((r3) => setTimeout(r3, 3e3));
      this.reloadFeatures();
      await this.mc.refresh();
      this.getDefer().setFlag(DeferFeaturesReady);
    });
    this.dispatcher.addListener({
      name: "resolver:unresolved",
      cb: (e2) => this.mc.update(e2.get().path).catch(console.error)
    });
    this.dispatcher.addListener({ name: "settings:changed", cb: (e2) => this.onSettingsChange(e2.get().actual) });
  }
  reloadFeatures() {
    this.fc.disableAll();
    const f3 = this.storage.get("features");
    const states = [];
    for (const feature of Object.values(Feature)) {
      states.push([feature, f3.get(feature).get("enabled").value()]);
    }
    for (const [id2, state] of states) {
      try {
        this.fc.toggle(id2, state);
      } catch (e2) {
        this.fc.toggle(id2, false);
        this.logger.log(e2);
        new import_obsidian21.Notice(e2);
      }
    }
  }
  registerCommands() {
    this.addCommand({
      id: "ofmt-features-stop",
      name: t4("command.features.disable"),
      callback: () => this.fc.disableAll()
    });
    this.addCommand({
      id: "ofmt-features-reload",
      name: t4("command.features.reload"),
      callback: () => {
        this.reloadFeatures();
        this.mc.refresh().catch(console.error);
      }
    });
  }
};
/*! Bundled license information:

reflect-metadata/Reflect.js:
  (*! *****************************************************************************
  Copyright (C) Microsoft. All rights reserved.
  Licensed under the Apache License, Version 2.0 (the "License"); you may not use
  this file except in compliance with the License. You may obtain a copy of the
  License at http://www.apache.org/licenses/LICENSE-2.0
  
  THIS CODE IS PROVIDED ON AN *AS IS* BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
  KIND, EITHER EXPRESS OR IMPLIED, INCLUDING WITHOUT LIMITATION ANY IMPLIED
  WARRANTIES OR CONDITIONS OF TITLE, FITNESS FOR A PARTICULAR PURPOSE,
  MERCHANTABLITY OR NON-INFRINGEMENT.
  
  See the Apache Version 2.0 License for specific language governing permissions
  and limitations under the License.
  ***************************************************************************** *)
*/

/* nosourcemap */